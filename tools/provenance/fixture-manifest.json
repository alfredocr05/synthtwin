{
  "fixtures": [
    {
      "generator": "tools/reference/make_generation_branch_vectors.py",
      "justification": "The twin cells synthtwin's generation method requires for the fourteen cases docs/spec/generation-method-v1.md section G14.3 adds beside its first nine, computed from that specification alone. This count read 'six' while the file held seven, and is corrected here with the eighth: the seventh is the month-span case of plan P4-D4.3, reaching the second resolution that names a span rather than an instant, and the eighth is long_tail_levels (residual R-P4-17), the first frozen case to reach any of the four roles Phase 4 added. It pins admission of a folded count above the categorical ceiling and the stand-in walk of G8.3, and it does not pin a generator branch belonging to the role, which is stated at that width in G14.3 rather than left to be discovered. The ninth is clock_ladder (residual R-P4-17), the first frozen case for time_of_day and the first for any role whose method section did not exist until it was written on 2026-08-27. Its ends are eleven seconds apart and eleven of its cells parsed, so the all-different repair of method G7A.4 has no slack and every interior rank must land on the one ordinal left for it; its mutant withdraws the step-up and the same column then writes two times twice. The tenth is affixed_brackets (residual R-P4-17), the first frozen case for affixed_number: it pins that the role's universal class counts answer for the CELLS while its quantitative block answers for the CORES, and that only the second set reaches the numeric rules. Its mutant hands over the cell counts and the oracle stops at the word budget, which G4.3 also reads over the cores. Every other case carrying n_distinct_values publishes the figure its own twin reaches, so none of them can exercise that miss. The eleventh is joined_readings (residual R-P4-17, which it closes), the first frozen case for joined_numbers and the one that pins method G6B.4's pairing walk, the only search in the method. Its column publishes a rank agreement of 0.4323, holds the earlier position above the later in only seven of twelve rows and repeats values in both positions, so the walk has real work: with its step 5 withdrawn, six of the twelve cells move, which is the registered mutant. Withdrawing each of the walk's other rules in turn moves its cells for six of the rules method G6B.4 states; five it does not reach are named in that section rather than left implied, and the rank origin is named as byte-invariant in either convention so no case can pin it. The walk does not converge here: it stops at its try ceiling at 0.2226 against the 0.4323 published, which the case records. All four roles Phase 4 added now have a frozen case. The twelfth is unrepresentable_exponent (residuals R-P4-48 and R-P4-68, which it closes with the landing that wrote method G10.5 revision 5): six cells of whole numbers too large for binary64 to hold, published at five and six characters wide -- widths no digit string can be written at, because a whole numeral needs 310 figures to be certain of leaving the format's range. It reaches the EXPONENT spelling family revision 5 adds, which unrepresentable_joint beside it cannot: every cell that case freezes is a four-hundred-character digit string or the fixed contradictory construction, so the family could have been withdrawn whole with both committed files byte-identical. Its mutant puts the too-large shape's floor back to the digit string's own 310 characters and the recount then reads min_length as 310 against a published 5. It is also the first case in either file carrying a positive and a negative group of one shape, which is what made the section state whether the spelling walk counts per shape or per shape and sign: two conforming programs could differ on that and agree on every byte committed before it. Four exist because review item P2-C3-F3 found that the nine reached no unrepresentable column, no joint class-and-alphabet packing of free text, no fold collision that a case change cannot build, and no cell carrying the literal decimal, leading-zero or leading-plus style; the fifth exists because review item P2-C4-C3 found that none of them carried a seconds field of 60, the one published end the ordinal space cannot hold. A defect or a withdrawn rule on any of those branches left every committed byte unchanged. This file is written by the same oracle as tests/reference/generation-reference-vectors.json, through the second entry point beside it, and shares that oracle's transform and its proof layer; it is a second FILE only because a committed fixture must stay under the 100000-byte cap and the nine cases already spend 88207 of it. Everything the first entry says of itself holds here: the vectors are a pure function of the uint64 words the file carries as inputs, the recorded seed is accepted and ignored, every binary64 published is proved correctly rounded or proved exact by integer comparison against the midpoints to its neighbours, and the run refuses a full-generator mutant before it writes a byte. The registered generator here is the second ENTRY POINT rather than the oracle itself, and its imports are stated literally (review item P2-C4-C4): it imports os, runpy and sys, and nothing else; it holds no transform and no proof layer of its own; and it executes the oracle beside it by one fixed sibling path, which is the file that imports only argparse, datetime, fractions, json, math, struct and sys. Neither file imports synthtwin, numpy or pandas. No value in this file comes from any real or synthetic table. REBUILT at the integer-grid landing, and TWO CELLS IN TWO CASES moved. `joined_readings`' eleventh reading goes 29/45 to 28/45: position 0 of that case publishes n_distinct_values 9, the committed cells held 8, and they hold 9 now. `affixed_brackets`' first cell goes [23] to [22]: that case published twelve different core numbers while its twin held eleven, with 23 written twice, and the twin now holds twelve. The second case is the one the case's own text called out as the only committed case where a conforming generator MISSES a published fact and reports it -- so this landing removes that case's miss, and G14.3 and the builder's own note move with it. THE FIRST WRITING OF THIS SENTENCE SAID ONE CELL OF ONE CASE AND 'otherwise character for character', which was false: the diff was read through a filter that matched only the cells being looked for. The cause of both moves is the same -- the oracle did not carry method G6.5a, because the method referenced that pass twice and never stated it. The oracle carries it now, from the text, still importing nothing of synthtwin. REBUILT AGAIN at landing L19 for residual R-P4-72, and ONE STRING IN ONE CASE moved: `affixed_brackets`' remark named --identifier where the column's codes are kept by --code, so a person doing exactly as the sentence told them published no value of the column at all. Contract NF35, the producer's rendering and this oracle's copy move in one commit, which is what that residual was waiting for. No cell of any case moved and the proof layer proved the same 544 numbers. REBUILT AT STAGE 2 (2026-09-14): every block of dates gained datetime_separators and all_at_midnight, the thirteenth and fourteenth cases, midnight_days and mixed_marks, pin the day-unit rule and the rotation of marks with a mutant each, and no cell of an earlier case moved. REBUILT AT LANDING 2b.4 (2026-09-15) for the class debt of a column of labels and the numbers of a column of text: a fifteenth case, label_numbers, pins method G8.3a -- the held-back numbers of a column of labels written as numbers, stepped from the published ones with the gaps first -- with a mutant that withdraws the gaps; and free_text_joint's doubled number moves from 00 to 0 under G9.5 step 3a's number length, so that case now publishes the length average 1.25 and middle 1 of the column it describes. The oracle now carries G8.3's exact settlement of the stand-in forms, which it approximated with a single greedy walk; neither label case already committed moved a byte. REBUILT AGAIN at landing 2b.4's repair (2026-09-15): a sixteenth case, label_number_tiers, pins what the census could hold beside the places a number wearing no named form may take -- a column pooling nothing ends the walk before a form it would have counted, and takes the published whole numbers' places instead -- with a mutant that withdraws the census rule and moves the level of two from 6 to 10.0; the oracle carries that rule, the second settlement of the forms, the code band's plain numbers and the two free-text exchanges from the method text, and no cell of the fifteen cases already committed moved. REBUILT AT LANDING 2b.3 (2026-09-15): five cases were added, pooled_marks, slashed_pool, midnight_mixed_forms, partial_midnight and midnight_two_offsets, which pin the withheld pool spent on the marks the census leaves unnamed, a slashed stamp's one permitted mark, bare dates beside midnight moments, the move onto midnight and midnight on two offsets, each with a mutant; mixed_marks' cells moved because its two pooled values are now written with the one mark its census leaves unnamed rather than with the commonest; and every block of dates gained n_at_midnight, nought on every earlier case but midnight_days, whose twelve are every value it holds, which moved no cell of any other case. REBUILT AT THE REPAIR PASS OF LANDING 2b.3 (2026-09-15): its skeptic found two rules no case reached, because regenerated after they changed both files were byte-identical, so two cases were added, midnight_bare_offsets and accidental_midnight, which pin the settling of the form and offset of every rank whose instant the published tail fixes on a joint column at local midnight on a real offset, and the move of an accidental value at midnight off a column publishing none, each with a mutant; no cell of an earlier case moved. MERGED at the integration of landings 2b.1 to 2b.5 (2026-09-15): the file was rebuilt from the one merged oracle and holds both the two label cases of landing 2b.4 and the seven timestamp cases of landing 2b.3, twenty-three in all; no rule of either landing moved a cell of the other's cases. REBUILT AT LANDING 2b.2 (2026-09-15): every block of numbers gained negative_form and the decimal_plus census, the first frozen cases publishing a mark between thousands arrived -- grouped_charges with a comma, signed decimals spread one in every two and zeros spent past order nought, grouped_decimal_comma with a point on a declared decimal-comma column beside two absent cells, and spaced_brackets with a space and accounting brackets -- each with a mutant that moves its cells, and no cell of an earlier case moved. Three and not four: a fourth numeric case carried this file past the 250000-byte cap. SPLIT at the integration of landings 2b.1 to 2b.5 (2026-09-15), when landing 2b.2's three cases joined: with them this one file would have passed the provenance byte cap, so it keeps the fourteen cases it held before the carried landings, and the twelve those landings added -- landing 2b.4's two, landing 2b.3's seven and landing 2b.2's three -- are the third file, tests/reference/generation-branch-vectors-2.json, written by the same oracle through its own entry point. No case was dropped and the cap was not raised; the only bytes of the fourteen that moved are the account in case_set, which now names both other files. REBUILT AT LANDING 2b.1 (2026-09-15), and FOUR CELLS OF ONE CASE moved: joined_readings' first position read 37, 40, 40, 40 in its seventh to tenth readings and reads 38, 38, 41, 41. Method G5.2a now caps every stratum, and a withheld mode pair over a straight-line ladder of twelve cells caps it at two, so the committed triple of 40 is no longer a layout the method builds. No other case moved, the reference file beside this one is byte-identical, and the oracle carries the cap, the grid reading and G5.3's grid value from the text. MERGED WITH LANDING 2b.1 at the same integration: the four cells of joined_readings that landing moved are the only cells of this file that moved, and every other case of the fourteen is byte-identical to the file before the merge. REBUILT AT LANDING 2b.17 (2026-09-16), and the ONLY bytes that moved are this file's own account of itself in case_set, which now names the FOURTH file beside it: no case of this file moved a cell and the proof layer proved the same numbers. The rule that forced it is the one saying no file may be read as the whole of the oracle -- each account names every other file -- so a fourth file existing makes every earlier account of three incomplete. The fourth is tests/reference/generation-document-vectors.json, the cases for the transforms that produce a whole document rather than one column's cells.",
      "path": "tests/reference/generation-branch-vectors.json",
      "seed": 0,
      "sha256": "5baadc1b7cce5db2dc0beecf913bca4247baf0f90369642a31f8fb090bf9f5af"
    },
    {
      "generator": "tools/reference/make_generation_branch_vectors_2.py",
      "justification": "The twin cells synthtwin's generation method requires for the twelve cases the carried landings 2b.4, 2b.3 and 2b.2 added to docs/spec/generation-method-v1.md section G14.3, computed from that specification alone by the same oracle as tests/reference/generation-reference-vectors.json and tests/reference/generation-branch-vectors.json, through the entry point tools/reference/make_generation_branch_vectors_2.py. They are a third file only because with them the second would pass the provenance manifest's byte cap, which is a rule about a committed file and not about coverage (method section G14.2): split at the integration of those landings on 2026-09-15 rather than dropped or the cap raised. They are committed rather than built at test time for the reason the other two files are: an oracle recomputed beside the code it checks could drift with it. The vectors are a pure function of the uint64 words the file itself carries as inputs; the oracle holds no random generator and no seed handling, so the recorded seed is accepted and ignored, and it imports neither synthtwin, nor numpy, nor pandas. Every binary64 the file publishes is proved correctly rounded, or proved exact, by integer comparison, and the run refuses a full-generator mutant before it writes a byte. No value in this file comes from any real or synthetic table. REBUILT when landing 2b.1 merged into the integration (2026-09-15): method G5.2a's stratum cap now reaches grouped_charges, a column of one value whose ladder is eleven equal rungs, so the placements recorded under its float64 proofs -- which segment and stratum each interior value was drawn in, and the fractions t and u there -- moved, while its column, its content list and every cell are byte-identical, since every stratum holds the one value 12345. No other case of the twelve moved. FIVE CASES ADDED at the same integration: apostrophe_minus_sign, quoted_trailing_minus, spaced_decimal_comma, narrow_spaced and thin_spaced freeze the marks and notations landing 2b.2 had pinned only by round trips -- the apostrophe, U+2019, U+00A0, U+202F and U+2009, the minus sign and the trailing minus, and the decimal-comma exchange as a mutant of its own -- each with a mutant and eleven rows, so that the file holds 245567 bytes under the cap; no cell of the twelve earlier cases moved. REBUILT AT LANDING 2b.17 (2026-09-16), and the ONLY bytes that moved are this file's own account of itself in case_set, which now names the FOURTH file beside it: no case of this file moved a cell and the proof layer proved the same numbers. The rule that forced it is the one saying no file may be read as the whole of the oracle -- each account names every other file -- so a fourth file existing makes every earlier account of three incomplete. The fourth is tests/reference/generation-document-vectors.json, the cases for the transforms that produce a whole document rather than one column's cells.",
      "path": "tests/reference/generation-branch-vectors-2.json",
      "seed": 0,
      "sha256": "3cfce87f7f77bea88acf2bbf55aebbb49caa7498f1368719a6e8df94328b82a1"
    },
    {
      "generator": "tools/reference/make_generation_document_vectors.py",
      "justification": "The bytes synthtwin's generation method requires for the five cases landing 2b.17 added to docs/spec/generation-method-v1.md section G14.3: the transforms that produce a WHOLE DOCUMENT rather than one column's cells. They are computed from that specification alone by the same oracle as the three generation vector files beside them, through the entry point tools/reference/make_generation_document_vectors.py, and they are a fourth file for the reason the third was a third -- the third holds 245567 bytes against this manifest's cap, which leaves room for no case of any size (method section G14.2). No case was dropped, no proof was shortened and the cap was not raised. WHY THESE CASES EXIST. Landings 2b.9, 2b.10 and 2b.11 built the written form of a delimited file, the arrangement of its rows and the workbook writer, and left every one of them with NO second implementation: a grep of the oracle for twin_text, arranged, row_order or workbook found nothing, so the only check on any of those rules was a round trip through the very code they check. The workbook writer could not be mirrored at all until its rules were STATED: the method document said nothing about the writer, and a rule that is not written down cannot be implemented a second time from its statement. Method section G2.2 was written at this landing for that reason, and these cases are written from it. The five are written_form_lines (method G2, a file carrying a separator hint, a byte-order mark, three runs of lines before the table, an always-quoted header, a padded column, a column taking three different quoting rules over its four cell classes, a trailing delimiter, a blank line inside the table, two runs of line endings, no ending on its last line and an end-of-file mark after it), row_arrangement (method G2.1 in both halves, which no single file can carry: the sort with the row sequence written in place last, and the records holding nothing placed leading, trailing and interior -- it carries two mutants for that reason), withheld_line_marks (contract FD11 and plan P4-D83: the shape a line before the table is published as, and the narrowing of a mark the twin could not write, measured on the quotation mark that once left a twin whose first line was a quoted field nothing closes), delimiter_reading (review item CODEX-5 on the file that item was measured on: every setting scored with the delimiter) and workbook_sheet (method G2.2 end to end, every part of the package). Each carries a mutant that moves bytes. The workbook's parts are frozen as TEXT and never as the packed bytes, because the compressed stream differs between library builds, which the landing that wrote the writer states as a limit rather than claiming past it. These cases are given NO WORDS and carry no word budget, and that is a fact about the transforms rather than an omission: not one of them draws a word or reads a column's published facts. The oracle holds no random generator and no seed handling, so the recorded seed is accepted and ignored, and it imports neither synthtwin, nor numpy, nor pandas. The registered generator is the fourth ENTRY POINT rather than the oracle itself, and its imports are stated literally: it imports os, runpy and sys and nothing else, holds no transform and no proof layer of its own, and executes the oracle beside it by one fixed sibling path. Every number this file publishes is a count or a place of a description's own dialect or workbook block, or a count of records in a reading, and each is named among the oracle's whole-number fields; the file publishes no binary64 at all, and the run still refuses a full-generator mutant before it writes a byte. No value in this file comes from any real or synthetic table: every cell, every name and every line is written by hand in the method document's own neutral vocabulary.",
      "path": "tests/reference/generation-document-vectors.json",
      "seed": 0,
      "sha256": "a858ff7fc05eac123b12bf751e4402062678a323d7083c5d10308fe2c7136e96"
    },
    {
      "generator": "tools/reference/make_generation_reference_vectors.py",
      "justification": "The twin cells synthtwin's generation method requires, for the nine cases docs/spec/generation-method-v1.md section G14.3 names, computed from that specification alone. They are committed rather than built at test time for the same reason the numeric vectors are: their whole purpose is to be an oracle the implementation cannot influence, and a value recomputed beside the code it checks could drift with it. The vectors are a pure function of the uint64 words the file itself carries as inputs; the generator holds no random generator and no seed handling, so the recorded seed is accepted and ignored. It imports only argparse, datetime, fractions, json, math, struct and sys - never synthtwin, never numpy, never pandas. numpy could not be imported here in any case: this runner's audit hook refuses ctypes and numpy imports ctypes, which is why the transform from words to bytes is written in exact standard-library integer arithmetic. Every binary64 the file publishes is proved correctly rounded, or proved exact, by integer comparison against the midpoints to its neighbours, and the run refuses a full-generator mutant before it writes a byte. No value in this file comes from any real or synthetic table. Both files gained the mode pair of contract Q18 on every block that carries a ladder, published as the withheld pair -- null beside a count of nought -- because that fact is REPORT-ONLY (plan P4-D4.11). Since landing 2b.1 it steers one rule, the stratum cap of method G5.2a, and a withheld pair hands that cap to the count of different numbers and the ladder; no cell of this file moved when it did. REBUILT AT LANDING 2b.3 (2026-09-15): every block of dates gained n_at_midnight, nought on each of its four date cases, and no cell of any case moved. REBUILT AT LANDING 2b.2 (2026-09-15): every block of numbers gained negative_form (the minus in front) and the decimal_plus census (empty), because no source column these cases describe wrote brackets, another minus or a signed decimal, and no cell of any case moved. MERGED at the integration of landings 2b.1 to 2b.5 (2026-09-15): rebuilt from the one merged oracle, which carries landing 2b.3's count of values at midnight and landing 2b.2's notation of a negative and count of signed decimals side by side; neither landing's rule moved a cell of the other's. REBUILT AT LANDING 2b.17 (2026-09-16), and the ONLY bytes that moved are this file's own account of itself in case_set, which now names the FOURTH file beside it: no case of this file moved a cell and the proof layer proved the same numbers. The rule that forced it is the one saying no file may be read as the whole of the oracle -- each account names every other file -- so a fourth file existing makes every earlier account of three incomplete. The fourth is tests/reference/generation-document-vectors.json, the cases for the transforms that produce a whole document rather than one column's cells.",
      "path": "tests/reference/generation-reference-vectors.json",
      "seed": 0,
      "sha256": "8ee6284dd1042fd3a0dc0d28cc73a28b65a30e8d13966493932f56305ce17b84"
    },
    {
      "generator": "tools/reference/make_numeric_reference_vectors.py",
      "justification": "Reference values for the profiler's mean, standard deviation, skewness and percentile ladder, computed by exact rational arithmetic. They are committed rather than built at test time because their whole purpose is to be an oracle the implementation cannot influence: a value recomputed beside the code it checks could drift with it, and round 1 of the Phase 1 review found exactly that defect in the golden hash. The generator imports only fractions, decimal, json, math and struct - never synthtwin, never numpy, never pandas - and every float64 it reports is proved correctly rounded by exact integer comparison against the midpoints to its neighbours. No value in this file comes from any real or synthetic table.",
      "path": "tests/reference/numeric-reference-vectors.json",
      "seed": 0,
      "sha256": "49807e54670e4988f485df7ef197aafd902b599814d4d928e7ff19bcf78e9e7a"
    }
  ],
  "schema_note": "Fixture allowlist for the synthtwin data-provenance guard (plan D13). Each entry in 'fixtures' binds one committed data fixture to its origin and must contain exactly these keys: 'path' (repository-relative POSIX path of the committed fixture file), 'generator' (repository-relative path of the seeded script that rebuilds the fixture; invoked as: python <generator> --seed <seed> --out <output-path>), 'seed' (integer seed; the same seed must always produce the same bytes on every platform), 'sha256' (64-character lowercase hex digest of the committed fixture bytes), and 'justification' (a written sentence explaining why this fixture must be committed instead of being built at test time). The checker tools/provenance/check_provenance.py re-runs every generator and byte-compares its output to the committed file; any difference fails. Both 'path' and 'generator' must be repository-relative (no absolute paths, no '..' components), must stay inside the repository, and must be tracked by git once the repository has commits. Generators are executed through tools/provenance/guard_runner.py, which installs a Python audit hook before the generator code runs; the hook refuses imports of network, process, and native-capability modules and blocks the known socket, process-creation, and native-call audit events. That runner is a best-effort in-process guard, not a sandbox or a confinement mechanism: repository source review of every generator script and the CI checks are the operative controls (CI is the authoritative automated verification; from Phase 3's visibility flip the branch ruleset makes it a mechanically enforced merge requirement - see SECURITY.md's activation record). JSON is treated as a data format: a tracked .json file is allowed only if it is one of the checker's known configuration files (KNOWN_CONFIG_JSON in the checker) or listed here. Committed fixtures must be tiny; the cap is the checker's own MAX_FIXTURE_BYTES, which carries the record of what it was raised from and why. THIS SENTENCE HAS GONE STALE TWICE and is written as a growth list rather than a number so the next entry has an obvious place to be recorded: it named a cap of 100000 after the checker's had been raised, and it said THREE were committed while four stood in this file. Five are committed today: the numeric reference vectors of Phase 1, and the four files of the generation reference vectors, which are ONE oracle in four files because each must stay under the cap - the fourth added by landing 2b.17 for the transforms that produce a whole document rather than one column's cells. All five are mathematics rather than data - they contain no value taken from any table, and each generator imports none of the code the vectors check."
}
