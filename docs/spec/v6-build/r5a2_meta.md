VERDICT WRONG errors=17

## ERRORS

1. CENTRAL RULE WRONG — AF7's population substitution. THE SECTION's substitution table row ('`n_present`, as the population a statistic was computed over | `n_affixed`') and its 'Concretely' sentence ('`n_left_out_of_statistics == n_affixed - n_core_numeric`', '`numeric_share == (...) / n_affixed`') contradict the key meanings this contract publishes. profile-contract-v4.md:1139-1141 define `n_used_in_statistics` as 'how many present cells the statistics were computed from', `n_left_out_of_statistics` as 'how many present cells were not', and `numeric_share` as 'the share of present cells whose writer meant a number'; v4:1187-1190 (Q9) says 'computed as a share of the present cells'. Under the section's substitution a straggler cell is in NEITHER count, so both keys silently answer for a narrower population than their own definitions. Corrected: the substitution reaches the three numeric-looking CENSUS counts only; `n_present` and `n_rows` are read unchanged.

2. SAME RULE SETTLED TWICE, TWO WAYS. v6-build/r5a.md is a complete, already-written section 6.12 covering exactly this material (C6-6 at :138, C6-7 at :176, C6-8/AF1-AF7 at :200-251, C6-9 at :270, forbidden keys at :297, the remark at :309). It settles the same question the other way: r5a.md:232 'n_left_out_of_statistics == n_present - n_core_numeric', :234 '(n_core_numeric + n_core_out_of_range + n_core_contradictory) / n_present', :225-226 '`n_present` and `n_rows` are read unchanged: they answer for the column's cells on every role', and its key table at :161-162 ('present CELLS that did not, cells wearing no pair included'; 'share of present CELLS'). v6-build/ASSEMBLY.md:128-134 names this as 'the one open question this build has raised that no artifact answers' and requires the affixed-number section to settle it — it cannot be settled two ways in two versions of one section. The corrected text adopts r5a.md's settlement, which is the one v4's key definitions support.

3. X2 WITHDRAWN FROM THIS ROLE. THE SECTION's substitution row 'X2, the cell census | AF4, the core census' says a `count` invariant naming X2 is read here as AF4 — i.e. X2 no longer closes over the cells on this role. That contradicts profile-contract-v6.md:263-275 (C6-7: the four universal counts 'keep the meaning version 4 gives them and answer for the CELLS') and v6-build/r5a.md:195-198 ('X2 closes over the cells here as on every role; AF4 is the core census's own closure'). X2 is a universal invariant (profile-contract-v4.md:1868) and AF4 stands BESIDE it, never in place of it. Corrected in prose.

4. DROPPED — Q10. AF7 reaches `n_negative_unrepresentable` (profile-contract-v4.md:1192-1194: '<= n_out_of_range' and '<= n_negative'), and Q10 is the reading that makes the multi-count substitution load-bearing: v6-build/r5a.md:239-249 shows a conforming ninety/ten milligram column where the un-substituted Q10 is '10 <= 0' and the document is refused. THE SECTION lists Q2, Q9, Q11 and P1 and omits Q10 entirely, and with it the stated reason the substitution reaches three counts rather than one. Restored.

5. STALE CONFLICT — 'C6-6 is SHORT BY ONE'. THE SECTION widens AF7's wording to 'this role's key table' and reports a conflict with C6-6. That is true of the old delta (profile-contract-v6.md:256-261) but FALSE of the build: v6-build/r5a.md:138-174 lists twenty-two added keys INCLUDING `fraction_widths` (:166) and states 'The block is forty-four keys', matching the matrix's 22 `afx` cells (v6-build/r6.md:226, :248-252). AF7 restored to 'every quantitative key of C6-6'; the note to the part-one author is void.

6. DROPPED — the class governs VALUES and does not silence `missing_by_source`. v6-build/r5a.md:289-293: this is not a nothing-publishing role, so an undeclared block names its absent spellings under the floor exactly as `datetime` does, and the class does not reach `missing_by_class`. Absent from THE SECTION; restored as one paragraph under the publication class.

7. DROPPED — the no-regression rider on the priced misroute. phase-4-columns.md:683-686 ('What this misroute can NOT do, by the no-regression rule: it cannot take a column away from any rule that handles it today — V1/V2/V3 visit labels stay categorical, because categorical runs first'), also v6-build/r5a.md:332-334. THE SECTION carries the misroute and drops the rider. Restored.

8. RESTATEMENT OF RULES OTHER SECTIONS OWN. THE SECTION's remark block restates the three arguments and the whole count-not-'every value' argument that v6-build/s45.md:566-589 (form D17) owns word for word, and its `n_unparsed` paragraph restates v6-build/r5a.md:172-174. Compressed to the role's own obligation plus pointers to 4.5.1/4.5.2; the 33-row forbidden-key table is KEPT because it is this section's real added value over r5a.md:297-307, which names only some of the rows.

9. INVENTED, no source — 'There is no suppression path for the pair, no `(withheld)` spelling of it'. Nothing in phase-4-columns.md:643-645, profile-contract-v6.md:289-295 or v6-build/r5a.md:270-281 states it; it is a derivation from the detection-time floor. Kept (it is sound and closes a route a producer could otherwise invent) but it needs owner ratification as a new sentence.

10. INVENTED RATIONALE — the `n_unparsed` and `min_length`/`max_length` arguments. No artifact gives a reason for either forbiddance on this role; v6-build/r6.md:277-287 gives only the differing-populations note for `min_length`/`max_length` across `numeric_unrepresentable` and `identifier`. The `n_unparsed` reason is rewritten to the one an artifact does state (v6-build/r5a.md:172-174, 'a key restating a subtraction'); the third-population argument is retained as reasoning, flagged as this document's own.

11. MISLEADING GROUP HEADING — 'the unrepresentable keys'. `n_negative` also stands on `numeric_unrepresentable` (v6-build/r6.md:218) and is REQUIRED on `affixed_number`; r6.md:265-270 states it is one key with one meaning asked of two populations. THE SECTION's heading invites a reader to forbid it here. A disambiguating sentence added after the table, plus an explicit note that the headings are a reading aid and C6-53's residual rule governs an unnamed key.

12. AF6 decidability — profile-contract-v6.md:1236 lists AF6 in the §9 table UNMARKED, and that table's preamble (:1198-1201) asserts every unmarked row is 'true or false of a parsed document'. THE SECTION says AF6 'fixes a meaning rather than a predicate'. The section's reading is the honest one and matches v4's Q8 (profile-contract-v4.md:1181-1185), so it is kept — but it is a deviation from §9's own claim and the assembler must reconcile the two rather than let both stand.

13. AF-R is coined and has no source row: profile-contract-v6.md:1219-1259 carries AF1-AF7 and AF-P and no remark row for this role, while the sibling DF-R exists at :1226 marked *producer*. Kept, unmarked, because the trigger is the published `role` and is loader-decidable — but the identifier must be checked for uniqueness at assembly, and whether more than one instance of the remark may stand on one column is still stated nowhere (v6-build/s45.md:566-589 fixes no multiplicity; :591-602 fixes 'exactly once' only for the slashed-date form).

14. UNRESOLVED, carried forward unchanged: the affix clause of `remark_no_reading_fits` (v6-build/s45.md:428 argument 6, :453 'a reading needs «3»') names the PARSE LINE, so it does not distinguish the floor-decline path of C6-9 and phase-4-columns.md:568-576; and a column whose pair fell below the floor may land in `long_tail_labels` and carry no such clause at all. The section's hedge to the `free_text` landing is kept; the note-grammar author or the owner must rule.

15. UNRESOLVED, not this section's to fix: profile-contract-v6.md:1136-1151 binds the affix arguments through the note's sibling `column` field on the ground that a note 'does not sit in a column block at all', while v6-build/s45.md:65 files this form at `columns[].remarks[]`, where there is no `column` sibling, and s45.md:68-70 says no rule binds a form to a path. One of the two routes is unresolvable for a guard. The corrected section points at 4.5.1/4.5.2 and states no binding of its own.

16. SHIPPED CODE — checked, no disagreement with this section. `grep -rin affix src/` returns 0 hits: the role is unimplemented, so no shipped constant fixes any enumeration here. Adjacent fact for the assembler: src/synthtwin/taxonomy.py:259 `ROLES_PUBLISHING_RANGES = (ROLE_COUNT, ROLE_CONTINUOUS, ROLE_DATETIME)` names three roles, so C6-9's ranges membership and the `time_of_day` membership both require that tuple to grow when the roles land; the nothing tuple at :260-264 already matches C6-52's three roles.

17. ENUMERATIONS VERIFIED element by element, both directions, against v6-build/r6.md:192-246. Marked for `afx`: rows `percentiles` through `n_core_not_numeric` = 22 cells (r6.md:212-233), matching r6.md:251's stated count of 22 and r5a.md:168-171's forty-four-key block. Unmarked: 55 - 22 = 33. THE SECTION's table lists 5 label + 13 datetime + 2 clock + 5 unrepresentable + 2 length + 6 text/code = 33, and every one of the 33 is an unmarked row and every unmarked row appears exactly once. No element added, none missing. Cross-references to sections 4.5.1, 4.5.2 and 6.11 are pre-assembly placeholders (ASSEMBLY.md:70-77) and must be repointed.

## GAPS

- AF7's population substitution is UNDERSPECIFIED in the source and I resolved it. v6's AF7 says only 'read over the cores and over n_core_numeric in place of n_numeric' and never says what replaces n_present in Q2, Q9 and the census. Given AF4 the only coherent answer is n_affixed, and I wrote that substitution out as a table. This is the one place in the section where I resolved rather than transcribed; it needs owner ratification.
- AF-R is an identifier I COINED, in the AF family, for a rule v6 states only in prose. Section 7B says the remark is 'carried by EVERY affixed_number column without condition' but section 9's invariant table has no row for it, although the sibling DF-R exists for the slashed-date remark. Unlike DF-R, AF-R is loader-decidable (the trigger is the published role), so it is not marked producer.
- Whether MORE THAN ONE instance of remark_affixed_numbers_may_be_codes may stand on one column is nowhere stated. AF-R as written requires at least one and bounds nothing above.
- Cross-references to sections 4.5.1, 4.5.2 and 6.11 are pre-assembly placeholders and must be repointed in the renumbering pass. C6-8 and C6-9 keep the numbers v6 gives them; C6-52 and C6-53 keep r6.md's.
- The group headings in the forbidden-key table ('the label keys', 'the datetime keys', and so on) are reading aids and not normative sets; only the key names are normative. The residual rule - every key not marked in the matrix - is what governs a key named in neither.

## CONFLICTS NOTED

- THE BRIEF vs THE ARTIFACT: this task names the family as 'AF1-AF4'. The artifact has SEVEN numbered members plus AF-P. v6 C6-8 and section 9's table both carry AF5 (n_core_numeric >= 1), AF6 (integer_valued is the routing fact, over cores, never the role name) and AF7 (the quantitative keys read over cores). I transcribed all seven plus AF-P, per the instruction to read the artifact rather than the expected list.
- C6-6's key enumeration is SHORT BY ONE against the matrix. C6-6 lists the quantitative set as fourteen keys ending at numeric_styles and omits fraction_widths, while v6 section 7A's row for the role and r6.md's matrix both require fraction_widths on affixed_number - and the arithmetic confirms the matrix: 3 + 4 + 14 + fraction_widths = 22 = the marked-cell count for afx. AF7 as v6 words it ('every quantitative key of C6-6') therefore does not reach fraction_widths at all. I widened AF7's wording to 'this role's key table' and stated the fraction_widths population explicitly. The part-one author must add fraction_widths to the key table or the two clauses stay in conflict.
- The affix clause of remark_no_reading_fits (s45.md form D11, argument 6) renders 'a reading needs N', naming the PARSE LINE - but the floor-decline path of C6-9 and P4-D4.1 is a different failure from falling short of the line, and that clause does not distinguish them. Worse, a column whose pair fell below the floor may land in long_tail_labels rather than free_text, and then carries no such clause at all. I hedged the section's sentence to the free_text landing; the note-grammar author or the owner should decide whether the floor case needs its own wording.
- v6's C6-ARG resolves the affix argument binding through the note's sibling `column` field, on the stated ground that a note 'does not sit in a column block at all' because notes live in top-level publication_notes. But s45.md files this remark in group D, the columns[].remarks[] surface, where there is no `column` sibling and the block IS the one the remark sits in. Both readings only work because s45 says no rule binds a form to a path. The binding rule should name both routes explicitly, or the guard has one path it cannot resolve.

## SOURCES

Transcribed from profile-contract-v6.md section 3.2, clauses C6-8 (AF1-AF7, element by element) and C6-9 (ranges class, the one exception, the floor); section 9's invariant table rows AF1-AF7 and AF-P (AF-P word for word, including its *producer* marking); section 7A's forbidden-key rule; section 7B's affixed remark, its unconditionality and its count-not-"every value" ruling. Plan phase-4-columns.md P4-D4.1 (floor-governed pair, the core block published as a count/continuous block, endpoints floor-free, the standing remark, the misroute it prices) and the publication-class paragraph (ranges class, one matrix-confined exception, no other ranges key may carry a spelling; detection-time floor; a raised floor or small table withholds the role). The forbidden-key list is the complement of the `afx` column of v6-build/r6.md's 55-row matrix: 22 cells marked for this role, 33 rows unmarked, and 55-22=33 checks out; the 22 also equals C6-6's key count (3 + 4 core counts + 14 quantitative + fraction_widths). Q2/Q3/Q9/Q11/P1/X2 read from profile-contract-v4.md lines 1153-1196, 1698, 1868, 1935-1965. C6-52 and C6-53 cited with the numbers r6.md assigns them; A3 and C6-1 from v6. The remark's rendering and argument binding checked against v6-build/s45.md form D17 so this section states only the role's obligation. Shipped source under src/synthtwin/ has no occurrence of "affix" at all - the role is unimplemented, so no constant fixes any enumeration here.