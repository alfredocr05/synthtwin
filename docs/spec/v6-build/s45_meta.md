VERDICT SOUND_WITH_CORRECTIONS errors=13

## ERRORS

1. VERIFIED CLEAN, reported first so the corrections are read at their real weight. I diffed all 37 inherited renderings the section transcribes unchanged against the shipped text programmatically (importing `taxonomy` and calling `rendered` on every key of `NOTE_ARITY`, src/synthtwin/taxonomy.py:572-611, 781-1082): every one is character-for-character identical, including the `--` double hyphens, the semicolons at taxonomy.py:951 and :958, and the terminal full stops present on E1-E3 and absent on the remarks. The 38 shipped identifiers are all present and none is invented. The census recomputes exactly from the section's own arities: 41 forms, 62 positions, 53 whole numbers, 3 package words, 4 nested forms, 2 bound affix strings; the delta from the material's inherited census (45/39/2/4/0, forms:124) is +17 positions = D7 +2, D11 +4, D14 +1, D16 +1, D17 +3, D18 +5, D19 +1, and +1 word (D18 argument 5) and +2 affix, which closes. The eleven `format` members match docs/spec/profile-contract-v6.md:1620-1623 element by element and in order. The four sentence paths match src/synthtwin/profile.py:659, 666, 676, 694. The eight `relationships` keys match docs/spec/profile-contract-v4.md:448-455. S10, S11 and S12 are transcribed word for word from v4:402-403, v4:405-408 and v4:457-461. The argument labels check out at every call site I read (taxonomy.py:3678-3681, 3691, 3719-3724, 3727, 3741-3746, 3786, 3820, 3826, 3873, 3934-3942, 4005-4010, 4233, 4249, 4563, 4567, 4570). The parse-line definition matches `_needed` at taxonomy.py:1666-1676 exactly. No delta framing of the forbidden kind survives: the strings 'supersedes', 'version 4', 'version 5', 'carried unchanged' and 'Revision 3/4' do not occur.

2. ERROR 1 — INVENTED REFUSAL, CONTRADICTING THE SECTION'S OWN NORMATIVE TEXT. §4.5.1 group C reads 'These are forms like any other and are refused as a leaf of the document'. No artifact refuses a fragment at a leaf. src/synthtwin/profile.py:972-981 checks only Note-ness, form membership in NOTE_ARITY, arity, argument classes and identical re-render — a `said_written_as_numbers` Note standing at `columns[].remarks[]` passes all five. src/synthtwin/taxonomy.py:531-534 says only that the two fragments 'appear inside a longer sentence rather than on their own'. And the section itself states three paragraphs earlier, from src/synthtwin/profile.py:965-970, that 'No rule binds a form to one of those four paths.' Both sentences cannot stand. Corrected to convention, on the same footing as the A–E groupings.

3. ERROR 2 — THE B5/C2 EXAMPLE RULE IS INCOMPLETE AND ITS LAST CLAUSE IS FALSE FOR FIVE OF THE ELEVEN FORMAT MEMBERS. The section says EXAMPLE is 'this package's fixed example spelling of the format argument 3 names, not the format name itself' and then lists six. src/synthtwin/parsing.py:94-95 (`if name not in _FORMAT_EXAMPLES: return name`) returns THE NAME ITSELF for any other member, so on `slashed-iso-date`, `iso-month`, `iso-mixed`, `month-first-datetime` and `day-first-datetime` the rendering is the wire spelling. As written the section states a falsehood about that case AND leaves B5 and C2 with no rendering at all on five of the eleven members a version 6 document may carry — two implementations reading only this text cannot agree. Corrected: the six-row table is given as a table, the fallback is stated as the rule it is, and the need to fix five examples is flagged inside the normative text rather than only in the gaps.

4. ERROR 3 — D7'S TRIGGER IS DECLARED TRANSCRIBED AND IS ABSENT FROM THE TEXT. The gaps say 'I transcribed the shipped trigger unchanged', but the D7 entry states no trigger, while D8, D17 and D18 all state theirs. The shipped trigger is src/synthtwin/taxonomy.py:3773-3780: a date format matched (`matched is not None`) AND `numeric_looking >= strict_needed`. Added, in the shipped form, so the gaps' second half (whether 'compact-date-versus-number' at plan:923 narrows it to `compact-date`) remains the open question it is and does not silently become an omission.

5. ERROR 4 — D11'S STATED COUNT OF FOUR IS AN ENUMERATION OF THREE. 'The four clauses exist because four readings can now fail quietly — a shared affix pair that fell short or was eaten below a line by stand-in removal, a clock shape this version does not describe, and a set of repeated non-numeric spellings that one declaration would turn into gaps' reads as three items joined by 'and'. The four obligations the plan actually fixes are separate: affix reach (docs/plans/phase-4-columns.md:599, :605), stand-in removal across a line (plan:628-630, :634-637), the time-of-day exclusions (plan:701-709) and the recoverable-distribution advice (plan:1708-1719). This is the identical defect the section itself reports in version 6 — 'Three sentences ... are new' at docs/spec/profile-contract-v6.md:998 numbering two at v6:1003-1017 — reproduced in the replacement. Corrected to four named items.

6. ERROR 5 — THE SECTION DECLARES A NOTATION AND THEN DOES NOT USE IT, ON TWO FORMS. The Notation paragraph fixes '«k» marks where argument k is written into the rendering. Renderings are given character for character.' D18's three first-clause renderings and its second clause then write bare `D`, `M`, `X`, `Y` ('read day first, which parses D of these values against the month-first reading's M.'), and D19 writes bare `NUMBER`. A guard rebuilding the string cannot tell a literal letter from an argument slot, which is exactly the ambiguity the notation exists to remove. Version 6 has the same defect at v6:1061-1063 and v6:1073-1075; a self-contained rewrite is where it gets fixed, not carried. Corrected: D18 uses «1»…«4» throughout, and D19 keeps its index argument but writes NUMBER through an explicit 1→`-9999`, 2→`-999`, 3→`9999` table, on the same mechanism B5 uses for EXAMPLE.

7. ERROR 6 — THE §4.5.2 HEADING OVER C6-NG6 AND C6-NG10 IS WRONG. The table is headed 'For the recoverable-distribution clause of `remark_no_reading_fits`', but C6-NG6 binds argument 1, which is present on EVERY instance of the form whether or not the clause is written, and C6-NG10 says of itself that it is 'the base sentence's own precondition'. Only C6-NG7, C6-NG8, C6-NG9 and C6-NG9-P are clause checks, and each of those three carries its own 'where C ≠ 0' guard while the other two do not. Read as headed, a loader would skip C6-NG6 and C6-NG10 on every instance whose argument 9 is zero. Corrected by splitting into two tables; the identifiers are unchanged, so nothing citing them moves.

8. ERROR 7 — THE WORD CLASS WIDENS TO THIRTEEN AND NO CHECK BINDS WHICH WORD MAY STAND WHERE. This hole is created by the section itself: version 6 never wrote the membership out (v6:1128 says only 'one of this package's own words'), and the shipped membership is `NOTE_ARGUMENT_WORDS = parsing.DATE_FORMATS`, six members (src/synthtwin/taxonomy.py:625, src/synthtwin/parsing.py:64-71), tested for membership only at taxonomy.py:697-698 and `_word` at taxonomy.py:727-734. With `day-first` and `month-first` now in the class, a producer can build `evidence_dates` with argument 3 = `day-first`; `parsing.format_example` returns the name itself (parsing.py:94-95), the leaf reads 'are dates written as day-first', and C6-NG14, C6-NG15, C6-NG16 and C6-NG17 all accept it — a false sentence with a true form, which is precisely what §4.5.2's own opening sentence exists to refuse. Added as C6-NG18 plus a clause in C6-ARG class 2, numbered last so no existing identifier moves. DRAFTED HERE: the restriction follows from the form table's own argument descriptions (B5 argument 3 and C2 argument 2 say 'the format name'; D18 argument 5 says 'the reading USED') and from C6-NG5's precedent, but no artifact states it and it needs ratification.

9. ERROR 8 — A DROPPED SOURCE POINTER. v4:433-434 names the file the four required-to-fail mutations are held in: `tests/test_p2c1f3_publication_guard.py`. The section says only 'the publication-guard tests'. The file exists (tests/test_p2c1f3_publication_guard.py). The rest of that paragraph's de-naming — `build_document`, `taxonomy.note`, `taxonomy.NOTE_ARITY`, `profile.check_publication`, `taxonomy.rendered` — is a deliberate and correct change, because the whole point of the rewrite is that the forms are now fixed by §4.5.1 rather than by naming a code table; the test filename carries no such authority and was simply lost. Restored.

10. ERROR 9 — DELTA FRAMING THAT SURVIVED, IN THREE PHRASES. (a) D14's rider: 'would have been false on exactly the columns the widened trigger was added to reach' and 'the columns the widening reaches' — 'widened' and 'was added' name a history this document does not state. (b) D18: 'both carried renderings claiming one reading parsed more than the other' reads as renderings carried from an earlier revision rather than as the other two of the three. (c) D16's closing 'the same tell-the-person-both-ways posture the affixed-column remark takes' inverts plan:679-681, which says the AFFIXED remark takes the posture the all-different remark already had. All three reworded; none changes a rule.

11. PRECISION DEFECT (carried in from the derivation material's own declared gap at forms:32) — FOUR ARGUMENT LABELS NAME THE WRONG DISTINCT COUNT. B8 argument 1, B9 argument 3, D10 argument 1 and D11 argument 4 are all labelled 'different values'. Every one of them is `folded_distinct` — the count after trimming and case folding, published as `n_distinct_folded` — at src/synthtwin/taxonomy.py:3826, :3873, :3820 and :3940 respectively (`folded_distinct = len(cells.folded_counts)`, taxonomy.py:3933). The document publishes both `n_distinct` and `n_distinct_folded` as universal keys (src/synthtwin/profile.py:699-700) and X2/X3 at v4:1868-1869 distinguish them, so a check written from the section's label would bind the wrong key. The rendered sentences say 'different values' and are correct as they stand; only the argument-meaning tables needed repair. Corrected to 'different values, counted after trimming and case folding' at all four.

12. PROVENANCE-TABLE CITATION ERRORS (do not affect the section text; reported so the table can be fixed before it is relied on again). (a) The row 'A2 `one_value_below_the_floor` … arg order tax:4001-4011' is wrong: taxonomy.py:4001-4011 is `_pooled_note`, which builds A4. A2's call site is taxonomy.py:3719-3724. (b) The row 'A4 `labels_pooled_below_the_floor` … arg order tax:3719-3724' is wrong for the same reason and inverted with (a); A4's call site is taxonomy.py:4002-4011. The material's own line (forms:60) lists the three sites in row order 2, 3, 4 and the section's author split it out of order. The argument MEANINGS in the section text are right in both cases (A2 argument 1 is `settings.small_cell_floor` at taxonomy.py:3722; A4's three are suppressed levels, floor, suppressed rows at taxonomy.py:4007-4009). (c) The row 'D8 trigger … forms:11, forms:140 (G2)' is fine, but the paired 'tax:3781-3782' is the guard `if format_name == "month-first-date"` — correct — while the section's text widens it to 'a month-first slashed reading'; that widening is declared in the gaps and is left standing here as an owner call, not silently repaired.

13. UNRESOLVED, NOT CORRECTED, AND I AGREE WITH THE AUTHOR'S HANDLING. (a) The D8/D18 co-occurrence question — nothing in v4, v5, v6 or the plan says whether a column may carry both, and D18's second first-clause rendering (v6:1062) would say the same thing better. No exclusion added, because no artifact supports one; flagged also at forms:140. (b) The D14/D16 widened trigger has no arithmetic anywhere: plan:919-922 names 'all-whole, nearly-never-repeating, or fixed-width leading-zero digit strings' and only the near-uniqueness test is defined (taxonomy.py:4252-4254), so a producer still cannot decide when to fire it. (c) The D19 identifier, arity, argument and rendering are this document's drafting; plan:918-927 requires the form and a repo-wide grep for `remark_` over docs, src and tests returns no identifier for it. I checked the author's arithmetic on the count and it holds: 38 inherited + D17 + D18 + D19 = 41, which is the brief's verified count, and it is reachable ONLY by demoting the recoverable-distribution advice to a clause of `remark_no_reading_fits` (plan:1708-1719) as the author did — version 6's own drafting, with `remark_a_declaration_would_restore_the_distribution` as a fourth new form, gives 42. (d) The C6-NG9 trigger's truth-preservation problem raised at part:146 is a conflict inside the governing plan text and correctly left to the owner. (e) 'Floor-clearing' is defined in-clause, which is what part:136 asked for, so C6-NG8 stands.

## GAPS

- THE 41ST FORM'S WIRE IDENTIFIER, ARITY, ARGUMENT AND RENDERING ARE NOT FIXED BY ANY ARTIFACT. The plan requires the form (plan:918-927: 'a NEW remark fires when a label-class column publishes a level whose spelling is one of the three built-in stand-in numbers ... naming --missing-value as the route'), but a repo-wide grep for `remark_` over docs, src and tests returns no identifier for it, and neither v6 nor the plan gives an arity or a rendering. I coined `remark_a_label_is_a_built_in_stand_in`, arity 1, argument = the one-based position in the fixed list `-9999`, `-999`, `9999` (v6:652), and wrote the rendering. All four of those are this document's drafting and need ratification. I chose an index argument rather than a spelling argument because the argument classes are closed at four and a stand-in spelling is in none of them; I chose arity 1 rather than 0 so the sentence can name WHICH of the three, since the plan's own example is 'the label column's -999'.
- D7's NEW RENDERING IS DRAFTED, NOT TRANSCRIBED. plan:923 requires only that 'the compact-date-versus-number remark states both readings' counts'. The arity change 0 -> 2 follows, and the two counts are the ones already computed at the emission site (tax:3775-3790: `len(pairs)` and `numeric_looking`), but no artifact fixes the resulting sentence. I also could not establish whether the plan's phrase 'compact-date-versus-number' NARROWS the trigger to `compact-date` columns: the shipped guard fires the form for any matched date format when the numeric line is also cleared (tax:3779-3780), and I transcribed the shipped trigger unchanged.
- D11's ARGUMENT ORDER, ZERO-ENCODING AND CLAUSE WORDING ARE DRAFTED. The plan fixes FOUR obligations on this form (affix reach plan:599/605/628-629; stand-in removal plan:628-630/634-637; time-of-day exclusions plan:701-709; recoverable-distribution advice plan:1708-1719) but fixes no argument, no order and no wording for any of them. The arity 5 -> 9, the choice to encode 'clause absent' as a zero argument, the order 6/7/8/9 and the four clause sentences are all mine. The zero-encoding has direct precedent in the shipped table (tax:824-836, 881-891, 892-899), and it is forced by the fact that `rendered` sees only form and arguments and cannot consult `settings`, so a clause's presence must be decidable from the arguments alone. Clause 6 reuses argument 3 (the parse line) rather than adding one; that is also a drafting choice.
- D14/D16's SECOND RENDERING IS DRAFTED, AND THE WIDENED TRIGGER'S OWN DEFINITION IS NOT AVAILABLE. plan:919-922 widens the code-shaped remark to 'all-whole, nearly-never-repeating, or fixed-width leading-zero digit strings ... on repeating code columns too', which makes the inherited opening 'every value in this column is different' false on exactly the columns the widening reaches. I gave both forms one argument (repeated present cells, 0 = all different) and a second rendering. No artifact fixes any of that. Nor does any artifact give the arithmetic of the three new shape tests ('all-whole', 'fixed-width leading-zero'), so a producer cannot yet decide when to fire the widened trigger; only the shipped near-uniqueness test is defined (tax:4253-4254).
- CO-OCCURRENCE OF D8 AND D18 IS UNRESOLVED. Nothing in v4, v5, v6 or the plan says whether `remark_slashed_dates_are_month_first` and `remark_slashed_dates_read_against_your_declaration` may sit on the same column. A column read under `--day-first` where the month-first reading won meets both triggers, and D18's second rendering (v6:1062) then says the same thing better. I wrote each trigger as its own artifact fixes it and did NOT add an exclusion rule, because no artifact supports one. Flagged also at forms:140 (G2).
- D8'S TRIGGER UNDER THE WIDENED SLASHED FAMILIES IS AN INTERPRETATION. The shipped guard is `format_name == "month-first-date"` (tax:3781-3782). A-P4-1 item 2 adds `month-first-datetime` and `day-first-datetime` (plan:1658-1664) and item 1 says 'Ambiguity handling is untouched: month-first tried first, the standing remark ... apply exactly as written' (plan:1645-1646). I read that as extending the standing remark to the ambiguous slashed DATETIME members and wrote the trigger as 'the chosen `format` is a month-first slashed reading'. If 'exactly as written' means the literal shipped guard, my wording is a widening; the plan is genuinely ambiguous here.
- B5/C2's EXAMPLE SPELLINGS EXIST FOR ONLY SIX OF THE ELEVEN FORMAT MEMBERS. `format_example` fixes examples for the six version 4 members (pars:73-80) and returns the NAME ITSELF for anything else, so under v6's eleven-member vocabulary `evidence_dates` would render 'are dates written as slashed-iso-date'. I transcribed the six that exist and stated the rule; the five members A-P4-1 adds (`slashed-iso-date`, `iso-month`, `iso-mixed`, `month-first-datetime`, `day-first-datetime`) have no example fixed by any artifact and one must be fixed before those forms can render.
- ARGUMENT 2 OF THE RECOVERABLE-DISTRIBUTION CLAUSE EQUALS NO PUBLISHED KEY. A declined `free_text` block publishes `n_distinct_by_occurrences` over RAW spellings (v4:1275, F2 at v4:1287) and its `missing_by_source` is empty (F3 at v4:1289-1290), so the coverage count C can be BOUNDED (C6-NG7, C6-NG8) and never equated to a published number. No loader check can confirm the figure itself, and no producer obligation in the shape of FW-P / RM-P (v6:1229-1230) is written for it. I did not invent one.
- C6-NG8's AUTHORITY. 'Floor-clearing' is defined in no contract document (its only occurrences are v6:1012, v6:1026, plan:1703). The verifier declared the check WEAK and asked for the term to be defined in the same clause or the check moved to gaps (part:136). I defined it in-clause as 'at least `small_cell_floor` present cells share the folded spelling' and kept the check. If the owner reads 'floor-clearing' differently, C6-NG8 falls.
- NO LOWER BOUND ON D OR M FOR D18. Neither v6:1110-1113 nor plan:891-892 says whether a slashed column whose cells parse under NEITHER reading (D = M = 0) carries the form; the tie rendering would then read 'both readings parse 0 of these values'. I added no such bound (part:159).
- THE POPULATION OF D18's FOUR COUNTS is bounded only by `n_present` (v6:1103). Whether the intended population is the column's SLASHED cells specifically I could not establish; no count of slashed cells is published anywhere (part:160).
- IDENTIFIER FAMILY FOR THE ARGUMENT-CONSISTENCY CHECKS. B1-B8, C1-C2, D1-D9, P1-P7, Q1-Q9, U1-U5 are all taken by version 4's appendix (v4:1905-1968), so the derivation material's working labels B1-B5 and C1-C5 cannot be used as contract identifiers. I coined `C6-NG1`..`C6-NG17` plus `C6-NG9-P` and `C6-NG13-P`. If the assembler prefers plain `C6-<n>` numbering, renumber these; nothing else in my section depends on the spelling.
- THE FORM TABLE IS PINNED BY NO TEST. The only shipped test over the table asserts `sorted(NOTE_FORMS) == sorted(NOTE_ARITY)` where `NOTE_FORMS = tuple(sorted(NOTE_ARITY))` (tax:615) -- a tautology that cannot detect an added or removed form (forms:28, forms:155). Plan R-P4-15 (plan:1570-1571) relies on 'the exact-shape test over the form table' turning a fifth argument class red; no such test is shipped. Writing the table into the contract makes it the FIRST artifact the code can be checked against, and a test comparing the shipped table to this document's list should land with it.

## DISAGREEMENTS

- THE ONE I WAS ASKED TO RESOLVE, RESOLVED IN THE PLAN'S FAVOUR. The current version 6 makes the recoverable-distribution advice a SEPARATE form, `remark_a_declaration_would_restore_the_distribution`, arity 2, with its own standalone rendering (v6:1026, v6:1049-1053). Plan amendment A-P4-1 item 4 (plan:1708-1719), which I read myself, says the opposite in its first words: 'The competing-readings remark of a declined column GAINS ONE CLAUSE, and its trigger is the arithmetic that makes the advice TRUE rather than hopeful', and closes 'Advisory, routing nothing, BUILT FROM COUNTS THE REMARK ALREADY CARRIES plus the floor, no new settings key.' 'The competing-readings remark' is the inherited form `remark_no_reading_fits` (built by `_competing_readings`, tax:3910-3943). Two independent phrases in that paragraph rule out a standalone form: 'gains one clause' names an existing sentence, and 'counts the remark already carries' is only true of a clause on that remark -- the standalone form re-states a count `remark_no_reading_fits` already carries inside its argument 1 fragment. THE PLAN GOVERNS, so I wrote it as clause 9 of `remark_no_reading_fits` and there is NO form `remark_a_declaration_would_restore_the_distribution` in my table. I kept the v6 rendering's wording for the clause, since the plan fixes no wording and that text was reviewed six times.
- THE ARITHMETIC OF THE VERIFIED COUNT CONFIRMS THAT READING, AND I REPORT IT AS EVIDENCE RATHER THAN AS A COINCIDENCE. The brief's verified count is 41 = 38 inherited + 3 new. Version 6 as drafted names three new forms, which would give 41 only if nothing else changed -- but the plan ALSO requires a new remark that version 6 omits entirely (P4-D4.7, plan:918-927, the label-class built-in stand-in remark). Under version 6's drafting the count would be 42. Under the plan's drafting it is 38 + `remark_affixed_numbers_may_be_codes` + `remark_slashed_dates_read_against_your_declaration` + the label stand-in remark = 41 exactly, with the recoverable-distribution advice demoted to a clause. The verified count is only reachable by following the plan on both points.
- ARGUMENT 1 OF THE RECOVERABLE-DISTRIBUTION ADVICE: v6:1026 says 'how many present cells READ as numbers' (= `n_numeric`); plan:1713-1715 says 'the numeric-looking count the remark already carries, the same count its existing sentences call written as numbers' (= `n_numeric` + `n_out_of_range` + `n_contradictory`). These differ whenever a column holds an out-of-range or contradictory spelling. The plan governs, and three further artifacts agree with it -- v6's OWN rendering of the same form says 'WRITTEN as numbers' (v6:1049-1050), the shipped code records the distinction deliberately (tax:3886-3889), and the amendment's verification record rules on it (docs/plans/reviews/phase-4-amendment-a-p4-1-verification.md:19, item P4-A1-F6, correction C6). I wrote the numeric-looking count. v6:1026 is a wording defect inside its own clause, not an open fork.
- VERSION 6's CHECK 4 FOR THE SLASHED-DATE FORM IS TOO WEAK AND I REPLACED IT. v6:1103-1104 bounds D and M SEPARATELY by `n_present`, which bounds nothing about the four blocks together: n_present=100, D=80, M=80, X=30, Y=30 passes every check version 6 states and needs 110 cells. I wrote the union bound D + Y <= n_present (equivalently M + X <= n_present), from which v6's own check 4 follows. This is the partition material's B4 (part:111), verified there; it is a strengthening of a version 6 clause, not a conflict with the plan, and no plan sentence speaks to it.
- VERSION 6 SAYS 'Three sentences of the profile document are new' (v6:998) and then numbers TWO (v6:1003-1017), the third appearing only in the arity table (v6:1027) and a trailing sentence (v6:1111-1113). Independently reported at forms:33/157 and part:151/163. My section carries no such count sentence, so the defect does not survive into the self-contained text.
- S11 IS NAMED NOWHERE IN VERSION 6. C6-ARG preserves only the two-key shape and S10 by name (v6:1119-1121); the string 'S11' does not occur in v6 at all, and it survives there only by section 2.2's total carrying. In a self-contained document that route does not exist, so I transcribed S11 from v4:405-408 in full, with its reason. Reported because a reader comparing my section against version 6 will find one more invariant than version 6 mentions, and that is correct rather than an addition.
- VERSION 6's SUPERSESSION ROW IS MISNAMED, AND THE MISNAMING IS WHAT MAKES THE SELF-CONTAINED REWRITE NECESSARY. v6:169 and v6:1117-1118 name the superseded item 'the `publication_notes` note grammar'. Version 4 §4.5 fixes the grammar over FOUR surfaces (v4:422-424), and the guard binds exactly four paths (prof:659, 666, 676, 694). Only 6 of the 38 inherited forms are ones the producer sends to `publication_notes`, and all three forms version 6 adds are `remark_*` forms. Read literally, version 6 as drafted deletes the authority for `detection_evidence`, `remarks` and `source.header_evidence` and replaces it with three remark forms. My section states the four paths explicitly at the head of 4.5.1 so the scope cannot be misread again.
- THE FORM-TO-SURFACE GROUPING IS NOT A RULE, and I marked it as convention. The derivation material's own headings assert bindings ('The six `publication_notes` forms', 'The ten `detection_evidence` forms'); prof:965-970 explicitly declines to check which form stands at which path. I carried the groupings as a reading aid with that disclaimer stated in the normative text (forms:23, forms:47).
- NOT TRANSCRIBED, RAISED FOR THE OWNER (from part:146). The recoverable-distribution trigger as the plan words it clears the parse line on the NUMERIC-LOOKING count, which does not deliver the clause's own promise that 'this column's distribution will be described': survivors clearing the line on numeric-looking but not on holdable cells take `numeric_unrepresentable` and publish no statistic (tax:3674-3676, 3685), and survivors with one or two distinct values take `constant`/`binary` ahead of the numeric rule (tax:3623-3626, 3736-3752). The truth-preserving trigger would be `n_numeric` >= line(n_present - C) PLUS survivor distinctness of at least three -- the first loader-decidable, the second not. plan:1710 calls the trigger 'the arithmetic that makes the advice TRUE rather than hopeful', so this is a conflict inside the governing text and an owner call. I transcribed the plan's trigger as C6-NG9 and did not silently repair it.

## SOURCES

Every rule in the section, and where it came from. Paths are relative
to the repo root `/Users/alfredowork/Library/CloudStorage/OneDrive-UniversityofIowa/Research/0- Synthetic dataset/synthtwin`.
`v4` = `docs/spec/profile-contract-v4.md`, `v5` = `docs/spec/profile-contract-v5.md`,
`v6` = `docs/spec/profile-contract-v6.md`, `plan` = `docs/plans/phase-4-columns.md`,
`tax` = `src/synthtwin/taxonomy.py`, `prof` = `src/synthtwin/profile.py`,
`pars` = `src/synthtwin/parsing.py`, `forms` = `docs/plans/reviews/material/phase-4-v6-derivation-forms.md`,
`part` = `docs/plans/reviews/material/phase-4-v6-derivation-partition.md`.

| rule / text in my section | source |
|---|---|
| §4.5 two-key shape table (`column`, `note`) | v4:395-400; v6:1119-1121 |
| "No third key may appear" | v4:395 ("exactly two keys"); prof:664-666 (`_OBJECT` with exactly those two paths) |
| Invariant S10 | v4:402-403, restated v4:1850; v6:1120 |
| Invariant S11 + its reason (canonical bytes / no shuffling) | v4:405-408, restated v4:1851 |
| The publication guard paragraph (producer obligation, plan P2-D2, item P2-R5-F5) | v4:410-419 |
| "How the shipped producer meets it" (item P2-C1-F3), four required-to-fail mutations, loader implements none | v4:421-439; prof:953-981 |
| The four sentence paths | prof:659, 666, 676, 694; forms:134 |
| "What the guard does NOT check" / groupings are convention | prof:965-970; forms:47, forms:23; grouping comments tax:511, 519, 531-534, 538, 556-561 |
| C6-ARG — classes closed at FOUR | v6:1127-1130; plan A-P4-7 :1995-1998 |
| class 1, whole number, booleans refused + reason | tax:674-714 (esp. 691-696), tax:718-721; v4:425-426 |
| class 2, package word | v4:425-426; tax:625; pars:64-71 |
| word-class membership: the eleven `format` members | v6:1620-1623 (transcribed element by element) |
| word-class membership: `day-first`, `month-first` | v6:1027, v6:1092-1093 |
| class 3, nested form + "no origin" reason | tax:699-713; tax:3881-3889 |
| class 4, bound affix string; POSITIONAL binding; swapped-pair counterexample | v6:1132-1141; plan A-P4-7 :2000-2005 |
| resolution through the sibling `column`, not an enclosing block | v6:1142-1151; prof:665 (`_KNOWN_NAME`) |
| "what this class costs" (original property gone; narrower replacement) | plan A-P4-7 :2007-2016; v6:1152-1162 |
| census: 41 forms, 62 positions, 53/3/4/2 | computed from the arities in this section; inherited base 45 verified at tax:573-610 and forms:124 |
| A1 `no_values_unrepresentable` id/arity/rendering | tax:512 / tax:573 / tax:781-785 |
| A2 `one_value_below_the_floor` | tax:513 / tax:574 / tax:786-791; arg order tax:4001-4011 |
| A3 `one_of_two_labels_below_the_floor` | tax:514 / tax:575 / tax:792-798; arg order tax:3741-3746 |
| A4 `labels_pooled_below_the_floor` | tax:515 / tax:576 / tax:799-805; arg order tax:3719-3724 |
| A5 `free_text_publishes_no_values` | tax:516 / tax:577 / tax:806-811 |
| A6 `identifier_publishes_no_values` | tax:517 / tax:578 / tax:812-818 |
| B1 `evidence_every_value_absent` | tax:520 / tax:579 / tax:819-823 |
| B2 `evidence_numbers_none_holdable` (two branches) | tax:521 / tax:580 / tax:824-836 |
| B3 `evidence_one_value` | tax:522 / tax:581 / tax:837-840 |
| B4 `evidence_two_values` | tax:523 / tax:582 / tax:841-845 |
| B5 `evidence_dates` + EXAMPLE rule | tax:524 / tax:583 / tax:846-851; example table pars:73-80 |
| B6 `evidence_counts_things` | tax:525 / tax:584 / tax:852-856 |
| B7 `evidence_written_as_numbers` | tax:526 / tax:585 / tax:857-861 |
| B8 `evidence_set_of_categories` | tax:527 / tax:586 / tax:862-868 |
| B9 `evidence_no_reading_fits` | tax:528 / tax:587 / tax:869-875 |
| B10 `evidence_declared_identifier` | tax:529 / tax:588 / tax:876-880 |
| C1 `said_written_as_numbers` (two branches) | tax:535 / tax:589 / tax:881-891 |
| C1 "written as, not read as" reason | tax:3886-3889 |
| C2 `said_read_as_dates` (two branches) + arity-2-not-both-load-bearing note | tax:536 / tax:590 / tax:892-899; forms:159 (G8) |
| fragments are never a leaf | tax:531-534, tax:3897, tax:3907 |
| D1 `remark_values_out_of_range` | tax:539 / tax:591 / tax:900-907 |
| D2 `remark_values_contradictory` | tax:540 / tax:592 / tax:908-916 |
| D3 `remark_rare_sentinels_unnamed` | tax:541 / tax:593 / tax:917-923 |
| D4 `remark_too_few_holdable_numbers` | tax:542 / tax:594 / tax:924-935 |
| D5 `remark_two_values_differ_in_case` | tax:543 / tax:595 / tax:936-940 |
| D6 `remark_two_values_also_read_otherwise` | tax:544 / tax:596 / tax:941-947 |
| D7 `remark_dates_also_read_as_numbers` — inherited text | tax:545 / tax:597 / tax:948-952 |
| D7 arity 0 → 2, "states both readings' counts" | plan P4-D4.7 :923 |
| D7 the two counts available at the emission site (`len(pairs)`, `numeric_looking`) | tax:3775-3790 |
| D7 new rendering wording | DRAFTED HERE — no artifact fixes it (see gaps) |
| D8 `remark_slashed_dates_are_month_first` | tax:546 / tax:598 / tax:953-959 |
| D8 trigger, and that it is not false under `day_first` | tax:3781-3782; plan:1645-1646; plan P4-D4.6 :884-892; forms:11, forms:140 (G2) |
| D9 `remark_values_differ_in_case` | tax:547 / tax:599 / tax:960-964 |
| D10 `remark_close_to_the_category_line` | tax:548 / tax:600 / tax:965-970; arg order tax:3819-3821 |
| D11 `remark_no_reading_fits` — inherited arity 5, args, base rendering | tax:549 / tax:601 / tax:971-996; `_competing_readings` tax:3910-3943 |
| D11 "reason is a set of counts, not a verdict" (item P1-R6-F7) | tax:3919-3927 |
| D11 clause 6, affix reading "how far it got" | plan:599, 605; plan:628-629, 634-635 |
| D11 clause 7, stand-in removal across a line | plan:628-630, 634-637 |
| D11 clause 8, time-of-day exclusions | plan:701-709 |
| D11 clause 9, recoverable-distribution advice AS A CLAUSE | plan A-P4-1 item 4, :1708-1719 (governs over v6:1026, 1049-1053) |
| D11 clause 9 wording (adapted from the superseded standalone form) | v6:1049-1053 |
| D11 argument order, zero-encoding, composition rule | DRAFTED HERE; zero-encoding precedent tax:824-836, 881-891, 892-899; composition-exactness precedent v6:1077-1083 |
| D12 `remark_some_values_are_not_numbers` | tax:550 / tax:602 / tax:997-1002 |
| D13 `remark_close_to_the_numeric_line` | tax:551 / tax:603 / tax:1003-1009; arg order tax:4246-4251 |
| D14 `remark_every_number_is_different` — inherited text | tax:552 / tax:604 / tax:1010-1018 |
| D14 shipped trigger (near-uniqueness) | tax:4253-4255 |
| D14/D16 trigger widened to repeating code columns | plan P4-D4.7 :919-922; forms:149 |
| D14/D16 second rendering + argument | DRAFTED HERE — no artifact fixes it (see gaps) |
| D15 `remark_spread_out_of_range` | tax:553 / tax:605 / tax:1019-1030 |
| D16 `remark_every_value_is_different` — inherited text | tax:554 / tax:606 / tax:1031-1049; emission tax:3991-3992 |
| D16 extends to the affixed-number role verbatim | plan:682-683 |
| D17 `remark_affixed_numbers_may_be_codes` id/arity/args | v6:1025 |
| D17 rendering | v6:1032-1038 |
| D17 unconditional + reason (three withdrawn inferences) | v6:1003-1010; plan P4-D4.1 :669-682 |
| D17 "the count is in the sentence" (P4-X5-F7) | v6:1040-1048 |
| D18 `remark_slashed_dates_read_against_your_declaration` id/arity/args | v6:1027 |
| D18 three first-clause renderings, incl. the tie | v6:1054-1070 |
| D18 second clause + trigger | v6:1072-1075; plan:900-909 |
| D18 exact composition | v6:1077-1083 |
| D18 carried once per slashed column read under the option | v6:1110-1113; plan:896-898 |
| D18 two-independent-clauses reason | plan P4-D4.6 :893-911 |
| D19 `remark_a_label_is_a_built_in_stand_in` — the obligation, trigger, `--missing-value` route, routes nothing | plan P4-D4.7 :918-927 |
| D19 the three built-in stand-in numbers and their order | v6:652; pars:58 (`NUMERIC_SENTINELS`) |
| D19 identifier, arity, argument and rendering | DRAFTED HERE — no artifact fixes them (see gaps) |
| E1 `header_names_because_you_said_so` | tax:562 / tax:607 / tax:1050-1054 |
| E2 `header_data_because_you_said_so` | tax:563 / tax:608 / tax:1055-1060 |
| E3 `header_names_by_convention` | tax:564 / tax:609 / tax:1061-1072 |
| E4 `header_names_shown_by_a_column` | tax:565 / tax:610 / tax:1073-1077 |
| E "why the header verdict is a form at all" | tax:556-561 |
| header forms built at | `src/synthtwin/reading.py`:337, :338, :345, :987-988 |
| §4.5.2 opening ("an argument that can disagree…") | v6:1085-1088 |
| parse-line count, defined arithmetically | tax:1666-1676 (`_needed`); tax:1158-1159; plan:395; setting at v4:286 |
| floor-clearing, defined | plan:1711-1713; v6:1012; term flagged undefined at part:27, part:136 |
| C6-NG1 | v6:1127-1130 (argument class); part:108 (B1) |
| C6-NG2 + counterexample D=90,M=80,X=10,Y=20 | v6:1095-1102; part:109, part:113 |
| C6-NG3 | v6:1094; part:110 |
| C6-NG4 (union bound) + counterexample n=100,D=80,M=80,X=30,Y=30 | part:111, part:113 (REPLACES the weaker v6:1103-1104) |
| C6-NG5 | v6:1092-1093; part:112; DF-P at v6:1225; plan:885-892 |
| "these five are complete" | part:114 |
| C6-NG6 (the numeric-looking equality) | part:123-130; plan:1713-1715; tax:2383-2387; tax:3897; v4:1868 (X2) |
| C6-NG7 | part:132-133 |
| C6-NG8 | part:135-136 (declared weak; term now defined in-clause) |
| C6-NG9 + C6-NG9-P | part:138-139; producer-marking pattern v6:1198-1207, v6:1230 |
| C6-NG10 + its `free_text` scope caveat | part:141-143; tax:3861-3864, tax:3800-3801, tax:3674-3676 |
| C6-NG11 | v6:1106-1107 |
| C6-NG12 | v6:1132-1141; plan:2000-2005 |
| C6-NG13 / C6-NG13-P | DRAFTED HERE from plan:918-927 and v6:652 |
| C6-NG14–C6-NG17 (form known, arity matches, arguments enumerated, re-render identical) | prof:974-981; v6:1110-1111 |
| C6-NG17 "the one check that is not a pattern" + reason | prof:953-965 |
| §4.6 eight keys and their values | v4:443-455 |
| Invariant S12 + the loader's message | v4:457-461, restated v4:1852, v4:1437 |
| "Why the block exists empty" + the one dispatch seam (plan P2-D5) | v4:463-469 |
| "version 6 is defined as a version in which all eight are null" | v4:468-469 adapted; C6-43 at v6:892-894 |
| "No cross-column fact enters this version" + the enumerated list | v6:892-894; `synthtwin/CLAUDE.md` "Honest limits" |
| v5 changes nothing in §4.5 or §4.6 | v5:163 (carries "the top-level structure of its 4.1 to 4.6"); no occurrence of `NOTE_ARITY`, `publication_notes` grammar or any form identifier in v5 |
| the 38 inherited forms are the version 4 table unchanged | forms:41 — proven by diff of `NOTE_ARITY` and all 38 `rendered()` branch bodies between commit 4f6d12c and HEAD |