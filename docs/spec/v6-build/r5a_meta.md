VERDICT SOUND_WITH_CORRECTIONS errors=18

## ERRORS

1. FACTUAL ERROR IN A NORMATIVE WORKED EXAMPLE (the C6-5 illustration is false against the classifier the same section fixes). The section says: '`5 mg` and `5mg` wear different pairs, ` mg` and `mg`'. They wear the SAME pair. `parse_number` strips surrounding whitespace (src/synthtwin/parsing.py:528, `body = text.strip()`), so `5 ` reads as the number 5; the longest-then-leftmost parsing substring of `5 mg` is therefore `5 ` (two characters), not `5`, and the split is ('', '5 ', 'mg'). Ran the section's own rule over the shipped classifier: `5 mg` -> ('', '5 ', 'mg'); `5mg` -> ('', '5', 'mg'); `5  mg` -> ('', '5  ', 'mg'); `$ 1,200` -> ('$', ' 1,200', ''); `$1,200` -> ('$', '1,200', ''). The consequence is load-bearing in the opposite direction from what the section claims: a real column mixing `5mg` and `5 mg` is a ONE-PAIR column that TAKES this role, where the section's sentence sends it to the mixed-affix decline of C6-5. Corrected: the false example is removed, `mg`/`MG` and `$`/`EUR` (both true) carry C6-5, and the whitespace consequence is stated with its reason.

2. FACTUAL ERROR IN THE AF7 JUSTIFICATION. The section claims that under the narrow reading 'Q9 publishes `numeric_share: 0.0` on a column carrying a full ladder' (repeated twice, in the section body and in the reported conflict). False. C6-7/AF7 as version 6 writes it DOES substitute `n_core_numeric` for `n_numeric`, so Q9 computes (n_core_numeric + n_out_of_range + n_contradictory)/n_present = (90 + 0 + 0)/100 = 0.90 on the milligram column, not 0.0. 0.0 is what you get with no substitution at all, which is not what the narrow rule says. Verified the census by hand and against the shipped classifier: classify_number('-1e999 mg') is NOT_A_NUMBER and number_out_of_range('-1e999 mg') is False, so n_out_of_range is 0 for the cells; classify_number('-1e999') is NUMBER_OUT_OF_RANGE, so n_core_out_of_range is 10. The widening is still REQUIRED, but by Q10 alone: 'n_negative_unrepresentable <= n_out_of_range' is 10 <= 0 and the loader REFUSES a conforming document. Corrected: Q10 is named as the clause that refuses, and Q9's true narrow value 0.90 replaces 0.0.

3. C6-COMPLETE VIOLATION — nine sites of delta framing, one of them making a version 6 obligation depend on version 4's text. AF7: 'obeys the invariant version 4 gives it on `count` and `continuous`'; the AF7 table header 'version 4 invariant'; 'read exactly as version 4 writes them'; 'P2 and P4 unchanged'; 'every column it claims published nothing at all under version 5'; 'Since such a column published nothing under version 5'; 'as it does today'; 'a plain numeric column can take today'; 'runs where it runs today'. s1.md:45-57 (C6-COMPLETE) forbids exactly this — 'no clause of the form "as version 4 has it"' — and ASSEMBLY.md:96-99 makes it standing check 2. Evidence this section is the outlier: `grep -c 'version 4\|version 5'` over every written section body returns 0 for r1.md, r2.md, r3.md, r6.md, s3.md, s4.md, s45.md and s5.md; only s1.md (the versioning section itself) mentions them. Corrected: AF7 and its table point at section 6.7, which states Q1-Q11 in this document; the rest are rewritten without the version reference while keeping the reason.

4. DANGLING CROSS-REFERENCES — four clause identifiers dropped that sibling sections already cite. The section carries C6-7 and C6-9 but silently drops C6-4, C6-5, C6-6 and C6-8. s1.md:196-197 cites C6-4 and C6-5 as the clauses that define the glossary terms 'the affix pair' and 'the core'; profile-contract-v6.md:1397-1399 (disclosure row 2) cites 'C6-4's detection rule'; profile-contract-v6.md:988 cites 'the quantitative set of C6-6'. ASSEMBLY.md:100 makes 'every cross-reference resolves' a standing check. Corrected: C6-4 on the detection rule, C6-5 on the pair's identity, C6-6 on the added-key table, C6-8 on the invariant list — the same six-clause sequence r6.md and s1.md were written against.

5. AF-P LOST ITS CONTENT. profile-contract-v6.md:1222 states AF-P as '*producer*: the pair is the pair the source cells wore, and every core is the cell's longest-then-leftmost parsing substring'. The section's AF-P drops the longest-then-leftmost clause entirely and replaces it with a parse-line claim. That clause is the only thing tying the published core census to the split rule the detection subsection fixes; without it a producer may publish any substring as a core and no rule contradicts it. Corrected: the clause is restored, scoped to the counted cells (the cells that were affixed numbers wearing the pair), which is the only scope on which it is true once the pair-wearing closure below admits a cell with a non-numeric core.

6. SECTION NUMBER CONTRADICTS THE ASSIGNMENT. The head reads '### 6.13'; the assigned slug is 'write:6.12-affixed-number'. The transcriber overrode its own assignment on a reasoned guess (r6.md has taken 6.10 and 6.11, so the three new roles follow at 6.12/6.13/6.14 and rule order would put `time_of_day` first). Set to 6.12 to match the assignment. ASSEMBLY.md:70-76 says the assembled document renumbers once in reading order and repoints every cross-reference in the same pass, so this number is a placeholder either way — but all three new-role heads must be fixed together, and r2.md:5 ('the section for `long_tail_labels`', no number) must be filled in at the same time.

7. NOTE-FORM LABELS THAT WILL NOT SURVIVE ASSEMBLY. The section cites 'form D11, argument 6', 'form D11, argument 7' and 'form D17'. ASSEMBLY.md:60-66 renumbers the 41 note forms NG1-NG41 in one sequence precisely because A, B, D and E already name invariant families in this same document, so D11 and D17 are labels that disappear. Corrected: the forms are named by their wire spellings — `remark_no_reading_fits` (arguments 6 and 7) and `remark_affixed_numbers_may_be_codes` — which survive the renumbering and are what s45.md:418, :566 and the guard rows C6-NG11/C6-NG12 at s45.md:796-797 key on.

8. MEANING CELL NARROWED. `n_core_out_of_range` is given as 'CORES too large or too small for binary64', dropping 'well-formed numbers', which is the whole of what separates that class from `n_core_not_numeric`. s5.md:5.1 writes the cell census as 'present cells that are well-formed numbers too large or too small for binary64', and src/synthtwin/parsing.py:663-700 splits on exactly that. Same for `n_core_contradictory`, aligned to s5.md's 'written in numeric notation whose meaning conflicts with itself'. Corrected in the key table.

9. IMPRECISION IN THE ADDED-KEYS HEADER. 'the fifteen a `count` or `continuous` block carries, computed over the cores' is false of one of the fifteen: `n_rows` is the table's row echo and is computed over nothing. Corrected to 'the quantitative ones computed over the CORES'. The count itself is right and was checked both ways: version 4 §6.7 lists fourteen added keys (v4:1129-1144) and C6-27 adds `fraction_widths`, giving fifteen, and r6.md's matrix marks 22 cells in its `afx` column (15 + this role's 7), with r6.md's own per-role tally reading '`affixed_number` 22'. 22 + 22 universal = the forty-four the section states.

10. CONFIRMED — the section's reported conflict about AF7/C6-7 being TOO NARROW is real, and the two sibling sites must be edited in the same pass or the defect survives in the form that killed six rounds. profile-contract-v6.md:274-276 and :285-288 both say the substitution is over `n_core_numeric` 'and nowhere else'; r6.md's last matrix bullet repeats it verbatim ('Every quantitative invariant this format states over `n_numeric` is read on that role over `n_core_numeric`, and nowhere else (AF7)'); s5.md:5.1's paragraph 'The four numeric census counts answer for CELLS' carries the identical sentence. Note that the defect survives BOTH readings of 'and nowhere else' — whether it means 'no other count is substituted' or 'no other role puts in place of' — because neither redirects Q10's `n_out_of_range`. Both sibling sentences must be widened to the three-count form.

11. CONFIRMED — the section's reported conflict about the all-different remark is real, and s45.md is the defective site. s45.md:561-564 hangs 'This form is carried on every role its trigger reaches, the affixed-number role included' on D16 `remark_every_value_is_different`, whose rendering (s45.md:541-556) says 'none of the forms synthtwin can read fits them' and 'Nothing from this column is published either way -- no value of it, and no distribution'. Both clauses are false of an `affixed_number` column, which publishes a pair and a full core distribution. D14 `remark_every_number_is_different` is the one whose wording fits ('the column is described as numbers, which keeps its distribution'), and the shipped code splits them exactly that way: src/synthtwin/taxonomy.py:4026 fires REMARK_ALL_DIFFERENT_TEXT inside the free-text verdict, :4289 fires REMARK_ALL_DIFFERENT_NUMBERS inside the numeric verdict. The section correctly declines to pick and defers to 4.5; 4.5 must move the sentence to D14 or state why not.

12. CONFIRMED — the section's reported conflict between C6-4's floor clause and AF2 is real and both quantities are in the corrected text unreconciled, as they must be until the owner rules. C6-4 (v6:236-237) puts the floor on 'the pair's cell count', which in that sentence is the count of affixed NUMBERS wearing it; AF2 (v6:265-266) bounds `n_affixed`, which the pair-wearing closure makes larger. A pair borne by ten numeric cells and one `many mg` cell publishes `n_affixed: 11` and clears AF2 at a floor of eleven while ten cells evidence the pair. C6-9's disclosure argument ('a published pair is always a floor-cleared fact') and phase-4-columns.md:645-647 both rest on the tighter reading, so the loose one weakens a privacy claim.

13. CONFIRMED AS INVENTED AND NOT YET ROUTED — the pair-wearing closure ('begins with `affix_prefix`, ends with `affix_suffix`, is at least as long as the two together; its CORE is the text between them'). No sentence of profile-contract-v6.md or phase-4-columns.md fixes which cells wear an already-fixed pair or what a non-numeric core is. It is necessary — the term table (v6:193) defines 'the core' only through the classifier, so without this closure a cell like `many mg` has no core, AF4 is not computable, and `n_core_not_numeric`, `n_core_out_of_range` and `n_core_contradictory` are all necessarily zero with AF4 a tautology. Kept in the corrected text because the alternative is an uncomputable invariant, but s1.md:40-43 requires a shape fixed here rather than inherited to be recorded in the decisions section, and this one is not. The decisions section owes it a row.

14. GAP CONFIRMED AND BLOCKING, exactly as declared: there is no detection-evidence note form for `affixed_number`. `detection_evidence` is a REQUIRED universal key (s5.md:5.1) whose string may be built only from the closed grammar, and that grammar's detection-evidence group is exactly ten forms B1-B10 (s45.md:206-290). Counted the whole grammar independently: A 6 + B 10 + C 2 + D 19 + E 4 = 41, matching the census at s45.md:152. src/synthtwin/taxonomy.py:520-529 ships exactly ten EVIDENCE_ constants, all version-4 roles. Neither profile-contract-v6.md nor phase-4-columns.md adds one. Three forms are owed (`affixed_number`, `time_of_day`, `long_tail_labels`) and the census '41 forms, 62 argument positions' restated with them.

15. FURTHER GAP THE SECTION DID NOT DECLARE. phase-4-columns.md:568-571 rules that 'every new line this plan draws is a named `Settings` constant recorded in every profile, applied as a count, with a borderline remark within the standing slack'. No borderline-remark form exists for the affix line: the grammar has D13 `remark_close_to_the_numeric_line` for the numeric line and D10 `remark_close_to_the_category_line` for the category line, and nothing for this one. The section neither claims one nor owes one, so its text is safe, but the plan's rider is unsatisfied and belongs beside the detection-evidence gap.

16. TERM COLLISION FOR THE ASSEMBLER, not a defect of this section. Version 4 §7.5.4 step 1 (v4:1631-1634) names its own intermediate result 'the **core**' — the trimmed, de-bracketed, comma-stripped cell used to pick a numeric style — while this document's glossary (v6:193, s1.md:197) gives 'the core' to the affix core. Both meet in this very section, where `numeric_styles` is a census of affix cores classified by the style ladder. ASSEMBLY.md:88-91 already lists 'numeric_styles in full' as unwritten; whoever writes it must rename the local term.

17. ASSEMBLY.md:31 MISCOUNTS THIS ROLE'S INVARIANT FAMILY as 'AF1-AF4' (and the task brief's identifier rule repeats it). profile-contract-v6.md:1231-1237 states AF1 through AF7, plus AF-P at :1222, and r6.md cites AF7 by name in two places. AF1-AF7 stands; the ASSEMBLY.md range is wrong and should be corrected there so the identifier-uniqueness sweep does not delete three live invariants.

18. SHIPPED CODE — NO DISAGREEMENT IS POSSIBLE, and the two facts the section rests on were verified directly rather than assumed. `grep -rn affix src/synthtwin/` returns 0 lines and taxonomy.py:206-215 and :235-244 carry ten roles with no `affixed_number`, so the role is entirely unbuilt and no shipped behaviour can contradict the section. The leaned-on facts hold: parsing.py:523-561 confirms parse_number trims, accepts valid group separators (verified `1,200.00` -> 1200.0 through _without_group_separators at :473-511), accepts a leading plus and accounting parentheses, and refuses a sign inside them; parsing.py:663-700 confirms classify_number's outcome set is exactly four, which is why the core census has four counts. Both worked examples were executed against the shipped classifier and are exact: `$1,200.00` -> ('$', '1,200.00', ''); `-12-34` -> two candidates tied at three characters, `-12` at offset 0 and `-34` at offset 3, leftmost wins.

## GAPS

- BLOCKING, and it is not mine to close: there is NO detection-evidence note form for `affixed_number`. `detection_evidence` is a REQUIRED universal key (s5.md:5.1) whose string may be built only from the closed 41-form grammar, and that grammar's detection-evidence group is exactly ten forms B1-B10 (s45.md:206-290), all of them version-4 roles; taxonomy.py:520-529 confirms ten shipped constants. Neither profile-contract-v6.md nor the plan adds one. A version 6 producer therefore cannot write a conforming block of this role — nor of `time_of_day` or `long_tail_labels`, and r3.md:52 already relies on a long-tail evidence sentence that has no form. Three forms must be added (and the census '41 forms, 62 argument positions' at s45.md:152 restated) before this section is satisfiable. My text says only that the block carries the sentence; it does not invent its wording.
- The section NUMBER 6.13 is a guess. No artifact fixes the numbering of the three new role sections (r2_meta.md:29,37 records this explicitly). r6.md has already claimed 6.10 for the publication class and 6.11 for the matrix, so the three new roles must follow them: 6.12 `time_of_day`, 6.13 `affixed_number`, 6.14 `long_tail_labels` is the only assignment consistent with what is written. If the assembler chooses otherwise, renumber this head and edit r2.md:5, which still says 'the section for `long_tail_labels`' with no number.
- No tie rule exists where TWO pairs each clear the parse line, and the case is reachable: `minimum_parse_rate` is permitted anywhere in 0.0-1.0 (s4.md), and at a rate at or below 0.5 a 40/40/20 column has two pairs each at or above the line with each other's cells inside the slack. C6-4 says 'ONE affix pair' and C6-5 only forbids a second pair PAST the slack, so nothing selects between them and two producers can disagree — the exact defect C6-10 closes for the two clock forms by naming the finer one. I did not invent a rule. The owner must add one (most cells, then code-point order of the prefix then the suffix, is the shape that matches C6-10's reasoning).
- The rule for WHICH CELLS wear an already-fixed pair is stated nowhere in v6 or the plan. C6-6 says `n_affixed` counts 'present cells that wore the pair' and AF4 makes the four core-class counts sum to it, but the term table defines 'the core' only for a cell the classifier reads as a number, so a pair-wearing cell with a non-numeric core has no defined core and AF4 is not computable. I supplied the only closure that makes AF4 and `n_core_not_numeric` mean anything — begins with the prefix, ends with the suffix, long enough that the two do not overlap, core is what lies between — and marked it in the text. It needs owner ratification, because without it `n_core_out_of_range`, `n_core_contradictory` and `n_core_not_numeric` are all necessarily zero and AF4 is a tautology.
- No invariant binds `n_core_numeric` to the parse line, so AF3 is strictly weaker than C6-4's own test: a document with `n_affixed` at the line and `n_core_numeric` of 1 satisfies AF1-AF7 and describes a column the detection rule would never have claimed. I stated the true line as producer obligation AF-P rather than making up an invariant, because the two populations differ (a cell can wear the pair with a holdable core without its OWN longest-then-leftmost split yielding that pair — pair ('-',''), cell `-5`). If the owner wants it loader-checkable, the invariant to add is over `n_core_numeric`, not over `n_affixed`.
- Section 5.6 as version 4 writes it opens 'Two fields carry a ladder: `percentiles` on the numeric roles and `date_percentiles` on `datetime`' (v4:668-671). Version 6 has THREE ladder fields and `percentiles` now stands on a third role. Whoever writes section 5.6 must restate that sentence; my section cites 5.6 for `percentiles` and L1-L3 on the assumption it will be.
- Section 9's disposition matrix has no group for this role (v4:9.4 covers `count` and `continuous`, 9.7 the invention roles). My key table carries the dispositions inline, taken from v4:2058-2071 and v6:1168-1175, but section 9 needs an `affixed_number` row group — including a ruling on `n_distinct`/`n_distinct_folded`, which v4 makes conditional on the spellings owner decisions 7, 8 and 10 permit and which the fixed affix pair changes the capacity of.

## CONFLICTS NOTED

- AF7 and C6-7 are TOO NARROW as version 6 writes them, and the narrow form refuses documents a conforming producer must write. Both say every quantitative invariant stated over `n_numeric` is read over `n_core_numeric` 'and nowhere else' (v6:274-276, v6:285-288; r6.md repeats it verbatim in its last matrix bullet). But Q9 and Q10 name the OTHER cell-census counts too. A hundred-cell milligram column of ninety `5 mg`-shaped cells and ten `-1e999 mg` cells publishes `n_out_of_range: 0` — no complete cell reads as a number — beside `n_core_out_of_range: 10` and `n_negative_unrepresentable: 10`; read literally, Q10's `n_negative_unrepresentable <= n_out_of_range` refuses that document, and Q9 publishes `numeric_share: 0.0` on a column carrying a full ladder. I wrote AF7 with the substitution at the width the two-population principle requires (`n_core_out_of_range` for `n_out_of_range`, `n_core_contradictory` for `n_contradictory`, `n_present` and `n_rows` unchanged) and wrote out all six resulting readings. THE SIBLING SITE MUST BE EDITED TO MATCH: r6.md's bullet 'Every quantitative invariant this format states over `n_numeric` is read on that role over `n_core_numeric`, and nowhere else' and the same sentence at s5.md:5.1 both carry the narrow form, and a rule stated three ways with one of them repaired is the exact failure that killed six review rounds.
- s45.md attaches the wrong all-different remark to this role. Its sentence after D16 reads 'This form is carried on every role its trigger reaches, the affixed-number role included, at the wording above and no other' — but D16 is `remark_every_value_is_different`, the code-shaped TEXT remark, whose rendering says 'none of the forms synthtwin can read fits them' and 'Nothing from this column is published either way -- no value of it, and no distribution'. Both clauses are FALSE of an `affixed_number` column, which publishes a full core distribution. D14, `remark_every_number_is_different`, is the one whose wording fits ('the column is described as numbers, which keeps its distribution'), and the shipped code splits them exactly that way — taxonomy.py:4026 fires the TEXT form on free text, taxonomy.py:4289 fires the NUMBERS form on the numeric roles. The plan's sentence (phase-4-columns.md:683, 'The all-different remark additionally extends to this role verbatim') follows a reference to 'the all-different text remark', which is how the error got in. My section deliberately does not pick, since the remark wording is section 4.5's to fix; it says only that the code-shaped remark reaches this role in the wording 4.5 fixes. Section 4.5 must move the sentence to D14 or say why not.
- C6-4's floor clause and AF2 are stated over different quantities and the looser one is what a loader gets. C6-4 requires 'the pair's cell count' to clear `small_cell_floor` at detection, where the pair's cell count is the count of affixed NUMBERS wearing it; AF2 bounds `n_affixed`, which also counts pair-wearing cells with unusable cores. So a pair borne by ten numeric cells and one `many mg` cell publishes `n_affixed: 11` and clears AF2 at a floor of eleven while the fact the floor protects — the number of cells that actually evidence the pair — is ten. The disclosure argument in C6-9 and in the plan ('a published pair is always a floor-cleared fact') rests on the tighter reading. I transcribed both as written and did not silently reconcile them; the owner should say which count the floor is read over.
- Not a disagreement but the check the standard requires: `grep -rn affix src/synthtwin/` returns nothing and there is no `affixed_number` in taxonomy.py's role tuple, so no shipped behaviour contradicts this section — the role is entirely unbuilt. The two shipped facts the section rests on were verified directly: parsing.py:513-561 confirms the classifier trims, accepts group separators, a leading plus and accounting parentheses and refuses a sign inside them, and parsing.py:663-700 confirms its outcome set is exactly four, which is why the four core-class counts are four and not three or five.

## SOURCES

Transcribed element by element from: profile-contract-v6.md lines 232-296 (C6-4 to C6-9, the whole of 3.2), 186-196 (the terms "affix pair" and "core"), 362-385 (C6-20: no settings key for the affix rule; it reads minimum_parse_rate, small_cell_floor, sentinel_outlier_iqr_multiple, sentinel_minimum_share), 386-421 (C6-Q1 and C6-NS, the two inherited restrictions this role widens), 513-536 (C6-27, fraction_widths as a SIBLING key on count/continuous/affixed_number, plan amendment A-P4-5), 859-878 (C6-39 declaration wins on every pass, C6-40 the collision rule), 907 and 966-995 (ranges class with the one named exception; the forbidden-key listing for the role), 1003-1010 and 1025-1050 (the standing remark, its arity and why the count is in the sentence), 1106-1162 (C6-ARG and the POSITIONAL affix binding), 1168-1175 (dispositions), 1221-1234 (AF1-AF4 in the invariant list), 1396-1411 (the disclosure rows). Plan phase-4-columns.md 486-584 (rule order 0-11, the publication-class ruling, floors consulted at detection time) and 587-684 (P4-D4.1 entire: what it reads, the sentinel ordering and its loud fall-through, what it publishes, what the twin writes, the standing remark). Version 4: 6.7 at v4:1121-1197 for the fifteen quantitative keys and Q1-Q11 verbatim; 5.6 at v4:668-696 for L1-L3; 7.5.2/7.5.3/7.5.6/7.5.7 at v4:1597-1730 for numeric_styles, P1-P4 and its disposition; 9.4 at v4:2058-2071 for the dispositions I transcribed into the table; X2 at v4:1868. Shipped source: src/synthtwin/parsing.py:513-561 (parse_number: strips, accepts group separators, a leading plus and accounting parentheses; refuses a sign inside parentheses) and :663-700 (classify_number returns exactly NUMBER, NUMBER_OUT_OF_RANGE, NUMBER_CONTRADICTORY, NOT_A_NUMBER — the four core classes are that enumeration and nothing else); taxonomy.py:520-529 (the ten evidence forms), :552-554 and :4026,:4289 (the two all-different remarks and which roles fire them). `grep -rn affix src/synthtwin/` returns nothing: no part of this role is implemented, so the section could not be checked against shipped behaviour. Adjacent v6-build sections read in full and agreed with, not re-stated: s5.md (22 universal keys, the axis table, the rule order 1-12, and its pointer "The `affixed_number` section states the split"), r1.md (section 6 head and the 6.1/6.2 shape I mirrored), r2.md, r3.md (6.6.2 datetime, the model for voice and for a role section with its own detection rule), r6.md (6.10 publication classes and the 6.11 matrix, whose `afx` column carries exactly the 22 keys my table lists), s4.md (settings, floor), s45.md (4.5 note grammar: D11 arguments 6 and 7, D17, C6-NG11/C6-NG12).