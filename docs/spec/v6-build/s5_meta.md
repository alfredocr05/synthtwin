VERDICT SOUND_WITH_CORRECTIONS errors=12

## ERRORS

1. DELTA FRAMING SURVIVED (defect class 5). Their 5.2 reads "They are the three most recently added and they are tested last before the fallback". "Most recently added" is a fact about version 5 vs version 6 and has no referent in a self-contained document; no source grounds the placement in recency. The plan grounds it in first-match-wins alone: docs/plans/phase-4-columns.md:505-508 ("Rules 8 through 10 sit AFTER categorical by design — the no-regression rule of the header"). Corrected: the clause is dropped and the placement is stated as the durable rule.

2. MEANING CHANGED IN TRANSCRIPTION (defect class 2). Their text: "`time_of_day` before `affixed_number` — clock text rarely splits as an affixed number, AND the time reading is the more specific claim". Source, docs/plans/phase-4-columns.md:508-510: "time before affix (clock text rarely splits as an affixed number, BUT the time reading is the more specific claim)". The plan's "but" concedes that the first clause nearly makes the ordering moot and rests the decision on specificity; "and" flattens one conceded reason plus one ground into two parallel grounds. Restored to "but".

3. DANGLING POINTER, AND A DROPPED REASON (defect classes 5 and 3). The disposition cell for `n_missing_blank` and `n_missing_withheld` reads "REPORT-ONLY, bound by the sum identity the reproduction rule states" — a reference to a rule by description whose content this document never states. The identity exists in exactly two places, neither of them contract prose: docs/spec/profile-contract-v6.md:1380 (matrix row) and docs/plans/phase-4-columns.md:1088. Its stated REASON — "a per-field equality would be false by construction", docs/spec/profile-contract-v6.md:1380 — is also dropped, and the brief requires reasons to be carried. Corrected: the identity and its reason are written into the cell.

4. C6-7 RENDERED AT HALF ITS WIDTH (defect classes 2 and 3). Their text: "no quantitative invariant of this format is read over `n_numeric` there". Source, docs/spec/profile-contract-v6.md:273-275: "Every quantitative invariant version 4 states over `n_numeric` is read on this role over `n_core_numeric`, and nowhere else." The negative half is carried; the positive half — WHERE those invariants are read instead — is lost, which is the half an implementer needs. Corrected to carry both halves. Also, docs/spec/profile-contract-v6.md:255-262 names the four core keys (`n_core_numeric`, `n_core_out_of_range`, `n_core_contradictory`, `n_core_not_numeric`); naming one of them here is what makes the pointer resolvable.

5. STATED REASON DROPPED FROM THE PUBLICATION-GUARD NOTE (defect class 3). docs/spec/profile-contract-v4.md:416-419 gives the guard's reason in full: "This is a producer obligation; it is recorded here because it is a property of the document the contract describes, and because a future note that interpolates a source spelling must fail at construction rather than at pattern matching." Their note carries none of it, and replaces it with "A loader implements none of that" — a stronger claim than the source's "producer obligation", which says where the obligation lives, not that a loader is forbidden to check. Corrected to v4's own reason and v4's own force.

6. REASON CITED IN THE PROVENANCE TABLE BUT NOT CARRIED INTO THE TEXT (defect class 3). Their provenance row for A4 cites "the refusal-rather-than-repair reason docs/spec/profile-contract-v4.md:2685-2688" (§13.9: "An axis triple outside the table is refused rather than repaired"). The transcribed A4 carries only v4:543-547 and no trace of that reason. Corrected by inserting the two-word qualifier into A4 rather than duplicating §13.9's paragraph.

7. INTERNAL CONTRADICTION IN THE "THREE CLOSED LISTS" PARAGRAPH (defect class 6). The paragraph states an absolute rule — "A closed list stated in two places is a list two implementations can read differently" — while the same table two rows above writes `quality_state` inline at three members and `structural_role` inline at two (both also stated at docs/spec/profile-contract-v4.md:2786-2787, §14), and while section 5.2 itself lists the thirteen role names TWICE: as the axis table's first column and as C6-1's rule order (the same doubling exists in the source, docs/spec/profile-contract-v6.md:1608-1615). The rule as written is falsified by its own page. Corrected: the paragraph now states why each inline copy is BOUND rather than free — A4 admits no `quality_state` outside the table's third column, A1 and the declaration rule fix `structural_role`'s two values, and A5's totality requirement is what forbids the rule order and the table from naming different roles.

8. TWO CONVENTIONS FOR ONE FACT INSIDE ONE TABLE (defect class 6). The `n_missing_blank` and `n_missing_withheld` rows carry "integer" in the JSON-type column and "≥ 0" in the range column, transcribed from version 5's own two-key table (docs/spec/profile-contract-v5.md:450-451, whose column headings are "JSON type | range"). Every other integer row of this merged table follows version 4's convention, "integer ≥ 0" in the type column (docs/spec/profile-contract-v4.md:489-499). Merging the two tables requires harmonizing; a loader author reading two conventions for the same constraint has to decide which is normative. Corrected to version 4's convention, with no loss of the "≥ 0" bound.

9. NOTE, NOT AN ERROR — SILENT CORRECTION OF A VERSION 4 TYPO, AND IT IS THE RIGHT ONE. Their A4 reads "The TRIPLE (`role`, `statistical_type`, `quality_state`)"; docs/spec/profile-contract-v4.md:543 reads "The PAIR" over three names. The checklist form at docs/spec/profile-contract-v4.md:1861 confirms three. Kept as corrected, recorded here so it is not read as drift.

10. NOTE, NOT AN ERROR — EVERY ENUMERATION IN THIS SECTION VERIFIED IN BOTH DIRECTIONS, NONE WRONG. Universal keys: 22 in their table, 22 in `contract.UNIVERSAL_COLUMN_KEYS` (src/synthtwin/contract.py:211-234, verified by import at PROFILE_VERSION 5), set-equal, no extra and none missing. Roles: 13 rows, matching the 13 names at docs/spec/profile-contract-v6.md:1608-1615 exactly. Statistical types: 13 distinct, one per role — bijection confirmed; the ten inherited pairs match `contract.AXIS_ROWS` and `taxonomy.ROLE_AXES` element for element (src/synthtwin/contract.py:275-286, src/synthtwin/taxonomy.py:324-335, verified equal to each other by import), and the three new pairs match docs/spec/profile-contract-v6.md:364-369 and :1617-1618. `quality_state`: 3, matching `contract.QUALITY_STATES` (src/synthtwin/contract.py:301). `structural_role`: 2, matching `contract.STRUCTURAL_ROLES` (src/synthtwin/contract.py:303). Rule order: 12 items, matching docs/spec/profile-contract-v6.md:206-209 item for item, and 13 role names across them. Absence classes cited at 6, matching docs/spec/profile-contract-v6.md:676-681. Disposition classes used are 3 of the 6 at docs/spec/profile-contract-v4.md:84-89, all spelled correctly. Nothing from docs/spec/profile-contract-v4.md:475-554 or docs/spec/profile-contract-v5.md:442-451 is dropped.

11. NOTE, NOT AN ERROR — THEIR THREE REPORTED CONFLICTS NOTED ARE ALL CONFIRMED AGAINST SOURCE. (a) docs/spec/profile-contract-v6.md:1379 excepts only "stand-in-spelled keys" while the governing C6-37 clause 1 at docs/spec/profile-contract-v6.md:836-838 excepts "a spelling a JUDGED PASS put there — one reading as a stand-in number, or as a calendar placeholder", with C6-38 at :846-855 giving one reason for both; writing the disposition at C6-37's width is correct and the matrix row is the defective site. (b) The fourth uncited five-class site is real: docs/spec/profile-contract-v4.md:100 defines "absent" as "for any of the five recorded reasons", and it is not among C1-a/b/c at docs/plans/reviews/material/phase-4-v6-derivation-closure.md:128-130. (c) The further ten-role sites are real: docs/spec/profile-contract-v4.md:701-706 ("There are **ten** roles"), :2678-2683 (§13.8, "a bijection between the ten roles and the ten `statistical_type` values") and :2690-2692 (§13.10, "Ten roles, not nine").

12. NOTE, NOT AN ERROR — ONE HANDOFF THIS SECTION CANNOT CLOSE AND DID NOT CLAIM TO. Invariants X1-X4 quantify purely over universal keys — X2 in particular, "`n_numeric + n_not_numeric + n_out_of_range + n_contradictory == n_present`" — and version 4 states them in NO defining clause at all: docs/spec/profile-contract-v4.md:1867-1870 (the checklist) is their only site, confirmed by grep. The cell-versus-core note in 5.1 presupposes X2. Under amendment A-P4-11 they must be written out somewhere; adding them here would create a second site against whoever transcribes the checklist, so this section leaves them and the handoff is registered instead.

## GAPS

- SECTION NUMBERS ARE ASSUMED TO FOLLOW VERSION 4's. My text points at 'section 5.4' (the absence classes and the two absent-cell maps), 'section 5.5' (a sentinel verdict entry), 'section 6' (the roles), 'section 9' (the disposition matrix) and 'section 4.5' (the publication notes and the note grammar). I could not establish the self-contained version 6 document's final section numbering; if it differs, these five pointers must be renumbered. Nothing else in my section depends on a number.
- THE DISPOSITION COLUMN IS A SECOND SITE FOR VERSION 4 SECTION 9.2, AND SOMEBODY MUST DECIDE WHICH SITE SURVIVES. The task asked for disposition in section 5.1, and version 5 set the precedent by carrying a disposition column on its own universal-key rows (profile-contract-v5.md:448-451). But version 4 states the universal-field dispositions in section 9.2 (profile-contract-v4.md:2036-2046). Two sites for one list is the exact defect amendment A-P4-11 exists to end. RECOMMENDATION to whoever writes section 9: make section 9.2's universal-field rows a pointer to this table rather than a second copy, or drop this column. Do not ship both copies.
- THE DISPOSITION OF `n_distinct` AND `n_distinct_folded` ON THE THREE NEW ROLES IS UNSTATED ANYWHERE. Version 4 defers both to the per-role-group sections 9.3-9.7 (profile-contract-v4.md:2045-2046); version 6's delta section 11 adds no row for them (profile-contract-v6.md:1372-1386) and section 8's key table does not list them because they are not new keys. So a `time_of_day`, `affixed_number` or `long_tail_labels` block has two universal keys with no disposition. Already registered in the vetted material at docs/plans/reviews/material/phase-4-v6-derivation-datetime.md:35. My table says 'set per role group, section 9' and cannot say more; section 9 must grow three role groups or the completeness assertion fails.
- THE PRESENTATION ORDER OF THE THIRTEEN AXIS ROWS IS NOT FIXED BY ANY ARTIFACT. I transcribed the ten shipped rows in `contract.AXIS_ROWS` order and appended the three new ones in the plan's own order (`time_of_day`, `affixed_number`, `long_tail_labels`), so every one of the ten is checkable line-for-line against contract.py:275-286. Note this is NOT the rule order: the shipped tuple puts `identifier` ninth while the rule order puts it second, and plan P4-D3 (phase-4-columns.md:516) says only that the three new roles 'enter ROLES in rule order', which does not settle whether they sit before or after `identifier` in the tuple. I therefore state in the text that the row order is presentational. If the implementing commit fixes a tuple order for the thirteen, this table and section 14's role list should be re-ordered to match it.
- THE RULE ORDER HAS NO INVARIANT IDENTIFIER AND NO PRODUCER MARK. C6-1 states the order but no lettered invariant makes it checkable, and section 9's producer/loader split (profile-contract-v6.md:1198-1207) gives no row for it. I stated the producer/loader consequence in prose from that section's own stated ground rather than making up an identifier. If the batteries need to cite it, it needs one — the shape would be a producer row alongside AF-P, T-P and DF-P.
- WHETHER A BELOW-FLOOR CALENDAR PLACEHOLDER COUNTS IN `n_sentinel_candidates_unpublished` IS NOT STATED BY NAME. C6-35 (v6:703-707) says a judged placeholder publishes 'through the standing verdict machinery', and version 4's V1 (v4:643-647) puts below-floor candidates into that count. The entailment is clean but no version 6 clause writes it down, so I wrote the neutral 'stand-in candidates' rather than either version 4's 'stand-in numbers' or an explicit claim about placeholders. Whoever writes section 5.5 should settle it there, once.
- I DID NOT ESTABLISH WHAT THE `affixed_number` SECTION'S NUMBER OR EXACT WORDING WILL BE, so my cell-versus-core note in 5.1 names 'the `affixed_number` section' without a number and restates none of C6-7's arithmetic.

## CONFLICTS NOTED

- THE DISPOSITION ROW FOR `missing_by_source` IS NARROWER THAN THE RULE IT REPORTS. profile-contract-v6.md:1379 excepts 'stand-in-spelled keys' only; the governing clause C6-37 (v6:835-844) excepts 'a spelling a JUDGED PASS put there — one reading as a stand-in number, OR AS A CALENDAR PLACEHOLDER', and C6-38 (v6:846-849) gives the reason for both alike. A producer following the section 11 row would reproduce a published placeholder spelling in the twin, which C6-37 forbids. I wrote the disposition at C6-37's width and flag the section 11 row as defective; the fix belongs in whoever writes the disposition matrix.
- COUNT CHECK — ALL VERIFIED COUNTS IN MY SCOPE AGREE, WITH ONE ARITHMETIC NOTE. 13 roles: agrees (C6-1 lists twelve RULES over thirteen roles, because `count` and `continuous` share rule 7; the thirteen names are enumerated at v6:1611-1615 and my table has thirteen rows). 13 statistical types: agrees, and I confirmed the mapping is a bijection — the ten shipped types are each reached by exactly one role (verified by import) and the three new types are each reached by their own role. 6 absence classes: agrees with C6-N3 (v6:676-681). The shipped constants remain at ten and five (contract.ROLES, contract.STATISTICAL_TYPES, contract.MISSING_CLASS_KEYS, contract.AXIS_ROWS — all verified by import at PROFILE_VERSION 5), which is expected: the code has not moved to version 6 yet. No count I could check contradicts the list I was given.
- A FOURTH UNCITED SITE FOR THE FIVE-CLASS COUNT, NOT IN THE VETTED MATERIAL. profile-contract-v4.md:100 defines the term 'absent' as 'a cell counted as holding no value, for any of the FIVE recorded reasons'. The closure material registers three sites for that enumeration — section 5.1 (item C1-a), section 5.4 (C1-b) and section 14 (C1-c) — and not this one. Whoever transcribes section 2.3 must write 'six recorded reasons'. Reported rather than fixed, because section 2.3 is not my section.
- TWO FURTHER SITES FOR THE TEN-ROLE COUNT, OUTSIDE MY SECTIONS. profile-contract-v4.md:2678-2683 (13.8) calls the derivation table 'a bijection between the TEN roles and the TEN statistical_type values', and profile-contract-v4.md:2690-2692 (13.10) is a whole decision headed 'Ten roles, not nine'. Both must be restated at thirteen, and 13.10's reasoning ('the producer's role tuple has ten entries, and this contract is written against the producer') is stale in a version whose producer does not exist yet. Also profile-contract-v4.md:701-706, the section 6 head, states 'There are TEN roles' and lists them — a fifth site, for whoever writes section 6.
- A WORDING TENSION BETWEEN A3 AND THE NOTHING-PUBLISHING OVERRIDE, PRESENT ALREADY IN VERSION 4 AND CARRIED FORWARD UNCHANGED. A3 (v4:539-541) says `structural_role == "identifier"` implies `role` is `identifier` or `empty` — two roles. C6-PUB-B (v6:944-951) and the shipped comment at contract.py:260-264 both say the override reaches 'any column the person declared with --identifier WHATEVER ROLE IT REACHED', which reads as though more than two were reachable. Under the rule order they are not: the declaration is decided at rule 2, so a declared column reaches only `empty` or `identifier`. The two sentences are compatible — the override is written generally so it cannot be outgrown — but a reviewer will meet them and ask. I transcribed A3 unchanged and added the explicit reachability argument beside it. If section 6A's author wants the override narrowed to match A3, that is a change to C6-PUB-B and needs a decision, not a transcription.

## SOURCES

Every path below is relative to the repo root
`/Users/alfredowork/Library/CloudStorage/OneDrive-UniversityofIowa/Research/0- Synthetic dataset/synthtwin`.
Shipped enumerations were confirmed by importing the package, not by
reading prose.

### Section 5 head

| rule | source |
|---|---|
| "A column block is an object … a loader refuses an unknown key, naming it and the column." | `docs/spec/profile-contract-v4.md:475-477`, transcribed verbatim |

### Section 5.1 — the universal-key table

| row / rule | source |
|---|---|
| twenty-two universal keys, and the key set itself | `src/synthtwin/contract.py:211-234` (`UNIVERSAL_COLUMN_KEYS`, 22 members, verified by import); the twenty version 4 rows at `docs/spec/profile-contract-v4.md:483-502` plus version 5's two at `docs/spec/profile-contract-v5.md:450-451` |
| "no optional keys … the guess is what fails silently" | `docs/spec/profile-contract-v4.md:69-74`; restated for the two version 5 keys at `docs/spec/profile-contract-v5.md:444-446`; same sentence in the shipped comment at `src/synthtwin/contract.py:208-210` |
| `name` row | `docs/spec/profile-contract-v4.md:483` |
| `position` row | `docs/spec/profile-contract-v4.md:484` |
| `role` row — "thirteen role names" | count and members: `docs/spec/profile-contract-v6.md:204-209` (C6-1) and `docs/spec/profile-contract-v6.md:1608-1615` (§14); version 4's row it replaces, "one of the ten role names", `docs/spec/profile-contract-v4.md:485`; shipped ten `src/synthtwin/contract.py:247-258` |
| `statistical_type` row — "thirteen statistical types" | `docs/spec/profile-contract-v6.md:364-369` (C6-19), `docs/spec/profile-contract-v6.md:1617-1618` (§14); version 4's inline ten-value list it replaces, `docs/spec/profile-contract-v4.md:486`; shipped ten `src/synthtwin/contract.py:288-299`, `src/synthtwin/taxonomy.py:295-306` |
| `quality_state` row | `docs/spec/profile-contract-v4.md:487`; vocabulary `src/synthtwin/contract.py:301` (3 members, unchanged in version 6) |
| `structural_role` row | `docs/spec/profile-contract-v4.md:488`; vocabulary `src/synthtwin/contract.py:303` (2 members, unchanged in version 6) |
| `n_present`, `n_missing` rows | `docs/spec/profile-contract-v4.md:489-490` |
| `missing_by_class` row — "exactly six keys" | `docs/spec/profile-contract-v6.md:676-681` (C6-N3); version 4's "exactly five keys" row it replaces, `docs/spec/profile-contract-v4.md:491`; shipped five `src/synthtwin/contract.py:309-315` |
| `missing_by_source` row | `docs/spec/profile-contract-v4.md:492`; one key space only, `docs/spec/profile-contract-v5.md:432-436` (C5-11) |
| `n_missing_blank`, `n_missing_withheld` rows (type, range, meaning) | `docs/spec/profile-contract-v5.md:448-451`, transcribed verbatim |
| `n_distinct`, `n_distinct_folded` rows | `docs/spec/profile-contract-v4.md:493-494` |
| `n_numeric`, `n_not_numeric`, `n_out_of_range`, `n_contradictory` rows | `docs/spec/profile-contract-v4.md:495-498` |
| `n_sentinel_candidates_unpublished` row | `docs/spec/profile-contract-v4.md:499`; "stand-in candidates" rather than version 4's "stand-in numbers" follows the candidate domain of `docs/spec/profile-contract-v6.md:416-420` (C6-CAND) and the wording of invariant V1 at `docs/spec/profile-contract-v4.md:643-647` |
| `sentinel_verdicts` row | `docs/spec/profile-contract-v4.md:500`; the widened candidate domain (stand-in number, calendar day spelling, or `(withheld)`) `docs/spec/profile-contract-v6.md:416-420` (C6-CAND) and `docs/spec/profile-contract-v6.md:703-707` (C6-35) |
| `detection_evidence`, `remarks` rows | `docs/spec/profile-contract-v4.md:501-502` |
| disposition — `name` | `docs/spec/profile-contract-v4.md:2039` |
| disposition — `position`, `role`, `statistical_type`, `quality_state`, `structural_role` | `docs/spec/profile-contract-v4.md:2040`; restated for the three axes at `docs/spec/profile-contract-v4.md:1386` |
| disposition — `n_present`, `n_missing` | `docs/spec/profile-contract-v4.md:2038` |
| disposition — `missing_by_class` | `docs/spec/profile-contract-v6.md:1381` |
| disposition — `missing_by_source` | `docs/spec/profile-contract-v6.md:1379`, widened to the placeholder case by the governing rule `docs/spec/profile-contract-v6.md:835-844` (C6-37 clause 1) and `docs/spec/profile-contract-v6.md:846-849` (C6-38) — see the disagreement register |
| disposition — `n_missing_blank`, `n_missing_withheld` | `docs/spec/profile-contract-v6.md:1380` |
| disposition — `n_distinct`, `n_distinct_folded` ("set per role group") | `docs/spec/profile-contract-v4.md:2045-2046` |
| disposition — the four numeric census counts | `docs/spec/profile-contract-v4.md:2042` |
| disposition — `n_sentinel_candidates_unpublished`, `sentinel_verdicts`, `detection_evidence`, `remarks` | `docs/spec/profile-contract-v4.md:2043` |
| "Three closed lists are named here rather than repeated here" | the design rule of plan amendment A-P4-11, `docs/plans/phase-4-columns.md:2209-2225`; the concrete failure it names is version 4 §5.1's own inline copies, registered at `docs/plans/reviews/material/phase-4-v6-derivation-closure.md:128` (item C1-a) |
| "The four numeric census counts answer for CELLS" | `docs/spec/profile-contract-v6.md:263-275` (C6-7) |
| "`detection_evidence` and `remarks` are built sentences" | `docs/spec/profile-contract-v4.md:410-419` and `421-432`; argument classes closed at four, `docs/spec/profile-contract-v6.md:1128-1130` (C6-ARG); the bound affix string, `docs/spec/profile-contract-v6.md:1132-1141` |

### Section 5.2 — the three axes

| rule | source |
|---|---|
| the three axes named; "the generator dispatches on the axes, never on `role`" (plan P2-D3) | `docs/spec/profile-contract-v4.md:504-509`, `docs/spec/profile-contract-v4.md:1382-1394` |
| derivation table, the ten inherited rows, in this order | `src/synthtwin/contract.py:275-286` (`AXIS_ROWS`) and `src/synthtwin/taxonomy.py:324-335` (`ROLE_AXES`) — verified equal to each other by import; same ten rows at `docs/spec/profile-contract-v4.md:511-522` |
| derivation table, the three added rows | `docs/spec/profile-contract-v6.md:364-369` (C6-19); `docs/spec/profile-contract-v6.md:1617-1618` (§14); `docs/plans/phase-4-columns.md:515-524` |
| "thirteen rows, thirteen statistical types, one row each" — the bijection | `docs/spec/profile-contract-v4.md:2678-2683` (§13.8, stated there over ten); `docs/plans/phase-4-columns.md:516-517` ("the role-to-statistical_type bijection gains three rows"); confirmed by import over the shipped ten |
| "four roles answer something other than their own name", with the four cases | `src/synthtwin/taxonomy.py:315-323`, transcribed from its prose; the count changes from "the other six" to "the other nine" because three roles are added, per C6-19 |
| "written out row by row rather than derived from the role string" | `src/synthtwin/taxonomy.py:320-323` |
| C6-19 — the three new roles name themselves, `quality_state` `ok`, `structural_role` `data` | `docs/spec/profile-contract-v6.md:364-369`; the "stated cost, not an oversight" reason `docs/plans/phase-4-columns.md:516-520` |
| `structural_role` is `identifier` exactly when declared | `docs/spec/profile-contract-v4.md:524-530` |
| C6-3 — the empty rule settles before the declaration | `docs/spec/profile-contract-v6.md:227-230`; `docs/plans/phase-4-columns.md:487-491`; the full triple of that column follows from the table's `empty` row |
| Invariant A1 | `docs/spec/profile-contract-v4.md:532-533`; checklist form `docs/spec/profile-contract-v4.md:1858` |
| Invariant A2, with its reason | `docs/spec/profile-contract-v4.md:535-537`; checklist form `docs/spec/profile-contract-v4.md:1859` |
| Invariant A3 | `docs/spec/profile-contract-v4.md:539-541`; checklist form `docs/spec/profile-contract-v4.md:1860` |
| "A2 and A3 stay narrow … and here is why they are still total" | new prose; the argument rests on the rule order `docs/spec/profile-contract-v6.md:204-209` (C6-1, declaration at rule 2) and on C6-19's `structural_role` `data` for the three added roles, `docs/spec/profile-contract-v6.md:364-369` |
| Invariant A4, with its reason | `docs/spec/profile-contract-v4.md:543-547`; checklist form `docs/spec/profile-contract-v4.md:1861`; the refusal-rather-than-repair reason `docs/spec/profile-contract-v4.md:2685-2688` (§13.9) |
| Invariant A5 | `docs/spec/profile-contract-v6.md:1259`; the "an axis a column sometimes lacks is an axis nobody can dispatch on" reason `src/synthtwin/taxonomy.py:310-313`; `ROLE_AXES` totality with its completeness test `docs/plans/phase-4-columns.md:523-524` |
| "Why the axes and not the role" | `docs/spec/profile-contract-v4.md:549-554`, transcribed with "a future role added to the taxonomy" changed to "a role added to the taxonomy" |
| C6-1 — the twelve rules over thirteen roles | `docs/spec/profile-contract-v6.md:204-212`; `docs/plans/phase-4-columns.md:474-501` (P4-D3, numbered 0-11); role list in rule order `docs/spec/profile-contract-v6.md:1611-1615` |
| "Why rules 9 through 11 sit after `categorical`", and their internal order | `docs/plans/phase-4-columns.md:505-517`; `docs/spec/profile-contract-v6.md:209-212` |
| "The order is a PRODUCER rule" | the ground is stated at `docs/spec/profile-contract-v6.md:1198-1207` ("a loader holds one document and never the table it describes"); A4 as the loader-enforced rule, `docs/spec/profile-contract-v4.md:543-547` |