VERDICT SOUND_WITH_CORRECTIONS errors=15

## ERRORS

1. ENUMERATIONS — ALL VERIFIED, both directions, element by element, no error. (1) The added-key list: 15 keys. Against v4:1131-1145 (14 rows: percentiles, mean, std, skew, std_unrepresentable, n_zero, n_negative, n_negative_unrepresentable, n_used_in_statistics, n_left_out_of_statistics, numeric_share, integer_valued, n_rows, numeric_styles) plus fraction_widths (v6:515-517). Against the shipped producer, src/synthtwin/taxonomy.py:3316-3345 publishes exactly those 14 (percentiles, n_zero, n_negative, n_negative_unrepresentable, n_rows, integer_valued, n_used_in_statistics, n_left_out_of_statistics, numeric_share, numeric_styles, plus the four _moments keys). Against the already-written matrix, docs/spec/v6-build/r6.md:181-215 marks `count` 15 and `continuous` 15 and r6.md:249-252 states those counts — the 15 marked names match the section's table one for one. Nothing present-but-not-in-source, nothing in-source-but-missing. (2) Q1 through Q11: all eleven present, in order, all transcribed against v4:1146-1197. (3) The six numeric styles are NOT enumerated here and correctly are not — src/synthtwin/contract.py:438-445 owns the list and ASSEMBLY.md:88-91 assigns v4 §7.5 in full to the additions section; the section states only the count `exactly six`, which condition 4 needs.

2. ERROR 1 — DELTA FRAMING SURVIVED. Section text: "Version 4's reason carries and reaches the third role for the same reason it reached the first two." This is lifted from the delta clause at docs/spec/profile-contract-v6.md:405-406 and is exactly what plan amendment A-P4-11 (docs/plans/phase-4-columns.md:2225) and standing check 2 at docs/spec/v6-build/ASSEMBLY.md:97-101 forbid — it carries a rule by reference to a prior version instead of stating it. CORRECTED: the reason is now stated in its own words ("These are the three roles whose twin cells are written as parsed numbers from the ladder — on `affixed_number`, as a parsed number inside the affix pair — …"), preserving v4:1600-1604's substance and v6:406-409's extension to the third role. This was the only surviving delta-framing hit; no "supersedes", no "unchanged from version 5", no `C6-` LETTER identifier (Q1 is correctly kept as `Q1`, not `C6-Q1` as v6:387 writes it, per ASSEMBLY.md:37-41).

3. ERROR 2 — DISAGREES WITH SHIPPED CODE, and the code is right. The paragraph "How a loader decides 'every parsed value is identical'" ends "…so none of the three invariants bites." That is false. src/synthtwin/contract.py:4151-4155 sets `flat` false whenever either endpoint is null, and src/synthtwin/contract.py:4163 then refuses `not flat and used >= 3 and skew is None`. So on a column with a null endpoint and `n_used_in_statistics >= 3`, Q5 DOES bite: `skew` must be a number. As written the section tells a loader author that a null rung suspends Q5 entirely, and two implementations following it and following the code would refuse different documents — the precise harm the paragraph exists to prevent. CORRECTED to: the identical-value clause of Q5, the whole of Q6 and Q7's identical-value clause do not bite, while Q5's remaining clause applies as on any column whose endpoints differ.

4. ERROR 3 — A TRANSCRIBED RULE CHANGED ITS ADDRESSEE AND LOST ITS NORMATIVE FORCE. Source, docs/spec/profile-contract-v4.md:2071-2073: "A mutant that collapses the nine interior rungs onto the endpoints must FAIL the rung envelope. So must a mutant that ignores, permutes or swaps rungs." Section: "A twin that collapses the nine interior rungs onto the endpoints, or that ignores, permutes or swaps rungs, fails that envelope." The source sentence is an obligation on the ENVELOPE — it must be tight enough that such a mutant implementation fails — and is what the validation method's mutation set is sized against; the transcription turns it into a factual remark about twins and drops "must FAIL". CORRECTED: v4's two sentences restored verbatim.

5. ERROR 4 — AF7 EXTENDED BEYOND ANY SOURCE (completion from pattern). Section: "…with `n_core_numeric` standing wherever `n_numeric` appears and `n_core_not_numeric`, `n_core_out_of_range` and `n_core_contradictory` standing beside it." AF7 at docs/spec/profile-contract-v6.md:285-287 names ONE substitution ("read over the cores and over `n_core_numeric` in place of `n_numeric`"), C6-7 at v6:274-275 says the same ("Every quantitative invariant version 4 states over `n_numeric` is read on this role over `n_core_numeric`, and nowhere else"), and the already-written docs/spec/v6-build/r6.md:297-299 repeats that single substitution. No artifact puts in place of the other three counts, and `n_core_not_numeric` is used by no Q invariant at all. CORRECTED: trimmed to AF7's own terms. NEW GAP, declared rather than guessed: Q9 reads `n_out_of_range` and `n_contradictory` and Q2 reads `n_present`, and AF7 as written fixes neither substitute on `affixed_number` — whether Q9's denominator there is `n_present` or `n_affixed`, and whether Q2's subtrahend is `n_present` or `n_affixed`, is undetermined by v4, v5, v6 and the plan. The `affixed_number` section must settle it; this section must not.

6. ERROR 5 — AN INVENTED FIFTH CONDITION, where the ratified plan already covers the case with its fourth. The section splits P5.c into a non-empty branch (conditions 1-4) and an empty branch carrying a new "condition 5: *W* ≤ 5 × (small_cell_floor − 1)". That number exists in no artifact: A-P4-6 gives three conditions (docs/plans/phase-4-columns.md:1974-1978), A-P4-8 gives "a fourth condition" (:2069-2074), and v6's own §9 row at docs/spec/profile-contract-v6.md:1249 says "bounded FOUR ways". The split is also unnecessary: *F* is DEFINED as the sum of ALL values of the census (v6:546-547; plan:2071 "*F* the total of `fraction_widths`"), so an empty census has *F* = 0 and condition 4 read at *F* = 0 IS *W* ≤ 5 × (floor − 1). Nothing needs extending into a free branch. CORRECTED to four conditions, with condition 1 carrying the plan's own rider "wherever the census is non-empty" (plan:1976, the only condition A-P4-6 qualifies that way), an explicit paragraph showing condition 4 at *F* = 0, and the fifty-one-cell counterexample re-worded to say what actually refuses it. This also resolves ERROR 6 below and removes the section's disagreement with the vetted material.

7. ERROR 5b — CONSEQUENCE FOR THE REPORTED DISAGREEMENT WITH THE VETTED MATERIAL. The section reported a disagreement with docs/plans/reviews/material/phase-4-v6-derivation-partition.md errors 2 and 9, which call binding the empty branch a RAISE needing a new amendment. Under the corrected reading neither side needs to win: reading condition 4 over its own defined quantity at *F* = 0 is the amendment as ratified, not an extension of it, so no RAISE arises and no new amendment is owed. The material's headline case (floor 11, `numeric_styles: {"(withheld)": 51}`, `fraction_widths: {}`) is refused by A-P4-8 as written. The section's substantive position was right; its five-condition presentation was what made it look like a raise.

8. ERROR 6 — A STATED REASON DROPPED FROM C6-27. docs/spec/profile-contract-v6.md:521-524 carries "The ratified plan said inside; the plan governs, so the plan was amended rather than this document deviating from it — plan amendment A-P4-5, which fixes the sibling placement and states this reason." The section kept only the parenthetical "(plan amendment A-P4-5)". That sentence is the governance precedent the whole build cites for how a contract-plan conflict is resolved (it is what docs/spec/v6-build/r6_meta.md:41 names as "the precedent C6-27 sets at v6:520-526"), and it is a stated REASON, which this rewrite exists to carry. RESTORED.

9. ERROR 7 — A MISATTRIBUTED CITATION, and it is the exact one the vetted material flagged. Section: "P2 states the first half as a bound on named values" — where the first half is "a style whose own count reaches `small_cell_floor` is published BY NAME and is never pooled". P2 at docs/spec/profile-contract-v4.md:1705-1706 states the CONVERSE ("Every value under a style name is at least `small_cell_floor`"), which is the direction the bounds do NOT need; docs/plans/reviews/material/phase-4-v6-derivation-partition.md error 3 flags precisely this. CORRECTED: P2 is now cited only for the bound it states, and the direction conditions 2 and 4 rest on is carried by what `(withheld)` means (v5:1391, v5:1397-1398; asserted in v6's own reasoning at v6:577-579). The section's declared gap on this point stands and is still owed to the numeric-styles section.

10. ERROR 8 — A CLAIMED CONFLICT WITH VERSION 4 THAT DOES NOT REPRODUCE. The section's conflict report says a column of `1`, `2`, `(3-)` is a `count` under v4:1123-1125 and a `continuous` under the shipped three-conjunct rule. Both halves fail on inspection of the artifacts. (a) `(3-)` is ordinary text, not contradictory notation: src/synthtwin/parsing.py:688-696 returns NUMBER_CONTRADICTORY only when the sign LEADS the bracketed inner text, so `(3-)` falls through to NOT_A_NUMBER — confirmed by running the shipped classifier. (b) Every NUMBER_CONTRADICTORY cell is WHOLE_UNKNOWN (src/synthtwin/parsing.py:1119-1133 returns WHOLE_UNKNOWN for that kind), so `n_whole == numeric_looking` at src/synthtwin/taxonomy.py:4297-4298 already fails on it and conjunct 1 rules the role out without conjunct 3. I brute-forced the shipped classifier over signed, bracketed, grouped, exponent, leading-zero and out-of-range forms: no string is numeric-looking, WHOLE_YES and SIGN_UNKNOWN at once, so on today's classifier conjunct 3 is implied by conjunct 1 and version 4's two-conjunct sentence and the shipped three-conjunct rule agree on every producible column. NO TEXT CHANGE TO THE RULE: the three-conjunct form is what taxonomy.py:4296-4305 states and its reason is sealed in the code comment at taxonomy.py:2254-2266, so it is transcribed as the task directs. But the paragraph that justified it rested on the contradictory cell alone, and that cell is reached by test 1 as well; CORRECTED to the code comment's own wording ("a cell whose sign or whose whole-number status the text does not settle"), which is what makes both tests' presence honest. The claimed live defect against version 4 must NOT enter the review record as found.

11. ASSEMBLY 1 — TWO SITES FOR ONE DISPOSITION. The section owns a `disposition` column for all fifteen keys, transcribed from v4:2058-2072. The adjacent written sections defer instead: docs/spec/v6-build/s5.md:29-30 writes "set per role group, section 9" in its own disposition column, and docs/spec/v6-build/r1.md:222-223 defers in prose ("Every count in this block is EXACT-OBSERVABLE … (section 9)"). I kept the column — it is v4's own content at the role's own site and it makes the section complete — but section 9's `count`/`continuous` rows must then carry ONLY the two universal-key dispositions (`n_distinct`, `n_distinct_folded`, v4:2065-2069) and point here for the rest. Left as it stands, this is the two-site defect A-P4-11 exists to end.

12. ASSEMBLY 2 — AN UNNUMBERED NEW DEFINITION. The paragraph "How a loader decides 'every parsed value is identical'" is normative, is in no prior contract, and is fixed only by src/synthtwin/contract.py:4151-4155. docs/spec/v6-build/ASSEMBLY.md:33-35 gives prose rules and definitions a plain `C6-` NUMBER. Either allocate one at assembly or place it explicitly as a rider under Q5-Q7; it must not ship as an anonymous normative paragraph.

13. ASSEMBLY 3 — CROSS-REFERENCES THAT CANNOT BE NUMBERED FROM INSIDE THIS SECTION, all of which must resolve to a clause that exists in the assembled document: "the numeric-styles section" (ASSEMBLY.md:88-91 already assigns v4 §7.5 in full to the additions section, and P1-P4 must keep their identifiers there); "section 5.6" for the ladder and L1/L2/L3 — NO v6-build section has yet written it, and this section cites L1, L2 and L3 by identifier while stating only L1 and L2 in full; "section 5.2" (the rule order and A4 — docs/spec/v6-build/s5.md:76, :159-164, :209-213); "section 6.11" (docs/spec/v6-build/r6.md:165); "the settings section" for S13 (docs/spec/v6-build/s4.md:361-375); "the `affixed_number` section" for AF7.

14. VERIFIED, NO CHANGE — deliberate departures from version 4's letter that are correct and are recorded so a later reviewer does not read them as drift. (a) `percentiles` typed "ladder of numbers or nulls" where v4:1132 says "ladder of numbers": L3 at v4:687-695 permits a null rung, so the section's type is the accurate one. (b) "invented digit strings inside a published length range" replaces v4:1606's "invented digit strings at one canonical width (residual R-P2-1)": C6-26 at v6:505-510 settles R-P2-1 and docs/spec/v6-build/r1.md:214-222 has the twin writing inside the published range, so version 4's clause is stale and the section's is right. (c) P6's second clause "and the `(withheld)` value is 0 or at least 1" (v6:593-594) correctly dropped as vacuous over non-negative integers — v6's own §9 row at v6:1250 already omits it. (d) Q3's "clears the parse line" for v4:1157's "clears `minimum_parse_rate`" — consistent with the count rule at plan:395 and src/synthtwin/taxonomy.py:1666-1676.

15. VERIFIED AGAINST SHIPPED CODE, NO DISAGREEMENT FOUND — recorded so it is not re-litigated. Role entry: `strict_needed = _needed(minimum_parse_rate, n_present)` (taxonomy.py:3677) with RULE 2 at :3708-3709 and RULE 6 at :3834 give exactly the section's two-test entry, and the shared-line reason at taxonomy.py:3698-3706 is carried word for word and agrees with docs/spec/v6-build/r1.md:148-155. Q1 contract.py:4100-4106; Q3 :4110-4116; Q2 :4117-4131; Q4 :4132-4145; Q5 :4145-4172; Q6 :4173-4181; Q7 :4182-4192; Q9 :4193-4200; Q10 :4201-4214; Q11 :4215-4221; the `std >= 0` range check :4108-4109. `n_negative` over present cells and `n_sign_unsettled_numeric` over numeric-looking cells only: taxonomy.py:2359-2372 with the narrowness reason at :2266-2275, transcribed faithfully. `integer_valued` = `whole_everywhere` including the non-emptiness conjunct (taxonomy.py:4296-4298) — unreachable under Q3 and AF5, correctly recorded as a producer/contract nicety rather than written into the text. THE DECLARED GAP IS TRUE: `grep -rn fraction_widths src/synthtwin/` returns nothing, so C6-27 through C6-30, P5, P6, P7 and FW-P are normative against code not yet written, and the shipped loader (contract.py:4261-4304, which checks membership, P3, P2 and P1 and bounds `(withheld)` only at >= 1 via `_counts(..., 1)` at :4273) would refuse a block carrying the key as unknown.

## GAPS

- `fraction_widths` DOES NOT EXIST IN THE SHIPPED CODE. A grep for the string over all of src/synthtwin/ returns no occurrence. Every clause of this section about it — C6-27 through C6-30, P5, P6, P7, FW-P — is normative against code not yet written; today's producer writes no such key and today's loader would refuse a block carrying it as an unknown key. This is expected for Phase 4 stage work, but it means none of the five P5.c conditions has ever been exercised against an implementation, and the reference vectors A-P4-6 and A-P4-8 each said the pooled case now needs do not exist either.
- Whether `fraction_widths` is REQUIRED or merely PERMITTED on the three roles is not stated in the word itself anywhere. C6-27 says such a block 'carries' it; the forbidden-key rule states only the forbidding direction. I read it as REQUIRED, because the forbidden-key matrix of the already-written section 6.11 marks it on all three roles and that section states 'A marked cell is REQUIRED as well as permitted'. If it is optional, case P5.b acquires a fourth sub-branch — the key absent altogether — that no clause covers.
- The rule that a numeric style whose count REACHES the floor is published by NAME and never pooled is what P5's conditions 2, 4 and 5 rest on, and version 4's P2 does not state it: v4:1705-1708 states only the converse (every value under a style name is at least the floor) and v4:1692-1694 mandates pooling only for below-floor styles. Neither forbids pooling a style at or above the floor. The rule is fixed by v5:1391 and v5:1397-1398 and asserted in v6's own reasoning at v6:577-579. I stated it as a premise in this section; if the numeric-styles section does not state it as a rule of the format, every fraction-width bound below rests on an inference — which is the exact failure mode this rewrite exists to end.
- The parse line has no arithmetic definition in ANY contract document. Version 4 publishes `minimum_parse_rate` as a setting but never says how a rate becomes a count; version 6 says only 'applied as a count and never as a compared share'. The function is fixed only at src/synthtwin/taxonomy.py:1666-1676 — the smallest whole number of cells reaching `rate × n_present` — with docs/plans/phase-4-columns.md:395. This section's role-entry paragraph and Q3 both lean on it, and so do AF3 and T5 in other sections. The settings section should name the function.
- No identifier exists for the `count`-versus-`continuous` producer rule. Section 9's producer list carries AF-P, U-P, DF-P, DF-R, CP-P, BD-P, RM-P, FW-P and TU-P; nothing names this one, even though it is a producer obligation of exactly that kind. I stated it in prose rather than invent an identifier the sealed generation method and the test suite do not cite.
- The two published consequences of the role rule — a `count` block carries `integer_valued: true` and `n_negative: 0` — are decidable from a parsed document, and no clause of version 4, 5 or 6 makes either a loader refusal. I stated them as consequences, not as rules. Whether they should become one is an owner call; making them rules needs a `C6-` number I cannot allocate.
- The cross-reference to the numeric-styles section cannot be numbered. Version 4 carries it at §7.5 inside a section titled 'The five version 4 additions, in full' — delta framing that amendment A-P4-11 cannot leave standing. I wrote 'the numeric-styles section'; whoever assembles the document must resolve the number, and must also settle whether P1 through P4 keep their identifiers at that site (they must, by the identifier rule).
- Q9's clause 'and is 0.0 when `n_present` is 0' is unreachable on these two roles by Q3, as version 4 itself observes. It is carried verbatim because version 4 states it and because the same key is read over cores on `affixed_number`, where AF5 makes it unreachable too. It is a rule with no reachable case anywhere it appears.

## CONFLICTS NOTED

- THE ROLE RULE AS VERSION 4 STATES IT IS INCOMPLETE AGAINST THE SHIPPED CODE, and the code is right. v4:1123-1125 says '`count` when every numeric-looking cell is whole and none is negative, `continuous` otherwise' — two conjuncts. The shipped rule has THREE (src/synthtwin/taxonomy.py:4296-4305): `whole_everywhere and n_negative == 0 and n_sign_unsettled_numeric == 0`. A column of `1`, `2`, `(3-)` — the third cell contradictory notation, which parsing.py:1086-1087 gives SIGN_UNKNOWN — is whole in every numeric-looking cell and has no negative cell, so version 4's sentence makes it a `count` while the producer writes `continuous`. I transcribed the three-conjunct rule as the task directs. The reason the third conjunct exists is in the code and is carried into the text: missing evidence is not evidence of nothing.
- AND THE THIRD CONJUNCT IS NOT CHECKABLE FROM A DOCUMENT. `n_sign_unsettled_numeric` is a producer-internal count (taxonomy.py:2275) and is not a key of this format — the forbidden-key matrix has no such row. Version 4 wrote the whole rule as though it were about published facts. I marked the rule a PRODUCER rule and said what a loader enforces instead (A4). Anyone who expects a loader to re-derive the role from the block will be disappointed, and the contract now says so.
- VERSION 6's CASE P5.c IS SELF-CONTRADICTORY AS WRITTEN, and this section closes it rather than carrying it forward. v6:558-559 permits the empty census in P5.c; v6:561-562 then attaches all FOUR conditions to P5.c with condition 1 at v6:564 reading '*F* is at least 1', and the §9 row at v6:1249 restates it the same way. Read literally, version 6 refuses the census its own sentence two lines earlier permits. The plan governs and it is not ambiguous: A-P4-6 words condition 1 as 'its total is at least 1 wherever the census is non-empty' (plan:1976) and adds 'The census may also be empty in that case' (plan:1982-1983). I split P5.c into a non-empty branch carrying conditions 1 through 4 and an empty branch carrying the fifth.
- THE FIFTH CONDITION IS A-P4-8's OWN SENTENCE, NOT A NEW RULE, and I disagree with the derivation material on this point. The vetted material (phase-4-v6-derivation-partition.md, errors 2 and 9) calls extending any bound into the empty branch a RAISE needing a new plan amendment. A-P4-8's closing sentence is plan:2077-2079: 'Where the right-hand side is zero or negative the condition is vacuous and the census may be empty, which is the ordinary case on a column with no decimal cell at all.' That sentence makes the vacuity of condition 4 the ADMISSION TEST for the empty census — which is exactly W ≤ 5 × (floor − 1). The material's own headline counterexample (floor 11, `numeric_styles: {"(withheld)": 51}`, `fraction_widths: {}`) is refused by the amendment as ratified, not by an extension of it. If the owner reads A-P4-8 the other way, condition 5 must come out and that document becomes conforming.
- TWO TIGHTER BOUNDS ARE SOUND AND I DID NOT TRANSCRIBE THEM, BECAUSE THE PLAN GOVERNS. Both are derived in the vetted material over the same partition and both refuse documents version 6 as amended admits. (a) The P-AWARE LOWER BOUND: with P the number of NAMED style keys, `F ≥ W − (5 − P) × (floor − 1)`, because only 5 − P styles other than decimal are unnamed and available to share the pool. A-P4-8 fixes the constant at a flat 5 (plan:2071-2074), which is correct only at P = 0. The material's case — floor 11, `numeric_styles: {"plain": 30, "leading_plus": 20, "(withheld)": 35}` — is admitted by the flat form and describes no table: three unnamed styles hold at most thirty cells and the pool claims thirty-five. (b) THE CASE-1 POOL CAPACITY: where `decimal` IS named, `W ≤ (6 − P) × (floor − 1)`. No amendment states it, nothing in the shipped loader refuses it (contract.py:4273 bounds the `(withheld)` value only at ≥ 1), and it contradicts a version 4 sentence carried in force — P2 at v4:1707-1708, 'its own value may be anything from 1 upwards' — so adopting it needs that clause named as superseded as well as an amendment. Its counterexample: floor 11, `{"decimal": 60, "(withheld)": 51}`, `n_numeric: 60 + 51 = 111`, which passes every rule in force. Each is a RAISE for the owner. Until they are ruled on, version 6 admits two classes of document that describe no table.
- P6's SECOND CLAUSE IS VACUOUS AND I DROPPED IT. v6:593-594 reads 'Every named width's count is at or above `small_cell_floor`, and the `(withheld)` value is 0 or at least 1.' Over non-negative integers the second clause excludes nothing; what the format actually needs there is P7 (a key is present only if its count is nonzero), and version 6's own §9 row at v6:1250 already states P6 without it. I transcribed the first clause only. If the second clause was meant to say something else — a lower bound on the pooled entry — the wording never said it and I did not guess at it.
- I ADDED THE LOADER-DECIDABLE SLICE OF Q7, drawn from the shipped loader. v4:1175-1179 states Q7 as 'null only when the exact mean is not a finite binary64 value' and adds 'a loader accepts null'. The shipped loader is narrower and refuses one case: contract.py:4180-4189 refuses a null `mean` where the ladder is flat, on the ground that the mean of identical values is that value. The two agree — a flat column's exact mean is finite — but only the shipped form is checkable, so I wrote both. Without it Q7 is a rule no loader can apply, and version 4's own wording invites two implementations to differ.
- WHERE THE REST OF §7.5 BELONGS, since the task asked. The six styles, the first-match-wins classification ladder with its normative order, the wire shape, invariants P1 through P4 and the recount identity belong in the numeric-styles section, stated once. What belongs HERE, beside the key table, is the role restriction (because it is a fact about which blocks carry the key) and the whole of `fraction_widths`, because that key's rules are stated over `numeric_styles`' published shape and splitting them across two sites is precisely the two-site defect A-P4-11 exists to end. If the numeric-styles section also states C6-27 through C6-30 or P5 through P7, one of the two sites must be cut before this document ships.
- A MINOR PRODUCER/CONTRACT MISMATCH, recorded and not written into the text. v4:1142 defines `integer_valued` as 'true when every numeric-looking cell is a whole number'; the producer computes `whole_everywhere = (n_whole == numeric_looking and numeric_looking > 0)` (taxonomy.py:4296-4298), adding a non-emptiness conjunct. On these two roles Q3 makes the conjunct unreachable, and on `affixed_number` AF5 does the same, so the two definitions agree on every producible profile. No wording change made.

## SOURCES

Every enumeration and every clause below was read at the line cited; nothing was completed from pattern.

PLAN (governs on conflict) — docs/plans/phase-4-columns.md: A-P4-5 at :1916-1953 (sibling placement, its reason, and the retraction of "binds nothing"); A-P4-6 at :1955-2001 (the three pooled bounds, and :1982-1983 "The census may also be empty in that case"); A-P4-8 at :2051-2101 (the fourth bound F ≥ W − 5 × (floor − 1) at :2074, its worked counterexample at :2057-2065, and its closing sentence at :2077-2079 tying the EMPTY census to the same condition); A-P4-11 at :2225 (version 6 states every rule itself); the parse-line-as-count rule at :395.

VERSION 6 — docs/spec/profile-contract-v6.md: Q1 at its true width :395-400; the numeric_styles role restriction and "`fraction_widths` sits beside it … on the same three roles" :402-410; C6-6's quantitative key list :251-261; C6-7 :263-275 and AF7 :285-287; C6-27 through C6-30 with P5.a/b/c, its four conditions and the revision-3 history :515-591; P6 and P7 :593-596; S13's fraction-width ENTRY :598-624; the §8 disposition row for `fraction_widths` :1182; the §9 producer row FW-P :1230 and the rows for P5, P6, P7 :1245, :1249, :1250.

VERSION 4 (most transcribed text) — docs/spec/profile-contract-v4.md: §6.7 entire :1121-1197 (key table and Q1 through Q11); §5.6 the ladder with L1, L2, L3 :668-695; §7.5.2 the role restriction and its `numeric_unrepresentable` reason :1597-1608; P1 through P4 :1696-1717 (referenced, not transcribed); the styles wire shape and pooling :1676-1694; §9.4 the numeric-role dispositions :2058-2074; §6.2's key meanings for the sign and whole families :748-760. Version 5 changes nothing in the numeric roles: a grep for the Q identifiers over docs/spec/profile-contract-v5.md returns no hit.

VETTED MATERIAL — docs/plans/reviews/material/phase-4-v6-derivation-partition.md, CHECK SET (a) at :44-97: the definitions of K, W, P, F at :48; the master identity at :50-52; the trichotomy and its CORRECTED citation at :54-61; A1 at :63-65; A3.1/A3.2/A3.3 at :75-84; the four catches at :85-89; the key-set corollary A4 at :91-93; and its completeness argument at :95. Its governance items 1, 2, 4 and 9 are carried into `conflicts noted` rather than into the text.

SHIPPED SOURCE — src/synthtwin/contract.py:438-445 (NUMERIC_STYLES, six members, read element by element: `plain`, `leading_zero`, `leading_plus`, `decimal`, `exponent_lower`, `exponent_upper`); :4090-4230 (the loader's Q1, Q3, Q2, Q4, Q5, Q6, Q7, Q9, Q10, Q11 refusals and the `std >= 0` range check); :4260-4304 (the styles loader: membership, P2, P1, P3). src/synthtwin/taxonomy.py:4296-4305 (`whole_everywhere` and `counts_things`, the three-conjunct role rule); :2266-2280 (`n_sign_unsettled_numeric` and the comment fixing why it is narrower than `n_sign_unknown`); :2340-2400 (the tally, and the repaired line that now counts ordinary text into both margins); :3316-3345 (`_numeric_details`, every published key); :3698-3712 (the unrepresentable rule and the "share ONE line" comment); :3826-3834 (RULE 6, `numeric_looking >= strict_needed`); :1666-1676 (`_needed`, the parse line as a count); :1659-1663 (`_share`). src/synthtwin/parsing.py:1070-1104 (`numeric_sign`: contradictory notation and ordinary text are SIGN_UNKNOWN, an out-of-range value keeps its sign) and :1108-1134 (`numeric_whole`).

ADJACENT SECTIONS read for agreement, and deliberately not duplicated — docs/spec/v6-build/s5.md (the twenty-two universal keys, the axes table, A4, and "The order is a PRODUCER rule"), r1.md:140-237 (`numeric_unrepresentable`, the shared parse line, U-P's population argument), r6.md:165-312 (the forbidden-key matrix: `count` 15 marked cells, `continuous` 15, and the three confinements), s4.md:322-451 (S13's list, which already carries `fraction_widths` at the ENTRY), s1.md:159-217 (the terms `the floor`, `withheld`, `the ladder`).