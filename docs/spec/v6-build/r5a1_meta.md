VERDICT SOUND_WITH_CORRECTIONS errors=15

## ERRORS

1. SELF-CONTRADICTION INSIDE C6-4. The draft writes 'applied as a COUNT and never as a compared share' and then, three lines later, 'the pair must clear the parse line AS A SHARE of the present cells AND clear the floor as a count'. The line is a count in both sources (docs/spec/profile-contract-v6.md:234-236; docs/spec/v6-build/r5b.md:14-17). Corrected to 'TWO bounds are consulted, not one — the affixed-number count against the line, the pair's cell count against the floor'.

2. A RULE SPLIT OUT OF THE CLAUSE THAT A SIBLING SECTION CITES. The draft lifts the longest-then-leftmost core rule into an unnumbered paragraph headed 'The core is a function of the cell.' The rule is inside C6-4 in both sources (docs/spec/profile-contract-v6.md:240-244; docs/plans/phase-4-columns.md:594-597), and docs/spec/v6-build/s1.md:197 cites C6-4 as THE clause fixing 'the core'. As drafted that cross-reference resolves to a clause that no longer contains the rule. Folded back into C6-4.

3. THE PARSE-LINE ARITHMETIC IS NOT AT SECTION 4.4. The draft cites 4.4 alone; 4.4 fixes only the setting, and the count's arithmetic is section 4.5.2 (docs/spec/v6-build/s45.md:706-720). The sibling role cites both (docs/spec/v6-build/r5b.md:15-17). Both citations added.

4. OVER-BROAD OPENING CLAIM. The draft says 'each of those columns was `free_text` before this version'. A low-cardinality affixed column was `categorical` and a two-valued one was `binary` — which is the draft's own later point (rules 5 and 8 run first) and the plan's (docs/plans/phase-4-columns.md:618-621, 689-690, which says only 'today lands in free text' / 'published nothing yesterday'). Narrowed to 'fell to `free_text` unless an earlier rule claimed it'.

5. IDENTIFIER COLLISION WITH THE `V` INVARIANT FAMILY. The draft writes bare '`V1`, `V2`, `V3` visit labels'. `V4` is an inherited invariant family this document preserves by name (docs/spec/v6-build/ASSEMBLY.md:22-26), so a reader meets `V1` in a normative sentence with two possible referents — exactly the collision standing check 4 exists for (ASSEMBLY.md:150-154). Reworded to 'a visit column whose cells read `V1`, `V2`, `V3`'.

6. THE COMPETING-READINGS REMARK IS CLAIMED TOO WIDELY, TWICE. The draft says a declining column carries it and that 'a column that fell to a later rule ... says so in its own evidence'. `remark_no_reading_fits` is a free-text form: its base sentence reads 'synthtwin describes it as free text and publishes no value of it at all' (docs/spec/v6-build/s45.md:418-444). A mixed-affix or removal-thinned column that lands in `long_tail_labels` carries no such remark, so as drafted the loudness claim is false for that landing. Scoped to `free_text` in both places and the form named, as the sibling names its own (r5b.md:104).

7. DELTA DEFECT CONFIRMED, DRAFT'S CALL UPHELD: C6-6 IS SHORT BY ONE KEY. docs/spec/profile-contract-v6.md:251-261 enumerates fourteen quantitative keys and omits `fraction_widths`, against docs/spec/profile-contract-v6.md:988 (matrix row), :405-410 (C6-NS 'on the same three roles'), :1168 (the §8 role row names `fraction_widths` explicitly), docs/spec/v6-build/r4a.md:3, 90, 92 ('the fifteen keys below', 'Fifteen keys'), r4a.md C6-27, docs/spec/v6-build/r6.md:297-300, and docs/plans/phase-4-columns.md:660-663. Fifteen is right; recorded as a defect to repair in C6-6 at assembly, not a narrowing to preserve.

8. A RANGE CELL WEAKER THAN THE INVARIANT IT ECHOES. The draft's `n_affixed` range gives only lower bounds; AF2 also bounds it above by `n_present` (docs/spec/profile-contract-v6.md:277-280), so a loader reading the table alone accepts `n_affixed > n_present`. This is the exact failure ASSEMBLY.md:120-123 records against an earlier section. Range cell now states AF2 and AF3 in full and cites them; `n_core_numeric` gains AF5's `>= 1`. The draft's own flagged conflict — JSON type 'integer >= 0' against AF2's floor — is resolved this way rather than by changing the type, since `small_cell_floor` is a setting and the type must not encode it.

9. THE OPEN QUESTION ASSEMBLY EXPLICITLY ASSIGNS TO THIS SECTION WAS LEFT OPEN. docs/spec/v6-build/ASSEMBLY.md:110-116: 'whether Q9's denominator and Q2's subtrahend on `affixed_number` are `n_present` or `n_affixed`. The affixed-number section must settle that, and it is the one open question this build has raised that no artifact answers.' The draft declared it a gap and pushed it to AF7, which does not answer it (r4a.md's closing and AF7 both substitute only `n_core_numeric` for `n_numeric`). Settled in C6-7: every invariant stated over `n_present` is read over `n_affixed`, so `n_left_out_of_statistics` is `n_affixed` less `n_core_numeric` and `numeric_share`'s denominator is `n_affixed`. This is the only reading that closes with AF4's census.

10. DEFINITIONAL HOLE UNDER THE DRAFT'S OWN (CORRECT) READING OF `n_affixed`. The draft counts pair-wearing cells whose core does not parse, but C6-4 defines 'core' only as a substring the classifier reads as a number, so such a cell has no defined core to classify into `n_core_not_numeric`. One sentence added: for a counted cell the core is the text between the published prefix and suffix, whether or not it parses; C6-4's total order chooses the split on an affixed number and the pair, once fixed, splits every other counted cell.

11. DROPPED RIDER RESTORED. docs/plans/phase-4-columns.md:667-673 requires a NEW plain-number straggler construction ('a conforming plain-number spelling that reads back as a number, collides with no published hole spelling and no date form'), 'named here because the existing straggler set covers only out-of-range, contradictory and text cells'. The draft said only 'the twin writes them through the straggler constructions', dropping the addition. Restored in one clause.

12. GLOSS DRIFT ON `n_zero`. docs/spec/v6-build/r4a.md:80 says 'parsed values equal to zero'; the draft says 'CORES equal to zero', which reads as a test on the core TEXT. Corrected to 'parsed CORE values equal to zero'.

13. SECTION NUMBER ALIGNED. Heading changed from 6.14 to 6.12, matching this write's slug (`6.12a`) and the prior draft of the same section (docs/spec/v6-build/r5a.md:1). Section order is not rule order in this build — r5b.md:1 numbers `time_of_day` 6.13 while r5b.md:4 makes it rule 9 — so the heading carries no ordering claim. Assembly renumbers regardless; the draft's own gap note on this stands.

14. GAP CONFIRMED AND STILL OPEN — NOT REPAIRED HERE, AND THE DRAFT'S CAUTION IS JUSTIFIED. Where `minimum_parse_rate` is at or below one half, twice the parse-line count can be at most `n_present`, so two disjoint pairs can each clear the line and the floor while neither puts the other 'past the slack the line leaves', and C6-5's decline sentence (profile-contract-v6.md:246-250; phase-4-columns.md:598-606) does not fire. The sibling role fixed exactly this shape ('where BOTH forms clear the line, the finer wins', r5b.md:47-55); no ordering over affix pairs is stated anywhere, and making up one is not a transcription. The owner or the assembler must rule. Left unwritten deliberately.

15. AF FAMILY SIZE — ASSEMBLY NOTE IS WRONG, NOT THE DELTA. ASSEMBLY.md:29 fixes the new family as `AF1`-`AF4`; the delta states AF1 through AF7 (profile-contract-v6.md:277-288) plus the producer obligation AF-P (:1224), eight identifiers. The corrected section cites AF2, AF3 and AF5, all of which part two states, so its cross-references resolve; ASSEMBLY.md:29 needs correcting to `AF1`-`AF7` plus `AF-P`.

## GAPS

- TIE-BREAK MISSING, and its sibling role has one. C6-5 and P4-D4.1 both say a column whose cells wear more than one pair past the slack does not take the role, which I transcribed as a decline. But where `minimum_parse_rate` is low enough that twice the parse-line count is at most `n_present`, TWO distinct pairs can each clear the line and the floor, and neither source says whether the column declines or one pair wins. `time_of_day` fixed exactly this shape explicitly (v6-build/r5b.md: 'where BOTH forms clear the line, the finer wins'); the affix rule has nothing. The assembler or the owner must rule.
- WHAT `n_affixed` COUNTS IS NOT STATED IN ONE PLACE, and the answer changes what three keys mean. The §8 table (v6 line 1170) says 'present cells wearing the pair'; C6-4 defines an affixed number as one whose core is HOLDABLE; AF4 makes the four core-class counts sum to `n_affixed`; C6-7 says the cores are 'classified by the one shipped classifier over the core substring alone'. If `n_affixed` counted affixed numbers only, `n_core_out_of_range`, `n_core_contradictory` and `n_core_not_numeric` would be identically zero in every conforming document. I therefore wrote the reading that gives them content — pair-wearing cells are counted, only holdable-core cells count toward the line — and stated it as a rule with its reason. If the owner intends the other reading, three keys and AF4 have to go.
- THE QUANTITATIVE DENOMINATORS OVER CORES ARE UNFIXED. AF7 says every `count` invariant is read over the cores with `n_core_numeric` for `n_numeric`, but Q2 (`n_left_out_of_statistics == n_present - n_numeric`) and Q9 (`numeric_share = (n_numeric + n_out_of_range + n_contradictory) / n_present`) also name `n_present`, and no source says whether `n_present` becomes `n_affixed` on this role. My key table says `numeric_share` is 'the share of the cores'; the arithmetic belongs to AF7, which part two owns.
- THE SENTINEL-OVER-CORES RULE IS ABSENT FROM THE DELTA. v6 §3.2 states no ordering clause at all (grep for 'sentinel' finds nothing between lines 232 and 292), while the calendar-placeholder pass got one, C6-34. I built C6-54 from P4-D4.1's 'stand-in numbers are judged before the role tests that can see them' paragraph plus repair rows P4-P1-F4, P4-P2-F6 and P4-P3-F3. It is new text in the contract, not a transcription, and should be checked against the plan by the reviewer.
- NUMBERING IS PROVISIONAL. The heading 6.14 and the clause number C6-54 are placeholders for the assembly renumber: r5b.md numbers `time_of_day` 6.13 and r5c.md numbers `long_tail_labels` 6.7, which already collides with r4a.md's numeric roles at 6.7.
- WHETHER `numeric_styles` AND `fraction_widths` CENSUS THE CORE SPELLING OR THE WHOLE CELL is stated only by implication. P4-D4.1 puts 'the styles block including P4-D4.5's fraction-width fact' inside the block 'computed over the cores', so I wrote CORES. Nothing says it in the contract's own words.
- NOT WRITTEN, by instruction, and owed by part two: AF1 through AF7 and AF-P; C6-9, the publication class and its one named ranges-class exception; the keys forbidden on this role; and the standing `remark_affixed_numbers_may_be_codes` with the `--identifier` route and its positional affix-argument binding (v6 lines 1003-1044, 1106-1107; amendment A-P4-7).

## CONFLICTS NOTED

- C6-6'S KEY LIST IS SHORT BY ONE — `fraction_widths` is missing from it. The delta's C6-6 enumerates fourteen quantitative keys (v6 lines 253-262) and does not name `fraction_widths`, while FOUR other governing sites require it on this role: the delta's own forbidden-key matrix row (v6 line 988) lists it; C6-NS's closing sentence says '`fraction_widths` sits beside it under C6-27, on the same three roles' (v6 lines 405-410); v6-build/r4a.md C6-27 says a `count`, `continuous` or `affixed_number` block carries it and counts FIFTEEN quantitative keys; and P4-D4.1 says the published block is 'the styles block including P4-D4.5's fraction-width fact'. The plan governs, so I wrote fifteen. C6-6's fourteen-key list is a defect to fix at assembly, not a narrowing to preserve.
- THE AFFIXED INVARIANT FAMILY IS BIGGER THAN THE ASSEMBLY NOTE SAYS. ASSEMBLY.md fixes the new family as `AF1`-`AF4`. The delta states AF1 through AF7 in C6-8 (v6 lines 277-288) and AF1-AF7 PLUS the producer obligation AF-P in the §9 table (v6 lines 1220-1240) — eight identifiers, not four. Part two must decide, and the assembly note needs correcting either way.
- THE TWO SOURCES NUMBER THE RULE ORDER DIFFERENTLY, and the phrase 'rules 0 through 7' is the trap. The plan is 0-based — `empty` is 0 and `affixed_number` is 9 (plan lines 486-501) — while v6-build/s5.md C6-1 is 1-based — `empty` is 1 and `affixed_number` is 10. I translated the plan's 'only after rules 0 through 7 have all declined' into s5.md's numbering as 'rules 1 through 8', i.e. through `categorical`, and the plan's 'rules 8 through 11' as 'rules 9 through 12'. Any section that copies the plan's numerals verbatim will be off by one.
- C6-6 TYPES `n_affixed` AS 'integer >= 0', A RANGE AF2 MAKES UNREACHABLE. AF2 requires `small_cell_floor <= n_affixed`, and `small_cell_floor` has a stated minimum of 1, so no conforming block ever carries 0. I wrote the JSON type as integer >= 0 and put the real floor in the range column, pointing at the invariants; a loader that enforces only the stated type accepts documents AF2 refuses.

## SOURCES

Read in full: docs/spec/profile-contract-v6.md §3.2 (C6-4 through C6-9, lines 232-292), plus its §3.5 settings note (lines 375-385), §3.6 C6-Q1/C6-NS (lines 386-410), the ranges-class row and forbidden-key matrix rows for this role (lines 907, 988-992), the §8 key table (lines 1166-1176) and the §9 invariant table (lines 1220-1240). Read in full: docs/plans/phase-4-columns.md P4-D4.1 (lines 587-690), the rule-order list (lines 486-513), the publication-class ruling (lines 545-560), amendment A-P4-7 (lines 2003-2040), residual R-P4-1 (lines 1507-1510), and the round-repair rows P4-P1-F4, P4-P1-F5, P4-P2-F6, P4-P3-F3, P4-P4-F5 (lines 2402-2460). Adjacent v6-build sections read whole: ASSEMBLY.md (identifier convention, standing checks), s5.md (twenty-two universal keys, the cells-vs-cores note, the axes table, C6-1 rule order), r4a.md (the fifteen quantitative keys, numeric_styles on this role, C6-27 fraction_widths, the Q family and its closing 'read over the cores' sentence), r5b.md (the sibling new role, for voice and for the ranges-class exception it points at), r6.md (publication classes; its lines 77, 297, 311 cite C6-7 and C6-9 by number), s1.md (lines 196-197 cite C6-4 and C6-5 for 'the affix pair' and 'the core'). Shipped source checked: grep for 'affix' over src/synthtwin/ returns NOTHING — the role is unimplemented, so no constant fixes any of these key spellings and no code disagreement is possible; the two independent enumerations in the delta (C6-6 and the §8 key table) were compared element by element and agree on all seven role-own keys. Identifiers: C6-4, C6-5, C6-6, C6-7 are kept at the delta's assignment because s1.md cites C6-4/C6-5 and r6.md cites C6-7 by number; the sentinel clause, which no source numbers, took C6-54 (highest number in use anywhere is C6-53).