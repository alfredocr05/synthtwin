VERDICT SOUND_WITH_CORRECTIONS errors=9

## ERRORS

1. SUBSTANTIVE, THE ONE WRONG DISPOSITION. `time_of_day` `n_distinct`/`n_distinct_folded` written EXACT-OBSERVABLE. The section derives it from plan P4-D8 item 1 (docs/plans/phase-4-columns.md:1305-1310, 'The G11 all-different table gains three rows — ... time-of-day through ordinals with its finite-space bound'). The inference is invalid, and its own closest analogue is the counterexample: `datetime` is IN that same G11 table (docs/spec/generation-method-v1.md:2902, '| `datetime` | the raw spelling | G7.3 with `P` ranks, plus the published offsets of G7.4 |') while its two distinctness counts are APPROXIMATED (docs/spec/profile-contract-v4.md:2099; docs/spec/generation-method-v1.md:3254-3292, closing 'Making the twin's cardinality exact is not in this phase's method; saying so plainly, every run, is'). 'The all-different obligation binds through mechanism X' therefore does not entail EXACT-OBSERVABLE. And the clock construction IS the calendar construction: docs/plans/phase-4-columns.md:735-742, 'Rank 0 and rank last pinned to the endpoints; interior ranks by the same floor-division interpolation the date rule uses' — a value per rank, over a domain strictly MORE constrained than datetime's (one published form, no offset family, no case-carrying spelling). A role cannot be exact where the role it copies is approximated. The same plan clause that the row cites also says 'the approximated-fields table gains the new roles' windowed facts', which is where this fact belongs. Corrected to APPROXIMATED under the same forward-referenced time-of-day clause the interior-rung row already cites, with the all-different obligation and the capacity refusal kept verbatim.

2. ENUMERATION STATEMENT FALSIFIED BY ITS OWN TABLE. §9.5, 'All six rows bind `long_tail_labels` identically' — the sixth row is `level_ceiling`, which the same table marks '(`categorical` only)' and which the sentence's own last clause says the role does not carry. Five rows bind. Sources: docs/spec/profile-contract-v6.md:351-357 (C6-17, `level_ceiling` FORBIDDEN on `long_tail_labels`); docs/spec/v6-build/r6.md:196 (matrix marks `level_ceiling` on `cat` alone); docs/plans/phase-4-columns.md:940-947 ('NOT `level_ceiling`'). Corrected to 'The first five rows bind ... and it carries no `level_ceiling`, so the sixth row reaches it with nothing to dispose.'

3. UNFLAGGED CONFLICT WITH A SIBLING ALREADY WRITTEN — the SUM identity. This section's §9.2 row says 'plus the judged-pass-sourced cells'; docs/spec/v6-build/s5.md:27 and :28 both say 'plus the stand-in-sourced cells', as do docs/spec/profile-contract-v6.md:1380 and docs/plans/phase-4-columns.md:1088-1090. THIS SECTION IS THE CORRECT ONE and the others must widen: C6-37 (docs/spec/profile-contract-v6.md:838-841) leaves calendar-placeholder spellings blank too, so an identity naming only stand-in-sourced cells is false by the same construction argument it rests on. The section flagged the `missing_by_source` half of this widening and missed the sum-identity half; the assembler must widen s5.md:27-28, v6:1380 and plan:1088-1090 rather than narrow this row. I added one clause to §9.2's exception paragraph making the reason explicit so the two sections cannot drift apart again.

4. `clock_form` — THE CONFLICT IS WIDER THAN DECLARED. The section flags only docs/spec/profile-contract-v6.md:1176 (EXACT-CONTROL). It also contradicts a v6-build section already written: docs/spec/v6-build/r5b.md:87 gives `clock_form` EXACT-CONTROL in the `time_of_day` role table. Two written sections now assign one key two dispositions, which is precisely what plan P4-D7 item 1 forbids ('exactly one disposition per fact'). I kept EXACT-OBSERVABLE — it is the better-supported answer, and the plan adds support the section did not cite: P4-D9 item 2 (docs/plans/phase-4-columns.md:1367-1372) lists 'the one form' beside endpoints and rungs among the checks that RE-DESCRIBE the written twin, which is EXACT-OBSERVABLE's evidence route (docs/spec/v6-build/s1.md:148) and not EXACT-CONTROL's 'a CSV cannot evidence' (s1.md:149) — but the owner must rule once and r5b.md:87 and v6:1176 must both take the answer.

5. STATED REASONS DROPPED (the brief requires carrying them). (a) docs/spec/profile-contract-v4.md:2005, 'the plan reserves the report line for facts a rule CAN meet' — the reason a refusal is not a report line; restored. (b) docs/spec/profile-contract-v4.md:2009-2011, 'that is owner decision 4 and it is what G9.5's packing rule requires' — the authority under 'This is not a licence anywhere else'; restored. (c) docs/spec/profile-contract-v4.md:2103-2108, the reason the withheld-offset corner is named in the matrix at all ('a matrix that claimed those fields were exact in every case would be a matrix an implementer trusts and a report the same implementer then cannot make honest'); restored. (d) docs/spec/profile-contract-v4.md:2207-2210, why G9.1 keeps an invented value from beginning with a sign — 'that is the character common spreadsheet software reads as the start of a formula' — without which the carve-out's price is unstated; restored. (e) docs/spec/profile-contract-v4.md:2197-2202, 'where the source's own values show another pairing that holds every published count ... the packing walks every carrier pair and finds the pairing the source's values prove exists'; restored. (f) docs/spec/profile-contract-v4.md:2091, 'No corner, no exception' dropped from the `date_percentiles.min`/`.max` row while kept on `earliest`/`latest`; the later paragraph's 'this table says the two ends are exact with no exception' rests on both rows carrying it; restored. (g) docs/spec/profile-contract-v4.md:2205, 'since owner decision 6 keeps the length' dropped from the `all_whole_numbers` row; restored.

6. CROSS-REFERENCES DELETED WHERE ASSEMBLY.md §2 EXPECTS THEM REPOINTED, and inconsistently with this section's own convention. It keeps 'section 2.2' and 'section 7.5.7' but drops '(owner decision 6, section 6.8)' at docs/spec/profile-contract-v4.md:2273, '(section 7.4)' at docs/spec/profile-contract-v4.md:2277, and 'section 4.3' from the two `source` header rows at docs/spec/profile-contract-v4.md:2013-2014. A deleted pointer cannot be repointed in the single renumbering pass. All three restored at version 4's numbers.

7. RESIDUAL DROPPED. The `min_length`/`max_length` row replaces v4's 'width | not published at all (residual R-P2-1)' (docs/spec/profile-contract-v4.md:2235) and does not say the pair CLOSES that residual, which both governing sources state: docs/spec/profile-contract-v6.md:509-511 ('This settles residual R-P2-1, open for the owner since Phase 2') and docs/plans/phase-4-columns.md:822-823 ('closes residual R-P2-1'). The section names R-P2-5, R-P2-7 and R-P4-12 elsewhere, so the omission is an internal inconsistency. Restored.

8. DELTA FRAMING SURVIVED, ONCE. §9.8, 'On the three roles this version adds the obligation BINDS' — 'this version adds' reads against an unnamed predecessor, which ASSEMBLY.md §4 item 2 bans for the self-contained document. Corrected to name the three roles: 'On `long_tail_labels`, `affixed_number` and `time_of_day` the obligation BINDS'. No `C6-` letter identifier appears anywhere in the section, and the words 'supersedes', 'carried', 'unchanged' and 'version 5' appear nowhere — checked.

9. §9.1 MISLABELS S4. 'membership and order: S1, S2, S3, S4' — S4 is the column-NAME rule (docs/spec/v6-build/s4.md:61-64), which is neither membership nor order; version 4 cited only S1-S3 here (docs/spec/profile-contract-v4.md:2018). Adding S4 is right (docs/spec/v6-build/s4.md:38 calls all four 'the whole of what `columns` means as a container') but the label is wrong. Corrected to 'the four container rules: S1, S2, S3, S4'.

## GAPS

- THE NEW TIME-OF-DAY REFUSAL HAS NO WIRE NAME ANYWHERE. Plan P4-D8 item 5 (lines 1329-1334) says the closed list of four grows by exactly one named refusal for the `n_distinct - n_unparsed` shape, but neither the plan nor version 6 spells its identifier, and the four existing ones are named strings (`generation-domain-too-small`, `generation-counts-contradict`, `generation-words-exceed-length`, `generation-whole-numbers-need-room`, method lines 2954-2965). I wrote the rule without a name rather than mint one. The generation-method amendment must fix it, and §10.7's refusal catalogue must then say five, not four.
- NO WINDOW EXISTS YET FOR THE TIME-OF-DAY INTERIOR RUNGS. Every other APPROXIMATED row in this section cites a G12 sub-clause (G12.2 through G12.8). The clock ladder's two-sided window is a Phase 4 amendment to the generation method that has not been written, so my row cites 'the generation method's time-of-day clause' as a forward reference. The assembler must repoint it once the amendment lands; plan P4-D9 item 2 requires the window to live in the method and be cited, never restated here.
- DISPOSITIONS FOR `n_distinct` AND `n_distinct_folded` ON THE THREE NEW ROLES ARE NOT STATED BY ANY SOURCE CLAUSE, and the vetted material flagged this as a live completeness hole (`docs/plans/reviews/material/phase-4-v6-derivation-datetime.md` line 35: version 4 §9.2 defers them to the per-role-group sections, version 6 §11 is delta-only, and §8's key table omits them because they are not new keys). I derived all three from plan P4-D8 item 1's G11 all-different rows plus P4-D5's 'categorical generation rule verbatim' and P4-D4.2's stated capacity, and said so in the rows. They are the one place in this section where a shape was chosen here rather than transcribed, and §13's decisions list must record it.
- `n_distinct_folded` ON `time_of_day` IS THE WEAKEST OF THOSE THREE. The plan's capacity argument (P4-D4.2 lines 739-748) is stated over `n_distinct` net of `n_unparsed` and says nothing about the FOLDED count. Two clock spellings in one form cannot fold together, so a folded count below raw on such a column can come only from the unparsed stand-ins, and no clause says whether the stand-in family must reproduce fold collisions the way the invention alphabet must (§9.7). I wrote EXACT-OBSERVABLE alongside the raw count; if the stand-in construction cannot always meet it, that is a second refusal or a second APPROXIMATED row and the owner must rule.
- SECTION AND CLAUSE NUMBERS ARE VERSION 4's, per ASSEMBLY.md §2. This section cites 'section 2.2' (the six classes), 'section 7.5.7' (the `numeric_styles` recount identity), 'S1-S4', 'D6', 'D10', 'D11', 'T2', 'AF6', 'G9.1', 'G9.3', 'G9.5', 'P3-D8.1'. All resolve in the sibling build files as written, but every one needs repointing in the single renumbering pass.
- NO `C6-` IDENTIFIER IS MINTED HERE. The completeness assertion, the feasibility posture and the judged-pass exception are written as prose, as version 4 wrote them. If the assembler wants any of the three citable by identifier, they take plain `C6-` numbers from the one document sequence; I did not guess numbers because `r6.md` has already consumed C6-49 through C6-53.

## CONFLICTS NOTED

- THE REPAIR THE BRIEF ASKED FOR, CONFIRMED AND MADE. The delta's §11 row (`docs/spec/profile-contract-v6.md` line 1379) reads 'EXACT-OBSERVABLE, per-spelling recount, stand-in-spelled keys excepted', and the governing reproduction rule C6-37 (lines 838-841) excepts wider: 'a spelling a JUDGED PASS put there — one reading as a stand-in number, or as a calendar placeholder — which stays blank'. C6-38 gives one reason covering both. A producer following the narrow row would reproduce a published calendar-placeholder spelling in the twin, which C6-37 forbids and which C6-40's collision rule would then have to police. My §9.2 row is written at C6-37's width. The same narrow phrasing is in the governing plan at `docs/plans/phase-4-columns.md` lines 1085-1086 ('stand-in-spelled keys excepted, their exception stated in the matrix row') and again at line 1355 ('stand-in keys excepted as the matrix states'), both of which predate the calendar-placeholder pass being folded into the same write rule — so the plan text needs the same widening by amendment, or the plan and its own contract disagree. `docs/spec/v6-build/s5.md` line 26 already writes the wide form, so the two sections now agree.
- `clock_form` — I WRITE EXACT-OBSERVABLE WHERE THE DELTA'S §8 TABLE SAYS EXACT-CONTROL (`docs/spec/profile-contract-v6.md` line 1176). EXACT-CONTROL is defined in `s1.md` §2.2 as 'a metadata or dispatch decision a CSV cannot evidence'. A twin's `time_of_day` cells are all written in the column's one published form — plan P4-D4.2 line 738, 'All cells written in the column's one published form' — so re-describing the twin gives the form back, and a CSV does evidence it. Its exact sibling on `datetime` is `resolution`, which fixes the canonical text of every cell and which version 4 §9.6 (line 2093) makes EXACT-OBSERVABLE. Writing EXACT-CONTROL would put a false sentence into the disposition registry and would drop a reproduction obligation the plan states. Flagged rather than silently kept either way: the owner should confirm, and the registry's role groups must take whichever answer wins.
- `fraction_widths` — THE DELTA'S §8 ROW SAYS 'EXACT-OBSERVABLE' FLAT (line 1182), WHICH IS TRUE ONLY WHERE NOTHING POOLED. The governing plan fixes the check as a window: 'recounted cells at a named width number at least the published count and at most the published count plus the pool … exact where nothing pooled, windowed where something did' (`docs/plans/phase-4-columns.md` lines 866-871). That is the same shape version 4 §9.4 already uses for `numeric_styles` — EXACT-OBSERVABLE *against a stated recount identity* — so I wrote the row that way on both `count`/`continuous` and `affixed_number`. A reader taking the flat wording would expect per-key equality and would call a conforming twin defective on any column with a pooled remainder.
- NO SOURCE CODE EXISTS FOR ANY OF THE THREE NEW ROLES, SO NO CODE/CONTRACT CONFLICT COULD BE FOUND FOR THEM. `src/synthtwin/taxonomy.py` lines 206-244 ships exactly ten roles and lines 258-263 ship the three publication tuples over those ten; `grep -rn 'time_of_day|affixed_number|long_tail_labels' src/` returns nothing. Every disposition in 9.4's affixed part, 9.5's long-tail sentence and 9.6's clock part is therefore transcribed from the plan and the delta only, and is unchecked against an implementation. The inherited rows in 9.1, 9.2, 9.3, 9.4's first table, 9.5, 9.6's datetime part, 9.7 and 9.8 are version 4's text against version 4's line range 1972-2291.

## SOURCES

Transcribed element by element from, in the order they win: (1) the ratified plan `docs/plans/phase-4-columns.md` — head/feasibility posture from its P2-D6 inheritance, P4-D4.1 (affixed: what it publishes, what the twin writes, cores), P4-D4.2 lines 711-750 (time-of-day: endpoints, ordinal ladder, "all cells written in the column's one published form", the finite-space capacity 1,440/86,400 and the `n_distinct - n_unparsed` refusal), P4-D4.3/A-P4-1 (eleven format members), P4-D4.4 lines 802-823 (unrepresentable lengths, retirement of the 400-figure width, width-pinned fold-partner clause), P4-D4.5 lines 824-880 (fraction widths: pinned cells never snapped, validator window "at least the published count and at most the published count plus the pool"), P4-D5 lines 932-975 (long-tail publishes the four shared label keys, "the categorical generation rule verbatim", no `level_ceiling`), P4-D6.1 lines 1016-1105 (reproduction rule, collision rule with no runtime escape, the moved dispositions and the SUM identity at lines 1085-1094), P4-D7 items 1-6 lines 1160-1230, P4-D8 items 1-5 lines 1300-1351 (G11 all-different table gains three rows: long-tail via the label mechanism, affixed via the numeric mechanism over cores, time-of-day via ordinals with its finite-space bound; refusal catalogue grows by exactly one), P4-D9 items 2-3. (2) `docs/spec/profile-contract-v6.md` — C6-6/C6-7/C6-8 (affixed keys and cores), C6-11/C6-12/C6-13 (clock keys, T1-T5, the line model), C6-16/C6-17 (long-tail keys, `level_ceiling` forbidden), C6-25 (`resolution_mix` REPORT-ONLY), C6-26 (U5, the two lengths), C6-27 through C6-30 (`fraction_widths` sibling placement, P5-P7), C6-35/C6-CAND (placeholder verdicts through the standing verdict machinery), C6-36 (`built_in_dates` LOADER-ONLY), C6-37/C6-38/C6-39/C6-40 (the twin's one write rule and why judged-pass cells stay blank), C6-Q1 (`n_rows` echo LOADER-ONLY on three roles), §8's key table lines 1166-1187, §9's invariant list lines 1219-1259, §11 lines 1372-1386. (3) `docs/spec/profile-contract-v4.md` lines 1972-2291 — §9 head, 9.1-9.8 transcribed; plus §7.4.6 line 1565-1577 (variants disposition and its consequence for raw distinctness) and §7.5.7 lines 1719-1777 (the `numeric_styles` recount identity and the pooled-cell rule). (4) v6-build siblings for consistency: `s1.md` §2.2 (the six classes and their evidence), `s4.md` §4.1/§4.3/§4.4 (the nine top-level keys, `source`'s five, the seventeen settings keys, the five declaration-record keys all LOADER-ONLY), `s5.md` §5.1 (the twenty-two universal keys, and the `missing_by_source` wording already written at the reproduction rule's width), `r1.md` §6.1/§6.2, `r2.md` §6.3.2, `r3.md` §6.6.2 (D9-D11, `format` and `resolution_mix` REPORT-ONLY), `r6.md` §6.10/§6.11 (publication classes over thirteen roles; the forbidden-key matrix, which fixed the exact per-role key sets this matrix must cover). (5) Shipped source checked for enumerations with a constant: `src/synthtwin/taxonomy.py` lines 206-262 confirms the shipped role tuple is the TEN pre-Phase-4 roles and the three new roles are absent from `src/`, so no shipped constant fixes their dispositions and none was inferred from code. Refusal names `generation-domain-too-small`, `generation-counts-contradict`, `generation-words-exceed-length`, `generation-whole-numbers-need-room` read from `docs/spec/generation-method-v1.md` lines 2954-2965.