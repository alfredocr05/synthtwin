VERDICT SOUND_WITH_CORRECTIONS errors=22

## ERRORS

1. ENUMERATION CHECK — the six numeric styles. Verified element by element against src/synthtwin/contract.py:438-445 (`plain`, `leading_zero`, `leading_plus`, `decimal`, `exponent_lower`, `exponent_upper`) and against docs/spec/profile-contract-v4.md:1610-1622. Six present, six in source, same order, gloss text identical word for word. NO ERROR.

2. ENUMERATION CHECK — the fifteen added keys. Verified both directions against docs/spec/profile-contract-v4.md:1129-1145 (fourteen) plus `fraction_widths` from docs/spec/profile-contract-v6.md:515-517, and against the matrix column in docs/spec/v6-build/r6.md:205-226, whose count line at r6.md:250-252 marks `cnt` 15 and `ctn` 15. Fifteen present, fifteen in source, none extra, none missing. Cross-checked against C6-6's list at v6:252-261 (fourteen quantitative keys) + C6-27. NO ERROR.

3. ENUMERATION CHECK — the eleven ladder rungs. `min`, `p01`, `p05`, `p10`, `p25`, `p50`, `p75`, `p90`, `p95`, `p99`, `max` match v4:672-674 and docs/spec/v6-build/s1.md:176 exactly. Eleven present, eleven in source. NO ERROR (but see the duplication finding).

4. ENUMERATION CHECK — the Q family. Q1 through Q11 all present and in order, transcribed from v4:1147-1197. Q2, Q3, Q4, Q5, Q6, Q7, Q8, Q9, Q10, Q11 are verbatim; Q1 is v4's text with only the role list widened, which is exactly what C6-Q1 at v6:395-401 licenses ("Only the role list moves; every other word is Q1's"). NO ERROR.

5. WRONG RULE — condition 1 of the count verdict is ambiguous and false on its natural reading. The section says "the count of present cells whose notation settles wholeness equals the numeric-looking count". "Settles wholeness" reads as settling the wholeness QUESTION, which is n_whole + n_fraction; the shipped rule is `cells.n_whole == numeric_looking` (src/synthtwin/taxonomy.py:4297-4299) and v4:1131 defines the key as "present cells whose notation settles that the value is a whole number". As written the clause admits a column of `1.5`, `2.5`, `3.5` as `count`. Corrected to "every numeric-looking cell's notation settles that its value is a WHOLE NUMBER".

6. DELTA FRAMING SURVIVED — "**Version 4's reason, carried:**" in the `numeric_styles` sub-section. Both "version 4" and "carried" are on the forbidden list (docs/spec/v6-build/ASSEMBLY.md:104-107: no "supersedes", no "carried", no "unchanged from version 5", no "as version 4 has it"). Corrected to "**The reason.**".

7. INTERNAL CONTRADICTION — the section restates version 4 §7.5.5's wire shape ("Keys are style names from those six … pooled into `(withheld)`, so a single oddly-written cell cannot be singled out") and then, in the next sentence, says "the wire shape … [is] stated in section 7.5 and … not restated here". docs/spec/v6-build/ASSEMBLY.md:88-91 records the agreed split ("the numeric roles author transcribed the role's use of it and left the full specification for the additions section"), so the wire-shape sentence is the one that goes. Removed.

8. INVENTED — five `range` cells that version 4 leaves as "—" (v4:1136-1140): `n_zero` "≤ n_numeric" (restates Q11); `n_negative` "≤ n_present" (stated by NO artifact — not v4, not v5, not v6, not the shipped loader); `n_negative_unrepresentable` "≤ n_out_of_range" (restates HALF of Q10 and silently drops "≤ n_negative"); `n_used_in_statistics` "== n_numeric" and `n_left_out_of_statistics` "== n_present − n_numeric" (restate Q2). Stating a bound in the table and again in the Q family is the two-sites failure this rewrite exists to end, and the Q10 cell states it weaker in one place than the other. All five reverted to "—".

9. DROPPED — the disposition cells lose the three pointers that fix where the approximations live. v4:2063 gives the interior rungs "APPROXIMATED, inside a rung-by-rung two-sided envelope — fixed by `docs/spec/generation-method-v1.md` G5.6, restated as G12.2"; v4:2066 gives `mean`, `std`, `skew` "APPROXIMATED, fixed formula and two-sided bound — both fixed by … G12.3"; v4:2068 gives `numeric_styles` "EXACT-OBSERVABLE against the recount identity of section 7.5.7: every published count is met or exceeded, the three forms the remainder cannot reach are exact, and the remainder is spelled by its own cells' values". The transcription keeps only the bare class names. All three restored in a paragraph under the table.

10. DROPPED — v4 §9.4 states a disposition for `n_distinct` and `n_distinct_folded` ON THIS ROLE GROUP (v4:2069, envelope G12.8, plus the both-ends-printed rider from review item P2-C2-F4), because v4:2044-2045 and docs/spec/v6-build/s5.md §5.1 both set those two "per role group, section 9". If this section becomes the single site for per-key dispositions, as the transcription's own note recommends, those two lose their home. Added a one-line pointer rather than a restatement.

11. DROPPED — P7's stated reason. v6:594-595 reads "A width key is present only if its count is nonzero. This closes the route residual R-P3-12 records." The residual closure is dropped. Restored. (The added "equivalently, F is zero if and only if `fraction_widths` is the empty object" IS sourced — derivation material CHECK A3.1 — and is kept.)

12. INVENTED — FW-P's last clause, "and two documents can satisfy every checkable condition and disagree about what the source held." No artifact states it for FW-P; v6:1230 stops at "the shape is a producer obligation". The sentence is modelled on RM-P (v6:1229), which carries its own worked 40/60-vs-50/50 case. Trimmed to v6:1230's wording.

13. OVER-STATED AGAINST THE PLAN — the fifth condition. The section writes "FIVE conditions bind, four on the non-empty branch and one on the empty branch", declares it a RAISE on the ratified plan, and demands the §9 row at v6:1249 be reworded from FOUR to FIVE. The declared conflict does not exist. Amendment A-P4-8 at docs/plans/phase-4-columns.md:2046-2048 attaches condition 4 to the empty branch itself: "Where the right-hand side is zero or negative the condition is vacuous AND THE CENSUS MAY BE EMPTY" — permission to be empty is tied to the vacuity of condition 4, so at F = 0 condition 4 already reads W ≤ 5 × (floor − 1) and already refuses the {"(withheld)": 51} / {} document. What v6 gets wrong is only condition 1: A-P4-6 states it at plan:1945 as "its total is at least 1 WHEREVER THE CENSUS IS NON-EMPTY", and v6:564 drops the qualifier — which is the whole of the self-contradiction between v6:558-559 and v6:561-564 that the vetted derivation names as its finding 6. Corrected to FOUR conditions with A-P4-6's own qualifier restored on condition 1, plus a paragraph reading condition 4 at F = 0 and carrying the floor-11 example. This closes the contradiction, keeps §9's "FOUR ways" correct, and deviates from the ratified plan in no direction.

14. DECLARED GAP THAT IS NOT A GAP — "REQUIRED vs PERMITTED for `fraction_widths` is not stated by any artifact in those words." It is settled twice: docs/spec/v6-build/r6.md:314-315, "**The matrix is total.** A conforming column block's key set is exactly the twenty-two universal keys plus the marked cells of its column", with `fraction_widths` marked on `cnt`, `ctn` and `afx` at r6.md:226; and docs/spec/v6-build/s5.md §5.1, "This format has no optional keys, on purpose". REQUIRED is right and case P5 acquires no absent-key sub-branch. The gap is closed; the transcription reached the right answer for the wrong reason.

15. DUPLICATION — the ladder paragraph restates the eleven rung names and L1, L2 and L3, which are fixed in section 2.3 (s1.md:176) and section 5.6 (v4:670-696). The restatement also drops L3's load-bearing pointer (v4:690-693: the value a generator writes for a null rung "is fixed by `docs/spec/generation-method-v1.md` G5.1, so a null rung is one rule and not two"), so it is strictly worse than a pointer. Replaced with a pointer, keeping the disposition split and v4:2075-2077's mutant rider, which belongs with the disposition.

16. DUPLICATION — the "At a floor of one" paragraph reproduces docs/spec/v6-build/s4.md:377-386 nearly sentence for sentence, including the two-cell width-2 example. S13 and its exhaustive list are section 4's, and s4.md already gives the ENTRY-not-field reading for all four positions. Compressed to a pointer plus the one consequence a reader of this key needs.

17. AGREES WITH SHIPPED CODE, with the gap now named in the text rather than only in a cover note. Condition 3 (`n_sign_unsettled_numeric == 0`, taxonomy.py:4303) is redundant under the shipped parser: `numeric_sign` returns SIGN_UNKNOWN for a numeric-looking cell only when it is NUMBER_CONTRADICTORY (parsing.py:1086-1087 — the later `ungrouped is None` branch at :1098-1100 is unreachable for NUMBER and NUMBER_OUT_OF_RANGE, both of which require valid grouping), and `numeric_whole` gives every contradictory cell WHOLE_UNKNOWN (parsing.py:1133), so any cell failing condition 3 already fails condition 1. v4's own voice states exactly this kind of fact in normative text (L3: "No producible profile is known to reach this"), so the rider is written into the section.

18. DISAGREEMENT WITH THE CONTRACT BASE, reported not resolved: v4:1123-1125 states the verdict in TWO conditions ("`count` when every numeric-looking cell is whole and none is negative"); the shipped rule at taxonomy.py:4297-4303 has THREE. `n_sign_unsettled_numeric` appears in no contract document and in no plan amendment — grep over docs/ returns nothing. The three-condition form is transcribed because the code is the repaired artifact and the two forms are currently equivalent, but a reviewer should know the third condition enters version 6 from source alone.

19. MINOR — `percentiles` JSON type given as "ladder object"; v4:1130 says "ladder of numbers". v4's wording is itself inaccurate (L3 permits null rungs), so "ladder object" is kept; recorded so the change is not mistaken for a slip.

20. MINOR, NOT AN ERROR — P6's dropped clause "and the `(withheld)` value is 0 or at least 1" (v6:594). The drop is authorized by docs/plans/reviews/material/phase-4-v6-derivation-partition.md, NOTE FOR THE TRANSCRIBER: the clause is vacuous over non-negative integers, P7 carries the intended rule, and v6's own §9 row at :1250 already omits it.

21. MINOR, NOT AN ERROR — "Falling short of the line decides nothing but this rule — the column goes on to `categorical`" is not invented: it transcribes taxonomy.py:3832-3834, and the code's RULE 7 is rule 8 `categorical` under section 5.2's numbering. "Rule 7" for count/continuous is correct against docs/spec/v6-build/s5.md's rule list.

22. ASSEMBLY CAUTIONS, confirmed: (a) the P family is split — P1-P4 govern `numeric_styles` in §7.5, P5-P7 govern `fraction_widths` here; both sites must exist or the citations dangle. (b) v6:166-167's change table cites `C6-Q1` and `C6-NS` by name and must be repointed to `Q1` and to the §7.5 prose, per ASSEMBLY.md:36-40. (c) The parse line still has no arithmetic definition in any contract document — this section says "the parse-line count" and section 4.4 or 2.3 must define it (taxonomy.py:1666-1676 `_needed`, `minimum_parse_rate` 0.99 at :1164, "applied as a COUNT, never as a compared share" at :1158-1159, plan:395). (d) `fraction_widths` has no recount identity anywhere and no occurrence in src/ at all, so P5-P7 and FW-P are unimplemented; the shipped loader enforces P1, P2, P3 and style-name membership only (contract.py:4273-4303). (e) The (5 − P) tightening (derivation CHECK A3.3) and the named-decimal pool bound (CHECK A2) are correctly NOT written and both still need owner amendments; A2 additionally needs v4:1707-1708's `(withheld)` clause restated with the bound in it, since "supersedes" may not be written.

## GAPS

- REQUIRED vs PERMITTED for `fraction_widths` is not stated by any artifact in those words. C6-27 says such a block "carries" it (v6:515-517) and the forbidden-key matrix states only the forbidding direction. I wrote it REQUIRED, matching C6-NS's wording for its sibling (v6:402-410, "on the same three roles, by the same argument") and v4's use of "carries". If it is OPTIONAL, case P5 acquires a sub-branch (key absent) that no clause covers and my table's disposition row is wrong.
- The PARSE LINE has no arithmetic definition in any of v4, v5 or v6. My detection paragraph and AF3/T5 all depend on it. The function is fixed only in code (taxonomy.py:1666-1676 `_needed`, with minimum_parse_rate 0.99 at :1164 and "applied as a COUNT, never as a compared share" at :1158-1159) and in plan:395. Some section of the self-contained v6 must state it; I referred to "the parse-line count" without defining it, on the assumption section 4.4 (settings) or section 2.3 does.
- No RECOUNT IDENTITY exists for `fraction_widths`. `numeric_styles` has one in v4 §7.5.7 and its disposition cites it; v6 marks `fraction_widths` EXACT-OBSERVABLE (v6:1182) and adds FW-P, but neither this contract nor docs/spec/generation-method-v1.md (no occurrence of the string `fraction_widths` anywhere under docs/spec/ except the v6 contract and the v6-build drafts) says what the twin owes it. My disposition cell says EXACT-OBSERVABLE with FW-P and nothing more, because nothing more is fixed.
- SECTION NUMBERS are cited in v4's scheme: section 5.1 (universal keys), 5.2 (axes and rule order), 5.6 (the ladder), 6.2 (`numeric_unrepresentable`), 6.11 (the forbidden-key matrix), 7.5 and 7.5.7 (`numeric_styles`), section 3 (serialization) and section 4 (the floor-of-one list). If the assembled document renumbers, every one of these needs substituting. In particular, if no section carries v4's §7.5 forward, then the classification ladder, the wire shape and invariants P1-P4 have no home and the `numeric_styles` rows here dangle.
- `fraction_widths` is not implemented in the shipped source at all: grep over src/synthtwin/ returns no occurrence. So P5, P6, P7 and FW-P have no code to be checked against, and I could not confirm any of them against an artifact other than the plan and the v6 delta. The shipped loader today enforces P1, P2, P3 and style-name membership only (contract.py:4273-4303).

## CONFLICTS NOTED

- THE (5 - P) TIGHTENING IS NOT TRANSCRIBED, DELIBERATELY. The vetted derivation (phase-4-v6-derivation-partition.md, CHECK A3.3) shows the plan's constant 5 is correct only where `numeric_styles` names no style: with P named styles, only 5 - P unnamed styles can share the pool with decimal, so the true bound is F >= W - (5 - P) x (floor - 1). At floor 11, `numeric_styles: {"plain": 30, "leading_plus": 20, "(withheld)": 35}` with `fraction_widths: {}` describes no table (three unnamed styles hold at most 30, not 35) and the plan's form admits it (F >= -15). I transcribed A-P4-8's constant because THE PLAN GOVERNS ON EVERY CONFLICT. This needs an owner amendment; it is a real hole in the ratified text, not a wording defect.
- CONDITION 5 (the empty branch) IS TRANSCRIBED AS INSTRUCTED, AND IT RAISES THE PLAN. A-P4-6 attaches its conditions to the non-empty census only ("its total is at least 1 wherever the census is non-empty", plan:1945) and then says "The census may also be empty in that case" (plan:1951-1952) with no arithmetic. Binding W <= 5 x (floor - 1) on the empty branch refuses documents the ratified plan admits, so it needs an amendment of its own. It also CLOSES a live self-contradiction in the delta, which is why I judged it worth writing: v6:558-559 permits the empty census in P5.c while v6:561-564 attaches all four conditions to P5.c with condition 1 reading "F is at least 1", and the §9 row at v6:1249 repeats "bounded FOUR ways". Read literally the delta both permits and refuses the same document. My branch-scoped wording is what resolves it; the §9 row must be reworded from FOUR to FIVE, branch-scoped, or the contradiction moves rather than closes.
- CHECK A2 IS NOT TRANSCRIBED. The same derivation shows that when `decimal` IS named (case P5.a), the pool is still bounded: W <= (6 - P) x (floor - 1), which refuses `numeric_styles: {"decimal": 60, "(withheld)": 51}` at floor 11. No plan amendment states any bound on the pool in that case, and the clause would contradict a sentence of v4 that this document carries in force -- P2's last clause, "`(withheld)` appears only when the pooled remainder is at least 1, and its own value may be anything from 1 upwards" (v4:1707-1708). Since "supersedes" may not be written in version 6, a self-contained v6 that wants A2 must simply state P2's `(withheld)` clause with the bound in it, in section 7.5. RAISED, not written.
- IDENTIFIERS: I dropped the delta's `C6-Q1` and `C6-NS`, which are C6-LETTER identifiers the rewrite forbids. Q1 keeps its letter and is stated here at its true width (three roles, LOADER-ONLY), which is what the identifier rule requires; the `numeric_styles` role restriction is written as prose because version 4 stated it as prose in §7.5.2 and never gave it a letter. Consequence for other sections: v6:166-167's change table cites `C6-Q1` and `C6-NS` by name, and any §9 row or test citing them must be repointed to `Q1` and to the §7.5 prose.
- DISPOSITION IS STATED TWICE IF §9.4 SURVIVES. I was asked for type, meaning and disposition per key, so the key table carries a disposition column transcribed from v4 §9.4 and v6:1182. Version 4 also states these in §9.4, and stating one rule in two places is the failure mode this rewrite exists to end. Recommend: this section is the single site for per-key dispositions and §9 keeps only the checkable-invariant index. Whoever assembles the document must pick one.
- WHERE §7.5 BELONGS. I transcribed into 6.7 only the three things the role section needs -- that the key is required on these roles, version 4's reason for that, and the six style members from contract.NUMERIC_STYLES -- and left the classification ladder (§7.5.4), the wire shape (§7.5.5), invariants P1-P4 (§7.5.6) and the twin's recount obligation (§7.5.7) to the section covering version 4's additions, citing it. If those move into 6.7 instead, delete my two `numeric_styles` paragraphs rather than keeping both.
- THE THIRD CONDITION OF THE COUNT RULE IS REDUNDANT UNDER THE SHIPPED PARSER, and I state it anyway. `numeric_sign` returns SIGN_UNKNOWN for a numeric-looking cell only when the cell is NUMBER_CONTRADICTORY (parsing.py:1086-1087; a NUMBER or NUMBER_OUT_OF_RANGE cell always has valid grouping, so the SIGN_UNKNOWN branch at :1098-1100 is unreachable for them), and `numeric_whole` gives every contradictory cell WHOLE_UNKNOWN (parsing.py:1133). So a cell failing condition 3 already fails condition 1, and `n_sign_unsettled_numeric` is today exactly `n_contradictory`. This is NOT a defect -- the two are different questions and a future notation could settle wholeness without settling sign -- but a reviewer checking the rule against the code should know the clause currently bites on no document, and the code's own comment (taxonomy.py:4290-4297) presents it as an independent test.
- Q3'S REASON IS WIDENED TO THE TWO-TEST FORM. Version 4 words it as clearing `minimum_parse_rate` of the present cells; the shipped order reaches these roles only after `numeric_unrepresentable` declined, so BOTH the numeric-looking count and the holdable count clear the one line (taxonomy.py:3708-3710 then :3827-3833). I wrote the detection paragraph in the two-test form, matching section 6.2's already-written text, and left Q3's own sentence as version 4 has it.
- `n_negative_unrepresentable` MEANING vs Q10. Version 4's key table says "out-of-range cells whose notation settles a negative sign" while the shipped counter increments for any cell whose sign is negative and whose kind is not NUMBER (taxonomy.py:2361-2363), which would also admit a contradictory cell -- except that a contradictory cell's sign is never settled, so the two coincide. I kept version 4's wording and Q10's two bounds unchanged; no change is needed, and it is reported only so the next reviewer does not re-derive it.

## SOURCES

PLAN (governs): docs/plans/phase-4-columns.md — A-P4-5 at :1916-1954 (sibling placement, and its own last clause superseded by A-P4-6); A-P4-6 at :1955-1999, conditions 1-3 at :1943-1949, the empty branch left free at :1951-1952; A-P4-8 at :2051-2100, condition 4 fixed as "F >= W - 5 x (small_cell_floor - 1)" at :2036-2048 with the vacuity sentence. V6 DELTA: docs/spec/profile-contract-v6.md — C6-Q1 at :395-401; C6-NS at :402-410; C6-6's quantitative list at :252-261; C6-7/AF7 at :262-288; C6-27 at :515-527; C6-28 at :528-532; C6-29 at :533-539; C6-30 and P5.a/b/c at :540-585; P6/P7 at :593-595; C6-S13 and the ENTRY-not-field paragraph at :596-624; disposition row for `fraction_widths` at :1182; FW-P at :1230; the §9 rows for P5/P6/P7 at :1245-1250. V4 BASE: docs/spec/profile-contract-v4.md — §6.7 head, key table and Q1-Q11 at :1121-1197 (transcribed key by key and invariant by invariant); the ladder and L1-L3 at :668-696; §7.5.1 reason at :1581-1596; §7.5.2 role restriction and its reason at :1597-1609; the six styles at :1610-1622; the wire-shape bullets at :1676-1695; P1-P4 at :1696-1718; §7.5.7 recount at :1719-1733; §9.4 dispositions at :2062-2073. V5: docs/spec/profile-contract-v5.md :1391 and :1397-1398 (the pooled `(withheld)` means "a group too small to name"); v5 contains no occurrence of the word `continuous` and changes nothing in this section. VETTED MATERIAL: docs/plans/reviews/material/phase-4-v6-derivation-partition.md — check set (a), A1-A4 with its COMPLETENESS argument, the four catches, and its governance items 1, 2, 4, 6 and gaps 6, 9, 10. SIBLING SECTIONS READ FOR AGREEMENT: docs/spec/v6-build/s5.md §5.2 (axes table, rule order, "the order is a PRODUCER rule"); docs/spec/v6-build/r1.md §6.2 (U1/U2 over the whole present population, the shared parse line); docs/spec/v6-build/r6.md §6.11 (the matrix marks `count` 15 and `continuous` 15, which is exactly the fifteen keys tabulated here) and its `n_rows`/`numeric_styles`/`fraction_widths` confinement paragraphs; docs/spec/v6-build/s3.md (key order for `fraction_widths`, and P5.b's empty object as a reachable empty form); docs/spec/v6-build/s1.md §2.2 (the six disposition classes) and §2.3 (the ladder's eleven rungs). CODE: src/synthtwin/contract.py:438-445 (NUMERIC_STYLES, the six members transcribed in that order) and :4273-4303 (the loader enforces P1, P2, P3 and key membership only); src/synthtwin/taxonomy.py:339 (SUPPRESSED_LABEL == "(withheld)"), :2264-2276 (the tally fields, including `n_sign_unsettled_numeric` and its stated reason), :2330-2360 (every present cell counted into both margins), :3290-3348 (`_numeric_styles` counts only NUMBER cells; `_numeric_details` builds the fifteen keys), :3660-3662 and :3700-3710 (rule 2's holdable test), :3827-3833 (rule 6's numeric-looking test), :4290-4306 (the repaired role rule: `whole_everywhere`, `n_negative == 0`, `n_sign_unsettled_numeric == 0`); src/synthtwin/parsing.py:663-701 (the three numeric-looking kinds), :1070-1105 (`numeric_sign`), :1107-1133 (`numeric_whole`).