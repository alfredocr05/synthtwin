VERDICT SOUND_WITH_CORRECTIONS errors=13

## ERRORS

1. FALSE FOR THE DECLARED CASE — 2.4 term row, `a nothing-publishing column`. The cell ends "and `empty` is NOT one", unqualified, which contradicts its own preceding clause ("or `structural_role` `identifier` whatever the role") in the same sentence. A declared all-absent column carries role `empty` AND `structural_role` `identifier` (docs/spec/profile-contract-v6.md:227-230, C6-3), and the structural override makes such a column nothing-publishing whatever its role (docs/spec/profile-contract-v6.md:944-951, C6-PUB-B; docs/spec/profile-contract-v4.md:1306-1308; src/synthtwin/contract.py:2736 `return structural_role == "identifier" or role in ROLES_PUBLISHING_NOTHING`). Corrected to "the role `empty` does not by itself make a column one".

2. FALSE FOR THE DECLARED CASE — 2.4 closing paragraph: "An `empty` column publishes its absent-cell source accounting under the floor, exactly as every other non-nothing column does." The governing plan says the opposite for a declared one: docs/plans/phase-4-columns.md:2190-2193 — "a DECLARED all-absent column carries `role: empty` with an empty source map and both counts zero, while the undeclared one publishes. Those two columns differ, they are meant to." The vetted shipped run records both readings: docs/plans/reviews/material/phase-4-v6-derivation-classconflict.md:147-155 (undeclared -> `missing_by_source {'NA': 20}`, `n_missing_blank 20`, `n_missing_withheld 0`; declared -> `{}` / 0 / 0). Corrected by qualifying the undeclared case and stating the declared one beside it.

3. INVENTED RULE, AND FALSE INSIDE THIS DOCUMENT — 2.4 head: "These eight terms, together with the seven counting terms of section 2.3, are the whole of the vocabulary this contract fixes" and "A word used in this document and not defined in one of the two tables is not a term of art here and carries its ordinary meaning." No source states closure: docs/spec/profile-contract-v6.md:186-195 and docs/spec/profile-contract-v5.md:204-214 add terms without any exhaustiveness claim, and docs/spec/profile-contract-v4.md:95-105 makes none. The claim is refuted by this document's own text: "the parse line" is a load-bearing term of art at docs/spec/profile-contract-v6.md:234, :279, :299, :322, :1233 and :1242, defined in neither table and absent from version 4 entirely (grep "parse line" docs/spec/profile-contract-v4.md returns nothing); "the categorical ceiling" likewise at docs/spec/profile-contract-v6.md:90-91. The transcriber declared the COUNT as their own but shipped the normative closure sentence, which is the larger defect. Replaced with a true statement that a later section's term is defined at its own site.

4. STALE / UNVERIFIABLE PRESENT-TENSE CLAIM — 1.6 price paragraph: "Every rule transcribed here was checked against its source, and the batteries that pin exact lists ... carry the version 6 enumerations." The plan states both as standing obligations, not accomplished facts: docs/plans/phase-4-columns.md:2250-2253 — "every transcribed rule is checked against its source and the batteries that pin exact lists — the disposition registry and the claim inventory — GAIN the version 6 enumerations." No battery carries them yet: docs/spec/profile-contract-v6.md:3-4 says the document is "written before any version 6 code exists", and the shipped wire version is still 5 (docs/spec/profile-contract-v5.md:3-7). This repository turns the suite red on exactly this shape of claim (docs/spec/profile-contract-v5.md:9-22, tests/test_claim_inventory.py). Corrected to the plan's own verb.

5. DROPPED RIDER — 1.5. docs/spec/profile-contract-v6.md:11-12 states "The Phase 4 plan is the authority for every decision here"; the transcription keeps only the conflict half ("the plan governs and this document is defective"). Restored, and the plan named in the clause rather than only in its heading.

6. DROPPED REASON — 1.9. docs/spec/profile-contract-v4.md:55-56: "This is not a contract decision; it is the plan's own revision-5 ruling applied consistently." The task requires stated reasons to be carried; without it a reader cannot tell whether the two-bound count is this contract's choice. Restored.

7. DROPPED REASON — 1.6 "what it buys". docs/plans/phase-4-columns.md:2257-2260: "An implementer working from the text alone — which is what the whole specification discipline exists for — reads one document instead of three and reaches a rule without resolving a chain of supersessions." This is the benefit that ties C6-COMPLETE to 1.1's "writable from this text ALONE", and it was the only one of A-P4-11's three stated benefits omitted. Restored.

8. RESIDUAL DELTA FRAMING — 2.1: "This holds for every key version 6 adds exactly as it holds for the keys it does not." In a document that carries nothing forward there are no keys version 6 adds and no keys it does not; the sentence only parses against version 5. The plan's own wording is flat — docs/plans/phase-4-columns.md:1174, "No optional keys anywhere in the format" — and the worked case (docs/spec/profile-contract-v6.md:352-357, C6-17) states it without a delta. Rewritten as a totality claim.

9. IMPRECISE, COUNT KEPT — 2.3 `absent` row: "for any of the six recorded reasons of C6-N3". Six is right (docs/spec/profile-contract-v6.md:676-681; :1632-1634 "Absence classes (6)"; and the change from version 4's five at docs/spec/profile-contract-v4.md:100 is required, not optional). But `(withheld)` is not a reason: docs/spec/profile-contract-v5.md:242-244 rules in terms that "`(withheld)` ... is not a sixth way. It is what the floor does to any of the five when the class it belongs to covers fewer rows than the floor", and C6-N3 keeps that role ("Each value other than `(withheld)` is 0 or at least `small_cell_floor`"). Rewritten as five reason classes plus the pooled remainder, so the count of six survives without calling a pooling a reason.

10. UNDERSTATED POINTER — 2.4, `the display boundary`, clause column reads "the spelling-storage rules of the absent-cell maps". The boundary also governs the `variants` key space, and that is the precedent version 5 reasons from: docs/spec/profile-contract-v5.md:88-92 — "The neighbouring field `variants` already stores its keys exactly and escapes them only at the moment of printing ... (version 4 section 7.4.2 and decision 13.5)." Broadened to name both sites.

11. IDENTIFIER RISK, ASSEMBLER-LEVEL, LEFT AS WRITTEN AND FLAGGED — the section points at other sections by identifier: `C6-N3` (twice), `C6-31`, `C6-32`, `C6-33` through `C6-35`, `C6-PUB`, `C6-PUB-A`, `C6-PUB-B`, `C6-4`, `C6-5`. `C6-N3` is a SUPERSESSION-prefixed identifier of exactly the form A-P4-11 abolishes: docs/plans/phase-4-columns.md:2232-2233, "No carrying by reference, no supersession table, no `C6-` supersession prefixes" — the `C6-`+letter convention exists only because it names what it supersedes (docs/spec/profile-contract-v6.md:120-127). Under the identifier rule in force the absence-class rule keeps its own N-family letter. I did not rename it here, because renaming another section's clause from inside this one is the guess the whole exercise forbids; the assembler must sweep every `C6-`+letter reference in one pass, together with the four section-number placeholders the transcriber declared.

12. GAP CLOSED, NO ERROR FOUND — the transcriber declared "I did not establish whether the six disposition classes have a shipped constant". They do, and the 2.2 table verifies clean in both directions: tests/dispositions.py:162-178 fixes exactly `EXACT-OBSERVABLE`, `EXACT-CONTROL`, `APPROXIMATED`, `REPORT-ONLY`, `LOADER-ONLY`, `STRUCTURAL` under the comment "the six dispositions of plan P2-D6", in that order. Six for six against docs/spec/profile-contract-v4.md:82-89, no member present that the sources lack, none missing, and both text columns transcribed verbatim.

13. HANDOFF GAP, OUTSIDE THIS SECTION BUT UNCLAIMED BY ANY DECLARED GAP — version 6's current §1, "What changed from version 5, and why" (docs/spec/profile-contract-v6.md:38-94), is displaced by this section's own §1 and nobody has claimed it. It carries live rules, not only narrative: docs/spec/profile-contract-v6.md:87-90, "The relationship manifest is still eight reserved slots, every one `null`, and a loader still refuses a document that fills any of them". Under C6-COMPLETE that rule must be written somewhere in the self-contained document; the assembler must confirm the top-level-structure section states it, or it is lost with the section that stated it.

## GAPS

- SECTION NUMBERS OUTSIDE MY SECTIONS ARE UNRESOLVED, and I deliberately did not guess them. The current v6 delta numbers its absent-cell material §5 and its publication classes §6A, but a self-contained v6 built on version 4's skeleton has §5 = the column block and §6 = the roles. I therefore reference every rule outside §1-2 by its CLAUSE IDENTIFIER (C6-N3, C6-31, C6-PUB, C6-44 ...) or by section NAME ("the settings section", "the loader section", "the decisions section", "the disposition matrix"), because identifiers survive renumbering and numbers do not. Four places will read better as numbers once the final section order is fixed: 1.5 (decisions section), 1.7 and 1.9 (loader section), 2.2 (disposition matrix), 2.3 (settings section). The assembler must fill these in as a final pass.
- MY CLAUSE IDENTIFIERS ARE PROPOSALS, NOT TRANSCRIPTIONS. `C6-COMPLETE` in 1.6 is the only new lettered identifier I introduce, and no source fixes it. I chose a word-suffixed form rather than a number because the plain `C6-` number sequence in the current draft already runs to at least C6-48 and is being rewritten by several hands at once, so any number I picked could collide. If the assembler prefers a number, nothing in 1.6 depends on the spelling. Clauses 1.1-1.5 and 1.7-1.9 carry no invariant identifier, which matches version 4's own §1 (its clauses are 1.1-1.4 with no letters).
- FRONT MATTER IS NOT IN MY DELIVERABLE, and it may duplicate 1.1-1.2. Version 4 and version 6 both open with a title plus Status / Authority / "What this document is for" / "What this document is not" blocks ABOVE §1 (v4:1-30, v6:1-36). The task told me §1 must say what this document is, so I folded "is for" and "is not" into 1.1 and 1.2. If a separate front-matter block is kept, those two clauses and the block will state the same thing twice, which is exactly the failure C6-COMPLETE exists to end. One of the two must be cut, and I could not decide which without knowing whether front matter is another agent's deliverable.
- EQUALITY PER PATH IS SHORT BY TWO PATHS AND I DID NOT EXTEND IT. Version 4's paragraph (v4:107-112) names five paths: raw spellings, folded identities, parsed numeric values, folded level facts, and the parsed datetime instant at the recorded resolution. Version 6 adds two value paths it does not cover: clock values on `time_of_day` (C6-11's `earliest`, `latest` and `clock_percentiles`, read in seconds-of-day per C6-12's T3) and parsed CORES on `affixed_number` (C6-7 is explicit that the cell census and the core census are different populations). Nothing in the plan, in v6, in v5 or in the vetted material extends the equality-per-path sentence, so I transcribed it unchanged rather than completing it from pattern. If the owner wants it total over version 6's paths, it needs a new clause and a ruling — I flag it rather than write it.
- THE STATUS / REVIEW-RECORD LINE IS STALE IN THE SOURCE AND I WROTE NO REPLACEMENT. `docs/spec/profile-contract-v6.md:3-9` says "revision 5, 2026-08-20" and names amendments A-P4-1 through A-P4-9 as the ratified text it transcribes; the plan now carries eleven (A-P4-10 at `docs/plans/phase-4-columns.md:2134`, A-P4-11 at `:2203`). `docs/spec/profile-contract-v6.md:1657-1661` also calls the specification reviews "pending" when five round files exist (`docs/plans/reviews/phase-4-contract-v6-review-round-1.md` through `-5.md`) — the closure material flags this same falsehood at `docs/plans/reviews/material/phase-4-v6-derivation-closure.md:16`. Both are front matter / review record, outside my sections; whoever owns them must move the amendment count and the review status.
- I DID NOT ESTABLISH WHETHER THE SIX DISPOSITION CLASSES HAVE A SHIPPED CONSTANT. `grep` for a disposition enumeration in `src/synthtwin/` returns only prose uses of the class words in `generation.py` docstrings, not a tuple. The registry the plan calls the "disposition seal" / "disposition registry" (`tests/disposition_seal.py`, `tests/test_p2c4f1_disposition_registry.py`) is where a constant would live, and I did not read those files. The six class names and definitions I transcribed come from `docs/spec/profile-contract-v4.md:82-89` only, and no version 5, version 6 or Phase 4 text alters them. If a shipped tuple exists, it should be checked against my table before this section is ratified.
- THE FIFTEEN-TERM CLOSURE CLAIM IN 2.4 IS MINE, NOT A SOURCE'S. No document says "this contract fixes fifteen terms". I counted them: seven in version 4 §2.3 (v4:99-105), four added by version 5 §2.3 (v5:211-214), four added by version 6 §2.3 (v6:192-195). If a later section of the self-contained document fixes a further term of art in its own words — and §7B, C6-GRAMMAR and C6-ARG are the likely places — the count and the closure sentence are both wrong. Whoever assembles the whole document must re-count against the finished text.
- I DID NOT VERIFY THE 41-FORM NOTE GRAMMAR, THE 11 `format` MEMBERS, THE 4 `resolution` MEMBERS, THE 2 `clock_form` MEMBERS, THE 17 SETTINGS KEYS OR THE 5 DECLARATION-RECORD KEYS AGAINST THEIR SHIPPED CONSTANTS. None of them appears in §1 or §2, so none is transcribed here. I read the §14 statements of them (`docs/spec/profile-contract-v6.md:1606-1655`) only far enough to confirm they do not contradict the two counts my section does use (23 published-vocabulary members and 6 absence classes, both of which I checked against C6-31 and C6-N3 as well as §14).

## CONFLICTS NOTED

- COUNT MOVED, five → six: version 4 §2.3 defines **absent** as "a cell counted as holding no value, for any of the FIVE recorded reasons" (`docs/spec/profile-contract-v4.md:100`). Version 6 makes it six: C6-N3 fixes `missing_by_class` at SIX keys — `(blank)`, `(date-sentinel)`, `(declared-missing)`, `(numeric-sentinel)`, `(text-code)`, `(withheld)` (`docs/spec/profile-contract-v6.md:676-681`), §14 states "Absence classes (6)" (`:1633-1636`), and the plan's own A-P4-11 counts "one of the six absence classes" among the things version 4's tables made unwritable (`docs/plans/phase-4-columns.md:2223`). I wrote SIX and pinned it to C6-N3. This agrees with the task's verified count of 6 absence classes. Left at five, the term would have contradicted C6-N3 inside the same document.
- LIST EXTENDED BY ONE, and I am flagging it rather than letting it pass as a transcription: version 5's **the reading rule** term lists five things — blanks, built-in words, stand-in numbers, `--missing-value` words, `--keep-value` words (`docs/spec/profile-contract-v5.md:211`). I added "the calendar placeholders". Basis: version 5 §3.1's heading is "The FIVE ways a cell becomes absent" (`docs/spec/profile-contract-v5.md:224`) and version 6 makes it six — the current v6 delta's own supersession table says C6-N3 and C6-33 through C6-35 "make it six" (`docs/spec/profile-contract-v6.md:163`), and C6-33 to C6-35 (`:683-706`) are the placeholder-judging rules. A term defined as "how each cell's raw text became either a value or no value" that omits one of the six ways is false in version 6. This is the one place I changed a version 5 term's content rather than its framing.
- COUNT IN THE SOURCE, NOT RECONCILED BY ME: version 6's own front matter says the plan carries nine amendments, A-P4-1 through A-P4-9 (`docs/spec/profile-contract-v6.md:14-16`), and the plan now carries eleven (A-P4-10 at `docs/plans/phase-4-columns.md:2134`, A-P4-11 at `:2203`). Separately, `docs/plans/phase-4-columns.md:2106` says "seven of the nine amendments this plan now carries came out of contract review", and the vetted classconflict material walks every amendment's provenance line and finds FOUR cited to contract v6 review, five counting A-P4-9 — never seven, and never eight (`docs/plans/reviews/material/phase-4-v6-derivation-classconflict.md:15`, `:194-222`). Neither number is in my section, so I transcribed neither. Both are open against the plan and the contract's front matter and need an owner ruling before either document is ratified.
- TERM DEFINITION CONTRADICTED ACROSS SOURCES, settled by the vetted material in favour of BINARY: `src/synthtwin/taxonomy.py:247-249` comments "THE THREE PUBLICATION CLASSES. A role belongs to exactly one" over a `ROLES` of ten and tuples covering nine (`:234-245`, `:258-264`), and `docs/plans/phase-4-columns.md:545` says "every role in exactly one". Both are false as written — `empty` is in none of the three, deliberately. The shipped battery has always written the invariant over FOUR buckets (`tests/test_column_analysis.py:573-583`), amendment A-P4-10 corrects the plan sentence to say so (`docs/plans/phase-4-columns.md:2134`), and C6-PUB-A states the carve-out (`docs/spec/profile-contract-v6.md:923-945`). I therefore wrote **a nothing-publishing column** as a BINARY term whose membership is the three nothing-class roles plus the structural override, and added a paragraph saying `empty` is not one — because a rule quantified over a three-way partition would silently pick up `empty` and, per the verified shipped run, empty its `missing_by_source`, zero both absence counts and make the twin write blanks where a recorded spelling belongs (`docs/plans/reviews/material/phase-4-v6-derivation-classconflict.md:139-155`). No code change is implied; the `taxonomy.py:247-249` comment remains inaccurate and is a stage-6 matter, not a contract one.
- SOURCE ARTIFACT DEFECT I DID NOT PROPAGATE: `docs/spec/profile-contract-v6.md:652` writes the three stand-in numbers with U+2212 MINUS SIGN and no code fences — "−9999, −999, 9999" — while `src/synthtwin/parsing.py:59` has `(-9999.0, -999.0, 9999.0)` and version 5 canonicalizes them `-9999.0`, `-999.0`, `9999.0` (`docs/spec/profile-contract-v5.md:1373-1375`); the closure material raises this as its item 21. My **published vocabulary** term names the three parts and their sizes and does NOT spell the numbers, so the defect is not carried into §2. Whoever writes C6-31's section must fix the spellings at that site.

## SOURCES

Every rule in the section above, with the artifact it was
transcribed from. Repo root is
`/Users/alfredowork/Library/CloudStorage/OneDrive-UniversityofIowa/Research/0- Synthetic dataset/synthtwin`;
paths below are repo-relative.

| rule in my section | transcribed from | notes |
|---|---|---|
| 1.1 "normative statement of what a version 6 description may contain" | `docs/spec/profile-contract-v6.md:12-14` | v6 Authority para |
| 1.1 "writable from this text ALONE ... every key ... type, permitted values, what a null means, which keys may not appear beside it, which invariants bind it ... checked mechanically" | `docs/spec/profile-contract-v4.md:11-16` | v4 "What this document is for", verbatim substance |
| 1.1 "a file some person may hold, hand to a colleague, or keep for a year" | `docs/spec/profile-contract-v6.md:27-30` | v6 "What this document is for" |
| 1.2 "does not say how the twin's values are computed ... frozen neutral reference vectors ... what the generator is given and what it owes" | `docs/spec/profile-contract-v4.md:18-23` | v4 "What this document is not" |
| 1.2 "does not say what `synthtwin validate` checks ... each amended at its own stage against this text" | `docs/spec/profile-contract-v5.md:38-42`; `docs/spec/profile-contract-v6.md:32-36` | v5 and v6 both state it; v6's wording taken |
| 1.3 "exactly one artifact ... `<stem>-profile.json` ... read by `synthtwin generate` and `synthtwin validate`" | `docs/spec/profile-contract-v6.md:100-104` | v6 2.1.1; v6's four-artifact exclusion list (summary, twin, twin's report, quality report) wins over v4:34-38's three |
| 1.3 "does not govern the plain-language summary, the twin CSV, the generation report, or the quality report" | `docs/spec/profile-contract-v6.md:102-104`; `docs/spec/profile-contract-v5.md:148-149` | |
| 1.4 "the ONLY input the generator receives ... never opens the real table ... nothing but the description and the seed (plan P2-D1)" | `docs/spec/profile-contract-v4.md:40-43` | verbatim substance; "profile document" reworded to "description" per v6's usage |
| 1.5 "the plan governs and this document is defective" | `docs/spec/profile-contract-v6.md:106-108`; `docs/spec/profile-contract-v4.md:45-47` | v6 2.1.2 names `docs/plans/phase-4-columns.md` |
| 1.5 "where the plan names a fact without fixing its shape, this document fixes the shape and says so, in the decisions section" | `docs/spec/profile-contract-v4.md:6-9`, `:45-47` | v4 front matter + 1.3; v6 keeps a decisions section at `docs/spec/profile-contract-v6.md:1570` |
| **1.6 C6-COMPLETE** (the completeness rule itself) | `docs/plans/phase-4-columns.md:2231-2236` (A-P4-11: "version 6 states every rule in force, itself. No carrying by reference, no supersession table ... written in the version 6 document, at its own wording, once"); `docs/plans/phase-4-columns.md:146-149` (stage 3: "COMPLETE AND SELF-CONTAINED (amendment A-P4-11)"); `docs/plans/phase-4-columns.md:1154-1160` (P4-D7 amendment marker) | GENUINELY NEW — takes a `C6-` identifier. Identifier `C6-COMPLETE` is MY choice, on the precedent of v6's existing word-suffixed identifiers `C6-PUB`, `C6-FKM`, `C6-GRAMMAR`, `C6-ARG`, `C6-CAND`, `C6-NS`; no source fixes it. See gaps. |
| 1.6 "why" paragraph — delta failure, two-to-four sites, six rounds, ten role names / five absence keys vs thirteen roles / six classes, "a site the contract never mentions is invisible" | `docs/plans/phase-4-columns.md:2209-2229` | A-P4-11's own sweep items, compressed; the universal-key-table fact is independently corroborated at `docs/plans/reviews/material/phase-4-v6-derivation-closure.md:33` (item 16, citing `docs/spec/profile-contract-v4.md:485`, `:486`, `:491`) |
| 1.6 "under-restatement cannot occur ... the question that consumed six rounds stops being a question anybody can get wrong" | `docs/plans/phase-4-columns.md:2258-2264` | A-P4-11 "What it buys" |
| 1.6 "The price" paragraph — transcription-error risk, batteries gain the enumerations | `docs/plans/phase-4-columns.md:2248-2255` | A-P4-11 "What it costs" |
| 1.7 "precedence is by the description's own version integer; no description is governed by two" | `docs/spec/profile-contract-v6.md:182-185` (2.2.4); `docs/spec/profile-contract-v5.md:199-202` (2.2.4); `docs/plans/phase-4-columns.md:2238-2242` ("a profile is still governed by exactly one version's documents") | |
| 1.7 "what a producer writes for that key and what a loader does with any other integer is the loader section" | `docs/spec/profile-contract-v6.md:1265-1273` (C6-44, C6-45) | pointer only — the rule stays in §10, not restated here |
| 1.8 "versions 4 and 5 keep their sealed text and keep governing the descriptions written under them" | `docs/plans/phase-4-columns.md:2238-2242` | A-P4-11, verbatim substance |
| 1.8 "never edited to change what it requires ... a person holding a version 5 description must be able to read the rules that governed it, unchanged" | `docs/spec/profile-contract-v6.md:176-180` (2.2.3); `docs/spec/profile-contract-v5.md:181-192` (2.2.3) | |
| 1.8 "editorial repair is outside this rule; every one still moves a digest in `tests/disposition_seal.py`" | `docs/spec/profile-contract-v5.md:194-197` | verbatim |
| 1.9 "exactly two parser bounds — nesting depth and numeric token length; revision 5 removed the container-entry limit (P2-R5-F7)" | `docs/spec/profile-contract-v4.md:49-56` | v4 1.4, compressed: the rule and its reason kept, the v4-internal correction history (three stale passages, item P2-C1-F8) dropped as version-4 archaeology |
| 2.1 normative-words table, all four rows | `docs/spec/profile-contract-v4.md:64-69` | verbatim, except OPTIONAL's row reads "version 6" for "version 4" |
| 2.1 "no optional keys on purpose ... the guess is what fails silently ... present on every column of that role, including when its content is empty" | `docs/spec/profile-contract-v4.md:71-75` | verbatim |
| 2.1 "holds for every key version 6 adds ... FORBIDDEN rather than sometimes-present" | `docs/plans/phase-4-columns.md:1174` ("No optional keys anywhere in the format, restated for the new material"); `docs/spec/profile-contract-v6.md:352-357` (C6-17, the worked case) | |
| 2.2 six disposition classes, all six rows, both columns | `docs/spec/profile-contract-v4.md:82-89` | verbatim, all six transcribed element by element |
| 2.2 "the classes are the plan's (P2-D6) ... the loader and the generator are both written against them" | `docs/spec/profile-contract-v4.md:78-80` | reason carried; "reproduced here" → "stated here in full" per C6-COMPLETE |
| 2.2 "exactly one disposition; a container's disposition does not cover its leaves" | `docs/spec/profile-contract-v4.md:91-93` | verbatim; v4's "(section 9)" replaced by a name-reference |
| 2.2 the class set is unchanged in version 6 | `docs/spec/profile-contract-v6.md:1372-1386` (§11 moves rows between existing classes, adds none); `docs/plans/phase-4-columns.md:1085-1092`, `:1376` | no Phase 4 amendment creates a class |
| 2.3 **present**, **raw identity**, **folded identity**, **withheld**, **the ladder** | `docs/spec/profile-contract-v4.md:99`, `:101`, `:102`, `:104`, `:105` | verbatim |
| 2.3 **absent** — "six recorded reasons of C6-N3" | `docs/spec/profile-contract-v4.md:100` (base wording, "five"); `docs/spec/profile-contract-v6.md:676-681` (C6-N3: six keys); `docs/spec/profile-contract-v6.md:1633-1636` (§14: "Absence classes (6)") | COUNT CHANGED five → six. See conflicts noted. |
| 2.3 **the floor** | `docs/spec/profile-contract-v4.md:103`; floor ≥ 1 and its cost at `docs/spec/profile-contract-v4.md:311-345` | default 11 verified in the shipped source: `src/synthtwin/contract.py:190` (`DEFAULT_SMALL_CELL_FLOOR = 11`) and `src/synthtwin/taxonomy.py:1141` (`small_cell_floor: int = 11`). v4's "(section 4.4's amendment)" replaced by a name-reference. |
| 2.3 Equality per path | `docs/spec/profile-contract-v4.md:107-112` | verbatim, unextended. See gaps. |
| 2.4 **the reading rule** | `docs/spec/profile-contract-v5.md:211` (base); calendar placeholders added from `docs/spec/profile-contract-v6.md:676-681` (C6-N3) and `:683-706` (C6-33 to C6-35) | LIST EXTENDED by one item. See conflicts noted. |
| 2.4 **the display boundary** | `docs/spec/profile-contract-v5.md:212` | verbatim |
| 2.4 **the published vocabulary** | `docs/spec/profile-contract-v6.md:644-656` (C6-31); count corroborated at `docs/spec/profile-contract-v6.md:655` ("Twenty-three members in all") and `:1637-1639` (§14) | Members deliberately NOT re-enumerated here — C6-31 owns the list, per C6-COMPLETE's "once". v5's 13-member definition at `docs/spec/profile-contract-v5.md:213` NOT used. |
| 2.4 **the exact-spelling member** | `docs/spec/profile-contract-v6.md:195` (term), `:648-651` and `:658-673` (C6-31, C6-32) | |
| 2.4 **a calendar placeholder** | `docs/spec/profile-contract-v6.md:194` (term), `:652-653`, `:683-706` | |
| 2.4 **a nothing-publishing column** — membership | `docs/spec/profile-contract-v5.md:214` (term); `docs/spec/profile-contract-v4.md:1304-1312` (§6.10); `docs/spec/profile-contract-v6.md:947-962` (C6-PUB-B, the structural override) | shipped ground truth: `src/synthtwin/taxonomy.py:258-264` and `src/synthtwin/contract.py:265-269` — `ROLES_PUBLISHING_NOTHING` is exactly `numeric_unrepresentable`, `identifier`, `free_text` |
| 2.4 **a nothing-publishing column** — BINARY, and `empty` is not one | `docs/plans/reviews/material/phase-4-v6-derivation-classconflict.md:44-48`, `:127-131`, `:139-155` (verified); `docs/spec/profile-contract-v6.md:923-945` (C6-PUB-A) | the verified material settles this; transcribed as binary, not as a three-class partition |
| 2.4 **the affix pair**, **the core** | `docs/spec/profile-contract-v6.md:192-193` (terms), `:234-244` (C6-4), `:246-250` (C6-5) | |
| 2.4 closing paragraph — why binary, `empty` in no value-publishing class, the consequence of reading it three ways | `docs/spec/profile-contract-v6.md:911-914`, `:923-945` (C6-PUB, C6-PUB-A); `docs/plans/reviews/material/phase-4-v6-derivation-classconflict.md:139-155` (the shipped-run evidence: 40 alternating cells at floor 11 give `missing_by_source: {"NA": 20}`, `n_missing_blank: 20`, `n_missing_withheld: 0`) | |
| 2.4 "twelve value-publishing roles" | `docs/spec/profile-contract-v6.md:911-912` | matches the 13-role count minus `empty` |