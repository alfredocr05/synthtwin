VERDICT SOUND_WITH_CORRECTIONS errors=19

## ERRORS

1. BLOCKING — 13.5 transcribes a rule that version 5 explicitly SUPERSEDED and that version 6 contradicts. The entry says '`missing_by_source` escapes its keys through the display boundary and the difference is deliberate'. Source: docs/spec/profile-contract-v5.md:306-312 — 'In version 5, a key of `missing_by_source` is the spelling exactly as the file wrote it, character for character, before trimming and before any fold. This supersedes the corresponding sentence of version 4 section 5.4 AND THE SECOND SENTENCE OF ITS DECISION 13.5.' The transcribed sentence IS that second sentence. It is doubly wrong in version 6, where `missing_by_source` is EXACT-OBSERVABLE and its keys are written back into twin cells (docs/spec/v6-build/s5.md:26), so there is no 'deliberate difference' left to state. The transcription also cut v4's stated reason ('`missing_by_source` is REPORT-ONLY and never written into a cell', docs/spec/profile-contract-v4.md:2661-2663) without replacing it, leaving a rule asserted with no justification. Rewritten in `corrected`.

2. BLOCKING — 13.25 invents one of the two named producer obligations. It writes '(K5, and the vocabulary-completeness claim)'. Source: docs/spec/profile-contract-v5.md:1330 — '13.10 Two producer obligations are stated as invariants although a loader cannot check them (C5-N7, C5-K5)', and docs/spec/profile-contract-v5.md:1009 defines C5-N7 as '*(producer)* a `missing_by_source` key is the source spelling character for character'. The second obligation is N7, not any vocabulary-completeness claim; no source names such a claim as one of the two. Corrected to name N7 and K5 with each one's subject.

3. MATERIAL — 12.2's universal-counts row calls all eleven counts 'floor-free', which is false for `n_missing_blank`. docs/spec/profile-contract-v5.md:450 and :482 — 'written when at least `small_cell_floor` cells did, and `0` otherwise, those cells being counted in `n_missing_withheld` instead' and '`n_missing_blank` is either `0` or at least `small_cell_floor`'; docs/spec/v6-build/s5.md:27 carries the same rule. The section CONTRADICTS ITSELF: its own 13.19 says 'The blank count is floor-governed.' Corrected in 12.2 and in 12.3 row 15's third bullet.

4. MATERIAL — 12.2's nothing-class row drops a qualifier and merges two terms the contract keeps apart. (a) It lists 'the whole-number and sign counts' as facts nothing-class blocks publish; sign counts (`n_positive`, `n_negative`, `n_sign_unknown`) exist only on `numeric_unrepresentable` (docs/spec/profile-contract-v4.md:749-755); `free_text` (v4:1269-1275) and `identifier` (v4:1207-1214) carry none. C6-49 states the qualifier — 'and on `numeric_unrepresentable` the whole-number and sign counts' (docs/spec/v6-build/r6.md:50-54) — and the transcription deleted it. (b) The row's header reads 'nothing-class blocks (`numeric_unrepresentable`, `identifier`, `free_text`, and any declared column)', running together C6-50's three-role CLASS with C6-49's binary nothing-publishing TERM. docs/spec/v6-build/r6.md:2-10 orders §6.10 specifically 'so that the two cannot be run together'. Both corrected.

5. MATERIAL — 14.1's nothing-publishing sentence drops C6-51's load-bearing 'by itself' and becomes false. The transcription ends '`empty` is not one.' Source: docs/spec/v6-build/r6.md:24-26, 'the role `empty` does not BY ITSELF make a column one (C6-51)', and C6-52 at r6.md:142-156, under which a DECLARED all-absent column carries `role: empty` with `structural_role: identifier` and IS a nothing-publishing column. As transcribed the sentence contradicts the override stated two clauses earlier in the same paragraph. Corrected.

6. FORBIDDEN IDENTIFIER — 12.3 row 18 cites `C6-NG8`, a `C6-` LETTER identifier, which the identifier convention prohibits outright (docs/spec/v6-build/ASSEMBLY.md:33-37, 'No `C6-` letter identifiers, anywhere'). Corrected to cite the check by its subject ('the argument-consistency check section 4.5.2 states for that argument'). The underlying fact is right — docs/spec/v6-build/s45.md:781 holds argument 9 at or above `small_cell_floor` whenever it is written — but the citation cannot be repaired by the assembler's own rename table, because that table maps `C6-NG1`…`C6-NG18` to `NG1`…`NG18` (ASSEMBLY.md:52) while the same file numbers the 41 note FORMS `NG1` through `NG41` (ASSEMBLY.md:66). Those two sequences collide over NG1–NG18 and 14.8's table is one of the colliding halves. Flagged for the assembler; not resolvable inside this section.

7. MATERIAL — C6-54 as written is false on its own terms, which is the exact failure this section exists to prevent. Its third sentence, 'A key or a sentence argument added to this format without a row here is a defect in this document', demands a row for EVERY key, while `created_with`, `profile_version`, `columns[].position`, `level_ceiling`, `suppressed_levels` and `suppressed_rows` have none. 12.5 already carries the correct escape ('either in a row above or publishes nothing of the table') but C6-54 does not. Corrected: C6-54 now matches 12.5, and 12.2 gains rows for `profile_version`/`created_with`, `position`, `level_ceiling`, `suppressed_levels` and `suppressed_rows` so the labels-class and top-level coverage is real rather than asserted.

8. MATERIAL — section 14's blanket subordination leaves the published vocabulary DEFINED NOWHERE. 14 opens 'Every list here is defined normatively somewhere else', but no written body section enumerates the 23 members: a grep for `DIV/0` across docs/spec/v6-build/ returns zero files, and `nan`/`9999.0` appear in no defining clause. Meanwhile s4.md's K1, K2, K5 and S7 all bind to 'section 14.1' as their list (docs/spec/v6-build/s4_meta.md:31), and version 5's own precedent titles its appendix clause '14.1 The published vocabulary — NORMATIVE from version 5' (docs/spec/profile-contract-v5.md:1350). As transcribed, the only site holding the list declares itself non-authoritative. Corrected by stating the one named exception at the head of 14 and at 14.4, and by adding the matching sentence to 13.36. THE ASSEMBLER OR OWNER MUST RATIFY THIS: the alternative is that §5.4 writes the 23 out and 14.4 becomes a copy — which reintroduces the two-site problem 13.36 refuses.

9. OMISSION — 14 omits `profile_version`'s single permitted value although it lists three other one-value enumerations (`declaration_matching`, `declaration_publication`, `long_tail_minimum_level`). docs/spec/v6-build/s4.md:14 and :25 fix it: 'the contract version. In this contract, exactly `6`' / '`profile_version` is the integer `6`'. This is the one wire constant a reader most often checks and the appendix did not carry it. Added at 14.2.

10. OMISSION — 14.8 omits a closed enumeration the appendix claims to gather: the package-word vocabulary. docs/spec/v6-build/s45.md:90-103 — 'One of this package's own words. The membership is closed at THIRTEEN and is written out rather than gathered' — the eleven `format` members plus `day-first` and `month-first`, with the positional binding of C6-NG18. 14.8 states that 3 argument positions take a package word but never says which words exist. Added, with the positional restriction, because membership without position is exactly the hole s45.md says it is.

11. MATERIAL — 12.3 row 5 cites 'the five new calendar members and the unpadded widening' and names none of them, so in a self-contained document the row cannot be checked. The five are `slashed-iso-date`, `iso-month`, `iso-mixed`, `month-first-datetime`, `day-first-datetime` (11 members at docs/spec/v6-build/r3.md:105-115 less the six at src/synthtwin/parsing.py:64-71); the unpadded reading is r3.md:135-138. The row's own D9 clause then depends on that unnamed set ('of the five new members only `iso-mixed`, `month-first-datetime` and `day-first-datetime`'), which is correct but unverifiable as printed. Corrected: the five are named at first use, row 15 points back at row 5, and the D9 clause points at 14.6's binding table. Row 5 also silently dropped three glosses the vetted CORRECTED MATERIAL carries (`resolution` 'the canonical form the published datetimes are written in', `subsecond_digits` 'the most fractional-second digits any cell writes', and the pointers to rows 8 and 13) — restored.

12. MATERIAL — 12.3 row 7 misattributes the fraction-census bounds. It says 'bounded above and below by P5's four conditions'. docs/spec/v6-build/r4a.md:185-215: P5 is by cases, and the four conditions bind only in case P5.c (the pooled branch); in P5.a the total equals the `decimal` key exactly and in P5.b it is zero. Reading the four conditions over all of P5 states a bound that does not hold in two of three cases. Corrected to state P5 by its cases and to name P6 for the named widths' floor.

13. DROPPED REASON — 12.4 drops the quality report's stated reason while keeping the summary's. Source: docs/spec/profile-contract-v4.md:2612-2617 — 'Two files were outside it. The quality report `synthtwin validate` writes states measurements taken from the file it checked; the profiler's plain-language summary repeats in words the labels and endpoints ... and it was never named although it is the half a person reads.' Also dropped: 'Nothing this contract publishes moves — the handling rule reaches further, and that direction is the only one it may move in' (v4:2617-2619), which is the sentence that stops the paragraph being read as a widening of what is published. Both restored. (The transcription's repointing of v4's 'section 7' to 'section 6' is a correct repair — labels and endpoints are published by the role sections, which are §6 in every written build section — and is kept.)

14. DROPPED REASONS — 13.15 and 13.16 drop two stated reasons the brief requires carrying. 13.15 loses 'A sentence restored in one place and weakened in another is the same lowering, and it is harder to see' (docs/spec/profile-contract-v4.md:2723-2724) and the closing guarantee 'The ends are exact on every description the loader accepts' (v4:2739-2741). 13.16 keeps the conclusion 'lowering a bar therefore means amending the ratified plan in the open' but drops its premise, 'The registry may authorize a lesser outcome only where the plan's own words name it' (v4:2775-2776), leaving a conclusion with no argument. All three restored.

15. MATERIAL — 13.26 drops the pointer to where the routes are stated, against its own reason. Version 5's 13.11 reads 'Route 4 and the below-floor route are written into the normative text (SECTION 7)' (docs/spec/profile-contract-v5.md:1336-1337); the transcription generalizes to 'are in the normative text' with no site, and then closes 'A limit a reader has to find somewhere else is a limit a reader does not find.' Corrected to name the sites (5.4 and section 7); the assembler must confirm both numbers.

16. MINOR — 12.2's note-forms row named three of the four sentence paths, omitting `source.header_evidence`, which 14.8 lists. Corrected. 14.9's gloss for `sentinel_verdicts[].candidate` was also rewritten to use the binary term C6-49 fixes ('the column is a nothing-publishing column') rather than v4's superseded 'the block's publication class permits no value'.

17. MINOR — 13.1 dropped v4's stated alternative ('If a reviewer prefers the wider reading, the change is additive: the key would become required on `numeric_unrepresentable` too, and P1's population would have to be restated', docs/spec/profile-contract-v4.md:2637-2641). Restored. Note also that the section's CONFLICTS note claims `affixed_number` IS that named additive change; it is not — v4 names `numeric_unrepresentable` explicitly, and the transcribed entry still excludes it. The entry text is otherwise correct.

18. MINOR — 13.6 dropped the rejected alternative it was written against ('rather than appearing only where a label has more than one spelling', v4:2665-2666); 13.4 dropped v4's two concrete examples of inexpressible forms ('an overflowing digit string, a sign inside brackets', v4:2656-2657). Both restored in `corrected`.

19. STALE DECLARED GAP (not an error in the text) — the gap 'THE JSON TYPE OF clock_percentiles IS STATED TWO WAYS ... and I did not resolve it' is answered by the sibling section written since: docs/spec/v6-build/r5b_alt.md:59 and :62 fix it as a ladder OBJECT in the shape of section 5.6, and r5b_alt.md:95 adds that no rung is ever `null`. Row 4 needs no change; the gap should be struck so the assembler does not chase it.

## GAPS

- SECTION NUMBERS ARE PLACEHOLDERS THE ASSEMBLER MUST REPOINT. Following the other authors I wrote cross-references at version 4's numbering: 1.4, 2.2, 2.3, 4.1, 4.3, 4.4, 4.5, 4.5.1, 4.6, 5.1, 5.2, 5.3, 5.5, 6.3.1, 6.6.2, 6.8, 6.9, 6.10, 6.11, 7.4, 7.4.2, 7.5, 7.5.4, 9.6, 9.7, 10.1, 10.2, and 'section 9' for the disposition matrix (cited in 12.3 row 15). Every one needs renumbering in the single assembly pass.
- ONE NEW C6- NUMBER IS PROVISIONAL. I used C6-54 for section 12's completeness rule. The highest number across the written build sections is C6-53 (r6.md), so C6-54 is next free at the time of writing, but the assembler owns the single sequence. Sections 13 and 14 introduce no identifier: 13's entries are numbered 13.1-13.37, and 14's governing sentence is deliberately prose rather than a numbered invariant so that a reading aid can never be cited as authority.
- THE NOTE FORMS ARE NUMBERED NG1-NG41 PER ASSEMBLY.MD'S REPAIR, and 14.8's table is that mapping in order. If the assembler instead keeps s45.md's A/B/C/D/E group labels, four of the five letters collide with invariant families in this same document and 14.8 must be renumbered together with 4.5.1. The count 41 and the arities are unaffected either way.
- I COULD NOT ESTABLISH WHETHER remark_no_reading_fits ARGUMENTS 6, 7 AND 8 ARE BOUND BY ANY FLOOR. Section 4.5.2 as written states C6-NG6 and C6-NG10 for the base sentence and C6-NG7 to C6-NG9-P for argument 9 only; no check names arguments 6, 7 or 8. Row 18 therefore prices them as floor-free and says so, rather than assuming a floor. If 4.5.2 gains bounds for them, row 18 must be amended.
- THE ARITY OF FOUR INHERITED NOTE FORMS DIFFERS BETWEEN s45.md AND THE SHIPPED TABLE, and I transcribed s45.md because it is the defining section: remark_no_reading_fits 9 (shipped 5), remark_dates_also_read_as_numbers 2 (shipped 0), remark_every_number_is_different 1 (shipped 0), remark_every_value_is_different 1 (shipped 0). If any of the four is a transcription slip rather than a version 6 widening, 14.8 and 12.3 row 18 both move.
- WHETHER A COLUMN READ BOTH WAYS THAT THEN DECLINES TO datetime STILL CARRIES THE SLASHED-DATE REMARK is settled by no clause I could find; 12.3 row 16 carries it as a stated open question, following the datetime material's own declared gap.
- THE JSON TYPE OF clock_percentiles IS STATED TWO WAYS in the sources (an eleven-rung ladder object versus an array of eleven strings) and I did not resolve it. Row 4 prices the fact without naming its container, which is correct either way; the time-of-day role section owns the resolution.
- SECTION 13 DROPS TWO INHERITED ENTRIES DELIBERATELY. Version 5's 13.12 ('no feasibility rule, no generation rule and no method text is changed here') was true of that version's landing and is false of this one, since P4-D8 changes the method in six places; transcribing it would have put a false sentence in the register. Version 5's 13.1, which decided that the predecessor is carried by reference, is likewise not transcribed; 13.34 records the opposite decision and names the amendment that reversed it.
- THE DELTA DOCUMENT'S REVIEW RECORD STATES THAT SPECIFICATION REVIEWS ARE 'pending' while five round records exist (docs/plans/reviews/phase-4-contract-v6-review-round-1.md through -5.md). That block is outside my four sections, but whoever writes the assembled document's review record must not carry the stale sentence forward.
- I DID NOT VERIFY THE ELEVEN format MEMBERS, THE FOUR resolution MEMBERS, THE SIX time_precision MEMBERS OR THE TWO clock_form MEMBERS AGAINST A SHIPPED PARSER, because none exists: parsing.py 64-71 still holds six formats and contract.py 430-434 still holds three resolutions and five precisions. Those four lists rest on the plan (P4-D7 item 2 as amended by A-P4-1) and on the defining sections, which is the correct authority order under 13.37, but no code confirms them today.
- 12.2's row for the ranges class names 'the ranges-class endpoint policy' as ratified without restating it. If no other section of the assembled document states that policy in full, it is a rule cited without being stated and must be written out wherever endpoints are specified.

## CONFLICTS NOTED

- THE APPENDIX'S OWN AUTHORITY SENTENCE IS INVERTED IN THE DELTA AND I DID NOT CARRY IT. The delta's section 14 says of the settings list: 'if a spelling here and a spelling there ever differ, the shipped constant governs and this line is defective.' Generalized -- and it invites generalization, being the only authority sentence the appendix carries -- that rule makes this contract's own resolution (4 versus contract.py:432's 3), format (11 versus parsing.py:64-71's 6), roles (13 versus contract.py:247-258's 10) and profile_version (6 versus contract.PROFILE_VERSION == 5) all defective on the day they are written. The document is written before the code by design (P4-D7: ratified at stage 3, before any version 6 code). I wrote the correct order instead, at 13.37 and in section 14's opening: plan, then contract, then code, and where the contract and the code disagree the CODE is repaired. That is the order the U1 repair established.
- VERSION 4'S 13.8 AND 13.10 WERE STALE AT TEN AND ARE RESTATED AT THIRTEEN. 13.8 called the derivation table 'a bijection between the ten roles and the ten statistical_type values'; 13.10 was headed 'Ten roles, not nine'. Both are now written at thirteen. 13.10's REASON is replaced rather than reworded: 'the producer's role tuple has ten entries, and this contract is written against the producer' is false of this version in two ways -- no version 6 producer exists, and the shipped tuple still has ten entries -- and it points the authority arrow the wrong way. What is true instead, and what 13.10 now says: the ratified plan's delta adds exactly three roles to the ten this format had, and the contract is written before the code so that the code is written against it.
- THE DELTA'S SECTION 14 GIVES THE THREE STAND-IN NUMBERS AS an en-dash-style minus sign with no code fences, while the shipped constant is (-9999.0, -999.0, 9999.0) and the canonical document form is -9999.0, -999.0, 9999.0. Section 14.4 writes the canonical wire forms in backticks. A minus sign that is not the ASCII hyphen, in a document that fixes exact wire spellings, is a defect a reader can copy into a file.
- THE DELTA'S SECTION 14 STATES TWO ENUMERATIONS AS POINTERS RATHER THAN LISTS. 'The ten version 5 fixed' names none of the ten absent-value spellings, and 'statistical_type (13): version 5's ten, plus ... each naming its own role' names none of the ten types -- and that phrase is wrong about four of them, since empty, numeric_unrepresentable, identifier and free_text answer unknown, numeric, code and text. Both are written out element by element in 14.1 and 14.4. A count with a pointer instead of a list cannot be audited, which is the failure this appendix exists to close.
- THE (withheld) PLACE LIST GAINED A SIXTH MEMBER WITHOUT BEING RESTATED. Version 5's five-place table is the last full statement of it; version 6 adds fraction_widths, which withholds under C6-28 and appears on C6-S13's floor-of-one list, yet no clause restates the place list over six. Section 14.9 states all six. This is the same class of defect -- an exhaustive list extended at one corner -- that C6-S13 was itself written to repair.
- THE DELTA'S SECTION 12 COMPLETENESS SENTENCE WAS SCOPED TO 'sections 3 through 6' AND WAS FALSE ON ITS OWN TERMS. Nothing priced a long-tail column's absent-cell facts (its missing_by_source spellings and counts, its two absence counts, its named sentinel candidates); nothing priced nine of a datetime block's thirteen role-added keys; nothing priced (date-sentinel); and nothing priced the counts the note grammar publishes by sentence rather than by key. Section 12 is now asserted over the WHOLE document and carries rows 15, 16, 17 and 18. Row 18 is mine rather than the material's: the material accounted for the recoverable-distribution clause alone, and remark_no_reading_fits has four conditional clauses, three of which publish counts on a nothing-publishing column that no key of a free_text block carries and that no stated floor bounds.
- ROW 12 PRICED ONLY THE DECLARED ROUTE FOR THE EIGHT NEW VOCABULARY MEMBERS. The automatic route is the larger of the two -- no declaration, four new facts including a whole numeric distribution and a role transition -- and it is now written into the row with the reason it is stated rather than left to be discovered.
- SECTION 13 NOW RECORDS numeric_styles AS REQUIRED ON THREE ROLES, NOT TWO. Version 4's 13.1 restricted it to count and continuous and named the wider reading as an additive change a reviewer might prefer; affixed_number is exactly that change, taken. The entry is transcribed at three, with the same reason and with the numeric_unrepresentable exclusion kept.
- ONE ADJACENT ITEM I FLAG RATHER THAN FOLD IN. The clause stating that a person's own marker word reaches a description by two routes calls both routes unchanged and unclosable. The ROUTE is unchanged, but the SET of columns the second route bites on shrank: a column crossing out of free_text into a ranges- or labels-class role leaves the class that route quantifies over. Whether that requires a qualifier belongs to the author of that section, not to mine.

## SOURCES

PRIMARY, in the order they win. (1) Ratified plan docs/plans/phase-4-columns.md: P4-D7 item 5 (settings 15 to 17, both new keys named), item 7 lines 1216-1237 (the disclosure-delta obligation and its illustrative list), P4-D8 items 1/4/5 (method changes; the closed refusal list grows by exactly one), P4-D4.2 lines 735-748 (time-of-day capacity: 1,440 or 86,400 spellings; n_distinct minus n_unparsed), amendment A-P4-11 lines 1154-1159 (self-containment, owner decision 2026-08-20), A-P4-1 pricing note lines 1745-1753 (the two rows section 12 gains), A-P4-5/6/8 (fraction-width placement and bounds), A-P4-10 (four buckets). (2) docs/spec/profile-contract-v6.md: section 12 rows 1-14 (1388-1489), section 13 (1570-1604), section 14 (1606-1655), C6-31 (642-656), C6-N3 (676-681), C6-S14/C6-36/C6-48 (738-827), C6-37/C6-38 (831-857), C6-4 to C6-20 (232-384), C6-21/C6-25/C6-26/C6-27 to C6-30/C6-S13 (426-636). (3) docs/spec/profile-contract-v4.md: section 11 lines 2512-2556; 13.1-13.16 lines 2620-2776; section 14 lines 2780-2819; 2.2 disposition classes 82-89; 2.3 ladder 105. docs/spec/profile-contract-v5.md: 13.2-13.11 lines 1284-1337; 14.1 lines 1350-1379; 14.2 lines 1381-1401. (4) Vetted material: docs/plans/reviews/material/phase-4-v6-derivation-datetime.md CORRECTED MATERIAL block, from which corrected row 5 and new rows 15, 16 and 17 and the widened completeness sentence are transcribed with all twelve verifier corrections applied; docs/plans/reviews/material/phase-4-v6-derivation-closure.md for the six-enumeration second-site finding (C1-c), the ten version 5 spellings and the canonical stand-in forms, the statistical_type pointer defect (row 2), the authority-inversion finding (item 14) and the sixth (withheld) place (U5); phase-4-v6-derivation-classconflict.md for the three-versus-four class reading. (5) Written sections in docs/spec/v6-build/: s4.md (17 settings keys in code-point order, 5 declaration keys, S14/K1-K5/S7, source's five keys), s45.md (41 forms with arities, the census of 62 argument positions, D11 at arity 9 with its four conditional clauses and C6-NG7/NG8, relationships' eight keys), s5.md (22 universal keys, the 13-row axis table), r2.md (four level-entry keys), r3.md (eleven formats with resolutions, resolution_mix, (none)), r6.md (four buckets, C6-49 to C6-51, 55 matrix rows, 107 marked cells), ASSEMBLY.md (identifier convention and standing checks). (6) Shipped source, for enumerations with a constant: src/synthtwin/contract.py 149-165 SETTINGS_KEYS(15), 168-176 DECLARATION_KEYS(4), 195-205 RELATIONSHIP_KEYS(8), 211-234 UNIVERSAL_COLUMN_KEYS(22), 247-258 ROLES(10), 275-286 AXIS_ROWS(10), 288-299 STATISTICAL_TYPES(10), 301-303 QUALITY_STATES/STRUCTURAL_ROLES, 309-314 MISSING_CLASS_KEYS(5), 317-331 SENTINEL_KEYS/VERDICTS/REASONS, 410-444 ladder/DATE_FORMATS/RESOLUTIONS/TIME_PRECISIONS/CLOCKS/NUMERIC_STYLES; src/synthtwin/parsing.py 42-53 MISSING_TEXTS(10), 59 NUMERIC_SENTINELS, 64-71 DATE_FORMATS(6); src/synthtwin/taxonomy.py 258-264 the three publication tuples, 372-379 NUMERIC_STYLES, 572-611 NOTE_ARITY(38); docs/spec/generation-method-v1.md 1670-1673 and 2076-2175 (G9, G9.4, the capacity table and the named refusal).