VERDICT SOUND_WITH_CORRECTIONS errors=14

## ERRORS

1. THIRD-COLUMN LEGEND CONFLICTS WITH THE SIBLING THAT OWNS IT (worst finding). docs/spec/v6-build/a8a.md:7-12 defines the third column of this one checkable list with FOUR values under the header `loader?` — `yes`, `producer`, `reading`, `contract`. The section under review redefines the same column of the same list with THREE different values under a different header (`decidable?`): `yes`, `one direction`, `no`. Assembled, section 8 carries two contradictory legends for one column, and it is also a rule restated by a section that does not own it. Repaired: legend deleted and pointed at part one; `Q8` becomes `reading`, `U-P` becomes `producer`, and the split rows read `yes` plus the half they decide, following a8a.md:21's own pattern of qualifying a value in the cell.

2. D10 STATES ITS THIRD CLAUSE OVER A WIDER DOMAIN THAN THE RULE DOES. docs/spec/v6-build/r3.md:370-373 scopes it 'where `resolution` is `datetime` AND `datetimes_read_at` is `utc`'; the row drops the resolution condition (copying docs/spec/profile-contract-v4.md:1932, whose index row has the same drop). It is not harmless: D9 (r3.md:339-341) forces both endpoint offset fields to `(none)`/`(withheld)` off `datetime`, while D5's withheld case (r3.md:304-307) still admits `datetimes_read_at: utc` there, so the row as written asks a loader to move an endpoint by an offset that is not one. Scope restored.

3. D4's THIRD COLUMN NAMES ONLY ONE OF THE TWO UNCHECKABLE HALVES. docs/spec/v6-build/r3.md:282-288 has three clauses; a loader can decide exactly one — a real offset in an endpoint field is a key of `utc_offsets`. The row's second clause, 'a field whose offset is not a key reads `(withheld)`', is NOT decidable (a loader reading `(withheld)` never is told which offset it stands for) yet the row puts it in the decidable half and names only the `(none)` case as the producer's. Both unrecomputable clauses moved to the producer half.

4. V4 IS DUPLICATED, INCLUDING ITS REASON PARAGRAPH. docs/spec/v6-build/r3.md:490-510 states V4 entire in its three-group form (matching docs/spec/profile-contract-v6.md:709-734); the section restates it in full and then repeats r3.md:503-510's reason paragraph nearly verbatim as its own closing paragraph. docs/spec/v6-build/a8a_meta.md:10 recorded this exact hazard ('Carrying V4 again here would duplicate it'). Compressed to an index row plus a pointer.

5. THE IDENTICAL-VALUE TEST IS RESTATED IN FULL rather than pointed at. docs/spec/v6-build/r4a_alt.md:303-314 states it whole inside the numeric-roles section, which enters the document by the union ASSEMBLY.md:105-121 fixes. Compressed to a pointer that keeps the load-bearing consequence (null endpoint => not identical => a null `skew` at `n_used_in_statistics >= 3` is refused), which src/synthtwin/contract.py:4151-4171 enforces exactly that way.

6. THE U1/U2 RIDER SWAPS THE SOURCE'S REASON FOR ONE THE SOURCE DOES NOT STATE. docs/spec/v6-build/r1.md:184-186 gives the reason as the tie the generation method's construction table fixes (G10.5 step 1) — dropped here. In its place the section imports the shipped repair narrative from src/synthtwin/taxonomy.py:2348-2358 ('the refusal told the reader their file had been changed since it was written -- blaming a person who had done nothing'). Verified present in that comment, so not invented, but it is a note about a defect already fixed in code, not a rule of the wire format. Tie kept, narrative dropped, role section pointed at.

7. U5's ROW STATES MORE THAN U5. docs/spec/v6-build/r1.md:197, docs/spec/profile-contract-v6.md:1248 and C6-26 (v6:505-510) all give U5 as `min_length <= max_length` alone; 'each an integer at or above 1' is the KEY's range (r1.md:168-169). ASSEMBLY.md:135-137 records that invented range cells in tables are one of the four wrong enumerations this build already caught. Trimmed.

8. V1 DROPPED ITS FLOOR-OF-ONE RIDER. docs/spec/profile-contract-v4.md:646-647 ends V1 'and at a floor of one it is zero under S13, because no candidate can be below one'. Since these rows are V1-V3's ONLY statement in version 6 (a8a_meta.md:10), the rider cannot be left to the reader; restored as a pointer at S13, which a8a.md:31 states over 4.4's closed list.

9. Q1 DROPPED 'LOADER-ONLY', present at docs/spec/profile-contract-v4.md:1934, r4a.md:274 and r5a.md:255-256. Not restored — dispositions are §9's and part one's rows carry none — but the assembler must confirm §9 carries it, or the fact that the per-column echo imposes nothing on the twin is stated nowhere.

10. ONE OF THE SECTION'S OWN CONFLICTS NOTED IS STALE. It says v4's section 8 preamble (docs/spec/profile-contract-v4.md:1832-1834, 'either true or false of a parsed document, with no interpretation left') will be transcribed unchanged and be false of Q7/Q8/D4/D5/U-P. Part one already softened it and added the four-value column (a8a.md:3-12), so that item is closed; its live form is the legend conflict above.

11. A CITATION THE GAP REPORT UNDERSTATES. The rows cite L2 and L3; a8a_meta.md:11 records by grep that L1-L4 are stated in NO v6-build section while five sections cite them. That is ASSEMBLY.md:240-243 standing check 2 (a rule cited by identifier and stated nowhere), not the renumbering task the section's gap describes.

12. NO ENUMERATION ERROR FOUND — checked element by element, both directions. D1: eleven members against the format table at r3.md:103-116 and C6-D1 at v6:446-477 — five `date` (`iso-date`, `month-first-date`, `day-first-date`, `compact-date`, `slashed-iso-date`), one `month` (`iso-month`), one `quarter` (`year-quarter`), four `datetime` (`iso-datetime`, `iso-mixed`, `month-first-datetime`, `day-first-datetime`) = 11, matching ASSEMBLY.md:239. D6: four resolutions and six precisions (`date`, `minute`, `second`, `subsecond`, `quarter`, `month`) against r3.md:312-317 = 4 and 6, twenty-four pairs. D9's 'every member but four' agrees with D1's four `datetime` members. V3's 'other four reasons' against the five at v4:639. Family completeness: Q1-Q11 (11 rows) against r4a.md:271-322 union r4a_alt.md:266-331; U1-U5 + U-P (6) against r1.md:171-208; D1-D11 (11) against r3.md:117,275-410; V1-V4 (4) against v4:642-663 and r3.md:490-510.

13. SHIPPED-CODE CHECKS, ALL CONFIRMED. Q5/Q6/Q7 identical-value handling: contract.py:4151-4192 — `flat` requires both endpoints non-null and equal, and contract.py:4163 refuses a null `skew` when `not flat and used >= 3`, so the union the section took is the code's reading and r4a.md alone is the incomplete one. Q9 compares with `!=` on binary64 at contract.py:4193-4198, so the section's open gap about exact-versus-tolerant comparison is real and belongs where Q9 is stated in full. U1/U2 over `n_present`: contract.py:3348-3366. V1/V2/V3 and V4's two orderings: contract.py:2955-2999; V2 is bidirectional in code (`publishes_nothing != (candidate == WITHHELD)`), which the corrected V2 row now says outright. The V4 defect the section reports is confirmed at contract.py:2996 — `_reads_as_a_number(candidate, ...)` on every non-withheld candidate refuses a calendar-placeholder candidate before ordering is reached, so no shipped loader can satisfy V4 as widened.

14. Q1's THREE ROLES ARE RIGHT AND v4's INDEX ROW IS THE OUT-OF-DATE ONE, as the section flags: docs/spec/profile-contract-v6.md:166 and :397 add `affixed_number`, and docs/spec/v6-build/r5a.md:253-256 states Q1 over the same three. Nobody should 'correct' it back against v4:1934. The AF7 pointer is also accurate and non-duplicating: r5a.md:219-241 and r5a2.md:30-48 write the substitution out and hold `n_present`/`n_rows` out of it, matching ASSEMBLY.md:144-170.

## GAPS

- V1, V2 and V3 are stated in NO other v6-build section (a8a_meta.md:23 records this; V2 is cited at r1.md:195 and r4b.md:90,191). My rows are their only statement in version 6, and no delivered section states the four-key sentinel_verdicts entry shape or its candidate domain (C6-CAND) either — s5.md:36 only points at 'section 5.5'. Someone must own that entry-shape part, or ASSEMBLY standing check 2 fails on V1-V3.
- U-P is a producer obligation living in the U family. I gave it a row marked 'no' so the index walks complete, but part two-b owns producer obligations: if it lists U-P too, delete one — never both, never neither.
- RM1, RM2 and the datetime producer obligations DF-P, DF-R, RM-P, CP-P (r3.md:160-176, 200-212, 475-477) sit in the datetime block but are not D-family, so they fall outside my four families. They must be placed exactly once by part two-b or by the producer-obligation list.
- Q9 states '==' over a recomputed quotient. The shipped loader (contract.py:4193-4198) compares exact binary64 equality; the text nowhere says the comparison is exact rather than tolerant, so two implementations can accept different documents. The fix belongs where Q9 is stated in full (r4a.md), not only in this index.
- My rows cite L2, L3, M1, M2, N3, AF7 and 'the floor' by identifier; all belong to other sections and must resolve at assembly. The heading carries no section number — this is part two of the one checkable list and the part-one author's heading governs.

## CONFLICTS NOTED

- r4a.md and r4a_alt.md disagree about Q7. r4a.md gives only 'null only when the exact mean is not a finite binary64 value' and says a loader accepts null; r4a_alt.md:300-301 adds that a null is refused where every parsed value is identical, and the shipped loader refuses exactly that (contract.py:4183-4192). I took the union, which is the code's reading. The same split covers Q5: r4a.md omits the min==max test and the null-endpoint case; r4a_alt.md:303-314 and contract.py:4163 have it, and the code refuses a null skew when an endpoint is null and n_used_in_statistics >= 3.
- v4's section 8 preamble claims every row 'is stated so that it is either true or false of a parsed document, with no interpretation left'. That is false of Q7, Q8, D4's (none) half, D5's second direction and U-P. If part one transcribes that preamble unchanged, the sentence is wrong about this half of the list; the third column here is the repair and the preamble should be softened to match.
- Shipped code cannot satisfy V4 as written: contract.py:2996 calls _reads_as_a_number on every non-(withheld) candidate, so a calendar-placeholder candidate such as 1900-01-01 is refused before the ordering is reached. Expected, since no version 6 code exists, but it is the exact line V4's widening and C6-CAND land on and it should be named in the implementation list.
- v4's index row for Q1 restricts the per-column n_rows echo to count and continuous. My row states three roles, adding affixed_number, per the plan's widening and r4a.md:271-276 — flagged so nobody 'corrects' it back against v4.

## SOURCES

Q rows: v6-build/r4a.md:271-328 and r4a_alt.md:266-337 (union, per ASSEMBLY.md 3A); v4 section 8 index and 6.7; contract.py:4055-4240 (_numeric_facts enforces Q1-Q11). Identical-value test: r4a_alt.md:303-314 with contract.py:4148-4192. Affixed substitution pointer: ASSEMBLY.md:144-170 (n_present and n_rows read unchanged; the cores reach the three census counts) and AF7.
U rows: r1.md:157-235 (keys, U1-U5, U-P); delta C6-26; taxonomy.py:2320-2410, whose repaired loop counts EVERY present cell into both families and whose comment records the defect; contract.py:3340-3364 refuses U1/U2 over n_present.
D rows: r3.md:98-419 - format table 103-116 (eleven members), D1 117-126, D2-D5 275-307, D6 309-326 (four resolutions, six precisions), D7-D11 339-419; delta C6-D1 446-477, C6-21, C6-24; v4 6.6.2 and 8.
V rows: V1-V3 transcribed from v4:642-665 (section 5.5); V4 from r3.md:490-510 and delta C6-V4 709-734; candidate domain at delta C6-CAND 415-420; contract.py:2929-3010 enforces V1-V4.
Counts cross-checked against ASSEMBLY.md:239 (11 formats, 4 resolutions, 6 precisions).