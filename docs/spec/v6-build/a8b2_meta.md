VERDICT SOUND_WITH_CORRECTIONS errors=12

## ERRORS

1. DROPPED OBLIGATION (most serious). AF-P is transcribed with two of its four clauses. The primary source, r5a.md:93-98, states four: (a) at least the parse-line count of the present cells were affixed numbers wearing the published pair; (b) the pair is the one those cells wore; (c) each core is that cell's longest-then-leftmost parsing substring; (d) the four core-class counts are the classifier's own verdicts over the cores of the cells that wore the pair. The section carries only (b) and (c) (r5a2.md:18's shorter form). ASSEMBLY.md:139-142 fixes the method as the UNION of r5a.md and the r5a1/r5a2 pair, so the primary's extra clauses cannot be dropped by choosing the second opinion. Clause (d) matters on its own: AF4 only closes n_core_* against n_affixed, so with (d) gone nothing in the contract obliges those four counts to be the classifier's verdicts over real cores. Restored.

2. UNLOCATED QUANTITIES. The NG preamble says 'D, M, X, Y its four counts', attributing them to the COLUMN. s45.md:595-598 fixes them as the slashed-date remark's arguments 1-4 (cells the day-first reading parsed, cells the month-first reading parsed, cells only day-first parsed, cells only month-first parsed). As written NG1-NG4 bind quantities a loader cannot locate in the document, and they read as facts of the column rather than of the note - while NG5 in the same table cites 'argument 5', so the frame is inconsistent inside one subsection. Fixed by binding them to arguments 1 to 4 and giving each its meaning.

3. DROPPED RULE. P5.c's own shape statement is gone. a7c.md:178-181 (and r4a.md:197-200) state that in case P5.c `fraction_widths` is EITHER `{}` OR the pooled decimal cells under its own `(withheld)`; r4a.md:229-232 states the corollary 'a reader should not have to derive' that no NAMED width key may appear there. The section jumps from the case heading to 'Writing W ... FOUR conditions bind', dropping both. Restored in one clause with its ground (P6 with condition 2).

4. REPORTED CONFLICT IS NOT A CONFLICT, and escalating it costs a round. The filed P7 conflict claims a7c.md:205-206 'permits by silence' a non-empty census totalling zero that r4a.md:253-255 forbids. It does not: 'a width key is present only if its count is nonzero' makes every PRESENT key at least 1, so a non-empty census has F >= 1, which is exactly r4a's biconditional 'F is zero if and only if `fraction_widths` is the empty object'. The two forms are equivalent, and no two implementations would refuse different documents. Corrected P7 states the equivalence outright so the question cannot be reopened.

5. DUPLICATION the brief forbids, plus a false preamble sentence. N7 and K5 are already rows of a8a.md:61 (section 8.3) and a8a.md:71 (section 8.4), each marked `producer`; the section restates both in full. Worse, its preamble asserts 'Producer rows stand once, in the last table', which is FALSE against a8a.md as delivered - part one carries two producer rows in its own tables. Corrected by deleting the two rows, pointing at them by identifier and section, and repairing the preamble. If the assembler prefers the opposite repair, a8a.md's two rows must be deleted instead and the preamble sentence then becomes true; what cannot stand is the pair of full statements plus the sentence denying they exist.

6. WEAKENED WORDING. NG18 lost the word 'only' from its position list. s45.md:814 reads 'a `format` member only at `evidence_dates` argument 3 or `said_read_as_dates` argument 2, and `day-first` or `month-first` only at ... argument 5'. The section's 'a `format` member at X or Y' reads as a non-exhaustive list of permitted positions, which is the whole content of the rule. 'only' restored at both positions.

7. LOOSENED BINDING. NG6 drops the positional binding s45.md:761 states ('whose own argument 1 equals ... and whose own argument 2 equals ...'); 'carrying X, then its `n_present`' does not fix which slot holds which. NG7's N likewise: s45.md:757 defines it as the numeric-looking count carried INSIDE argument 1 - i.e. that nested fragment's own argument 1 - which the section leaves as 'that numeric-looking count'. Both restored to the source's positional form.

8. OVERSTATED CONSEQUENCE. T2's reason reads 'untied, a twin pinned to the ladder holds values earlier than its published endpoint'. r5b.md:136-139 states it as what an untied pair WOULD LET a document do ('would let a document publish a ladder end below `earliest`'), a possibility and not a certainty. Changed to 'can hold'.

9. DROPPED REASONS the brief requires carrying. P1's exclusion reason (a7c.md:69-72: out-of-range and contradictory cells are written in forms the six styles cannot express) and P4's `5.0` example (a7c.md:81-83) were absent; both restored inside the cap. Three more could not be fitted and stay in their owning sections, named here so the assembler can judge: P3's ground (a7c.md:78-80, Q3 plus every counted cell landing in a key), AF3's consequence (r5a.md:206-209, a block whose pair never cleared the detection line cannot conform), and NG10's scope reason (s45.md:786-789, a `numeric_unrepresentable` column is reached only at or above the parse line, so a wider rule would refuse every legitimate one).

10. PRECAUTION, not a check failure. AF7 was written with 'n_present and n_rows UNCHANGED'. tools/spec/check_assembly.py:64-71 flags only the phrase 'unchanged from version 5', so the bare word passes; it is nonetheless the one delta-flavoured word in the section and reads as a version comparison. Changed to 'untouched'.

11. CONFIRMED, and it is the section author's report rather than a defect of this transcription: the NG identifier collision is live. ASSEMBLY.md:50 renames the grammar invariants C6-NG1...C6-NG18 to NG1...NG18, while ASSEMBLY.md:64 (repeated at :212) renumbers the 41 note FORMS as NG1...NG41. Under both, NG11 and NG12 name a grammar invariant AND a note form, and this section cites NG11/NG12 from AF-R, so the collision is in the assembled text. It is the same class of defect as the D5 collision at ASSEMBLY.md:209-213 and check_assembly.py's duplicate-identifier pass will only see it after both renamings are applied. One family needs a different prefix.

12. CONFIRMED, and outside this section to fix: r5b.md:87 still carries `clock_form` as EXACT-CONTROL against ASSEMBLY.md:174-183, which settles it as EXACT-OBSERVABLE on the evidence. This section restates no disposition, which is the right call; the losing answer must be corrected in r5b.md before assembly.

## GAPS

- Section numbers are placeholders (`8.x`). Part two-A's numbering is not visible from here, so the assembler must number these seven subsections in one pass. a8a.md's closing line still says the list continues in THE next section (singular); there are now two parts, and that pointer needs updating.
- C6-GRAMMAR and C6-ARG have no assigned identifiers yet. ASSEMBLY.md section 1 says each takes a plain `C6-` NUMBER, and none has been allocated, so NG16 cites 'the four argument classes' by description and NG14 cites 'the note grammar' by description. Both must be repointed at assembly.
- N7 and K5 are already rows in a8a.md sections 8.3 and 8.4, and a8a.md also assigns 'the producer obligations' to the continuation section. They therefore appear twice in the assembled section 8. I kept them in the roll-up because the brief asks for EVERY producer obligation; the assembler must delete one copy, not both.
- a8a.md section 8.6 is titled 'the label roles - B, and the six that restrict them' and carries C1, C2, Y1, Y2, G1, G2 only. LT1, LT2 and G1L are not in it, which is why they land here. If part two-A also carries them, that is a duplicate to resolve.
- AF-R exists as an identifier only in r5a2.md (the split second opinion). r5a.md, the primary, states the same obligation in prose without naming it. I took the union per ASSEMBLY.md section 3A. If the assembler prefers the primary alone, the AF-R row has no identifier and must go.
- No Phase 4 subject of this section is implemented in shipped source: grep over src/synthtwin finds no `fraction_widths`, `n_affixed`, `clock_form`, `resolution_mix` or `long_tail` anywhere, and contract.py still reads `profile_version` 5. So P5-P7, the whole AF, T, LT and RM families and every NG rule could not be cross-checked against code. Only P1, P2 and P3 could be, and they match contract.py exactly.
- The 10,000-character cap forced dropping reasons the sources do state: AF7's six worked readings and the `-1e999 mg` example (r5a.md), NG2's and NG4's counterexamples and the closing NG17/NG18 note (s45.md), P5.c's `{"(withheld)": 51}` counterexample (a7c.md), the T family's 'no rung is ever null' consequence and LT's 'levels is never empty' consequence. Each survives in its owning section; none fits here under the cap.

## CONFLICTS NOTED

- ASSEMBLY.md gives the identifiers `NG1`-`NG18` TWO different meanings, both in s45.md, and no reader or check has caught it. Its rename table (line 50) maps the grammar invariants `C6-NG1 ... C6-NG18` to `NG1 ... NG18`; its collision repair (line 64, repeated at line 212) renumbers the 41 note FORMS as `NG1` through `NG41`. Under both, `NG11` and `NG12` name a grammar invariant AND a note form. My section cites NG11 and NG12 from AF-R, so the collision is live in the text as written. This is the same class of defect the D5 collision was, and the mechanical check will not see it until both renamings are applied. One of the two families needs a different prefix.
- a7c.md and r4a.md state P7 at different merits. a7c.md line 205: 'A width key is present only if its count is nonzero, so a present `(withheld)` value is at least 1'. r4a.md lines 252-255: 'a width key is present only if its count is nonzero; equivalently, *F* is zero if and only if `fraction_widths` is the empty object'. The r4a.md form is strictly stronger: it forbids a census that is non-empty with total zero, which a7c.md's form permits by silence. I transcribed a7c.md's, since 7.6 is that section's, but two implementations reading the two documents would refuse different documents.
- `clock_form`'s disposition is given twice and differently: r5b.md's key table says EXACT-CONTROL, the disposition matrix says EXACT-OBSERVABLE, and ASSEMBLY.md section 3B settles it as EXACT-OBSERVABLE on the evidence. Dispositions are not this section's, so nothing here restates it, but r5b.md's own table still carries the losing answer and must be corrected before assembly, since T1 through T3 are checkable against a written twin only under the observable reading.

## SOURCES

Transcribed element by element, each row against its owning section.

P1-P4 and the six styles: docs/spec/v6-build/a7c.md §7.5.4 (P1 population and the not-counted classes, P2 floor both ways, P3 never empty, P4 independence). P5-P7 and the P5.a/b/c cases: a7c.md §7.6 C6-30, cross-checked against v6-build/r4a.md lines 251-262 (its P6/P7 and FW-P) and r4a_alt.md line 93. The FOUR conditions and condition 1's "wherever the census is non-empty" rider: docs/plans/phase-4-columns.md amendment A-P4-6 (lines 1957-1995, three conditions) and A-P4-8 (lines 2053-2085, the fourth, F >= W - 5 x (floor-1), with "Vacuous where the right-hand side is zero or negative"), plus ASSEMBLY.md section 3A, which records that both numeric-roles verifiers independently rejected a fifth bound and reached four. a7c.md's "Condition 4 also decides an EMPTY census; no fifth is needed" carried.

AF1-AF7, AF-P: r5a.md C6-8 lines 200-251 (primary) unioned with r5a2.md lines 1-70 (second opinion), which supplies AF-R and the nine-rule count. AF7's substitution and the n_present/n_rows-unchanged reading: ASSEMBLY.md section 3A "The open question is SETTLED", r5a.md's six-reading table, r5a2.md's substitution table. AF4-beside-X2: r5a.md C6-7, a8a.md row X2.

T1-T5, T-P, TU-P: r5b.md lines 124-176, unioned with r5b_alt.md lines 100-145 (identical in substance).

LT1, LT2, G1L: r5c.md lines 102-127.

RM1, RM2, RM-P: r3.md lines 189-212 (C6-25 key sets, RM1, RM2, RM-P).

NG1-NG18, NG9-P, NG13-P: s45.md lines 715-830, transcribed table by table (five slashed-date rows; NG6 and NG10; NG7-NG9 plus NG9-P; NG11-NG12; NG13 plus NG13-P; NG14-NG18). "41 forms" from s45.md line 152 and ASSEMBLY.md section 4 check 1. "four argument classes" from s45.md C6-ARG, "closed at FOUR".

U-P: r1.md lines 199-208, cross-checked against r6.md lines 277-288 (the two populations for min_length/max_length). DF-P, DF-R: r3.md lines 160-174. CP-P: r3.md line 475. K5: s4.md lines 240-252; a8a.md row K5. N7: a8a.md row N7; a14.md 13.25. The unnumbered built-sentence obligation: s5.md lines 64-75.

Code: src/synthtwin/contract.py NUMERIC_STYLES (lines 438-445) and the numeric-styles reader (lines 4270-4305) confirm P1, P2 and P3 including the (withheld) exemption and the >= 1 value floor.