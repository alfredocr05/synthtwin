VERDICT SOUND_WITH_CORRECTIONS errors=22

## ERRORS

1. ENUMERATIONS — ALL CORRECT, BOTH DIRECTIONS, counts stated. (1) The two clock forms: `hh-mm`/`HH:MM` and `hh-mm-ss`/`HH:MM:SS`, hours 00–23, minutes 00–59, seconds 00–59, two-digit fields — element for element against docs/plans/phase-4-columns.md:688-691 and docs/spec/profile-contract-v6.md:299-303; the two wire spellings against the §14 constant at docs/spec/profile-contract-v6.md:1630 (`clock_form` (2): `hh-mm`, `hh-mm-ss`). Nothing present that is not in source, nothing in source missing. (2) The FIVE added keys — `clock_form`, `earliest`, `latest`, `clock_percentiles`, `n_unparsed` — match docs/spec/profile-contract-v6.md:313-316 (C6-11), the per-role key set at docs/spec/profile-contract-v6.md:989, and the five marked cells of the `time_of_day` column in the matrix (docs/spec/v6-build/r6.md:210-211, :250). (3) The five dispositions match docs/spec/profile-contract-v6.md:1176-1179 one row at a time. (4) The T-family T1–T5 plus T-P and TU-P match docs/spec/profile-contract-v6.md:1238-1242, :1211, :1229-1231. (5) The two ordinal units and the two capacities 1,440 / 86,400 match plan:728-731 and plan:739-742. (6) The ten forbidden datetime keys are exactly datetime's thirteen (docs/spec/v6-build/r3.md:68-83) less the three shared names, consistent with `datetime` 13 at docs/spec/v6-build/r6.md:250. No enumeration was completed from pattern.

2. SHIPPED-CODE DISAGREEMENT, AND THE ONE SUBSTANTIVE DEFECT. C6-10 as transcribed ("At least the parse-line count of its present cells match ONE of exactly two clock forms") omits the at-least-one-parsed-cell guard that the shipped sibling rule carries: src/synthtwin/taxonomy.py:3395 reads `if len(good) >= needed and good:` — the line AND a cell — and `_needed` at src/synthtwin/taxonomy.py:1666-1676 is ceil(share × total), so at `minimum_parse_rate` 0.0 (permitted, docs/spec/v6-build/s4.md:122) the parse-line count is 0 and the transcribed rule claims EVERY column that reaches rule 9, clockless ones included. The block then fails T4 (`n_unparsed < n_present`, docs/spec/profile-contract-v6.md:1241) — a producer writing what its own loader refuses, the exact U1-class defect this review exists to catch. The section already ASSERTS the guard in T4's reason ("cannot reach this role at all"), which is not derivable from C6-10 as written. FIXED: the guard is written into C6-10 with its reason. Note the role has zero implementation to check against — `grep -rn 'time_of_day\|clock_form\|TIME_OF_DAY\|clock_percentiles' src/synthtwin/` returns nothing — so this is a disagreement with the shipped ANALOGUE, not with a shipped clock reader.

3. MEANING CHANGED — the null-rung reason contradicts the invariant it cites. Section text: "`percentiles` admits a null rung because an interpolated numeric rung can fall outside binary64". Source, docs/spec/profile-contract-v4.md:685-691: "A `percentiles` rung may be `null`, and means the exact rung is not a finite binary64 value. No producible profile is known to reach this — every interpolated rung lies between two finite neighbours and is therefore finite — but a loader accepts `null` rather than refusing a document over a case it cannot rule out." L3's reason is that a LOADER cannot rule the case out, and L3 explicitly denies that an interpolated rung can be non-finite. The transcription states the denied thing as the reason. FIXED to L3's own reason. (The conclusion — no clock rung is ever null — is sound and matches L3's second half at docs/spec/profile-contract-v4.md:694 and the row at docs/spec/profile-contract-v4.md:1896, "a `date_percentiles` rung may not".)

4. DROPPED — the interpolation METHOD. plan:728-729: "interior ranks by the same floor-division interpolation the date rule uses, in the ordinal unit THE PUBLISHED FORM ITSELF SETS". The section keeps the unit and drops "the same floor-division interpolation the date rule uses", which is the only thing that makes two generators fill the interior identically. RESTORED.

5. DROPPED — the twin's form obligation. plan:736: "All cells written in the column's one published form." Nothing in the section states it; the C6-11 meaning cell covers only the block's PUBLISHED values (that is T1). The section's own conflicts note relies on this sentence to argue about `clock_form`'s disposition, so its absence is load-bearing. RESTORED to the generation paragraph.

6. INVENTED ILLUSTRATION THAT COLLIDES WITH THE DATETIME RULE. Section text: "exactly as ninety-nine ISO dates beside one datetime cell make an `iso-date` column with one unparsed cell." plan:690-693 authorizes only the bare analogy ("exactly as one date format must clear the line today"). The invented numbers are true only under the single-member-before-joint precedence at docs/spec/v6-build/r3.md:60-65, and version 6 now has a joint ISO reading (`iso-mixed`), so a reader meets a worked example that appears to contradict the mixed-resolution rule. REPLACED with the plan's own analogy, no invented counts.

7. DISPOSITION WORDING DROPS TWO WORDS THAT CARRY WEIGHT. Section: "EXACT-OBSERVABLE as counted stand-ins, outside the clock representation obligation". Source, docs/spec/profile-contract-v4.md:2096: "EXACT-OBSERVABLE as counted neutral stand-ins, explicitly OUTSIDE the parsed-value representation obligation". "Neutral" is the property that keeps a stand-in from being read as a value, and "explicitly" is what stops the exclusion being read as an oversight. RESTORED, with v4's own noun ("parsed-value representation obligation").

8. TWO ADJACENT SENTENCES READ AS A CONTRADICTION. C6-14 says "no key of a `time_of_day` block may carry a spelling of the table", and the next paragraph publishes `earliest`, `latest` and eleven rungs, which on this role are byte-identical to cells of the source. The class is stated correctly at docs/spec/v6-build/r6.md:77 as "no spelling appears; order statistics computed from the values do" — the gloss that resolves it, and the one an adjacent section already carries. FIXED by stating the class as r6.md states it and confining the no-spelling sentence to shared cell text.

9. A RULE CITED THAT THIS DOCUMENT DOES NOT STATE (ASSEMBLY.md:101-105, checks 2 and 3). "the datetime role's resolution-sets-the-unit rule transposed to the clock" points at a rule no version 6 section states — docs/spec/v6-build/r3.md:71 defines `resolution` as which canonical form the published datetimes are written in, and the unit rule lives in the generation method. plan:730-732 makes it a COMPARISON ("exactly as the datetime rule's resolution sets its unit"), not a citation. FIXED to a comparison. Same repair applied to "the analogue of the datetime rules that pin its ladder", now naming D11 (docs/spec/v6-build/r3.md:400-403) and L1 (docs/spec/profile-contract-v4.md:675-680), both of which this document does state.

10. DELTA FRAMING SURVIVING IN A DEFINITIONAL SENTENCE (ASSEMBLY.md:101-103: no "unchanged from version 5", no "as version 4 has it"). The opening sentence ends "— under version 5 such a column was free text and published no value of itself at all". The claim is TRUE (a column reaching rule 9 was declined by categorical and fell to the version 5 fallback), but a role definition is not where the format carries its history. The disclosure inventory is, and version 6 already prices it there (docs/spec/profile-contract-v6.md:1408-1410 row 4, :1472-1476 row 13). FIXED: dropped from the opening, kept once in C6-14 where the inventory owns it. The assembler should still rule on whether §12's own "new for columns that were free text under version 5" survives check 2.

11. TABLE HEADER MISLABELS ITS COLUMN. The last column is headed "spellings" and holds 1,440 and 86,400, which are COUNTS of spellings, not spellings; and the section never says what the capacity is for, so the two numbers sit in a normative table with no rule attached. plan:739-745 attaches them to the all-different obligation ("the space holds 1,440 or 86,400 distinct spellings by form"). FIXED: header renamed, one sentence added naming what the capacity bounds. The infeasible-shape REFUSAL (`n_distinct − n_unparsed` past capacity) is correctly left out — plan:743-747 puts it in P4-D8's generator refusal catalogue — and the transcriber's gap asking the assembler to confirm it is stated there is a live handoff, not a defect here.

12. CONFLICT CONFIRMED — `clock_percentiles` WIRE TYPE, and the transcriber's resolution is right. docs/spec/profile-contract-v6.md:315-316 calls it "an eleven-rung ladder of clock values in the ladder's fixed rung order"; "the ladder" is the defined term (docs/spec/profile-contract-v4.md:105; §5.6 at docs/spec/profile-contract-v4.md:669-673 fixes it as an OBJECT with the eleven named keys). docs/spec/profile-contract-v6.md:1178 instead types it "array of 11 strings". The object reading is the one the rest of the format supports: the sibling row at docs/spec/v6-build/r3.md:70 types `date_percentiles` "ladder of strings", T2 is stated as an endpoint identity (docs/spec/profile-contract-v6.md:1239), and docs/spec/v6-build/s3_meta.md:19 already registered the collision as one §3 could not settle. The §8 row must be corrected to "ladder of strings". The object form is kept in the corrected text.

13. THE SECTION SILENTLY CORRECTED ITS SOURCE'S ARITHMETIC AND DID NOT REPORT IT — the correction is right, the silence is the defect. docs/spec/profile-contract-v6.md:307-308 says both forms clearing the line is "reachable whenever `minimum_parse_rate` is set below one". That is false for any rate in (0.5, 1): with `_needed` = ceil(r × n) (src/synthtwin/taxonomy.py:1666-1676) and a+b ≤ n_present, both forms clear only where 2L ≤ n_present. The section's stricter statement is the correct one and is kept; recorded here so the §3.3 sentence is repaired rather than left to contradict §6.

14. CONFLICT CONFIRMED AND CORRECTLY LEFT AS PUBLISHED — `clock_form` EXACT-CONTROL (docs/spec/profile-contract-v6.md:1176) against §2.2's definition (docs/spec/v6-build/s1.md:148, "a metadata or dispatch decision a CSV cannot evidence"). With plan:736 ("All cells written in the column's one published form") restored and T4 guaranteeing a parsed cell, the form IS recountable from the written twin, which is EXACT-OBSERVABLE (s1.md:147). The datetime precedent supports the objection: `resolution`, `time_precision` and `subsecond_digits` are EXACT-OBSERVABLE at docs/spec/profile-contract-v4.md:2094, and only `format` is weakened (v4:2095), for a reason — ISO twin syntax rather than the source's lexical family — that has no counterpart here. Transcribing as published was correct; this needs an owner ruling, because the evidence test written to EXACT-CONTROL (a dispatch assertion plus a misrouting mutant) would not catch a twin whose cells reprofile as the other form.

15. CROSS-SECTION DEFECT CONFIRMED, IN s4.md AND NOT HERE. docs/spec/v6-build/s4.md:122 gives `minimum_parse_rate` the meaning "THE line for the numeric roles and for the datetime role, and the only one", while C6-20 twelve lines later (docs/spec/v6-build/s4.md:130-133) says the affix rule, THE CLOCK RULE and the placeholder pass "read `minimum_parse_rate`". C6-10 and AF3 (docs/spec/profile-contract-v6.md:279) both rest on the clock and affix readings of that key. The meaning cell must name them, or a reader is told the line that governs this role does not govern it.

16. GAP CONFIRMED BY DIRECT COUNT — NO DETECTION-EVIDENCE FORM EXISTS FOR THIS ROLE. The ten evidence forms of the closed grammar are B1–B10 at docs/spec/v6-build/s45.md:208-284, and none of them says a column read as clock times; the clock sentence in the grammar is argument 8 of `remark_no_reading_fits` (docs/spec/v6-build/s45.md:430, :455), which belongs to a column that DECLINED. `detection_evidence` is a universal REQUIRED key, so a conforming `time_of_day` block cannot currently be built. The same hole hits `affixed_number` and `long_tail_labels`. The transcriber was right not to invent a form; this needs an owner decision before the contract is complete.

17. GAP CONFIRMED — no generation-method clause fixes the two-sided window for the nine interior rungs. §2.2 defines APPROXIMATED as reproduction "inside a two-sided finite-sample bound" and requires the achieved value beside the published one in the report (docs/spec/v6-build/s1.md:150) — which authorizes the section's report sentence, so that is NOT an invention — but docs/spec/generation-method-v1.md has no `clock_form` clause, and G12.2/G12.4 cover the numeric and date rungs only. Citing no G-number was correct; the method owes a G-clause.

18. GAP CONFIRMED — the remarks paragraph is right. D16's zero rendering reads "none of the forms synthtwin can read fits them … Nothing from this column is published either way" (docs/spec/v6-build/s45.md:539-547) and D14's reads "the column is described as numbers" (s45.md:501-504); both are false of a column this role claimed, so neither trigger reaches it. plan:680-682's "the all-different remark additionally extends to this role verbatim" sits inside P4-D4.1 and its "this role" is `affixed_number`; plan:739's all-different sentence inside P4-D4.2 is the GENERATION distinctness obligation, not the remark. "None of its own" stands.

19. SECTION NUMBER. The heading reads 6.7; the assigned slot is 6.13 (the section id is `write:6.13-time-of-day`), docs/spec/v6-build/r6.md occupies 6.10 and 6.11, and docs/spec/v6-build/r2_meta.md:37 guesses 6.12/6.13/6.14 for the three new roles. Nothing in the text depends on it — every rule position cited (rule 9, tested after categorical) is section 5.2's and matches docs/spec/v6-build/s5.md:191 and :117-120. Set to 6.13 in the corrected text; the assembler still fixes it in one pass.

20. STRUCTURAL, FOR THE ASSEMBLER, NOT AN ERROR IN THE TEXT. This key table carries a `disposition` column; the adjacent role section does not (docs/spec/v6-build/r3.md:68-83 has key / JSON type / permitted values / meaning), and version 4 puts per-role dispositions in §9 (docs/spec/profile-contract-v4.md:2087-2098). Keeping the column risks the two-homes-for-one-rule failure that motivated the rewrite; dropping it risks losing the dispositions entirely, since docs/spec/v6-build/s5_meta.md:33 records that §9 has no role group for any of the three new roles. The column is kept and tied to §2.2's class definitions; the assembler must ensure §9 does not restate it. Related and still open: `n_distinct` and `n_distinct_folded` are universal keys with NO disposition on this role anywhere (s5_meta.md:33).

21. NO OTHER INVENTIONS FOUND. Each of these is derived, correct and load-bearing, and each is stated as a consequence rather than as a new rule: "no cell matches both forms" (the two grammars are five and eight characters, and the tie-break argument needs it); the ordinal ranges 0–1439 and 0–86399 (arithmetic on plan:728-731's two units); the reason for T4 standing beside T5 (correct: at rate 0 the parse-line count is 0 and T5 alone admits a block with no parsed cell); the fixed-width reason under refusal 3, which plan:701-709 leaves unreasoned but which follows from plan:730-735's one-canonical-spelling argument; the `subsecond_digits` reason under refusal 1, which follows from the forbidden-key list. The axes sentence matches docs/spec/v6-build/s5.md:97 and :120-124 including its "no declared column can reach" shorthand, tightened here to name `--identifier` and rule 2 so it cannot be read as covering a missing-value declaration.

22. NO SURVIVING DELTA FRAMING BEYOND ITEM 10. No "supersedes", no "carried", no "carried unchanged", no `C6-` letter identifier (C6-10 through C6-14 are numbers; T1–T5, T-P, TU-P, D11, L1, L3, Q1, R-P4-5 keep their inherited forms, as ASSEMBLY.md:17-41 requires), and every rule the section cites by identifier is stated either here or in a named section of this document.

## GAPS

- Section number: I wrote 6.7 to match the number a parallel draft of this same section used (it appeared in my scratchpad mid-task). The build's numbering is NOT settled — r6.md occupies 6.10 and 6.11, v4's 6.7/6.8/6.9 were count-continuous / identifier / free_text, and r2.md refers to 'the section for long_tail_labels' with no number at all. Six roles must be fitted into whatever numbers remain; the assembler must fix them. Nothing in my text depends on the number: the rule positions I cite (rule 9, rules tested after categorical) are section 5.2's and are stable.
- Section 5.6 (the ladder) is not yet written in v6-build, and v4's opens 'Two fields carry a ladder'. Version 6 has THREE — percentiles, date_percentiles, clock_percentiles. Whoever writes 5.6 must widen that sentence and say explicitly whether L1 and L2 quantify over clock_percentiles. I did not assume they do: I stated T2 and T3 as this role's own endpoint and ordering rules, which is what C6-12 and the §9 list carry.
- NO detection-evidence form exists for this role. The note grammar is closed at 41 forms and its ten evidence forms include nothing that says a column read as clock times, yet detection_evidence is a universal REQUIRED key that must be built from a grammar form. The same hole hits affixed_number and long_tail_labels (C6-17 even relies on a long-tail evidence sentence to record the ceiling the column passed). I did not invent a form; my closing paragraph says only that the sentence is built from the grammar. This needs an owner decision before the contract is complete.
- NO generation-method clause fixes the two-sided window for the nine interior clock_percentiles rungs. G12.2 covers the numeric rungs and G12.4 the date rungs; docs/spec/generation-method-v1.md has zero occurrences of clock_form. Section 2.2 defines APPROXIMATED as reproduction 'inside a stated rule and a two-sided finite-sample bound', so the disposition currently names a bound that does not exist. I stated the interpolation rule from P4-D4.2 (ends pinned, interior filled in the form's own ordinal unit, report naming achieved beside published) and cited no G-number.
- s4.md's meaning cell for minimum_parse_rate names only 'the numeric roles and the datetime role, and the only one', while C6-20 in the same section and C6-10 here both have the clock rule reading it. That cell should name the clock and affix rules too, or a reader is told the line that governs this role does not govern it.
- Whether the code-shaped / all-different remarks reach this role is UNSETTLED in both sources. D16 is 'carried on every role its trigger reaches, the affixed-number role included', but its rendering says 'none of the forms synthtwin can read fits them', which is false of a column this role claimed, and D14's says 'the column is described as numbers'. I therefore wrote that the role carries no remark form of its own. If an all-different clock column is meant to raise anything, it needs a form of its own and that paragraph changes.
- The infeasible-shape refusal of P4-D4.2 (a description whose n_distinct minus n_unparsed exceeds the form's capacity) is a GENERATOR refusal in P4-D8's catalogue, not a loader invariant, so it is not stated here. I carried the two capacities (1,440 and 86,400) into the ordinal table because they are facts about the forms. Confirm the refusal is stated in the refusal-catalogue section so it is not lost between the two.

## CONFLICTS NOTED

- clock_percentiles WIRE TYPE — a contradiction inside v6 itself, and the one a reader would implement wrongly. C6-11 says 'an eleven-rung ladder of clock values in the ladder's fixed rung order', and 'the ladder' is a defined term (s1.md §2.3; v4 line 105 and §5.6): the fixed eleven rungs min, p01, p05, p10, p25, p50, p75, p90, p95, p99, max, carried as an OBJECT with those keys by both existing ladder fields. v6 §8's key table instead says 'array of 11 strings'. An array has no rung names, cannot be read 'in the ladder's fixed rung order' as anything but position, and would make this the only ladder in the format with a different shape — while T2 is stated in §9 as 'first rung is earliest, last is latest', i.e. the D11 analogue over min and max. I wrote the OBJECT form and stated T2 as clock_percentiles.min == earliest / .max == latest. The §8 row should be corrected to 'ladder of strings'; two implementations reading those two sentences will not interoperate.
- clock_form DISPOSITION is weaker than its own precedent and looks wrong. v6 §8 gives it EXACT-CONTROL, defined in §2.2 as 'a metadata or dispatch decision a CSV cannot evidence'. But P4-D4.2 requires every cell of the twin's parsed population to be written in the column's one published form, and T4 guarantees at least one parsed cell, so the form IS recountable from the written twin — the definition of EXACT-OBSERVABLE. The analogous datetime fields resolution, time_precision and subsecond_digits are EXACT-OBSERVABLE in v4 §9.6; only format is weakened there, and only because the twin is written in ISO syntax rather than the source's lexical family, which has no counterpart here. I transcribed EXACT-CONTROL as published rather than choosing the convenient answer, but an evidence test written to EXACT-CONTROL (a dispatch assertion plus a misrouting mutant) would not catch a twin whose cells reprofile as the other form.
- No shipped-code disagreement is reportable for this section, and that is itself worth recording: the role does not exist in src/synthtwin at all (zero hits for time_of_day, clock_form, TIME_OF_DAY), so nothing here was checked against a running producer. The two-form enumeration, the field ranges, the five keys and the T-family were transcribed from P4-D4.2 and v6 §3.3, which agree element for element; the ladder rung names came from v4 §5.6 and the disposition classes from v4 §9.6 and v6 §8.

## SOURCES

PLAN (governs): docs/plans/phase-4-columns.md — P4-D4.2 in full (lines 688-748: the two forms and their field ranges, the one-form-clears-the-line rule, n_unparsed, the three exclusions, the no-joint-reading rule, the datetime-transposed publication set, the two restated ladder invariants, the form-unit ordinal for the twin, the 1,440/86,400 capacities); rule order and internal ordering (490-512); publication classes, time_of_day joins ranges (545-563); ranges-class endpoint policy "ladder's two ends are exact values ... published floor-free" (652-655); P4-D12 line-not-circle bound (1484-1489); R-P4-5 (1523-1529); disclosure rows (1237-1239). V6 DELTA: docs/spec/profile-contract-v6.md §3.3 C6-10 through C6-14 (297-336); §6A ranges membership (907); §7A per-role key set (989); §8 key/disposition rows (1176-1179); §9 invariant rows T1-T5, T-P, TU-P and the "three MEASURED facts marked producer" paragraph (1209-1242); §12 disclosure rows 4 and 13 (1407-1410, 1472-1476); §14 clock_form enumeration, 2 members (1630). V4 BASE: profile-contract-v4.md §5.6 the ladder, L1-L3 (668-695); §9.6 datetime dispositions, incl. n_unparsed "EXACT-OBSERVABLE as counted neutral stand-ins" (2087-2098); the ladder term (105). ADJACENT V6-BUILD SECTIONS (read for agreement, not redefined): r1.md (§6 preamble; every-key-not-listed-is-forbidden); r3.md (§6.6.2 datetime — D8, D10, D11, the format table's two clock forms, and the wording I transposed); r6.md (§6.10 classes; §6.11 matrix — time_of_day carries exactly 5 marked cells: earliest, latest, n_unparsed, clock_form, clock_percentiles, and datetime's 13 give the ten forbidden here); s5.md (axes row time_of_day/time_of_day/ok, C6-19, C6-1 rule order); s4.md (§4.4 minimum_parse_rate, C6-20); s45.md (§4.5.1 grammar, 41 forms; remark_no_reading_fits argument 8 is the clock clause); s1.md (§2.2 disposition classes, §2.3 "the ladder"). SHIPPED SOURCE: grep over src/synthtwin/ returns ZERO hits for time_of_day, clock_form or TIME_OF_DAY — the role is not implemented, so no code artifact could fix any enumeration here; every element was transcribed from P4-D4.2 and v6 §3.3, which agree element for element.