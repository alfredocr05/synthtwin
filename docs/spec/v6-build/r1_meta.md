VERDICT WRONG errors=1

## ERRORS

1. CRITICAL — U1 AND U2 WERE REWRITTEN AGAINST THREE GOVERNING ARTIFACTS ON THE STRENGTH OF ONE SHIPPED FUNCTION. The draft restates both sums as '== n_numeric + n_out_of_range + n_contradictory' and adds a paragraph arguing that a loader enforcing '== n_present' 'would refuse a description a conforming producer writes'. That argument inspects the producer only. I checked the loader and the method as well, and both side with version 4: (a) docs/spec/profile-contract-v4.md:758-760 states both sums as '== n_present'; (b) THE SHIPPED LOADER ENFORCES '== n_present' — src/synthtwin/contract.py:3348 and :3358 raise ProfileError naming U1 and U2 when the triples do not equal n_present; (c) the sealed generation method's construction table, docs/spec/generation-method-v1.md:2803-2812 (G10.5 step 1), ties the row 'ordinary text | n_not_numeric | n_whole_unknown | n_sign_unknown', which is precisely the tie that makes both sums close on n_present, and :2361-2363 calls them 'three families over one set of cells'. I ran the pipeline: taxonomy.profile_column on 198 four-hundred-digit overflow cells plus 'apple' and 'pear' returns role numeric_unrepresentable, n_present 200, n_not_numeric 2, details n_whole 198 / n_fraction 0 / n_whole_unknown 0; feeding those numbers to contract._unrepresentable_facts raises ProfileError 'it is called U1 in synthtwin's description contract'. So the shipped PRODUCER writes a description the shipped LOADER refuses — a live code defect in src/synthtwin/taxonomy.py:2327 (`if cell.kind != parsing.NOT_A_NUMBER` guarding both triples), not a defect in the invariant. Under the brief's own source order the shipped source is ground truth 'for every enumeration with a constant', which U1/U2 are not; version 4 is the authority for the invariant. RESTORED to '== n_present', and the six key meanings restored from 'numeric-looking cells' to 'present cells' (v4:750-755). ADDED in place of the draft's paragraph a rider saying where a non-numeric straggler and a self-contradictory cell land, sourced to G10.5 step 1, because the slack is real and otherwise ambiguous. OWNER ACTION: file the producer defect; it is a code change, outside this contract and outside the ratified plan.

## GAPS

- SECTION NUMBERS ARE ASSUMED TO FOLLOW VERSION 4's. My text points at 'section 5.1' (universal keys), 'section 5.2' (axes and rule order), 'section 5.3' (the multiplicity map), 'section 5.5' (a sentinel verdict entry), 'section 9' (the disposition matrix) and 'section 6' (the twin's reproduction of recorded hole spellings, C6-37, whose home section I could not establish). If the assembled document renumbers, those six pointers must move. I deliberately used no number for the publication-class section or the forbidden-key rule, citing C6-PUB / C6-PUB-A / C6-PUB-B and C6-FKM instead.
- THE TERM 'the parse-line count' IS DEFINED INSIDE 4.5.2, NOT IN 2.3. v6-build/s45.md:714-719 defines it there 'because three checks depend on it'. Section 6.2 now depends on it too, as do the three new-role sections. Whoever assembles should consider moving that definition to the terms section; I used the term as s45 defines it and added no second definition.
- WHICH TEXT min_length AND max_length MEASURE IS NOT WRITTEN IN ANY SOURCE. Neither C6-26 (v6:505-511) nor P4-D4.4 (phase-4-columns.md:802-822) says whether the character count is of the raw cell or the trimmed one. I wrote 'characters of the cell's text as the file spells it' because that is what the shipped producer does on the only role that carries these keys today (taxonomy.py:1703-1705 over _split_missing's untrimmed present list; verified: a cell '  abc  ' yields max_length 7 on an identifier column). This needs an owner ruling, because the disclosure argument the plan prices ('for decimal numerals length bounds magnitude') reads more naturally over the trimmed numeral, and surrounding whitespace inflates max_length above the numeral's own width. The identifier role's section must be written to the same reading whichever is chosen.
- I COULD NOT ESTABLISH WHERE THE FOUR-OTHERS THRESHOLD IN THE STAND-IN JUDGEMENT IS WRITTEN. taxonomy.py:4581-4585 requires at least four non-candidate numbers before a candidate is judged at all, and that is what makes 'an empty column carries no verdicts' a theorem rather than a stipulation. Version 4's 5.5 (v4:630-640) publishes the reason word `too_few_other_values` but never the number. I argued from the reason word alone and did not introduce the threshold as a rule. If the self-contained 5.5 states it, this paragraph can be tightened.
- NO SITE BINDS A NOTE FORM TO A ROLE. v6-build/s45.md:66-74 says explicitly that no rule binds a form to a path and that the A-E groupings are not normative. So nothing in the contract says an `empty` block's detection_evidence is B1, or that a `numeric_unrepresentable` block carries A1 and D4. I followed version 4 and mentioned notes in neither section; a reviewer expecting the binding will not find it anywhere.
- I DID NOT VERIFY THAT NO CALENDAR-PLACEHOLDER PASS CAN EMPTY A COLUMN BY RUNNING IT — the placeholder machinery is not shipped. I read C6-34 (v6:691-701), which says the pass enters only when the non-candidate remainder itself clears the datetime parse line, so the remainder is non-empty and the column cannot reach `empty` through it. That is a reading of the clause, not a measurement.

## CONFLICTS NOTED

- U1 AND U2 AS VERSION 4 WRITES THEM ARE FALSE OF DOCUMENTS THE SHIPPED PRODUCER WRITES, and I did not conform to them. v4:758-760 states both sums as '== n_present'. taxonomy.py:2327 guards both triples with `if cell.kind != parsing.NOT_A_NUMBER`, so both count the numeric-looking cells only. Three read-only runs of the shipped producer, each a 200-cell column at the default parse rate: 198 overflow + 2 words of text gives both triples 198 with n_present 200; 198 overflow + 2 accounting-parenthesis contradictions gives both triples 200 with n_not_numeric 0; 100 overflow fractions + 98 negative overflows + 2 words gives both triples 198. A loader enforcing '== n_present' refuses the first and third. I therefore state U1 and U2 over `n_numeric + n_out_of_range + n_contradictory` and mark the change in the text. This is a defect fix, not a new rule, so the identifiers stay U1 and U2 — but it lowers no obligation and raises none, and it should get an explicit owner ruling rather than riding in as transcription. The alternative disposition is that the PRODUCER is wrong and must count the stragglers; that is a code change, outside this contract and outside the ratified plan.
- n_positive COUNTS ZEROS AND VERSION 4 DOES NOT SAY SO. v4:753 reads 'present cells whose notation settles a positive sign'. taxonomy.py:2331-2337 counts SIGN_ZERO in `n_positive`, and the classifier gives '0', '0.0' and '-0' sign 'zero'. Two implementations reading v4's sentence would disagree about a column carrying a zero among its overflow values — reachable, since this role admits holdable numbers below the line. I added 'a cell denoting zero is counted here'.
- THE IDENTIFIER-FAMILY LIST IN MY BRIEF IS SHORT ON TWO FAMILIES, and I report it rather than trimming to it. The brief lists 'Q1-Q2' and 'AF1-AF4'. Version 4 carries Q1 through Q11 (v4:1938-1949) and version 6's C6-8 states AF1 through AF7 (v6:277-287). Neither family is mine, but the E family (E1, E2) and the X family (X1-X5), which my sections cite, are absent from the brief's list as well — so that list should be read as partial, not as the closed set of surviving identifiers.
- MY BRIEF ASKED SECTION 6.0 TO CARRY THE RULE ORDER AND THE NO-REGRESSION PROPERTY; v6-build/s5.md ALREADY CARRIES BOTH. C6-1's numbered list is at s5.md:180-197 and the after-categorical argument at s5.md:199-207. Writing them out again in the section 6 head would recreate exactly the two-sites defect this rewrite exists to end, so I gave the thirteen names (bound to the axis table by A5, as s5.md binds its own inline copies) plus a one-clause statement of the property and a pointer. If the assembler would rather section 6 own the order, the trade is that section 5.2's A2/A3 totality argument depends on the declaration sitting at rule 2 and would then need a pointer of its own.
- THE `empty` CARVE-OUT NOW APPEARS AT THREE SITES: the closed term at v6-build/s1.md:195 with its reason at :199-218, C6-PUB-A, and my 6.1. I kept 6.1's statement operational — what this block's absence keys hold, declared and undeclared — and pushed the whole argument to C6-PUB-A by name. Someone should confirm at assembly that the reason is argued in exactly one place.
- s1.md WRITES `C6-N3` FOR THE SIX-CLASS RULE (s1.md:164), which my brief forbids as a C6- letter identifier. I avoided the identifier entirely in my sections, calling it 'the six absence classes' and citing the source-accounting sum as N3 (version 5's C5-N3 keeps the letter it superseded). Whichever spelling wins, it must win in both sections.

## SOURCES

RULE-BY-RULE PROVENANCE. Paths relative to repo root
"/Users/alfredowork/Library/CloudStorage/OneDrive-UniversityofIowa/Research/0- Synthetic dataset/synthtwin".

SECTION 6 HEAD
- "thirteen roles", names + rule order: profile-contract-v6.md:204-212 (C6-1) and :1608-1615 (section 14); replaces v4's "There are ten roles" at profile-contract-v4.md:701-706.
- "thirteen roles in twelve rules, count and continuous share a rule": v6:208 ("`count` or `continuous`"); already stated at v6-build/s5.md:196-197.
- A5 binds the two lists: v6-build/s5.md:166-168.
- reading order vs rule order; identifier has no value rule: taxonomy.py:217-232 (ROLES comment), :3610-3616 (_decide docstring).
- "three rules tested last claim only what every earlier rule declined": phase-4-columns.md:52-77 (plan header no-regression rule), :505-512 (P4-D3); v6:210-212 (C6-1), :1573-1576 (13.1); wording aligned with v6-build/s5.md:199-207.
- "every key not listed is FORBIDDEN; the listings ARE the matrix": v4:708-712 (section 6 head) plus v6:966-994 (C6-FKM, which replaces v4's 6.11 matrix outright).
- no optional keys: v6-build/s1.md:129-138.
- "some parts cover more than one role": v4:793-797 (6.3 label roles), :1121 (6.7 count and continuous); long_tail_labels takes the same four label keys, v6:346-349 (C6-16).

6.1 empty
- definition, "added keys: none": v4:714-719; "twenty-two universal keys" from v6-build/s5.md:9.
- fact table rows: v4:721-729, transcribed unchanged.
- E1: v4:731; index row v4:1902. E2: v4:733-734, index row v4:1903; "the blocks invariant Q1 names" from v6:395-400 (Q1 restated over count, continuous, affixed_number).
- X4 / X2 read on this role: v4:1868-1870.
- distinctness paragraph: v4:736-738.
- stand-in rows reason: reason vocabulary at v4:638 (`too_few_other_values`); mechanism verified in taxonomy.py:4525-4527 (judging gate), :4581-4585 (`len(others) < 4`), and by running the shipped producer (see gaps).
- missing_by_class published on every empty column incl. declared: verified by running the shipped producer (declared run returns the six-key map with its counts while missing_by_source empties); consistent with v6:676-681 (six classes, on every block of every role) and v5:511-514 (C5-N6 names only the two counts and the source map).
- undeclared accounting + the closing sum: v5:468-478 (C5-N3, kept as N3); floor rule v5:480-490 (C5-N4).
- the 40-cell worked example: phase-4-v6-derivation-classconflict.md:46 and :138-143, both from a read-only run of the shipped producer; re-verified independently here.
- the below-floor variant: verified by running the shipped producer.
- declared column publishes nothing: v6:944-962 (C6-PUB-B); v4:1304-1312 (6.10 structural override); classconflict:46.
- "empty is in no value-publishing class": v6:909, :918-942 (C6-PUB, C6-PUB-A); term already fixed at v6-build/s1.md:195, :199-218.
- twin consequence: v6:829-845 (C6-37) and v6:1379 (disposition row).

6.2 numeric_unrepresentable
- opening definition: v4:740-744.
- detection rule ("parse-line count of numeric-looking cells, fewer holdable"): taxonomy.py:3642-3676 (`numeric_looking >= strict_needed and len(cells.numbers) < strict_needed`), :3660-3673 (the one-line reason); numeric-looking = n_numeric + n_out_of_range + n_contradictory, taxonomy.py:2383-2387, matching v4:1188 (Q9); "parse-line count" defined at v6-build/s45.md:714-719; the same statement already appears at v6-build/s45.md:786-789.
- added keys, first seven: v4:748-756, transcribed; shipped detail keys verified by running the profiler on an overflow column (exactly those seven).
- min_length/max_length: v6:505-511 (C6-26); phase-4-columns.md:802-822 (P4-D4.4); forbidden-key listing v6:979-981; key table v6:1181.
- "a cell denoting zero is counted in n_positive": taxonomy.py:2331-2337 (`SIGN_POSITIVE or SIGN_ZERO`), classifier verified on '0', '0.0', '-0'.
- U1, U2: v4:758-760 and index rows v4:1904-1905, CORRECTED to the numeric-looking population — taxonomy.py:2327 guards both triples with `if cell.kind != parsing.NOT_A_NUMBER`; verified on three shipped runs (see gaps/conflicts noted).
- the 200-cell example: `_needed(0.99, 200) == 198` verified; run returns role numeric_unrepresentable, n_present 200, n_not_numeric 2, both triples 198.
- U3: v4:762-763, index v4:1906; M1/M2 at v4:576-580; verified on the shipped runs.
- U4: v4:765-766 widened by v5:1052-1055 (U4/I3/F3 read with C5-N6) and v5:468-478; V2 at v4:649-653; the unpublished-candidate count at v4:642-648 (V1).
- U5: v6:509-510, index row v6:1248.
- U-P: v6:1224 (producer row) and v6:1204-1207 (why); population reason phase-4-columns.md:804-808 and the review item it closes, phase-4-columns.md:2405 (P4-P1-F7); "characters of the cell's text as the file spells it" from taxonomy.py:1703-1705 (`_lengths`) over `_split_missing`'s untrimmed present list, taxonomy.py:2713-2726, verified on the identifier role.
- width paragraph: rewritten from v4:768-775; disclosure price v6:1416-1418; residual closure v6:1564 and phase-4-columns.md:270-281; disposition v6:1181.
- cross-tabulation paragraph: v4:777-791, transcribed with its population sentence corrected to match U1/U2; X2 at v4:1868.