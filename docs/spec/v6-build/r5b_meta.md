VERDICT SOUND_WITH_CORRECTIONS errors=14

## ERRORS

1. MATERIAL — WRONG REFUSAL PATH AND WRONG SECTION. Transcription: 'Only that shape is refused, by name, in the refusal catalogue of section 10.7.' Section 10.7 is the LOADER refusal catalogue R1–R19 (docs/spec/profile-contract-v4.md:2466-2497), whose triggers are unreadable paths, non-canonical JSON, unknown keys, wrong types and violated invariants. The capacity conflict is not a loader refusal at all: v4:1992-2003 — 'the generation-feasibility stage runs after the loader and before any cell is built, and where the published facts are PROVED to have no joint answer it refuses GENERATION rather than the description ... The method's G12 carries the closed list of those refusals'; v4:2222-2223 — 'method G12 has four'; v4:2530-2545 — 'A profile that exceeds the invention capacity is a VALID version 4 profile and the loader accepts it.' The plan's 'closed list of four grows by exactly one named refusal' (docs/plans/phase-4-columns.md:1324-1327) is that G12 list. As written, a loader implemented from this text would refuse a conforming document.

2. MATERIAL — DROPPED STANDING MESSAGE OBLIGATIONS. plan:1327-1329 requires the refusal to carry 'the standing message obligations (the profile is valid, the two facts that cannot both hold are named, remediation does not assume the table)', restated at v4:1996-2000 and v4:2537-2545. The transcription carries none of the three. Restored.

3. MATERIAL — FALSE STATEMENT CARRIED FROM v6. TU-P: 'T4 and T5 bound it from both sides' (transcribed from docs/spec/profile-contract-v6.md:1231). T4 is n_unparsed < n_present and T5 is n_unparsed <= n_present − parse-line count: BOTH are upper bounds, and T5 is the stronger wherever the parse-line count is at least one — a fact the section's own T4 paragraph establishes. The lower bound comes from the key's own type (integer >= 0). Rewritten as 'its own type bounds it below at zero; T4 and T5 bound it above — T5 the tighter of the two wherever the parse-line count is at least one'.

4. MATERIAL — APPROXIMATED WITH NO BOUND, WHICH SECTION 2.2 DOES NOT ADMIT. v4:86 defines APPROXIMATED as 'reproduced under a stated rule inside a two-sided finite-sample bound ... named in the generation report with the achieved value beside the published one'. The parallel row for `date_percentiles` names its window (v4:2092, 'the window is fixed by generation-method-v1.md G12.4'). The transcription's `clock_percentiles` row gives the nine interior rungs as APPROXIMATED with no window at all, so the disposition is unsatisfiable as stated. plan:1305-1309 puts the new roles' windowed facts in the generation method's approximated-fields table; the row now names that table. No G-number invented — none exists for this role yet.

5. DROPPED RULE — THE ALL-DIFFERENT OBLIGATION IS NEVER SAID TO BIND. plan:740-741: 'The all-different obligation binds through the ordinal mechanism, with its capacity stated'; plan:1305-1307: 'The G11 all-different table gains three rows — ... time-of-day through ordinals with its finite-space bound'. The transcription states the capacity bound but never states the obligation whose bound it is, and v4:2259-2290 states that obligation as one rule with three named instances where it cannot bind, precisely so 'a fourth instance arriving undetected' is impossible. Restored, and stated as binding rather than as an instance of non-binding.

6. docs/spec/v6-build/r6.md:174 — `level_ceiling` IS NOT A LABELS-CLASS KEY. The forbidden-key matrix marks it on `categorical` alone, and C6-17 (v6:990) says `long_tail_labels` explicitly does NOT carry it. The transcription's bullet lists it among keys that 'belong to the labels class'. Reworded: the four level keys to the labels-class roles, `level_ceiling` to `categorical` alone.

7. docs/spec/v6-build/r6.md:100 — N6 MISSING. The adjacent section states this exact sentence as 'its two absence counts are written exactly as N3 and N6 have them'. N3 (v4:1874) governs `missing_by_source`; N6 (v5:1008) governs `n_missing_blank` and `n_missing_withheld`. The transcription cites N3 alone for the whole absent-cell accounting. Both now cited.

8. docs/spec/v6-build/r3.md:249, :369-380 — THE LEAP-SECOND CONTRAST IS OVERSTATED. Transcription: '`datetime` admits `SS` of `60`'. The datetime side admits it only conditionally: D10 forbids a seconds field of `60` on `earliest`/`latest` when `datetimes_read_at` is `utc`, and r3.md:390-393 says 'on the `local` clock — which is every column but the few that mix offsets — `SS` of `60` is accepted and written back unchanged'. Corrected to 'accepts an `SS` field of `60` on its local-clock endpoints'. The contrast and its reason are otherwise transcribed faithfully from plan:703-707.

9. plan:695-697 — DROPPED RIDER. The joint-reading refusal's reason ends 'the named candidate for a later widening on the resolution-mix precedent'. The transcription drops 'on the resolution-mix precedent', which is what names WHICH precedent licenses the later widening. Restored.

10. INTERNAL TENSION IN T3'S UNIT. T3 as transcribed (from v6:1240) reads 'never decrease in seconds of day', while the paragraph three lines above fixes the ordinal unit per form — minutes of day on an `hh-mm` column. The two orders are identical (the map is monotone), so the rule is not wrong, but as written a reader meets a unit the form does not use. One clause added; the statement and the identifier are unchanged, since the sealed method and the test suite cite T3.

11. SUPPLIED REASON, NOT IN ANY SOURCE — refusal 3. plan:706-707 gives 'no single-digit hours' with no reason at all, unlike the other two exclusions. The transcription supplies 'Both forms are fixed-width, which is what merits the two properties above; a variable width would give one ordinal two spellings.' Kept, because it is only the plan's own two-digit-fields rule (plan:690-691) restated as its consequence and it is not a new normative claim — but it is a reason no source states, and it is flagged so the owner can strike it.

12. SECTION NUMBER — the transcriber's declared gap is real and I confirm it: v4 §6.7 is `count` and `continuous` (docs/spec/profile-contract-v4.md:1121), so `6.7` collides. Set to `6.13`, this section's own assigned slot; docs/spec/v6-build/ASSEMBLY.md:69-77 renumbers section 6 in one pass regardless, and nothing in the text depends on the number (every cross-reference is by role name or invariant identifier).

13. HOUSE STYLE — `#### C6-13.` and `#### C6-14.` put a bare identifier in the heading text, which no adjacent written section does (r3.md:317 '**Invariant D6 (…).**' under a prose heading; r6.md:71-73 '**C6-50.**' under '#### The three value-publishing classes'). Restyled to prose headings with the identifier in the body. Both remain `C6-` NUMBERS, which the identifier rule permits.

14. PARSE-LINE COUNT — the arithmetic that defines it is section 4.5.2 (docs/spec/v6-build/s45.md:712-720), not section 4.4, which fixes only the setting. The transcription cites 4.4 alone, agreeing with r3.md:62-63. Both citations added so the count is reachable from this section, since T5 is checkable only against the arithmetic.

## GAPS

- SECTION NUMBER NOT FIXED BY ANY SOURCE. I used 6.7, which collides with version 4's `count`/`continuous` at 6.7. The already-written build sections pin 6.1-6.2 (r1), 6.3 (r2), 6.6/6.6.1/6.6.2 (r3) and 6.10/6.11 (r6), leaving no free slot for the three added roles; r2.md:5 refers to `long_tail_labels` by 'the section for' rather than by number, so the build has not settled this. The assembler must renumber section 6 as a whole. Nothing in my text depends on the number: every cross-reference is by role name or invariant identifier.
- `clock_percentiles` JSON TYPE IS STATED TWO WAYS IN v6 AND I HAD TO CHOOSE. v6:1178 says 'array of 11 strings'; C6-11 (v6:316) says 'an eleven-rung ladder of clock values in the ladder's fixed rung order'. The vetted derivation already registered this as unresolved (material/phase-4-v6-derivation-datetime.md:34). I wrote it as a LADDER in section 5.6's shape (an object with the eleven named keys) because the plan governs and says 'an eleven-rung ordinal ladder ... like the date ladder' (plan:713-714, 721-726), because T2's 'first rung / last rung' is D11's shape, and because the generator pins `min` and `max` by name. If the owner rules for a bare array instead, T2, T3 and section 5.6 all need rewriting and my paragraph on named rungs must be deleted.
- SECTION 5.6 (THE LADDER) IS NOT YET IN THE v6-BUILD SET, and when it is written it must name `clock_percentiles` as a third ladder-carrying field with its comparison rule. As things stand L1 is quantified over `percentiles` and `date_percentiles` only, while T3 governs a third field; I wrote T3 as 'the reading L1 takes on this field' so the two cannot drift, but the 5.6 author has to make that true.
- THE DISPOSITION OF `n_distinct` AND `n_distinct_folded` ON THIS ROLE IS SETTLED NOWHERE. s5.md:29-30 defers both to section 9 ('set per role group'); v6's section 11 delta has no `time_of_day` row; the plan defers the new roles' windowed facts to the generation method's approximated-fields table (plan:1305-1309). On `datetime` they are APPROXIMATED with a named envelope. Because the capacity refusal is stated in terms of `n_distinct`, a reader cannot currently tell which class it is in. I made no claim about it rather than guess; whoever writes section 9 must add the row.
- NO SHIPPED CONSTANT EXISTS FOR `clock_form`. `clock_form`, `time_of_day` and `hh-mm` do not occur anywhere under src/synthtwin/ — the role is unlanded. The two members rest on v6:1630, C6-11 (v6:313) and plan:690-691 only, transcribed element by element from those three, and cannot be checked against code the way `format` and the settings keys can.
- NO NOTE FORM FIRES ON A CLAIMED `time_of_day` COLUMN. The nineteen forms of v6-build/s45.md (D1-D19) include no clock form; D11's argument 8 is written on the column this rule DECLINED. I stated that plainly rather than leave the absence to be read as an oversight, but if the owner wants a clock column to carry a remark of its own, none exists to carry and the grammar would have to gain one.
- TRANSCRIPTION CHOICE ON `n_unparsed`'s DISPOSITION. v6:1179 gives it as bare EXACT-OBSERVABLE; v4:2098 qualifies the identical field on `datetime` as 'EXACT-OBSERVABLE as counted neutral stand-ins, explicitly OUTSIDE the parsed-value representation obligation'. I carried the qualified form, transposed to the clock, because the unqualified one would read as an obligation to reproduce unparsed CELLS rather than their count.

## CONFLICTS NOTED

- v6 C6-10 IS ARITHMETICALLY WRONG ABOUT WHEN THE TIE-BREAK IS REACHABLE. v6:307-308 says both forms clearing the line is 'reachable whenever `minimum_parse_rate` is set below one'. A cell matches at most one of the two forms, so two disjoint sets must each reach the line: both clear only where twice the parse-line count is at most `n_present` — roughly a rate at or below one half. At 0.9, and at the shipped default of 0.99 (src/synthtwin/taxonomy.py:1164), the case cannot arise at any `n_present`. The rule itself (the finer form wins) is right and I carried it unchanged; I replaced the reachability sentence with the correct condition and did not cite the default, since the contract publishes the run's actual setting rather than a default.
- SECTION 4.4's `minimum_parse_rate` ROW DOES NOT NAME THE CLOCK RULE, WHICH IS THE EXACT DEFECT THIS REWRITE EXISTS TO REMOVE. v6-build/s4.md:121 reads 'THE line for the numeric roles and for the datetime role, and the only one', while C6-20 twelve lines later (s4.md:132-134) says the clock rule reads that same key, and C6-10 and T5 both depend on it. One rule, two places, one of them left un-updated. The row should name the clock rule and the affix rule, or say that it is the line for every rule that has one. Flagged for the section 4 author; I did not change my section, which cites section 4.4 as the definition site.
- THE LEAP SECOND IS ADMITTED BY ONE ROLE AND REFUSED BY THE OTHER, AND ONLY ONE END SAYS SO. `datetime` accepts `SS` of `60` and calls that deliberate (v4:951, carried at v6-build/r3.md:249); `time_of_day` refuses it (plan:703-707). The two are consistent — the datetime endpoint construction writes an instant's own fields while this role writes ordinals — but r3's `SS` row does not mention the clock role, so a reader meeting the two rules in either order sees a contradiction. I stated the contrast and its reason at my end; the datetime section should carry the matching sentence.

## SOURCES

PRIMARY. Plan (governs): docs/plans/phase-4-columns.md P4-D4.2 at 688-748 (what it reads, the two forms and field ranges, one form at the parse line, other-form cells counted in n_unparsed, no joint reading with its mixed-ISO contrast, the three exclusions with their reasons, what it publishes, the line-not-circle model, the twin's form-unit interpolation, the 1,440/86,400 capacities and the net-of-unparsed refusal shape); residual R-P4-5 at 1524-1529 (the four refusals, in-slack minority form is not a decline); refusal catalogue growth at 1324-1329; ranges-class floor-free endpoint policy at 61, 262-263, 1237-1239; P4-D2 third class (counted stand-in cells) at 400-424; generation landing rows at 1295-1309. CONTRACT v6: docs/spec/profile-contract-v6.md C6-10 to C6-14 at 297-336; key table row set at 1176-1179; invariants T1-T5, T-P, TU-P at 1223-1242; publication classes at 904-916; forbidden-key listing at 986-994; disclosure rows 4 and 13 at 1407-1410 and 1472-1476; clock_form enumeration at 1630. CONTRACT v4 (transcribed voice and carried rules): docs/spec/profile-contract-v4.md 6.6.2 datetime at 910-1119 (role-section shape, D8, D11, endpoint-range table, format REPORT-ONLY), 5.6 the ladder at 668-695 (eleven rung names, L1-L3), 2.2 the six disposition classes at 76-94, 9.6 disposition rows at 2086-2100 (the n_unparsed wording I carried verbatim in sense: EXACT-OBSERVABLE as counted neutral stand-ins, outside the parsed-value obligation), 6.11 matrix at 1314-1373. ADJACENT WRITTEN SECTIONS (agreed with, not redefined): docs/spec/v6-build/s5.md 5.1 universal keys at 7-75, axes table and C6-19 at 85-124, rule order C6-1 at 178-213 (time_of_day is rule 9); v6-build/s4.md settings table at 107-124 and C6-20 at 126-136; v6-build/s45.md parse-line-count definition at 714-720 and note forms D1-D19 at 331-640, D11 argument 8 and its clock clause at 418-456; v6-build/r3.md 6.6.2 datetime at 59-460 (SS=60 at 249, the two clock forms cited by the slashed-datetime members at 114-115); v6-build/r6.md 6.10 publication class at 67-112 and 6.11 matrix at 165-299 (time_of_day column: exactly five marked cells; the shared-name note at 271-276); v6-build/r1.md section 6 preamble at 1-46. CODE: src/synthtwin/taxonomy.py _needed at 1666-1676 (parse-line count) and minimum_parse_rate default 0.99 at 1164; contract.py SETTINGS_KEYS at 160. VETTED MATERIAL: docs/plans/reviews/material/phase-4-v6-derivation-datetime.md:34 and :63 (the clock_percentiles type gap, already registered).