VERDICT SOUND_WITH_CORRECTIONS errors=12

## ERRORS

1. FALSE ADDED CLAIM, contradicted by its own source — the worst defect here. The added code-point paragraph in 3.2 says "Several of this contract's mappings key themselves on the figures of a whole number — a fraction width, A GROUP SIZE — and a numeric sort of those keys is not the canonical order." The group-size mapping is the multiplicity map, and docs/spec/profile-contract-v4.md:562-567 requires the opposite: "Each key is the row count in base ten, left-padded with zeros to the width of the LARGEST key in the SAME mapping. Padding is what makes the canonical sorted-key order a numeric order: written bare, `"10"` sorts before `"2"`." For that mapping the numeric order IS the canonical order, by construction and for exactly this reason. No later clause changes it: docs/spec/profile-contract-v5.md:528-529 only restates that `variants_withheld` is a multiplicity map, and a grep of v5, v6 and docs/plans/phase-4-columns.md for `multiplicity`/`padded`/`padding` returns nothing else. Read normatively, the draft's paragraph authorizes a producer to write unpadded multiplicity keys, which invariant M2's numeric reading (docs/spec/profile-contract-v4.md:577-578) and §5.3's stated reason forbid. Corrected: the paragraph now splits the two cases and cites both.

2. BARE-IDENTIFIER CROSS-REFERENCE, AND A LIVE LETTER COLLISION. 3.2.1 cites "this contract's own T1 (section 10.5)" and never states it — a transcription of docs/spec/profile-contract-v4.md:144, but criterion 5 makes a rule cited by identifier without being stated a defect in a self-contained document. Worse, the letter is now ambiguous: docs/spec/profile-contract-v4.md:2424-2427 names the loader's type rule T1 ("integers are integers"), while docs/spec/profile-contract-v6.md:318-321 and docs/spec/profile-contract-v6.md:1238 name a `time_of_day` invariant T1 ("every published clock value is written in the form `clock_form` names"), and the identifier rule makes BOTH keep their letters. The author's declared gap says "The T1 citation is safe under the identifier rule" — it is not; it is the one citation in this section that is unsafe. Corrected: T1's content is now stated in 3.2.1 and the letter disambiguated locally. The assembling editor still has to rule on the collision document-wide.

3. RELATIONAL FRAMING SURVIVING INTO A SELF-CONTAINED DOCUMENT (criterion 5). 3.4's added sentence reads "The three roles version 6 adds do not deepen it" — without naming them — and then "every key `affixed_number` and `time_of_day` add" and "every key version 6 adds elsewhere". A self-contained document carries no earlier version, so "adds" resolves to nothing and the three roles are an unnamed enumeration in a document whose premise is that every enumeration is written out. Corrected: the roles are named (`affixed_number`, `time_of_day`, `long_tail_labels`, per docs/spec/profile-contract-v6.md:146 and the §14 list at docs/spec/profile-contract-v6.md:1612) and the depth bound is restated as a closed fact about the format rather than as a diff.

4. BOTH "EVERY KEY" ENUMERATIONS IN 3.4 ARE INCOMPLETE. Against docs/spec/profile-contract-v6.md:251-261 (C6-6), the `affixed_number` list omits `n_affixed` and the entire quantitative key set — which contains the OBJECTS `percentiles` and `numeric_styles`, the only two members of that set that are containers at all and therefore the only ones the depth claim actually turns on. Against docs/spec/profile-contract-v6.md:1184-1187 ("Every new and changed key, in one table"), the "elsewhere" list omits the `(date-sentinel)` key of `missing_by_class`, the `settings` key `day_first` and the `settings` key `long_tail_minimum_level`. Every omitted key does sit at four or less, so the conclusion survives; the enumerations do not, and this is the exact failure mode the rewrite exists to stop.

5. 3.4 NAMES ONLY ONE OF THE TWO CONTAINERS AT THE SIXTH LEVEL. `variants_withheld` stands beside `variants` as a key of a `levels` entry and reaches the same depth: src/synthtwin/profile.py:716-724 gives `("columns", EACH, "levels", EACH, "variants_withheld", KEY_OF)`, six steps, exactly as src/synthtwin/profile.py:713-715 does for `variants`. A mechanical enumeration of every path in `profile.PUBLICATION_RULES` gives four six-step paths (`variants` and `variants_withheld`, key and value) and eight five-step paths (a `levels` entry, a `sentinel_verdicts` entry), nothing deeper. v4's own sentence at docs/spec/profile-contract-v4.md:184-186 named only `variants`, which was tolerable while the sentence was inherited; a section that now re-derives the bound must name the second container or its derivation is not checkable. Also, docs/spec/profile-contract-v6.md:346-349 (C6-16) gives `long_tail_labels` `variants` AND `variants_withheld`, so the draft's re-derivation sentence is short of its own source there too.

6. WRONG PROVENANCE CITATION for the empty-container bullet. The table cites `v4:1394` for "`publication_notes` may be empty". docs/spec/profile-contract-v4.md:1394 reads "The axes are what the version 4 GENERATOR dispatches on." — nothing to do with it. The real statement is docs/spec/profile-contract-v4.md:395, "An array, possibly empty, of objects each having exactly two keys". Two stronger witnesses exist and are not cited: docs/spec/profile-contract-v4.md:571-572, "**Empty map.** `{}` is valid and means the thing being counted has no members", and docs/spec/profile-contract-v4.md:849, invariant B8, "`levels == []` is valid". The bullet's CONTENT is right — probe of the shipped serializer confirms `{"a": {}, "b": []}` renders both inline and closes a non-empty container at the parent's indent — but a citation that does not resolve is the thing this rewrite is supposed to make impossible. Corrected: the bullet now carries the three real witnesses.

7. THE BYTE-LENGTH OBLIGATION IS STATED PERMISSIVELY AND CARRIES NO IDENTIFIER, although it is genuinely new normative content. The draft's 3.3 paragraph says "A loader that decodes that way CLOSES THE GAP by comparing…", which describes a technique rather than imposing a duty. The duty exists only in code: src/synthtwin/contract.py:1534 refuses on unequal text and src/synthtwin/contract.py:1536-1537 refuses AGAIN, under the same R10, when `_utf8_length(rewritten) != size`, with the reason at src/synthtwin/contract.py:1269-1278. No clause of v4, v5 or v6 states it — docs/spec/profile-contract-v4.md:2383 says only "require the result to equal the file's text byte for byte", which an implementer reading through docs/spec/profile-contract-v4.md-style universal-newline decoding (src/synthtwin/contract.py:1241) satisfies while accepting a CR LF file. The author was right to write it down and right to flag it; but under the identifier rule genuinely new normative content takes a `C6-` number, and this paragraph has none. Corrected to normative wording; the assembling editor must allocate the number, since §3 cannot know the document's plain-number sequence.

8. HANDOFF, NOT A §3 DEFECT — a type disagreement found while checking the depth claim. docs/spec/profile-contract-v6.md:315-316 calls `clock_percentiles` "an eleven-rung ladder of clock values in the ladder's fixed rung order", which reads as an object keyed by the eleven rung names like every other ladder in this format, while docs/spec/profile-contract-v6.md:1178 types it "array of 11 strings". Both sit at depth four, so 3.4's bound is unaffected either way, but the section that states `clock_percentiles` must settle it and §3 cannot.

9. MINOR, INHERITED, LEFT UNCHANGED ON PURPOSE. 3.3's "Section 10.4 makes this the loader's first semantic check" is docs/spec/profile-contract-v4.md:181-182 word for word, but docs/spec/profile-contract-v4.md:2311-2312 puts the `profile_version` check at step 5 and the round trip at step 6, and docs/spec/profile-contract-v6.md:1265-1269 (C6-44) keeps that order deliberately. "First" was already loose in v4; the identifier rule says keep the wording, so I kept it, and flag it so §10's author and the editor do not read it as authorizing a different step order.

10. MINOR, FIXED BY A POINTER. The added escape bullet ends "and every non-ASCII character is written as itself", which the very next bullet — docs/spec/profile-contract-v4.md:131, transcribed — states as its own rule. docs/spec/profile-contract-v5.md:173-179 gives the standing reason against a second place for one rule to live ("A copy of two thousand eight hundred lines is a second place for a rule to live, and this repository has a four-entry history of an obligation being restored in one place and weakened in the paragraph after it"). Both bullets are needed — the escape bullet must close its list to say "nothing else" — so the added bullet now points at the transcribed one rather than competing with it.

11. VERIFIED CLEAN, recorded so the editor knows what was checked and passed. (a) v4 §3 is docs/spec/profile-contract-v4.md:116-187 and every one of its rules appears in the draft: 3.1 at 118-120, 3.2 and its five bullets at 122-136, 3.2.1 at 138-172, the G6.2 tie at 174-178, 3.3 at 180-182, 3.4 at 184-187. NOTHING IS DROPPED and no transcribed sentence changed meaning; I compared them line by line. (b) The five serializer arguments are exactly five and exactly right: src/synthtwin/canonical.py:125-129 gives `sort_keys=True`, `indent=2`, `separators=(",", ": ")`, `ensure_ascii=False`, `allow_nan=False`, with `+ "\n"` at src/synthtwin/canonical.py:131 and no sixth argument. (c) The escape set is exactly three groups and no fourth — probe of the shipped Python: U+0000 → `<NUL>`, U+0007 → `<BEL>`, U+0008 → `\b`, U+0009 → `\t`, U+000A → `\n`, U+000B → `<VT>`, U+000C → `\f`, U+000D → `\r`, U+001B → `<ESC>`, U+001F → `<US>`, lower-case hex throughout; `/`, U+007F, `é`, U+200B, U+202E, U+2028 and U+2029 all literal. This matches docs/spec/profile-contract-v5.md:339-343 (C5-5), which says canonical JSON escapes the C0 controls, the quote and the backslash while a bidirectional override or zero-width mark is written as itself. (d) Key order confirmed by probe: `(withheld)` < `0` < `1` < `10` < `2` < `A` < `_` < `a` < `é`. (e) The number grammar matches docs/spec/generation-method-v1.md:851-873 clause for clause, and the shipped spellings `1e+16`, `1000000000000000.0`, `0.0001`, `1e-05`, `-2.5`, `2.0` all probe as stated. (f) `resolution_mix` keys are format-member strings, not figures — docs/spec/profile-contract-v6.md:491-496 — so `fraction_widths` really is the only bare-integer-keyed mapping version 6 adds. (g) No §3 change in v5 or v6: docs/spec/profile-contract-v5.md:162 names "the encoding and canonical serialization rules of its section 3 and the number grammar of its 3.2.1" as carried at v4's wording, and the superseded list at docs/spec/profile-contract-v6.md:144-171, which docs/spec/profile-contract-v6.md:137-143 declares complete, has no row for section 3. (h) The plan and the six vetted derivation documents contain nothing on encoding, canonical form, key order, number spelling or nesting depth — I re-ran the grep over all seven and confirm the author's finding. (i) NEGATIVE ZERO remains genuinely undecided, as declared: the INTEGER bullet gives "a leading `-` when negative" and the FLOAT bullet leaves the sign implicit; `json.dumps(-0.0)` probes as `-0.0`; and docs/spec/generation-method-v1.md:858 and :893 rule "`0` is written `0`, never `-0`" for the TWIN's cells only. Whether a profile `skew` may read `-0.0` is decided by no source, and I have not invented an answer — the editor should add a sentence.

12. VERSION-NUMBER MISMATCH IN THE SHIPPED CONSTANT, confirmed as reported and harmless here. src/synthtwin/contract.py:109 reads `PROFILE_VERSION = 5`, so `MAXIMUM_DEPTH = 32` (src/synthtwin/contract.py:120) and the comment justifying it at src/synthtwin/contract.py:112-119 ("a conforming document is six levels deep whatever the data is") are the version 5 shipped state. I re-derived the six independently from `profile.PUBLICATION_RULES` — four six-step paths, all under `levels`/entry/`variants`|`variants_withheld` — and from every key docs/spec/profile-contract-v6.md:1168-1187 adds, and the sentence is still true under version 6. 3.4's "version 6" is therefore correct but still backed by no shipped constant.

## GAPS

- NEGATIVE ZERO has no stated spelling. 3.2.1's INTEGER bullet says "a leading `-` when negative" and its FLOAT bullet describes only the digits and the point position, leaving the sign implicit. The shipped serializer writes the float negative zero as `-0.0` (probe of `json.dumps(-0.0)`), and `docs/spec/generation-method-v1.md:856` rules for the TWIN's cells that "`0` is written `0`, never `-0`" — a rule about twin cells, not about profile numbers. Whether a profile `mean`, `skew` or ladder rung may read `-0.0` is not decided by v4, v5 or v6, and I did not invent an answer. If a `skew` of `-0.0` is producible, two implementations can disagree, so the assembling editor may want a sentence.
- Three paragraphs and two bullets in this draft are ADDITIONS rather than transcriptions, each marked as such in the provenance table: the line-ending paragraph in 3.1, the byte-comparison paragraph in 3.3, the code-point-order paragraph in 3.2, and the empty-container and string-escape bullets in 3.2. None is a new RULE — each states what 3.2's construction or 3.3's byte definition already fixes — so I gave none of them a `C6-` identifier, on the ground that a plain number would put a non-rule into the rule sequence. If the editor wants them numbered, those are the five places.
- The maximum-depth re-derivation in 3.4 is mine, from the version 6 clauses and the shipped producer's path table. No source document restates the depth for version 6: v5 carries v4's sentence by reference (`v5:162`), and v6's superseded list has no row for it. I confirmed six is still right and confirmed no version 6 key nests deeper than four, but nobody has ratified that re-derivation.
- I did not establish whether the assembled self-contained document keeps v4's section numbers for §10 (the loader) and §10.5 (the type rules T1–T4). This section cites "section 10.4", "step 4 of section 10.1 (R5)" and "T1 (section 10.5)" exactly as v4 does. The T1 citation is safe under the identifier rule; the three bare section numbers need a check once §10 is written.
- `docs/plans/phase-4-columns.md` and the six vetted derivation documents in `docs/plans/reviews/material/` contain nothing about encoding, canonical form, key order, number spelling or nesting depth — I grepped all seven for `canonical`, `serializ`, `utf-8`, `3.2.1`, `number grammar`, `encoding`, `nesting` and `depth`. So the plan neither confirms nor contradicts anything in this section, and none of the vetted material covers it. Section 3 rests on v4's text and the shipped source alone.

## CONFLICTS NOTED

- NO CHANGE TO SECTION 3 IN EITHER LATER VERSION, and this is the direct answer to the question the task asked. Version 5 names section 3 and 3.2.1 explicitly as carried unchanged (`profile-contract-v5.md:162`), and version 6's superseded list — which its own 2.2.2A says is complete, "all of them, in one place" (`v6:137-143`) — has no row for encoding, canonical form, key order, number grammar or nesting depth (`v6:144-171`). I found no unregistered change. The self-contained §3 is therefore v4's §3 verbatim plus the five self-containment additions listed in `gaps`.
- UNREGISTERED RULE IN CODE, NOT IN ANY CONTRACT: the loader's byte-length check. `contract.py:1534-1537` refuses a document under R10 when `_utf8_length(rewritten) != size`, a second condition beyond the text comparison, and `_utf8_length`'s own comment (`contract.py:1269-1278`) says why: the only permitted reading call, `read_text(encoding="utf-8")` at `contract.py:1241`, translates line endings, so a CR LF file decodes to text that compares EQUAL and must still be refused. No clause of v4, v5 or v6 states this. `v4:2383` says only "require the result to equal the file's text byte for byte", which an implementer reading a file with universal newlines does not satisfy. I have written it into 3.3 as the byte-comparison paragraph, because 3.3 is where CANONICAL is defined over bytes; the author of §10 should be told, since §10.4 is where v4 put the round trip and its table of what the check catches has no CR LF row.
- VERSION-NUMBER MISMATCH IN THE SHIPPED CONSTANT, expected but recorded because §3 was checked against the source: `src/synthtwin/contract.py:109` reads `PROFILE_VERSION = 5`. The shipped loader is a version 5 loader, so every constant I checked against — `MAXIMUM_DEPTH = 32` (contract.py:120), `MAXIMUM_NUMBER_CHARACTERS = 64` (contract.py:121), the serializer (canonical.py:122-131) — is the version 5 shipped state. Nothing in section 3 differs between the two versions, so this affects no rule I wrote, but 3.4's "version 6" is a document-side statement that no shipped constant yet backs.
- THE COMMENT ABOVE THE DEPTH CONSTANT WILL GO STALE, and it is load-bearing for 3.4. `contract.py:113-114` reads "a conforming document is six levels deep whatever the data is" as the justification for `MAXIMUM_DEPTH = 32`. That sentence is still true under version 6 — I verified it against every key version 6 adds — but nobody re-derived it when the three new roles landed, and the derivation now lives in this section rather than in the code.

## SOURCES

| rule in §3 | transcribed from | verified against |
|---|---|---|
| 3.1 — single JSON object; UTF-8; LF; no BOM; exactly one terminal newline | `docs/spec/profile-contract-v4.md:118-120`, word for word | `src/synthtwin/writing.py:2111` (`destination.write_text(text, encoding="utf-8", newline="\n")`); `src/synthtwin/canonical.py:122-132` (the `+ "\n"`) |
| 3.1 — the added paragraph "line endings are the producer's to control" | NOT in v4/v5/v6; entailed by 3.1 + 3.3 | `src/synthtwin/writing.py:2111`, which passes `newline="\n"` explicitly rather than defaulting |
| 3.2 — canonical text defined by construction; the five `json.dumps` arguments; the trailing newline | `docs/spec/profile-contract-v4.md:122-125`, word for word | `src/synthtwin/canonical.py:110-132` — `sort_keys=True`, `indent=2`, `separators=(",", ": ")`, `ensure_ascii=False`, `allow_nan=False`, `+ "\n"`, all five present and no sixth |
| 3.2 bullet — keys in ascending code-point order | `docs/spec/profile-contract-v4.md:127` | `canonical.py:125` (`sort_keys=True`); probe run of the shipped Python confirms `(withheld)` < `0` < `1` < `10` < `2` < `A` < `a` < `é` |
| 3.2 bullet — two-space indent per level | `docs/spec/profile-contract-v4.md:128` | `canonical.py:126` |
| 3.2 bullet — member separator `,` plus the indent mode's newline; key/value separator `: ` | `docs/spec/profile-contract-v4.md:129-130` | `canonical.py:127` |
| 3.2 bullet — EMPTY object `{}` / empty array `[]` on one line; non-empty containers one member per line | ADDED; not stated in v4, v5 or v6 prose. Entailed by 3.2's construction | probe of the shipped Python: `{"a": {}, "b": [], "e": [1, 2]}` serializes with `"a": {}` and `"b": []` inline and `"e"` opened across lines. Reachable in a conforming document — `publication_notes` may be empty (`v4:1394`), `fraction_widths` is `{}` in case P5.b (`docs/spec/profile-contract-v6.md:552-554`) |
| 3.2 bullet — the escape set: `\\`, `\"`, U+0000–U+001F with `\b \t \n \f \r` and otherwise lower-case `\u00XX`; `/`, U+007F and all non-ASCII unescaped | first stated in prose at `docs/spec/profile-contract-v5.md:340-343` (C5-5: "Canonical JSON escapes the C0 controls, the quote and the backslash"); the exact set is entailed by 3.2's construction | probe of the shipped Python with `ensure_ascii=False`: U+0000→`<NUL>`, U+0007→`<BEL>`, U+0008→`\b`, U+0009→`\t`, U+000A→`\n`, U+000B→`<VT>`, U+000C→`\f`, U+000D→`\r`, U+001B→`<ESC>`, U+007F literal, `/` literal, `é` / U+200B / U+202E literal |
| 3.2 bullet — non-ASCII written as themselves, not `\u` escapes | `docs/spec/profile-contract-v4.md:131`, word for word | `canonical.py:128` (`ensure_ascii=False`) |
| 3.2 bullet — `NaN`, `Infinity`, `-Infinity` not writable | `docs/spec/profile-contract-v4.md:132-133`, word for word | `canonical.py:129` (`allow_nan=False`); the refusal it raises is `contract.py:1524-1529` (R7) |
| 3.2 bullet — numbers by the grammar of 3.2.1, two cases, on KIND not value | `docs/spec/profile-contract-v4.md:134-136`, word for word | — |
| 3.2 — the added "key order is over CODE POINTS" paragraph and its `fraction_widths` example | ADDED; the ordering rule itself is v4:127. The paragraph exists because version 6 introduces integer-spelled mapping keys | key grammar from `docs/spec/profile-contract-v6.md:533-538` (C6-29: `0`, `1`, `2`, `10`, pooled key exactly `(withheld)`); order confirmed by probe |
| 3.2.1 — the whole clause, both bullets, and the P2-C1-F8 provenance | `docs/spec/profile-contract-v4.md:138-163`, word for word | `docs/spec/generation-method-v1.md:851-867` states the identical float rule (`-4 < decpt <= 16`, `.0` appended, `d[.ddd]e±XX`, lower-case `e`, sign always, two digits) |
| 3.2.1 — the `2.0` / `2` paragraph and the `1.0e2`, `+5`, `05` examples | `docs/spec/profile-contract-v4.md:165-172`, word for word | probe confirms `1e+16`, `1000000000000000.0`, `0.0001`, `1e-05`, `-2.5`, `2.0` are the shipped spellings |
| 3.2.1 — "the same grammar the twin's own numeric cells use" | `docs/spec/profile-contract-v4.md:174-178`, word for word | `docs/spec/generation-method-v1.md:851-874` (G6.2), and its own boundary examples `1e+16`, `1000000000000000.0`, `0.0001`, `1e-05`, `5.0`, `-2.5` |
| 3.3 — CANONICAL is bytes equal to the canonical text; §10.4 is the loader's first semantic check | `docs/spec/profile-contract-v4.md:180-182`, word for word | `src/synthtwin/contract.py:1498-1537` (`_round_tripped`) |
| 3.3 — the added "comparison is over BYTES" paragraph | ADDED; not in v4, v5 or v6 prose. It is the shipped loader's rule, stated in code and not in the contract | `src/synthtwin/contract.py:1241` (`read_text(encoding="utf-8")`, universal newlines), `contract.py:1266-1300` (`_utf8_length` and its "WHY THIS EXISTS" comment), `contract.py:1534-1537` (text equality AND byte-length equality, both raising R10) |
| 3.4 — maximum nesting depth six, and the chain that reaches it | `docs/spec/profile-contract-v4.md:184-187`, word for word, with "version 4" read as "version 6" | `src/synthtwin/contract.py:112-121` ("a conforming document is six levels deep whatever the data is"); deepest producer path is `("columns", EACH, "levels", EACH, "variants", <key>)` at `src/synthtwin/profile.py:713-715` and `src/synthtwin/canonical.py:77-80` |
| 3.4 — the added sentence that version 6's new roles and keys do not deepen it | ADDED; a re-derivation the self-contained document needs, since v4's claim was made over ten roles | `long_tail_labels` levels/variants: `docs/spec/profile-contract-v6.md:346-349` (C6-16). Flat block keys: C6-6 at `v6:251-261`, C6-11 at `v6:313-316`, C6-26 at `v6:505-509`, C6-27 at `v6:515-526` (`fraction_widths` a sibling of `numeric_styles`, a key of the BLOCK), `resolution_mix` at `v6:1251-1252`. Declaration records: C6-S14 at `v6:1246`, three lists at `profile.py:632-641` — `("settings", "declared_missing_values", "built_in_texts", EACH)` is four steps |
| §3 has no version 5 or version 6 change at all | `docs/spec/profile-contract-v5.md:157-171` names "the encoding and canonical serialization rules of its section 3 and the number grammar of its 3.2.1" as carried at their version 4 wording; `docs/spec/profile-contract-v6.md:112-117` carries version 5 total, and the superseded list at `v6:144-171` contains no row for section 3 | grep of v5 and v6 for `canonical`, `json.dumps`, `sort_keys`, `ensure_ascii`, `allow_nan`, `separators`, `indent`, `utf-8`, `byte-order`, `newline`, `nesting`, `depth`, `encod` returns no clause changing any of them |