VERDICT SOUND_WITH_CORRECTIONS errors=15

## ERRORS

1. DEFECT 1 — INVENTED RATIONALE, §6.4 invariant C2. The clause "and it is written out because it is the whole of what a reader checks by eye in a `constant` block" is stated by no source. v4:867-870 gives C2 with no rationale; docs/plans/phase-4-columns.md and docs/spec/profile-contract-v6.md say nothing about C2 at all (grep for C2 in v6 returns no rule). The writer's own provenance table declares only the arithmetic half. The arithmetic half IS verifiable — C1 (v4:865) plus B2 (v4:828-829) yields len(levels)+suppressed_levels==1 — so it is kept; the invented motive is cut. Corrected text ends the paragraph at "C2 is what C1 and B2 come to together."

2. DEFECT 2 — PARTIAL ENUMERATION READABLE AS CLOSED, §6.3 floor paragraph. The transcription says the empty range at a floor of one "is what invariant S13 states about `suppressed_levels`, `suppressed_rows`, `suppressed_level_counts` and `variants_withheld`". S13's list is TEN positions and its exhaustiveness is itself load-bearing: docs/spec/v6-build/s4.md:363-373 lists those four plus `n_sentinel_candidates_unpublished`, `n_missing_withheld`, and the `(withheld)` ENTRIES of `missing_by_class`, `utc_offsets`, `numeric_styles` and `fraction_widths`; docs/spec/profile-contract-v5.md:1036-1047 states in bold that THE LIST IS EXHAUSTIVE and that a walk depends on it. Naming four of ten with no marker that the list continues is exactly the enumeration failure mode this contract is being rewritten to stop. Reworded so S13 is stated as a rule and the four are explicitly a subset ("lists every field it reaches — this section's ... among them").

3. DEFECT 3 — RULE STATED TWICE, §6.3 head sentence. "and no other role publishes a level" is a second, independent statement of the forbidden-key matrix, whose `levels` / `suppressed_levels` / `suppressed_rows` / `suppressed_level_counts` rows admit exactly constant, binary and categorical at docs/spec/profile-contract-v4.md:1321-1324, replaced whole by C6-FKM at docs/spec/profile-contract-v6.md:966-990 with the `long_tail_labels` row at v6:990. A rule in two sites is the defect the version 6 rewrite exists to remove. Rewritten as a pointer: "the forbidden-key matrix admits `levels` on no other role" — no section number, because the matrix is §6.11 in v4 and §7A in the v6 delta and the assembler places it, but the matrix's NAME is stable in both.

4. DEFECT 4 — AMBIGUOUS WORDING, §6.3.2 invariant B6 addition. "the two keys together are a total order" reads against this document's own vocabulary, in which "key" means a wire key of a JSON object — and `count` and `label` are wire keys of a level entry (contract.py:333, LEVEL_KEYS). A reader can take "the two keys" for B6 and B7. Named the sort explicitly: "descending `count` then ascending `label` is a total order". The claim itself is CORRECT: src/synthtwin/contract.py:3518-3530 raises B6 on `count > previous_count or (count == previous_count and label < previous_label)`, and contract.py:3511-3517 (B7) removes the equal-label case, so exactly one sequence conforms.

5. OUT-OF-SECTION, MUST BE ACTED ON — the writer's own reported disagreement is CONFIRMED and is the one live contradiction in the assembled document. docs/spec/profile-contract-v4.md:1455-1457 (§7.4.2) reads "They are REQUIRED on every entry of `levels` on the three label roles and FORBIDDEN everywhere else". C6-16 (docs/spec/profile-contract-v6.md:346-349) and P4-D5 (docs/plans/phase-4-columns.md:945-949) require `variants` and `variants_withheld` on every `long_tail_labels` level entry, and C6-FKM (v6:990) lists `levels` for that role. Transcribed unedited, §7.4.2 FORBIDS what C6-16 REQUIRES. The section 7 writer must widen "the three label roles" to "the four label roles: `constant`, `binary`, `categorical` and `long_tail_labels`". Nothing in section 6.3-6.5 can fix this and nothing there duplicates it.

6. VERIFIED, NOT A DEFECT — every enumeration checked element by element, both directions. (a) Four shared label keys: section lists `levels`, `suppressed_levels`, `suppressed_rows`, `suppressed_level_counts` = 4; v4:801-804 = the same 4; src/synthtwin/contract.py:335-341 LABEL_KEYS = the same 4 (`levels`, `suppressed_level_counts`, `suppressed_levels`, `suppressed_rows`); contract.py:342 CATEGORICAL_KEYS = LABEL_KEYS + (`level_ceiling`,) and contract.py:3227-3228 returns bare LABEL_KEYS for ROLE_CONSTANT and ROLE_BINARY, so `level_ceiling` FORBIDDEN on both is confirmed in code. No extra, none missing. (b) Four level-entry keys: section lists `label`, `count`, `variants`, `variants_withheld` = 4; v4:813-816 = the same 4; contract.py:333 LEVEL_KEYS = (`count`, `label`, `variants`, `variants_withheld`) = the same 4, enforced closed at contract.py:3494. No extra, none missing. (c) Exactly eight B invariants: section has B1..B8 = 8; v4:822-851 has B1..B8 = 8, no B9; contract.py:653-676 carries plain-language text for B1..B7 and contract.py:3424-3429 records B8 as a permission rather than a checked rule. Count agrees with the brief. (d) Four label roles: section head = 4; C6-PUB labels row v6:906 = `constant`, `binary`, `categorical`, `long_tail_labels` = 4; plan:552-554 puts `long_tail_labels` in the labels class; taxonomy.py:258 ROLES_PUBLISHING_LABELS ships 3, the fourth being the version 6 addition, exactly as the provenance says. (e) Four role-level invariants C1, C2, Y1, Y2 — all four at their v4 letters, C1 and Y1 confirmed live in the shipped refusal table at contract.py:677-678 and raised at contract.py:3643-3646.

7. VERIFIED, NOT A DEFECT — nothing dropped. A literal diff of docs/spec/profile-contract-v4.md:793-886 against the transcription shows the ONLY deletions are two pieces of delta framing, both declared: v4:808-809 "Two of them are version 4 additions" and the two "(version 4 addition)" parentheticals in the level-entry table rows v4:815-816. Every table row, every invariant, the disposition paragraph (v4:853-856) and the `n_distinct` note (v4:883-886) survive at v4's wording. v5 touches none of this: docs/spec/profile-contract-v5.md:1049-1056 lists the version 4 invariants version 5 reaches and B1-B8, C1, C2, Y1, Y2 are not among them (W1-W7 are named "unchanged" at v5:1053). v6 touches none of it either — no C6 rule names C1, C2, Y1, Y2, and C6-16 (v6:346-349) invokes B1-B8 "verbatim".

8. VERIFIED, NOT A DEFECT — the §6.3.2 preamble is true as stated. Reading B1 through B8 at v4:822-851 in full, not one of the eight names a role; the vetted closure judgement reaches the same finding independently at docs/plans/reviews/material/phase-4-v6-derivation-closure.md:145 (row U8, "only the head sentence is widened"); and the shipped loader confirms it structurally — `_levels` (contract.py:3417-3568) enforces B1-B7 once over a mapping and is called by `_label_facts` (contract.py:3631) and `_categorical_facts` (contract.py:3663) with no role argument reaching any B check. So none of B1-B8 becomes a new rule and none takes a C6- number, which is why there is correctly no `C6-B` anything in this section.

9. VERIFIED, NOT A DEFECT — the two clarifying phrases state what the shipped rule does. B4 "non-decreasing": contract.py:3458-3464 raises only on `found < previous_size`, so two held-back labels of equal size conform, and v4's bare "sorted ascending" is genuinely ambiguous about repeats — two independent implementations would disagree on an ordinary document. B6 total order: verified above. Both retained.

10. VERIFIED, NOT A DEFECT — no delta framing survives. The section contains no "supersedes", no "carried", no "unchanged from version 5", no "as version 4 has it", no `C6-` identifier of any kind (letter or number), and no rule referenced by identifier without being stated. The one reworded sentence, v4:885 "Version 4 publishes those spellings" to "This contract publishes those spellings", is the correct treatment and is declared. `parsing.folded` (src/synthtwin/parsing.py:364-375, `text.strip().casefold()`) confirms "trimmed and case-folded" in B1 and in the `label` row.

11. VERIFIED, NOT A DEFECT — cross-references. "section 4.4" resolves to the settings section (docs/spec/v6-build/s4.md:95, with `small_cell_floor` at s4.md:125). "invariant S13" resolves to docs/spec/v6-build/s4.md:363. "Section 5.3" resolves to the multiplicity map (v4:556; already cited as 5.3 by the written section 3 at docs/spec/v6-build/s3.md:30,65). "Section 7.4" and "W1 to W7" resolve to v4:1439-1535, and the expanded pointer is accurate: §7.4.2 gives what the keys hold and where they may not appear, §7.4.3 gives exactly W1 through W7 (v4:1505,1509,1514,1517,1521,1526,1531 — seven, none missing). "section 6.6.1" is categorical (v4:893). The placement decision to leave §7.4 whole in section 7 is sound and is confirmed by the shipped docstring citing 7.4 by number (contract.py:3574).

12. VERIFIED, NOT A DEFECT — the head sentence's role list cannot collide with the structural identifier override. Invariant A3 (docs/spec/v6-build/s5.md:146-148) confines `structural_role` `identifier` to roles `identifier` and `empty`, and the rule order settles the declaration at rule 2 (s5.md:184), ahead of `constant` at 4, `binary` at 5, `categorical` at 8 and `long_tail_labels` at 11. So no label-role block can ever be a nothing-publishing column by the structural route, and "the four are the labels publication class" is unconditional.

13. GAP CONFIRMED, ASSEMBLER MUST CLOSE — the §6.3 head still reads "the section for `long_tail_labels`" with no number, because the assembler's numbering of the three new role sections is fixed by no artifact (docs/spec/v6-build/s5_meta.md:34 records that even the axis-table row order is unfixed). Edit the head once the number exists. The same section must carry C6-16, C6-17, LT1 and LT2 — correctly absent here rather than duplicated.

14. GAP CONFIRMED, OWNER RULING NEEDED — the brief's identifier roster does not list C1, C2, Y1, Y2 (nor W1-W7 or G1). Keeping them at their version 4 letters is right under the transcription rule and is backed by the shipped refusal table (contract.py:677-678 for C1 and Y1). No plan clause and no version 5 or version 6 clause renames them. If the roster was meant to be exhaustive, that is a defect in the roster, not in this section.

15. MINOR, PROVENANCE TABLE ONLY (does not affect the section text) — several shipped-source line citations in the writer's provenance drift by one or two lines: B3 is contract.py:3558-3567 not 3558-3570; B4 is 3458-3484 not 3457-3480; B5's held-back half is 3451-3457 not 3450-3456 and its published half 3504-3510 not 3505-3511; B6 is 3518-3530 not 3517-3529; B7 is 3511-3517 not 3512-3516; `_levels` runs to 3568 not 3563. Every cited rule is at the cited site; only the endpoints drift.

## GAPS

- Cross-reference numbering for the fourth label role. My §6.3 head says "sections 6.4 and 6.5, section 6.6.1, and the section for `long_tail_labels`" because I cannot know what number the assembler gives the `long_tail_labels` role section. Version 4's head said "sections 6.4 to 6.6". If the assembler numbers the three new roles (e.g. 6.12 `time_of_day`, 6.13 `affixed_number`, 6.14 `long_tail_labels`, or inserts them in rule order), the head sentence should be edited to name it, and the same section must carry C6-16, C6-17/G1L, LT1 and LT2 — none of which I wrote, to avoid duplicating them.
- Publication-class cross-reference left unnumbered. I wrote "The four are the labels publication class" with no section number, because the version 6 delta calls that block "6A" (v6:892-906) while version 4 numbered it §6.10 (v4:1304). The assembler should add the number once the publication-class section is placed.
- The identifier roster in my brief does not list C1, C2, Y1, Y2 (nor W1-W7 or G1). I kept all four at their version 4 letters under the transcription rule ("a rule transcribed from version 4 or 5 KEEPS its identifier"), and the shipped refusal table confirms C1 and Y1 as live shipped identifiers (src/synthtwin/contract.py:677-678). If the brief's list is meant to be exhaustive rather than illustrative, this needs an owner ruling — but nothing in the plan or in version 5 renames them, and the generation and validation methods cite them.
- Y2's three-case enumeration ("both cleared, one did, neither did") is mine; version 4 states Y2 as the bare equation at v4:881 with no enumeration, while stating the parallel enumeration for C2 at v4:867-870. I added it for symmetry with C2. If the assembler prefers strict transcription, delete the sentence after "this is Y1 and B2 together" and Y2 reads exactly as version 4 has it.
- `n_distinct` may exceed 1 on a `constant` column for exactly the reason version 4 gives for `binary` (`Yes`/`YES` is one folded identity, two raw spellings). Version 4 states the note only under `binary` (v4:883-886) and I did not add a parallel note to §6.4, since C1 is written over `n_distinct_folded` and already carries the point. Flagged in case a reviewer reads the asymmetry as an omission.

## CONFLICTS NOTED

- PLACEMENT DECISION — version 4 §7.4 (label spelling variants, W1 to W7) STAYS IN SECTION 7. I did not transcribe it here and did not duplicate any of it. Reasons: (a) version 4 itself splits it that way and §6.3.1 points forward (v4:818), so keeping the split preserves every existing cross-reference — the shipped source docstrings cite "7.4" by number (src/synthtwin/contract.py:3563-3570 `_variants`, "How the rows under one published label actually wrote it (7.4)"), as do version 5 (v5:88-101, v5:314-315, v5:438-439, v5:498-502) and the version 6 delta; (b) §7.4 is much more than a wire shape — it carries 7.4.1 (what it fixes, and the owner's reversal of the implementer's recommendation), 7.4.4 (why the withheld map is needed), 7.4.5 (the disclosure delta, the casefold reading, the SECURITY.md obligation, residual R-P2-11, and the standing correction that contract text saying case is not preserved is wrong from version 4 on) and 7.4.6 (disposition); moving that body into §6.3 would put a disclosure argument inside a key table; (c) the W family keeps its own letters either way, so nothing is gained by relocation. ASSEMBLER: §6.3.1 must keep its forward pointer, and section 7 must keep §7.4 whole.
- OBLIGATION HANDED TO THE SECTION 7 WRITER — version 4 §7.4.2 opens "They are REQUIRED on every entry of `levels` on the three label roles and FORBIDDEN everywhere else" (v4:1462-1465). In version 6 that is FOUR label roles: `constant`, `binary`, `categorical`, `long_tail_labels`. The widening is forced by C6-16 (v6:346-349), which spells the level entry as carrying `label`, `count`, `variants`, `variants_withheld` on the long-tail role. I did not write it, because it is §7.4.2's sentence and the brief forbids duplicating §7.4 here. If section 7 is transcribed without that edit, a version 6 document is required by C6-16 and forbidden by §7.4.2 at the same time — the exact class of defect C6-FKM and the three C6- widenings of v6 §3.6 exist to close. THIS IS THE ONE THING IN MY SOURCES THAT IS STILL WRONG IF NOBODY ACTS.
- B1 THROUGH B8 NEED NO CHANGE AT ALL FOR THE FOURTH ROLE, and I say so in the section itself. They are quantified over a block that carries `levels`; not one of the eight names a role. The vetted closure judgement reaches the same finding independently (docs/plans/reviews/material/phase-4-v6-derivation-closure.md:145, row U8: "only the head sentence is widened"), and the shipped loader confirms it structurally — `_levels` (src/synthtwin/contract.py:3417-3563) enforces B1-B7 once and is called by both `_label_facts` and `_categorical_facts` without a role argument reaching any B check. Only the §6.3 HEAD SENTENCE widens from three roles to four. Note the consequence for identifiers: because nothing changes, none of B1-B8 becomes a new rule, so none of them takes a C6- number and there is no `C6-B` anything.
- TWO CLARIFYING PHRASES ADDED, both stating what the shipped rule already does and neither changing a rule. (1) B4: version 4 says "sorted ascending", which is ambiguous about repeats; the shipped check refuses only a strict decrease (contract.py:3457-3463), so two held-back labels of the same size conform. I wrote "non-decreasing, because ... the array is a multiset of sizes, not a set". Two independent implementations would otherwise disagree on a real document — a column with two held-back levels of one row each is ordinary. (2) B6: I added that B6 with B7 is a total order, so one set of levels has exactly one conforming sequence. That is what makes B6's canonical-bytes claim checkable. Strike either if the assembler wants pure transcription, but B4's ambiguity is a genuine interoperability hole.
- DELTA FRAMING DROPPED, listed so it is a decision and not an accident: (a) v4:807-808 "Two of them are version 4 additions" — deleted; it is a fact about version 4's history, not a rule about a version 6 document. (b) v4:885 "Version 4 publishes those spellings" — reworded to "This contract publishes those spellings". Nothing else in the transcribed text moved.
- I ADDED ONE REASON THAT VERSION 4 LEAVES IMPLICIT, taken from a live version 6 clause rather than invented: §6.4 and §6.5 now say WHY `level_ceiling` is forbidden (it is `categorical`'s own key, and the format has no optional keys, so it is absent rather than sometimes-present). Version 4 states the bare prohibition at v4:862-863 and v4:876-877. The reason is transcribed from C6-17 (v6:351-357) and P4-D5 (plan:949-956), which give exactly that reason for the same prohibition on `long_tail_labels`. Stating it identically on all four roles is what keeps a reader from concluding the prohibition means something different on the new role than on the old three.

## SOURCES

RULE-BY-RULE PROVENANCE (paths relative to repo root
"/Users/alfredowork/Library/CloudStorage/OneDrive-UniversityofIowa/Research/0- Synthetic dataset/synthtwin")

§6.3 head sentence, four label roles
- "constant, binary and categorical all publish LEVELS. Their shared keys are specified once here" — docs/spec/profile-contract-v4.md:793-797 (three roles).
- Fourth role long_tail_labels joins — docs/spec/profile-contract-v6.md:346-349 (C6-16: "Exactly the four shared label keys, under the shared label invariants B1 through B8 verbatim"); docs/plans/phase-4-columns.md:945-947 (P4-D5, "The four shared label keys, under the shared label invariants (B1 through B8), verbatim").
- "the four are the labels publication class" — docs/spec/profile-contract-v6.md:906 (C6-PUB table row: labels = constant, binary, categorical, long_tail_labels); docs/plans/phase-4-columns.md:552-554; src/synthtwin/taxonomy.py:258 (ROLES_PUBLISHING_LABELS, three roles as shipped — the fourth is the version 6 addition).

§6.3 shared-key table (4 rows, verbatim)
- docs/spec/profile-contract-v4.md:799-804.
- Wire spellings and their count confirmed against src/synthtwin/contract.py:335-341 (LABEL_KEYS = levels, suppressed_level_counts, suppressed_levels, suppressed_rows) and contract.py:1035-1043 (LabelFacts). CATEGORICAL_KEYS = LABEL_KEYS + ("level_ceiling",) at contract.py:342; contract.py:3227-3228 returns LABEL_KEYS for ROLE_CONSTANT and ROLE_BINARY.

§6.3 floor paragraph (new connective text, no new rule)
- "the floor" is small_cell_floor — docs/spec/v6-build/s4.md:125 (settings §4.4 row).
- "at least the floor / below the floor" phrasing and the empty range at a floor of one — docs/spec/v6-build/s4.md:351-359; src/synthtwin/contract.py:1878-1890 (_below_the_floor docstring).
- S13's positions include suppressed_levels, suppressed_rows, suppressed_level_counts and every variants_withheld block — docs/spec/v6-build/s4.md:365-373; docs/spec/profile-contract-v5.md:1020-1034 (C5-S13).

§6.3.1 level entry (4 keys, verbatim)
- docs/spec/profile-contract-v4.md:806-818.
- Four-key membership confirmed against src/synthtwin/contract.py:333 (LEVEL_KEYS = "count", "label", "variants", "variants_withheld") and contract.py:3495 (_keys(block, seat, LEVEL_KEYS, "every published label")).
- "trimmed and case-folded" is exactly parsing.folded — src/synthtwin/parsing.py:364-375 (text.strip().casefold()).
- Refusal clause ("naming the key and the column") — house rule of the rewrite, docs/spec/v6-build/s5.md:1-5 and docs/spec/v6-build/s4.md:8-9.
- Forward pointer to §7.4 — docs/spec/profile-contract-v4.md:818 ("Section 7.4 specifies variants and variants_withheld in full").
- Forward pointer to §5.3 for multiplicity maps — docs/spec/profile-contract-v4.md:812 (row wording) and docs/spec/v6-build/s3.md:30,65 (§5.3 fixes the form).
- DROPPED delta framing: v4:807-808 "Two of them are version 4 additions." Removed as delta framing per the self-contained rule; the fact is not a rule.

§6.3.2 B1–B8 (exactly eight, transcribed)
- docs/spec/profile-contract-v4.md:820-851. B1 at 822-826; B2 at 828-829; B3 at 831-832; B4 at 834-837; B5 at 839-841; B6 at 843-846; B7 at 848; B8 at 850-853.
- Unchanged at version 5 — docs/spec/profile-contract-v5.md:1049-1056 lists what version 4 invariants version 5 touches; B1–B8 are not among them, and W1–W7 are explicitly "unchanged".
- Enforcement, one site, over a block carrying `levels` and not over a role list — src/synthtwin/contract.py:3417-3563 (_levels), called by _label_facts (contract.py:3631-3660) for constant and binary and by _categorical_facts (contract.py:3663-3690).
- Per-invariant enforcement lines: B1 contract.py:3497-3504; B2 contract.py:3545-3557; B3 contract.py:3558-3570; B4 contract.py:3457-3480; B5 contract.py:3450-3456 (held-back half, against _below_the_floor) and 3505-3511 (published half); B6 contract.py:3517-3529; B7 contract.py:3512-3516; B8 contract.py:3424-3428 (docstring: "B8 is a permission rather than a rule").
- Plain-language restatements of B1–B7 for the refusal messages — src/synthtwin/contract.py:653-676.

§6.3.2 preamble ("stated over a block that carries levels, not over a role list")
- docs/plans/reviews/material/phase-4-v6-derivation-closure.md:145 (row U8): "B1 through B8 (v4:822-851, exactly eight) are stated over a block that carries `levels` rather than over the three roles, so only the head sentence is widened."
- Confirmed by reading B1–B8 themselves (v4:822-853): no role name occurs in any of the eight.

§6.3.2 B4 clarification ("non-decreasing")
- v4 wording is "sorted ascending" (v4:837). The shipped check is `if place and found < previous_size: raise B4` — src/synthtwin/contract.py:3457-3463 — so equal consecutive sizes conform. The clarifying clause states what the shipped rule does; it changes nothing.

§6.3.2 B6 clarification ("total order")
- Shipped check src/synthtwin/contract.py:3517-3529 (`count > previous_count or (count == previous_count and label < previous_label)`), read with B7 (contract.py:3512-3516). Consequence only.

§6.3 disposition paragraph
- docs/spec/profile-contract-v4.md:855-859, verbatim.
- Confirmed for the fourth role at docs/spec/profile-contract-v6.md:1180 (disposition matrix row: "the four label keys of C6-16 | long_tail_labels block | as on categorical | published levels and the held-back tail | EXACT-OBSERVABLE").

§6.4 `constant`
- Whole subsection — docs/spec/profile-contract-v4.md:858-870. Opening sentence v4:860; added-keys sentence v4:862-863; C1 v4:865; C2 v4:867-870.
- level_ceiling FORBIDDEN, with the reason: v4:862-863 states the prohibition; the reason ("categorical's own key", "the format has no optional keys") is transcribed from docs/spec/profile-contract-v6.md:351-357 (C6-17) and docs/plans/phase-4-columns.md:949-956, which state it for long_tail_labels in the same terms.
- Enforcement: C1 at src/synthtwin/contract.py:3643-3653 (rule_one = "C1" if role == ROLE_CONSTANT); key set at contract.py:3227-3228; plain-language C1 at contract.py:677.
- C2-as-consequence clause: C2 = C1 + B2 arithmetic (B2 at v4:828-829). No shipped check named "C2" exists (grep for '"C2"' in src/synthtwin/*.py returns nothing), which is consistent with it being the conjunction of two checked rules.

§6.5 `binary`
- Whole subsection — docs/spec/profile-contract-v4.md:872-886. Opening sentence v4:874; added-keys v4:876-877; Y1 v4:879; Y2 v4:881; the n_distinct note v4:883-886.
- Enforcement: Y1 at src/synthtwin/contract.py:3643-3653; plain-language Y1 at contract.py:678. No "Y2" check exists in src (same grep), consistent with Y2 = Y1 + B2.
- Reworded from delta framing: v4:885 "Version 4 publishes those spellings" → "This contract publishes those spellings", the only change to that note.

COUNTS CHECKED
- Four shared label keys: contract.py:335-341 (4 entries). Matches.
- Four level-entry keys: contract.py:333 (4 entries). Matches.
- Exactly eight B invariants: v4:822-853 and contract.py:653-676 (B1–B7 have refusal text; B8 is a permission, contract.py:3424-3428). Matches the brief's "there are exactly eight".
- Thirteen roles / labels class of four: docs/spec/v6-build/s5.md:86-98 and docs/spec/profile-contract-v6.md:906. Consistent with the verified count of 13 roles.