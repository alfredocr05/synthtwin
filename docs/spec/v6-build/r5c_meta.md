VERDICT SOUND_WITH_CORRECTIONS errors=15

## ERRORS

1. INVENTED, and it contradicts the governing plan. "**A column past the ceiling with no such level is not this role, and it is not a near miss.**" No source states any near-threshold treatment of the long-tail detection line, in either direction, and "near miss" is this contract's own term for the `near_threshold_slack` borderline remarks (D10 `remark_close_to_the_category_line`, docs/spec/v6-build/s45.md:411-417; D13 `remark_close_to_the_numeric_line`, s45.md:490). The plan's standing posture is the opposite: "every new line this plan draws is a named `Settings` constant recorded in every profile, applied as a count, **with a borderline remark within the standing slack**" (docs/plans/phase-4-columns.md:565-568), and `long_tail_minimum_level` is exactly such a line (docs/plans/phase-4-columns.md:1209-1213). Meanwhile the closed grammar has no such form: the census is fixed at 41 forms (docs/spec/v6-build/s45.md:152) and the three sentences version 6 adds are the affix remark, the recoverable-distribution remark and the slashed-date remark (docs/spec/profile-contract-v6.md:996-1030) — none of them a long-tail borderline. So the transcription asserts as settled a thing the plan promises and the grammar cannot yet write. CORRECTED: the clause is deleted and replaced with "and there is no partial form of it", which is the fall-through fact C6-15 actually states (docs/spec/profile-contract-v6.md:340-344). GAP for the assembler and the plan owner: the long-tail line has no borderline form, against docs/plans/phase-4-columns.md:567.

2. OVERSTATED, and false in a constructible case. "`suppressed_level_counts` holds one integer per below-floor level, and on this role that tail is **by construction long**". The source says only that the bound is `n_rows`: "`suppressed_level_counts` holds one integer per below-floor level; the bound is `n_rows`; no cap is added" (docs/plans/phase-4-columns.md:1003-1006). Nothing makes the tail long by construction: a 1000-row column with 25 folded levels of 40 rows each, against a ceiling of 20, satisfies LT1 and LT2 with `suppressed_levels == 0` and `suppressed_level_counts == []` (B5, docs/spec/v6-build/r2.md:69-71, forbids a held-back level at or above the floor). CORRECTED to "on this role there may be many of them: their number is bounded by nothing but `n_rows`."

3. MISATTRIBUTED SOURCE in the twin sentence. "...are EXACT-OBSERVABLE, **as section 6.3.2 states for every label role**: the twin writes each published label at exactly its count, **byte for byte through the published variants**, and invents that many neutral labels at exactly the held-back sizes, **fold collisions included**." Section 6.3.2 as written says only "the twin writes each published label at exactly its count and invents that many neutral labels at exactly the held-back sizes" (docs/spec/v6-build/r2.md:86-89) — it says nothing about variants byte for byte and nothing about fold collisions. The byte-level obligation is section 7.4's: "the twin writes each named spelling at exactly its count, and invents exactly that many neutral spellings of the parent at exactly the held-back sizes" (docs/spec/profile-contract-v4.md:1565-1570); fold collisions come from the plan's twin rule (docs/plans/phase-4-columns.md:966-970). CORRECTED: the 6.3.2 attribution is held to the count-and-invent clause and the variants clause is attributed to section 7.4.

4. CROSS-REFERENCE TO A GUESSED NUMBER. "its block is exactly what **section 6.9** specifies for that role". `free_text` is §6.9 only in version 4 (docs/spec/profile-contract-v4.md:1263); in the assembled version 6, six role parts must fit where version 4 had three (flagged at docs/spec/v6-build/r2_meta.md:29), so 6.9 will not be `free_text`. The section's own §6.7 head is a declared placeholder, but a placeholder pointing at somebody else's placeholder is the one that survives assembly wrong. CORRECTED to "the section for `free_text`", which is the house form r2.md:5 already uses for an unfixed number ("the section for `long_tail_labels`").

5. MALFORMED, and it reads as delta framing. "**The floor-cleared label spellings**, from columns that **a contract without this role publishes no value of at all**." The sentence is ungrammatical, and "a contract without this role" is version-delta framing in disguise, which amendment A-P4-11 removes (docs/plans/phase-4-columns.md:2225ff). The source row says "Floor-cleared label spellings from columns that publish no value today" (docs/spec/profile-contract-v6.md:1392-1395). CORRECTED to "from columns that would otherwise publish no value of the table at all", which states the same fact without a version or a hypothetical contract.

6. DROPPED RIDER, and it is one of two the plan explicitly says must be stated rather than discovered. P4-D5's first pricing bullet carries TWO widenings: the rows-not-people caveat, and "**the summary lists every column whose labels will be visible BEFORE anything is written, long-tail columns included**" (docs/plans/phase-4-columns.md:988-990). The transcription carries the first and drops the second, while keeping the "stated rather than discovered" framing that announced both. CORRECTED: the second widening is restored in the same bullet.

7. IMPRECISE, and the imprecision inverts the fact. "A published level's raw spellings appear in its `variants`, **under the floor**, exactly as on any other label role." `variants` holds "exact spelling → count, for every spelling of this label **that cleared the floor**" (docs/spec/v6-build/r2.md:35); the spellings that did not clear it are counted in `variants_withheld` (r2.md:36). "Under the floor" is this document's idiom for floor-governed (r6.md:98-99 uses it that way), but in a sentence whose subject is a key whose sibling means below-the-floor, it reads as the opposite of what it says. CORRECTED to name both halves explicitly.

8. IMPRECISE INFERENCE. "a level meeting it clears the floor and is therefore **published under B5**". B5 bounds published counts from below and held-back sizes from above (docs/spec/v6-build/r2.md:69-71); it is B5 together with the completeness identities B2 and B3 (r2.md:56-60) that leaves a floor-clearing level nowhere to be but `levels`. CORRECTED to "cannot be one of the below-floor levels B5 bounds — so it is published", which is what B5 alone supports.

9. DROPPED REASONS (minor), both stated in the plan and both load-bearing for the `max`. (a) The constitutive-floor reason: "publishing a floor-clearing spelling is constitutive of the role, so a column that cannot publish one under the recorded settings takes the next rule instead" (docs/plans/phase-4-columns.md:568-571). The transcription gives an equivalent reason in its own words but not the plan's, which is the phrase the reviewer will search for. (b) The tiny-table consequence: "a raised floor **or a tiny table** withholds the new roles" (docs/plans/phase-4-columns.md:574-575) — in a table of fewer than eleven rows no level can meet the line, so the role is unreachable. CORRECTED: both restored, in one sentence each.

10. DROPPED (minor, and arguably out of scope): the plan's draw-budget row for this role, "long-tail: zero content words like every label role" (docs/plans/phase-4-columns.md:1302-1303). It is a generation-method fact rather than a wire fact, but the section already contrasts this twin with the free-text twin, and the contrast is exactly that one invents labels and the other invents language. CORRECTED with a six-word clause; drop it if the assembler holds generation facts out of section 6.

11. ADJACENT DEFECT, outside this section, reported because it is the same failure class. D10 `remark_close_to_the_category_line` renders "this column was close to the line between a set of categories **and free text**" (docs/spec/v6-build/s45.md:414-417). Version 6 makes that rendering false: past the categorical ceiling a column may take `time_of_day` (rule 9), `affixed_number` (rule 10) or `long_tail_labels` (rule 11) before it reaches the fallback (docs/spec/v6-build/s5.md:183-207). The remark is raised in the shipped categorical branch (src/synthtwin/taxonomy.py:3853-3855). The form's rendering is fixed and closed, so this is a grammar repair for the assembler and the plan owner, not something this section may fix.

12. GAPS RE-CONFIRMED, each verified independently and none repaired here. (a) No detection-evidence form exists for this role: the four sentence paths are fixed (docs/spec/v6-build/s45.md:59-64), the census is 41 forms (s45.md:152), the ten evidence forms are B1-B10 (s45.md:208-288), and none is a long-tail detection — so C6-17's relocation of the passed ceiling into `detection_evidence` (docs/spec/profile-contract-v6.md:351-357) is currently unsatisfiable. The section names no form, which is correct. (b) `n_distinct` and `n_distinct_folded` still carry the disposition "set per role group, section 9" (docs/spec/v6-build/s5.md:29-30 — actually r1.md:29-30) with no group covering the three new roles, and LT2 and B2 are both stated over `n_distinct_folded`. (c) The disclosure inventory has no fixed section number, so the section names it rather than numbering it. (d) The §6.7 head is a placeholder; r2.md:5 and r3.md:44-57 must be repointed at whatever number assembly gives it.

13. CHECK, NO DEFECT — enumerations, both directions, with counts. Added keys: FOUR (`levels`, `suppressed_levels`, `suppressed_rows`, `suppressed_level_counts`) — matches C6-16 (docs/spec/profile-contract-v6.md:346-350), the per-role key row (v6:990), the shared table (docs/spec/v6-build/r2.md:9-14) and the `ltl` column of the matrix, which r6.md:248-254 counts at exactly 4. Nothing present that is not in source; nothing in source missing. Level-entry keys: FOUR (`label`, `count`, `variants`, `variants_withheld`) — matches r2.md:31-36. Detection conditions: THREE — matches C6-15 (v6:340-344) element for element. Shared label invariants: EIGHT, named B1-B8 with the same short names r2.md:50-84 gives them — no ninth invented, none of the eight dropped. Role invariants: LT1, LT2, G1L and no others — grep of `long_tail` across v6 returns exactly these three invariant rows (v6:1243, :1244, :1258) plus the key row, the class row, the axes and the settings key. Publication-class membership: `long_tail_labels` in labels, one row only (v6:906, r6.md:76).

14. CHECK, NO DEFECT — shipped code. The role does not exist in the implementation: `grep -rn 'long_tail\|LONG_TAIL' src/ tests/` returns zero hits, `ROLES_PUBLISHING_LABELS = (ROLE_CONSTANT, ROLE_BINARY, ROLE_CATEGORICAL)` (src/synthtwin/taxonomy.py:258), so no shipped enumeration can contradict this section and none does. The two code facts the section rests on are verified: `DEFAULT_SMALL_CELL_FLOOR = 11` (src/synthtwin/contract.py:190) and `small_cell_floor: int = 11` (src/synthtwin/taxonomy.py:1141), which is what licenses "at the default floor of 11 the two terms coincide". One further check that strengthens LT2: the shipped categorical branch tests exactly `folded_distinct <= ceiling` and nothing else (src/synthtwin/taxonomy.py:3842), and `_categorical_ceiling` computes `max(min(categorical_ceiling, share_of_rows), categorical_floor)` (taxonomy.py:3636-3639) — so the arithmetic the section transcribes from §6.6.1 is the shipped arithmetic, and a column reaching the fallback necessarily exceeds the ceiling, which is what makes LT2 true by construction on a conforming producer and decidable on a loader.

15. CHECK, NO DEFECT — delta framing. No "supersedes", no "carried", no "unchanged from version 5", no "as version 4 has it", no `C6-` letter identifier. `G1L` is a v6 invariant-list identifier, not a `C6-` letter (v6:1258), and it is kept. Every identifier the section cites is stated in this document: B1-B8 in §6.3.2, G1 and G2 in §6.6.1 (docs/spec/v6-build/r3.md:37-42), LT1/LT2/G1L stated here in full. The forbidden-key general rule is cited by SECTION (6.11) and not by the retired identifier `FKM`, which r6.md renumbered to C6-53 — correct.

## GAPS

- SECTION NUMBER IS UNFIXED, AND MY 6.7 IS A GUESS THE ASSEMBLER MUST SETTLE. No artifact numbers the three new role parts (already flagged at docs/spec/v6-build/r2_meta.md:29 and :37). r1.md/r2.md/r3.md occupy 6.1-6.6 and r6.md occupies 6.10-6.11, which leaves 6.7-6.9 for SIX parts: `count`/`continuous`, `identifier`, `free_text`, `time_of_day`, `affixed_number` and this one. The whole of section 6 must therefore be renumbered when the parts are assembled. I chose 6.7 because r2.md's §6.3 head lists this role immediately after 6.6.1. Two edits follow whatever number is chosen: r2.md:5 ('the section for `long_tail_labels`') must name it, and r3.md:44-57 refers to this role's 'own section' without a number.
- NO NOTE FORM EXISTS FOR THIS ROLE'S `detection_evidence`, SO THE RULE I TRANSCRIBED IS CURRENTLY UNSATISFIABLE. C6-17 (v6:351-357), r3.md:50-52 and r6.md:308-310 all relocate the passed ceiling into the column's `detection_evidence` sentence. But `columns[].detection_evidence` is one of the four sentence paths the closed note grammar governs (s45.md:59-64), that grammar's census is fixed at '41 forms and 62 argument positions' (s45.md:131-135), and its ten detection-evidence forms are B1-B10 (s45.md:206-288) — none of which is a long-tail detection. B8 `evidence_set_of_categories` says the column IS a set of categories and B9 `evidence_no_reading_fits` says no reading fits, and neither is true of this role. I did NOT invent a form: my section says the ceiling is recorded in that sentence and cites the grammar, and names no form. The same hole appears to bite `time_of_day` and `affixed_number` (no clock or affix evidence form is in B1-B10 either), so this is a grammar-level omission for the assembler and the plan owner, not a per-section one.
- WHICH PUBLICATION NOTES A LONG-TAIL COLUMN RAISES IS FIXED BY NOTHING. Form A4 `labels_pooled_below_the_floor` (s45.md:183-189, arity 3: suppressed levels, the floor, rows covered) is the obvious note for this role's below-floor tail and is exactly the note a categorical tail raises, but no source says a `long_tail_labels` column must raise it, and s45.md:126-133 says explicitly that no rule binds a form to a path. I left it out of the section rather than assert it.
- `n_distinct` AND `n_distinct_folded` HAVE NO DISPOSITION ON THIS ROLE. s5.md:29-30 gives both the disposition 'set per role group, section 9'; version 4's section 9 sets them in its per-role-group parts 9.3-9.7, which have no group for the three new roles, and version 6's section 11 is delta-only and adds none. This bites here specifically because LT2 and B2 are both stated over `n_distinct_folded`. Already flagged independently in the vetted material (docs/plans/reviews/material/phase-4-v6-derivation-datetime.md:35); repeated because my section relies on the key.
- THE DISCLOSURE INVENTORY HAS NO SECTION NUMBER I COULD CITE. My 'never a nothing-publishing column' paragraph and my two-bullet pricing both point at 'this document's disclosure inventory'. In the delta it is section 12 (rows 1, 11 and the vetted NEW ROW 15), but the self-contained document's numbering for it is not fixed by any artifact I could read, so I referred to it by name. The assembler should insert the number.

## CONFLICTS NOTED

- NOT A DISAGREEMENT WITH SHIPPED CODE, AND THE REASON IS WORTH RECORDING: this role does not exist in the code at all. `grep -rn 'long_tail\|LONG_TAIL' src/ tests/` returns zero hits; `ROLES_PUBLISHING_LABELS` at src/synthtwin/taxonomy.py:258 names exactly `(ROLE_CONSTANT, ROLE_BINARY, ROLE_CATEGORICAL)` and the role vocabulary is still ten (taxonomy.py:234-245, contract.py:288-298). So there was no shipped enumeration for me to check this section's rules against, and nothing here can be contradicted by the code yet. The one code fact the section does rest on is verified: `DEFAULT_SMALL_CELL_FLOOR = 11` (contract.py:190) and `small_cell_floor: int = 11` (taxonomy.py:1141), which is what makes 'at the default floor the two terms coincide' a transcription rather than an assumption.
- AN ASYMMETRY BETWEEN LT2 AND G2 THAT I WROTE OUT RATHER THAN GLOSSED. r3.md:33-35 says of `categorical` that the ceiling arithmetic 'is the producer's rule for fixing the number, and no invariant ties the published value to it' — so a loader never recomputes the ceiling for a categorical column. LT2 (v6:1244) is 'n_distinct_folded exceeds the categorical ceiling the settings imply', is not marked *producer*, and therefore requires a loader to recompute exactly that arithmetic for a long-tail column. The two are consistent but they are checked by different means, and a reviewer will read one against the other. I stated the asymmetry and its reason in the section ('The two roles are checked differently, and here is why') instead of leaving it to be found. If the owner intends LT2 to be a producer obligation instead, that is a change to the section-9 marking and not to my text.
- C6-15's WORDING SAYS 'FOLDED LEVELS' WHILE LT1 MUST BE READ OVER PUBLISHED `levels`. C6-15 (v6:340-344) states the detection condition over the column's folded levels — a fact about the table. LT1 (v6:1243) states it over a level's `count` — a fact about the document. The two agree, but only because the detection line is at or above `small_cell_floor`, so a qualifying folded level always clears the floor and is always published under B5. I wrote that derivation into the section, together with its consequence that `levels` is never empty on this role (B8's empty case being unreachable here), because the agreement is load-bearing for LT1's loader-decidability and is nowhere stated in the sources.

## SOURCES

Read in the order they win. (1) The ratified plan, "docs/plans/phase-4-columns.md": P4-D5 in full (lines 932-1006) — the rule, what it publishes, the level_ceiling exclusion and its reason, the twin rule, the owner-gated pricing with its three bullets, and the document-size paragraph; P4-D3's taxonomy line 502 and 552 (long_tail_labels joins the labels class); P4-D7 lines 1209-1213 (the settings key and its single value); amendment A-P4-11 (line 2225, self-contained contract); review-record rows 2404 and 2459 (the folded-vs-raw delta, and P4-P4-F4 on level_ceiling). (2) "docs/spec/profile-contract-v6.md" section 3.4, clauses C6-15 through C6-18 (lines 338-360) transcribed clause by clause with the delta framing dropped; C6-19/C6-20 (lines 364-384) for the axes and the settings key; the invariant list rows LT1 (:1243), LT2 (:1244), G1L (:1258) and the section-9 header rule that an unmarked row is loader-decidable (:1196-1219); the forbidden-key summary row :990; the publication-class row :906; disclosure rows 1 (:1392-1395) and 11 (:1467-1483). (3) Version 4 "docs/spec/profile-contract-v4.md" for the role-section voice and the transcribed base: 6.3 and 6.3.1 (:793-819), B1-B8 (:820-857), 6.4/6.5 (:858-887), 6.6.1 with G1 and G2 (:893-909), 6.9 free_text (:1263-1302), 6.10 publication class (:1304-1312), 6.11 matrix (:1314-1375). (4) Sections already written under "docs/spec/v6-build/", read so this part agrees with them and redefines nothing: r1.md (the section-6 head: thirteen roles, reading order, "every key not listed is forbidden", no second matrix), r2.md (6.3 shared label shape, 6.3.1 level entry, 6.3.2 B1-B8 stated over a block carrying `levels` and binding all four label roles identically, the EXACT-OBSERVABLE closing sentence, and 6.4/6.5's `level_ceiling` wording I matched), r3.md (6.6.1's ceiling arithmetic, G1, G2, and its forward reference at :44-57 naming `long_tail_labels`, which my section is written to meet exactly), r6.md (6.10 C6-49/C6-50/C6-51/C6-52 and 6.11's matrix — four marked cells in the `ltl` column, `level_ceiling` confined to `categorical` at :304-310), s4.md (4.4's seventeen settings keys, `long_tail_minimum_level` at :120 and :155-168, `categorical_*` at :109-111, the floor-of-one ruling at :322-348), s5.md (5.1's twenty-two universal keys including `n_distinct_folded` and `detection_evidence`; 5.2's axis table row `long_tail_labels`/`long_tail_labels`/`ok`, C6-19's `structural_role` `data` reasoning, and the twelve-rule order placing this rule at 11), s45.md (4.5.1's grammar preamble: the four sentence paths, C6-ARG, and the census of 41 forms). (5) Vetted material: "docs/plans/reviews/material/phase-4-v6-derivation-datetime.md" NEW ROW 15 (:164-197), which establishes that a long-tail column is a labels-class role crossing out of the nothing class and so newly publishes four kinds of absent-cell fact — the paragraph I wrote as "never a nothing-publishing column". (6) Shipped source: `DEFAULT_SMALL_CELL_FLOOR = 11` (src/synthtwin/contract.py:190) and `small_cell_floor: int = 11` (src/synthtwin/taxonomy.py:1141), which is what licenses "at the default floor of 11 the two terms coincide"; `ROLES_PUBLISHING_LABELS = (ROLE_CONSTANT, ROLE_BINARY, ROLE_CATEGORICAL)` (src/synthtwin/taxonomy.py:258) and the absence of any `long_tail` symbol anywhere in src/ or tests/ (grep, zero hits), confirming the code has not moved to version 6.