# Data formats never enter this repository (plan D13); fixtures are
# generated by committed seeded scripts, not committed as data.
*.csv
*.tsv
*.parquet
*.xlsx
*.xls
*.feather
*.pkl
*.pickle
*.db
*.sqlite*
*.zip
*.tar
*.tar.*
*.gz

# Maintainer-private material (defense in depth; lives outside the repo)
planning-notes/

# Python
__pycache__/
*.pyc
*.egg-info/
dist/
build/
wheelhouse/
.venv/
venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS noise
.DS_Store

# uv project lock (requirements-dev.lock is the governed lock)
uv.lock

# Agent scratch worktrees. A tool running in this folder may make a
# worktree under `.claude/`; those are working copies of this same
# repository and never part of it.
.claude/
